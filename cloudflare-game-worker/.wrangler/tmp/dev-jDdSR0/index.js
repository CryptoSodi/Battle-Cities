var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __esm = (fn, res, err) => function __init() {
  if (err) throw err[0];
  try {
    return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
  } catch (e) {
    throw err = [e], e;
  }
};
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// wrangler-modules-watch:wrangler:modules-watch
var init_wrangler_modules_watch = __esm({
  "wrangler-modules-watch:wrangler:modules-watch"() {
    init_modules_watch_stub();
  }
});

// ../node_modules/wrangler/templates/modules-watch-stub.js
var init_modules_watch_stub = __esm({
  "../node_modules/wrangler/templates/modules-watch-stub.js"() {
    init_wrangler_modules_watch();
  }
});

// ../node_modules/@hapi/hoek/lib/stringify.js
var require_stringify = __commonJS({
  "../node_modules/@hapi/hoek/lib/stringify.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    module.exports = function(...args) {
      try {
        return JSON.stringify.apply(null, args);
      } catch (err) {
        return "[Cannot display object: " + err.message + "]";
      }
    };
  }
});

// ../node_modules/@hapi/hoek/lib/error.js
var require_error = __commonJS({
  "../node_modules/@hapi/hoek/lib/error.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Stringify = require_stringify();
    module.exports = class extends Error {
      constructor(args) {
        const msgs = args.filter((arg) => arg !== "").map((arg) => {
          return typeof arg === "string" ? arg : arg instanceof Error ? arg.message : Stringify(arg);
        });
        super(msgs.join(" ") || "Unknown error");
        if (typeof Error.captureStackTrace === "function") {
          Error.captureStackTrace(this, exports.assert);
        }
      }
    };
  }
});

// ../node_modules/@hapi/hoek/lib/assert.js
var require_assert = __commonJS({
  "../node_modules/@hapi/hoek/lib/assert.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var AssertError = require_error();
    module.exports = function(condition, ...args) {
      if (condition) {
        return;
      }
      if (args.length === 1 && args[0] instanceof Error) {
        throw args[0];
      }
      throw new AssertError(args);
    };
  }
});

// ../node_modules/@hapi/hoek/lib/reach.js
var require_reach = __commonJS({
  "../node_modules/@hapi/hoek/lib/reach.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var internals = {};
    module.exports = function(obj, chain, options) {
      if (chain === false || chain === null || chain === void 0) {
        return obj;
      }
      options = options || {};
      if (typeof options === "string") {
        options = { separator: options };
      }
      const isChainArray = Array.isArray(chain);
      Assert(!isChainArray || !options.separator, "Separator option no valid for array-based chain");
      const path = isChainArray ? chain : chain.split(options.separator || ".");
      let ref = obj;
      for (let i = 0; i < path.length; ++i) {
        let key = path[i];
        const type = options.iterables && internals.iterables(ref);
        if (Array.isArray(ref) || type === "set") {
          const number2 = Number(key);
          if (Number.isInteger(number2)) {
            key = number2 < 0 ? ref.length + number2 : number2;
          }
        }
        if (!ref || typeof ref === "function" && options.functions === false || // Defaults to true
        !type && ref[key] === void 0) {
          Assert(!options.strict || i + 1 === path.length, "Missing segment", key, "in reach path ", chain);
          Assert(typeof ref === "object" || options.functions === true || typeof ref !== "function", "Invalid segment", key, "in reach path ", chain);
          ref = options.default;
          break;
        }
        if (!type) {
          ref = ref[key];
        } else if (type === "set") {
          ref = [...ref][key];
        } else {
          ref = ref.get(key);
        }
      }
      return ref;
    };
    internals.iterables = function(ref) {
      if (ref instanceof Set) {
        return "set";
      }
      if (ref instanceof Map) {
        return "map";
      }
    };
  }
});

// ../node_modules/@hapi/hoek/lib/types.js
var require_types = __commonJS({
  "../node_modules/@hapi/hoek/lib/types.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var internals = {};
    exports = module.exports = {
      array: Array.prototype,
      buffer: Buffer && Buffer.prototype,
      // $lab:coverage:ignore$
      date: Date.prototype,
      error: Error.prototype,
      generic: Object.prototype,
      map: Map.prototype,
      promise: Promise.prototype,
      regex: RegExp.prototype,
      set: Set.prototype,
      weakMap: WeakMap.prototype,
      weakSet: WeakSet.prototype
    };
    internals.typeMap = /* @__PURE__ */ new Map([
      ["[object Error]", exports.error],
      ["[object Map]", exports.map],
      ["[object Promise]", exports.promise],
      ["[object Set]", exports.set],
      ["[object WeakMap]", exports.weakMap],
      ["[object WeakSet]", exports.weakSet]
    ]);
    exports.getInternalProto = function(obj) {
      if (Array.isArray(obj)) {
        return exports.array;
      }
      if (Buffer && obj instanceof Buffer) {
        return exports.buffer;
      }
      if (obj instanceof Date) {
        return exports.date;
      }
      if (obj instanceof RegExp) {
        return exports.regex;
      }
      if (obj instanceof Error) {
        return exports.error;
      }
      const objName = Object.prototype.toString.call(obj);
      return internals.typeMap.get(objName) || exports.generic;
    };
  }
});

// ../node_modules/@hapi/hoek/lib/utils.js
var require_utils = __commonJS({
  "../node_modules/@hapi/hoek/lib/utils.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    exports.keys = function(obj, options = {}) {
      return options.symbols !== false ? Reflect.ownKeys(obj) : Object.getOwnPropertyNames(obj);
    };
  }
});

// ../node_modules/@hapi/hoek/lib/clone.js
var require_clone = __commonJS({
  "../node_modules/@hapi/hoek/lib/clone.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Reach = require_reach();
    var Types = require_types();
    var Utils = require_utils();
    var internals = {
      needsProtoHack: /* @__PURE__ */ new Set([Types.set, Types.map, Types.weakSet, Types.weakMap])
    };
    module.exports = internals.clone = function(obj, options = {}, _seen = null) {
      if (typeof obj !== "object" || obj === null) {
        return obj;
      }
      let clone = internals.clone;
      let seen = _seen;
      if (options.shallow) {
        if (options.shallow !== true) {
          return internals.cloneWithShallow(obj, options);
        }
        clone = /* @__PURE__ */ __name((value) => value, "clone");
      } else if (seen) {
        const lookup = seen.get(obj);
        if (lookup) {
          return lookup;
        }
      } else {
        seen = /* @__PURE__ */ new Map();
      }
      const baseProto = Types.getInternalProto(obj);
      if (baseProto === Types.buffer) {
        return Buffer && Buffer.from(obj);
      }
      if (baseProto === Types.date) {
        return new Date(obj.getTime());
      }
      if (baseProto === Types.regex) {
        return new RegExp(obj);
      }
      const newObj = internals.base(obj, baseProto, options);
      if (newObj === obj) {
        return obj;
      }
      if (seen) {
        seen.set(obj, newObj);
      }
      if (baseProto === Types.set) {
        for (const value of obj) {
          newObj.add(clone(value, options, seen));
        }
      } else if (baseProto === Types.map) {
        for (const [key, value] of obj) {
          newObj.set(key, clone(value, options, seen));
        }
      }
      const keys = Utils.keys(obj, options);
      for (const key of keys) {
        if (key === "__proto__") {
          continue;
        }
        if (baseProto === Types.array && key === "length") {
          newObj.length = obj.length;
          continue;
        }
        const descriptor = Object.getOwnPropertyDescriptor(obj, key);
        if (descriptor) {
          if (descriptor.get || descriptor.set) {
            Object.defineProperty(newObj, key, descriptor);
          } else if (descriptor.enumerable) {
            newObj[key] = clone(obj[key], options, seen);
          } else {
            Object.defineProperty(newObj, key, { enumerable: false, writable: true, configurable: true, value: clone(obj[key], options, seen) });
          }
        } else {
          Object.defineProperty(newObj, key, {
            enumerable: true,
            writable: true,
            configurable: true,
            value: clone(obj[key], options, seen)
          });
        }
      }
      return newObj;
    };
    internals.cloneWithShallow = function(source, options) {
      const keys = options.shallow;
      options = Object.assign({}, options);
      options.shallow = false;
      const seen = /* @__PURE__ */ new Map();
      for (const key of keys) {
        const ref = Reach(source, key);
        if (typeof ref === "object" || typeof ref === "function") {
          seen.set(ref, ref);
        }
      }
      return internals.clone(source, options, seen);
    };
    internals.base = function(obj, baseProto, options) {
      if (options.prototype === false) {
        if (internals.needsProtoHack.has(baseProto)) {
          return new baseProto.constructor();
        }
        return baseProto === Types.array ? [] : {};
      }
      const proto = Object.getPrototypeOf(obj);
      if (proto && proto.isImmutable) {
        return obj;
      }
      if (baseProto === Types.array) {
        const newObj = [];
        if (proto !== baseProto) {
          Object.setPrototypeOf(newObj, proto);
        }
        return newObj;
      }
      if (internals.needsProtoHack.has(baseProto)) {
        const newObj = new proto.constructor();
        if (proto !== baseProto) {
          Object.setPrototypeOf(newObj, proto);
        }
        return newObj;
      }
      return Object.create(proto);
    };
  }
});

// ../node_modules/@hapi/joi/package.json
var require_package = __commonJS({
  "../node_modules/@hapi/joi/package.json"(exports, module) {
    module.exports = {
      name: "@hapi/joi",
      description: "Object schema validation",
      version: "17.1.0",
      repository: "git://github.com/hapijs/joi",
      main: "lib/index.js",
      browser: "dist/joi-browser.min.js",
      files: [
        "lib/**/*",
        "dist/*"
      ],
      keywords: [
        "schema",
        "validation"
      ],
      dependencies: {
        "@hapi/address": "^4.0.0",
        "@hapi/formula": "^2.0.0",
        "@hapi/hoek": "^9.0.0",
        "@hapi/pinpoint": "^2.0.0",
        "@hapi/topo": "^5.0.0"
      },
      devDependencies: {
        "@hapi/bourne": "2.x.x",
        "@hapi/code": "8.x.x",
        "@hapi/lab": "22.x.x",
        "@hapi/joi-legacy-test": "npm:@hapi/joi@15.x.x"
      },
      scripts: {
        prepublishOnly: "cd browser && npm install && npm run build",
        test: "lab -t 100 -a @hapi/code -L",
        "test-cov-html": "lab -r html -o coverage.html -a @hapi/code"
      },
      license: "BSD-3-Clause"
    };
  }
});

// ../node_modules/@hapi/joi/lib/schemas.js
var require_schemas = __commonJS({
  "../node_modules/@hapi/joi/lib/schemas.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Joi2 = require_lib4();
    var internals = {};
    internals.wrap = Joi2.string().min(1).max(2).allow(false);
    exports.preferences = Joi2.object({
      allowUnknown: Joi2.boolean(),
      abortEarly: Joi2.boolean(),
      cache: Joi2.boolean(),
      context: Joi2.object(),
      convert: Joi2.boolean(),
      dateFormat: Joi2.valid("date", "iso", "string", "time", "utc"),
      debug: Joi2.boolean(),
      errors: {
        escapeHtml: Joi2.boolean(),
        label: Joi2.valid("path", "key", false),
        language: [
          Joi2.string(),
          Joi2.object().ref()
        ],
        render: Joi2.boolean(),
        stack: Joi2.boolean(),
        wrap: {
          label: internals.wrap,
          array: internals.wrap
        }
      },
      externals: Joi2.boolean(),
      messages: Joi2.object(),
      noDefaults: Joi2.boolean(),
      nonEnumerables: Joi2.boolean(),
      presence: Joi2.valid("required", "optional", "forbidden"),
      skipFunctions: Joi2.boolean(),
      stripUnknown: Joi2.object({
        arrays: Joi2.boolean(),
        objects: Joi2.boolean()
      }).or("arrays", "objects").allow(true, false),
      warnings: Joi2.boolean()
    }).strict();
    internals.nameRx = /^[a-zA-Z0-9]\w*$/;
    internals.rule = Joi2.object({
      alias: Joi2.array().items(Joi2.string().pattern(internals.nameRx)).single(),
      args: Joi2.array().items(
        Joi2.string(),
        Joi2.object({
          name: Joi2.string().pattern(internals.nameRx).required(),
          ref: Joi2.boolean(),
          assert: Joi2.alternatives([
            Joi2.function(),
            Joi2.object().schema()
          ]).conditional("ref", { is: true, then: Joi2.required() }),
          normalize: Joi2.function(),
          message: Joi2.string().when("assert", { is: Joi2.function(), then: Joi2.required() })
        })
      ),
      convert: Joi2.boolean(),
      manifest: Joi2.boolean(),
      method: Joi2.function().allow(false),
      multi: Joi2.boolean(),
      validate: Joi2.function()
    });
    exports.extension = Joi2.object({
      type: Joi2.alternatives([
        Joi2.string(),
        Joi2.object().regex()
      ]).required(),
      args: Joi2.function(),
      base: Joi2.object().schema().when("type", { is: Joi2.object().regex(), then: Joi2.forbidden() }),
      coerce: [
        Joi2.function().maxArity(3),
        Joi2.object({ method: Joi2.function().maxArity(3).required(), from: Joi2.array().items(Joi2.string()).single() })
      ],
      flags: Joi2.object().pattern(internals.nameRx, Joi2.object({
        setter: Joi2.string(),
        default: Joi2.any()
      })),
      manifest: {
        build: Joi2.function().arity(2)
      },
      messages: [Joi2.object(), Joi2.string()],
      modifiers: Joi2.object().pattern(internals.nameRx, Joi2.function().minArity(1).maxArity(2)),
      overrides: Joi2.object().pattern(internals.nameRx, Joi2.function()),
      prepare: Joi2.function().maxArity(3),
      rebuild: Joi2.function().arity(1),
      rules: Joi2.object().pattern(internals.nameRx, internals.rule),
      terms: Joi2.object().pattern(internals.nameRx, Joi2.object({
        init: Joi2.array().allow(null).required(),
        manifest: Joi2.object().pattern(/.+/, [
          Joi2.valid("schema", "single"),
          Joi2.object({
            mapped: Joi2.object({
              from: Joi2.string().required(),
              to: Joi2.string().required()
            }).required()
          })
        ])
      })),
      validate: Joi2.function().maxArity(3)
    }).strict();
    exports.extensions = Joi2.array().items(Joi2.object(), Joi2.function().arity(1)).strict();
    internals.desc = {
      buffer: Joi2.object({
        buffer: Joi2.string()
      }),
      func: Joi2.object({
        function: Joi2.function().required(),
        options: {
          literal: true
        }
      }),
      override: Joi2.object({
        override: true
      }),
      ref: Joi2.object({
        ref: Joi2.object({
          type: Joi2.valid("value", "global", "local"),
          path: Joi2.array().required(),
          separator: Joi2.string().length(1).allow(false),
          ancestor: Joi2.number().min(0).integer().allow("root"),
          map: Joi2.array().items(Joi2.array().length(2)).min(1),
          adjust: Joi2.function(),
          iterables: Joi2.boolean(),
          in: Joi2.boolean()
        }).required()
      }),
      regex: Joi2.object({
        regex: Joi2.string().min(3)
      }),
      special: Joi2.object({
        special: Joi2.valid("deep").required()
      }),
      template: Joi2.object({
        template: Joi2.string().required(),
        options: Joi2.object()
      }),
      value: Joi2.object({
        value: Joi2.alternatives([Joi2.object(), Joi2.array()]).required()
      })
    };
    internals.desc.entity = Joi2.alternatives([
      Joi2.array().items(Joi2.link("...")),
      Joi2.boolean(),
      Joi2.function(),
      Joi2.number(),
      Joi2.string(),
      internals.desc.buffer,
      internals.desc.func,
      internals.desc.ref,
      internals.desc.regex,
      internals.desc.special,
      internals.desc.template,
      internals.desc.value,
      Joi2.link("/")
    ]);
    internals.desc.values = Joi2.array().items(
      null,
      Joi2.boolean(),
      Joi2.function(),
      Joi2.number().allow(Infinity, -Infinity),
      Joi2.string().allow(""),
      Joi2.symbol(),
      internals.desc.buffer,
      internals.desc.func,
      internals.desc.override,
      internals.desc.ref,
      internals.desc.regex,
      internals.desc.template,
      internals.desc.value
    );
    internals.desc.messages = Joi2.object().pattern(/.+/, [
      Joi2.string(),
      internals.desc.template,
      Joi2.object().pattern(/.+/, [Joi2.string(), internals.desc.template])
    ]);
    exports.description = Joi2.object({
      type: Joi2.string().required(),
      flags: Joi2.object({
        cast: Joi2.string(),
        default: Joi2.any(),
        description: Joi2.string(),
        empty: Joi2.link("/"),
        failover: internals.desc.entity,
        id: Joi2.string(),
        label: Joi2.string(),
        only: true,
        presence: ["optional", "required", "forbidden"],
        result: ["raw", "strip"],
        strip: Joi2.boolean(),
        unit: Joi2.string()
      }).unknown(),
      preferences: {
        allowUnknown: Joi2.boolean(),
        abortEarly: Joi2.boolean(),
        cache: Joi2.boolean(),
        convert: Joi2.boolean(),
        dateFormat: ["date", "iso", "string", "time", "utc"],
        errors: {
          escapeHtml: Joi2.boolean(),
          label: ["path", "key"],
          language: [
            Joi2.string(),
            internals.desc.ref
          ],
          wrap: {
            label: internals.wrap,
            array: internals.wrap
          }
        },
        externals: Joi2.boolean(),
        messages: internals.desc.messages,
        noDefaults: Joi2.boolean(),
        nonEnumerables: Joi2.boolean(),
        presence: ["required", "optional", "forbidden"],
        skipFunctions: Joi2.boolean(),
        stripUnknown: Joi2.object({
          arrays: Joi2.boolean(),
          objects: Joi2.boolean()
        }).or("arrays", "objects").allow(true, false),
        warnings: Joi2.boolean()
      },
      allow: internals.desc.values,
      invalid: internals.desc.values,
      rules: Joi2.array().min(1).items({
        name: Joi2.string().required(),
        args: Joi2.object().min(1),
        keep: Joi2.boolean(),
        message: [
          Joi2.string(),
          internals.desc.messages
        ],
        warn: Joi2.boolean()
      }),
      // Terms
      keys: Joi2.object().pattern(/.*/, Joi2.link("/")),
      link: internals.desc.ref
    }).pattern(/^[a-z]\w*$/, Joi2.any());
  }
});

// ../node_modules/@hapi/hoek/lib/escapeHtml.js
var require_escapeHtml = __commonJS({
  "../node_modules/@hapi/hoek/lib/escapeHtml.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var internals = {};
    module.exports = function(input) {
      if (!input) {
        return "";
      }
      let escaped = "";
      for (let i = 0; i < input.length; ++i) {
        const charCode = input.charCodeAt(i);
        if (internals.isSafe(charCode)) {
          escaped += input[i];
        } else {
          escaped += internals.escapeHtmlChar(charCode);
        }
      }
      return escaped;
    };
    internals.escapeHtmlChar = function(charCode) {
      const namedEscape = internals.namedHtml[charCode];
      if (typeof namedEscape !== "undefined") {
        return namedEscape;
      }
      if (charCode >= 256) {
        return "&#" + charCode + ";";
      }
      const hexValue = charCode.toString(16).padStart(2, "0");
      return `&#x${hexValue};`;
    };
    internals.isSafe = function(charCode) {
      return typeof internals.safeCharCodes[charCode] !== "undefined";
    };
    internals.namedHtml = {
      "38": "&amp;",
      "60": "&lt;",
      "62": "&gt;",
      "34": "&quot;",
      "160": "&nbsp;",
      "162": "&cent;",
      "163": "&pound;",
      "164": "&curren;",
      "169": "&copy;",
      "174": "&reg;"
    };
    internals.safeCharCodes = (function() {
      const safe = {};
      for (let i = 32; i < 123; ++i) {
        if (i >= 97 || // a-z
        i >= 65 && i <= 90 || // A-Z
        i >= 48 && i <= 57 || // 0-9
        i === 32 || // space
        i === 46 || // .
        i === 44 || // ,
        i === 45 || // -
        i === 58 || // :
        i === 95) {
          safe[i] = null;
        }
      }
      return safe;
    })();
  }
});

// ../node_modules/@hapi/formula/lib/index.js
var require_lib = __commonJS({
  "../node_modules/@hapi/formula/lib/index.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var internals = {
      operators: ["!", "^", "*", "/", "%", "+", "-", "<", "<=", ">", ">=", "==", "!=", "&&", "||", "??"],
      operatorCharacters: ["!", "^", "*", "/", "%", "+", "-", "<", "=", ">", "&", "|", "?"],
      operatorsOrder: [["^"], ["*", "/", "%"], ["+", "-"], ["<", "<=", ">", ">="], ["==", "!="], ["&&"], ["||", "??"]],
      operatorsPrefix: ["!", "n"],
      literals: {
        '"': '"',
        "`": "`",
        "'": "'",
        "[": "]"
      },
      numberRx: /^(?:[0-9]*\.?[0-9]*){1}$/,
      tokenRx: /^[\w\$\#\.\@\:\{\}]+$/,
      symbol: /* @__PURE__ */ Symbol("formula"),
      settings: /* @__PURE__ */ Symbol("settings")
    };
    exports.Parser = class {
      constructor(string2, options = {}) {
        if (!options[internals.settings] && options.constants) {
          for (const constant in options.constants) {
            const value = options.constants[constant];
            if (value !== null && !["boolean", "number", "string"].includes(typeof value)) {
              throw new Error(`Formula constant ${constant} contains invalid ${typeof value} value type`);
            }
          }
        }
        this.settings = options[internals.settings] ? options : Object.assign({ [internals.settings]: true, constants: {}, functions: {} }, options);
        this.single = null;
        this._parts = null;
        this._parse(string2);
      }
      _parse(string2) {
        let parts = [];
        let current = "";
        let parenthesis = 0;
        let literal = false;
        const flush = /* @__PURE__ */ __name((inner) => {
          if (parenthesis) {
            throw new Error("Formula missing closing parenthesis");
          }
          const last = parts.length ? parts[parts.length - 1] : null;
          if (!literal && !current && !inner) {
            return;
          }
          if (last && last.type === "reference" && inner === ")") {
            last.type = "function";
            last.value = this._subFormula(current, last.value);
            current = "";
            return;
          }
          if (inner === ")") {
            const sub = new exports.Parser(current, this.settings);
            parts.push({ type: "segment", value: sub });
          } else if (literal) {
            if (literal === "]") {
              parts.push({ type: "reference", value: current });
              current = "";
              return;
            }
            parts.push({ type: "literal", value: current });
          } else if (internals.operatorCharacters.includes(current)) {
            if (last && last.type === "operator" && internals.operators.includes(last.value + current)) {
              last.value += current;
            } else {
              parts.push({ type: "operator", value: current });
            }
          } else if (current.match(internals.numberRx)) {
            parts.push({ type: "constant", value: parseFloat(current) });
          } else if (this.settings.constants[current] !== void 0) {
            parts.push({ type: "constant", value: this.settings.constants[current] });
          } else {
            if (!current.match(internals.tokenRx)) {
              throw new Error(`Formula contains invalid token: ${current}`);
            }
            parts.push({ type: "reference", value: current });
          }
          current = "";
        }, "flush");
        for (const c of string2) {
          if (literal) {
            if (c === literal) {
              flush();
              literal = false;
            } else {
              current += c;
            }
          } else if (parenthesis) {
            if (c === "(") {
              current += c;
              ++parenthesis;
            } else if (c === ")") {
              --parenthesis;
              if (!parenthesis) {
                flush(c);
              } else {
                current += c;
              }
            } else {
              current += c;
            }
          } else if (c in internals.literals) {
            literal = internals.literals[c];
          } else if (c === "(") {
            flush();
            ++parenthesis;
          } else if (internals.operatorCharacters.includes(c)) {
            flush();
            current = c;
            flush();
          } else if (c !== " ") {
            current += c;
          } else {
            flush();
          }
        }
        flush();
        parts = parts.map((part, i) => {
          if (part.type !== "operator" || part.value !== "-" || i && parts[i - 1].type !== "operator") {
            return part;
          }
          return { type: "operator", value: "n" };
        });
        let operator = false;
        for (const part of parts) {
          if (part.type === "operator") {
            if (internals.operatorsPrefix.includes(part.value)) {
              continue;
            }
            if (!operator) {
              throw new Error("Formula contains an operator in invalid position");
            }
            if (!internals.operators.includes(part.value)) {
              throw new Error(`Formula contains an unknown operator ${part.value}`);
            }
          } else if (operator) {
            throw new Error("Formula missing expected operator");
          }
          operator = !operator;
        }
        if (!operator) {
          throw new Error("Formula contains invalid trailing operator");
        }
        if (parts.length === 1 && ["reference", "literal", "constant"].includes(parts[0].type)) {
          this.single = { type: parts[0].type === "reference" ? "reference" : "value", value: parts[0].value };
        }
        this._parts = parts.map((part) => {
          if (part.type === "operator") {
            return internals.operatorsPrefix.includes(part.value) ? part : part.value;
          }
          if (part.type !== "reference") {
            return part.value;
          }
          if (this.settings.tokenRx && !this.settings.tokenRx.test(part.value)) {
            throw new Error(`Formula contains invalid reference ${part.value}`);
          }
          if (this.settings.reference) {
            return this.settings.reference(part.value);
          }
          return internals.reference(part.value);
        });
      }
      _subFormula(string2, name) {
        const method = this.settings.functions[name];
        if (typeof method !== "function") {
          throw new Error(`Formula contains unknown function ${name}`);
        }
        let args = [];
        if (string2) {
          let current = "";
          let parenthesis = 0;
          let literal = false;
          const flush = /* @__PURE__ */ __name(() => {
            if (!current) {
              throw new Error(`Formula contains function ${name} with invalid arguments ${string2}`);
            }
            args.push(current);
            current = "";
          }, "flush");
          for (let i = 0; i < string2.length; ++i) {
            const c = string2[i];
            if (literal) {
              current += c;
              if (c === literal) {
                literal = false;
              }
            } else if (c in internals.literals && !parenthesis) {
              current += c;
              literal = internals.literals[c];
            } else if (c === "," && !parenthesis) {
              flush();
            } else {
              current += c;
              if (c === "(") {
                ++parenthesis;
              } else if (c === ")") {
                --parenthesis;
              }
            }
          }
          flush();
        }
        args = args.map((arg) => new exports.Parser(arg, this.settings));
        return function(context) {
          const innerValues = [];
          for (const arg of args) {
            innerValues.push(arg.evaluate(context));
          }
          return method.call(context, ...innerValues);
        };
      }
      evaluate(context) {
        const parts = this._parts.slice();
        for (let i = parts.length - 2; i >= 0; --i) {
          const part = parts[i];
          if (part && part.type === "operator") {
            const current = parts[i + 1];
            parts.splice(i + 1, 1);
            const value = internals.evaluate(current, context);
            parts[i] = internals.single(part.value, value);
          }
        }
        internals.operatorsOrder.forEach((set) => {
          for (let i = 1; i < parts.length - 1; ) {
            if (set.includes(parts[i])) {
              const operator = parts[i];
              const left = internals.evaluate(parts[i - 1], context);
              const right = internals.evaluate(parts[i + 1], context);
              parts.splice(i, 2);
              const result = internals.calculate(operator, left, right);
              parts[i - 1] = result === 0 ? 0 : result;
            } else {
              i += 2;
            }
          }
        });
        return internals.evaluate(parts[0], context);
      }
    };
    exports.Parser.prototype[internals.symbol] = true;
    internals.reference = function(name) {
      return function(context) {
        return context && context[name] !== void 0 ? context[name] : null;
      };
    };
    internals.evaluate = function(part, context) {
      if (part === null) {
        return null;
      }
      if (typeof part === "function") {
        return part(context);
      }
      if (part[internals.symbol]) {
        return part.evaluate(context);
      }
      return part;
    };
    internals.single = function(operator, value) {
      if (operator === "!") {
        return value ? false : true;
      }
      const negative = -value;
      if (negative === 0) {
        return 0;
      }
      return negative;
    };
    internals.calculate = function(operator, left, right) {
      if (operator === "??") {
        return internals.exists(left) ? left : right;
      }
      if (typeof left === "string" || typeof right === "string") {
        if (operator === "+") {
          left = internals.exists(left) ? left : "";
          right = internals.exists(right) ? right : "";
          return left + right;
        }
      } else {
        switch (operator) {
          case "^":
            return Math.pow(left, right);
          case "*":
            return left * right;
          case "/":
            return left / right;
          case "%":
            return left % right;
          case "+":
            return left + right;
          case "-":
            return left - right;
        }
      }
      switch (operator) {
        case "<":
          return left < right;
        case "<=":
          return left <= right;
        case ">":
          return left > right;
        case ">=":
          return left >= right;
        case "==":
          return left === right;
        case "!=":
          return left !== right;
        case "&&":
          return left && right;
        case "||":
          return left || right;
      }
      return null;
    };
    internals.exists = function(value) {
      return value !== null && value !== void 0;
    };
  }
});

// ../node_modules/@hapi/joi/lib/annotate.js
var require_annotate = __commonJS({
  "../node_modules/@hapi/joi/lib/annotate.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Clone = require_clone();
    var Common = require_common();
    var internals = {
      annotations: /* @__PURE__ */ Symbol("annotations")
    };
    exports.error = function(stripColorCodes) {
      if (!this._original || typeof this._original !== "object") {
        return this.details[0].message;
      }
      const redFgEscape = stripColorCodes ? "" : "\x1B[31m";
      const redBgEscape = stripColorCodes ? "" : "\x1B[41m";
      const endColor = stripColorCodes ? "" : "\x1B[0m";
      const obj = Clone(this._original);
      for (let i = this.details.length - 1; i >= 0; --i) {
        const pos = i + 1;
        const error = this.details[i];
        const path = error.path;
        let node = obj;
        for (let j = 0; ; ++j) {
          const seg = path[j];
          if (Common.isSchema(node)) {
            node = node.clone();
          }
          if (j + 1 < path.length && typeof node[seg] !== "string") {
            node = node[seg];
          } else {
            const refAnnotations = node[internals.annotations] || { errors: {}, missing: {} };
            node[internals.annotations] = refAnnotations;
            const cacheKey = seg || error.context.key;
            if (node[seg] !== void 0) {
              refAnnotations.errors[cacheKey] = refAnnotations.errors[cacheKey] || [];
              refAnnotations.errors[cacheKey].push(pos);
            } else {
              refAnnotations.missing[cacheKey] = pos;
            }
            break;
          }
        }
      }
      const replacers = {
        key: /_\$key\$_([, \d]+)_\$end\$_"/g,
        missing: /"_\$miss\$_([^|]+)\|(\d+)_\$end\$_": "__missing__"/g,
        arrayIndex: /\s*"_\$idx\$_([, \d]+)_\$end\$_",?\n(.*)/g,
        specials: /"\[(NaN|Symbol.*|-?Infinity|function.*|\(.*)]"/g
      };
      let message = internals.safeStringify(obj, 2).replace(replacers.key, ($0, $1) => `" ${redFgEscape}[${$1}]${endColor}`).replace(replacers.missing, ($0, $1, $2) => `${redBgEscape}"${$1}"${endColor}${redFgEscape} [${$2}]: -- missing --${endColor}`).replace(replacers.arrayIndex, ($0, $1, $2) => `
${$2} ${redFgEscape}[${$1}]${endColor}`).replace(replacers.specials, ($0, $1) => $1);
      message = `${message}
${redFgEscape}`;
      for (let i = 0; i < this.details.length; ++i) {
        const pos = i + 1;
        message = `${message}
[${pos}] ${this.details[i].message}`;
      }
      message = message + endColor;
      return message;
    };
    internals.safeStringify = function(obj, spaces) {
      return JSON.stringify(obj, internals.serializer(), spaces);
    };
    internals.serializer = function() {
      const keys = [];
      const stack = [];
      const cycleReplacer = /* @__PURE__ */ __name((key, value) => {
        if (stack[0] === value) {
          return "[Circular ~]";
        }
        return "[Circular ~." + keys.slice(0, stack.indexOf(value)).join(".") + "]";
      }, "cycleReplacer");
      return function(key, value) {
        if (stack.length > 0) {
          const thisPos = stack.indexOf(this);
          if (~thisPos) {
            stack.length = thisPos + 1;
            keys.length = thisPos + 1;
            keys[thisPos] = key;
          } else {
            stack.push(this);
            keys.push(key);
          }
          if (~stack.indexOf(value)) {
            value = cycleReplacer.call(this, key, value);
          }
        } else {
          stack.push(value);
        }
        if (value) {
          const annotations = value[internals.annotations];
          if (annotations) {
            if (Array.isArray(value)) {
              const annotated = [];
              for (let i = 0; i < value.length; ++i) {
                if (annotations.errors[i]) {
                  annotated.push(`_$idx$_${annotations.errors[i].sort().join(", ")}_$end$_`);
                }
                annotated.push(value[i]);
              }
              value = annotated;
            } else {
              for (const errorKey in annotations.errors) {
                value[`${errorKey}_$key$_${annotations.errors[errorKey].sort().join(", ")}_$end$_`] = value[errorKey];
                value[errorKey] = void 0;
              }
              for (const missingKey in annotations.missing) {
                value[`_$miss$_${missingKey}|${annotations.missing[missingKey]}_$end$_`] = "__missing__";
              }
            }
            return value;
          }
        }
        if (value === Infinity || value === -Infinity || Number.isNaN(value) || typeof value === "function" || typeof value === "symbol") {
          return "[" + value.toString() + "]";
        }
        return value;
      };
    };
  }
});

// ../node_modules/@hapi/joi/lib/errors.js
var require_errors = __commonJS({
  "../node_modules/@hapi/joi/lib/errors.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Annotate = require_annotate();
    var Common = require_common();
    var Template = require_template();
    exports.Report = class {
      constructor(code, value, local, flags, messages, state, prefs) {
        this.code = code;
        this.flags = flags;
        this.messages = messages;
        this.path = state.path;
        this.prefs = prefs;
        this.state = state;
        this.value = value;
        this.message = null;
        this.template = null;
        this.local = local || {};
        this.local.label = exports.label(this.flags, this.state, this.prefs, this.messages);
        if (this.value !== void 0 && !this.local.hasOwnProperty("value")) {
          this.local.value = this.value;
        }
        if (this.path.length) {
          const key = this.path[this.path.length - 1];
          if (typeof key !== "object") {
            this.local.key = key;
          }
        }
      }
      _setTemplate(template) {
        this.template = template;
        if (!this.flags.label && this.path.length === 0) {
          const localized = this._template(this.template, "root");
          if (localized) {
            this.local.label = localized;
          }
        }
      }
      toString() {
        if (this.message) {
          return this.message;
        }
        const code = this.code;
        if (!this.prefs.errors.render) {
          return this.code;
        }
        const template = this._template(this.template) || this._template(this.prefs.messages) || this._template(this.messages);
        if (template === void 0) {
          return `Error code "${code}" is not defined, your custom type is missing the correct messages definition`;
        }
        this.message = template.render(this.value, this.state, this.prefs, this.local, { errors: this.prefs.errors, messages: [this.prefs.messages, this.messages] });
        if (!this.prefs.errors.label) {
          this.message = this.message.replace(/^"" /, "").trim();
        }
        return this.message;
      }
      _template(messages, code) {
        return exports.template(this.value, messages, code || this.code, this.state, this.prefs);
      }
    };
    exports.path = function(path) {
      let label = "";
      for (const segment of path) {
        if (typeof segment === "object") {
          continue;
        }
        if (typeof segment === "string") {
          if (label) {
            label += ".";
          }
          label += segment;
        } else {
          label += `[${segment}]`;
        }
      }
      return label;
    };
    exports.template = function(value, messages, code, state, prefs) {
      if (!messages) {
        return;
      }
      if (Template.isTemplate(messages)) {
        return code !== "root" ? messages : null;
      }
      let lang = prefs.errors.language;
      if (Common.isResolvable(lang)) {
        lang = lang.resolve(value, state, prefs);
      }
      if (lang && messages[lang] && messages[lang][code] !== void 0) {
        return messages[lang][code];
      }
      return messages[code];
    };
    exports.label = function(flags, state, prefs, messages) {
      if (flags.label) {
        return flags.label;
      }
      if (!prefs.errors.label) {
        return "";
      }
      let path = state.path;
      if (prefs.errors.label === "key" && state.path.length > 1) {
        path = state.path.slice(-1);
      }
      const normalized = exports.path(path);
      if (normalized) {
        return normalized;
      }
      return exports.template(null, prefs.messages, "root", state, prefs) || messages && exports.template(null, messages, "root", state, prefs) || "value";
    };
    exports.process = function(errors, original, prefs) {
      if (!errors) {
        return null;
      }
      const { override, message, details } = exports.details(errors);
      if (override) {
        return override;
      }
      if (prefs.errors.stack) {
        return new exports.ValidationError(message, details, original);
      }
      const limit = Error.stackTraceLimit;
      Error.stackTraceLimit = 0;
      const validationError = new exports.ValidationError(message, details, original);
      Error.stackTraceLimit = limit;
      return validationError;
    };
    exports.details = function(errors, options = {}) {
      let messages = [];
      const details = [];
      for (const item of errors) {
        if (item instanceof Error) {
          if (options.override !== false) {
            return { override: item };
          }
          const message2 = item.toString();
          messages.push(message2);
          details.push({
            message: message2,
            type: "override",
            context: { error: item }
          });
          continue;
        }
        const message = item.toString();
        messages.push(message);
        details.push({
          message,
          path: item.path.filter((v) => typeof v !== "object"),
          type: item.code,
          context: item.local
        });
      }
      if (messages.length > 1) {
        messages = [...new Set(messages)];
      }
      return { message: messages.join(". "), details };
    };
    exports.ValidationError = class extends Error {
      constructor(message, details, original) {
        super(message);
        this._original = original;
        this.details = details;
      }
      static isError(err) {
        return err instanceof exports.ValidationError;
      }
    };
    exports.ValidationError.prototype.isJoi = true;
    exports.ValidationError.prototype.name = "ValidationError";
    exports.ValidationError.prototype.annotate = Annotate.error;
  }
});

// ../node_modules/@hapi/joi/lib/ref.js
var require_ref = __commonJS({
  "../node_modules/@hapi/joi/lib/ref.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Clone = require_clone();
    var Reach = require_reach();
    var Common = require_common();
    var Template;
    var internals = {
      symbol: /* @__PURE__ */ Symbol("ref"),
      // Used to internally identify references (shared with other joi versions)
      defaults: {
        adjust: null,
        in: false,
        iterables: null,
        map: null,
        separator: ".",
        type: "value"
      }
    };
    exports.create = function(key, options = {}) {
      Assert(typeof key === "string", "Invalid reference key:", key);
      Common.assertOptions(options, ["adjust", "ancestor", "in", "iterables", "map", "prefix", "separator"]);
      Assert(!options.prefix || typeof options.prefix === "object", "options.prefix must be of type object");
      const ref = Object.assign({}, internals.defaults, options);
      delete ref.prefix;
      const separator = ref.separator;
      const context = internals.context(key, separator, options.prefix);
      ref.type = context.type;
      key = context.key;
      if (ref.type === "value") {
        if (context.root) {
          Assert(!separator || key[0] !== separator, "Cannot specify relative path with root prefix");
          ref.ancestor = "root";
          if (!key) {
            key = null;
          }
        }
        if (separator && separator === key) {
          key = null;
          ref.ancestor = 0;
        } else {
          if (ref.ancestor !== void 0) {
            Assert(!separator || !key || key[0] !== separator, "Cannot combine prefix with ancestor option");
          } else {
            const [ancestor, slice] = internals.ancestor(key, separator);
            if (slice) {
              key = key.slice(slice);
              if (key === "") {
                key = null;
              }
            }
            ref.ancestor = ancestor;
          }
        }
      }
      ref.path = separator ? key === null ? [] : key.split(separator) : [key];
      return new internals.Ref(ref);
    };
    exports.in = function(key, options = {}) {
      return exports.create(key, Object.assign({}, options, { in: true }));
    };
    exports.isRef = function(ref) {
      return ref ? !!ref[Common.symbols.ref] : false;
    };
    internals.Ref = class {
      constructor(options) {
        Assert(typeof options === "object", "Invalid reference construction");
        Common.assertOptions(options, [
          "adjust",
          "ancestor",
          "in",
          "iterables",
          "map",
          "path",
          "separator",
          "type",
          // Copied
          "depth",
          "key",
          "root",
          "display"
          // Overridden
        ]);
        Assert([false, void 0].includes(options.separator) || typeof options.separator === "string" && options.separator.length === 1, "Invalid separator");
        Assert(!options.adjust || typeof options.adjust === "function", "options.adjust must be a function");
        Assert(!options.map || Array.isArray(options.map), "options.map must be an array");
        Assert(!options.map || !options.adjust, "Cannot set both map and adjust options");
        Object.assign(this, internals.defaults, options);
        Assert(this.type === "value" || this.ancestor === void 0, "Non-value references cannot reference ancestors");
        if (Array.isArray(this.map)) {
          this.map = new Map(this.map);
        }
        this.depth = this.path.length;
        this.key = this.path.length ? this.path.join(this.separator) : null;
        this.root = this.path[0];
        this.updateDisplay();
      }
      resolve(value, state, prefs, local, options = {}) {
        Assert(!this.in || options.in, "Invalid in() reference usage");
        if (this.type === "global") {
          return this._resolve(prefs.context, state, options);
        }
        if (this.type === "local") {
          return this._resolve(local, state, options);
        }
        if (!this.ancestor) {
          return this._resolve(value, state, options);
        }
        if (this.ancestor === "root") {
          return this._resolve(state.ancestors[state.ancestors.length - 1], state, options);
        }
        Assert(this.ancestor <= state.ancestors.length, "Invalid reference exceeds the schema root:", this.display);
        return this._resolve(state.ancestors[this.ancestor - 1], state, options);
      }
      _resolve(target, state, options) {
        let resolved;
        if (this.type === "value" && state.mainstay.shadow && options.shadow !== false) {
          resolved = state.mainstay.shadow.get(this.absolute(state));
        }
        if (resolved === void 0) {
          resolved = Reach(target, this.path, { iterables: this.iterables, functions: true });
        }
        if (this.adjust) {
          resolved = this.adjust(resolved);
        }
        if (this.map) {
          const mapped = this.map.get(resolved);
          if (mapped !== void 0) {
            resolved = mapped;
          }
        }
        if (state.mainstay) {
          state.mainstay.tracer.resolve(state, this, resolved);
        }
        return resolved;
      }
      toString() {
        return this.display;
      }
      absolute(state) {
        return [...state.path.slice(0, -this.ancestor), ...this.path];
      }
      clone() {
        return new internals.Ref(this);
      }
      describe() {
        const ref = { path: this.path };
        if (this.type !== "value") {
          ref.type = this.type;
        }
        if (this.separator !== ".") {
          ref.separator = this.separator;
        }
        if (this.type === "value" && this.ancestor !== 1) {
          ref.ancestor = this.ancestor;
        }
        if (this.map) {
          ref.map = [...this.map];
        }
        for (const key of ["adjust", "iterables"]) {
          if (this[key] !== null) {
            ref[key] = this[key];
          }
        }
        if (this.in !== false) {
          ref.in = true;
        }
        return { ref };
      }
      updateDisplay() {
        const key = this.key !== null ? this.key : "";
        if (this.type !== "value") {
          this.display = `ref:${this.type}:${key}`;
          return;
        }
        if (!this.separator) {
          this.display = `ref:${key}`;
          return;
        }
        if (!this.ancestor) {
          this.display = `ref:${this.separator}${key}`;
          return;
        }
        if (this.ancestor === "root") {
          this.display = `ref:root:${key}`;
          return;
        }
        if (this.ancestor === 1) {
          this.display = `ref:${key || ".."}`;
          return;
        }
        const lead = new Array(this.ancestor + 1).fill(this.separator).join("");
        this.display = `ref:${lead}${key || ""}`;
      }
    };
    internals.Ref.prototype[Common.symbols.ref] = true;
    exports.build = function(desc) {
      desc = Object.assign({}, internals.defaults, desc);
      if (desc.type === "value" && desc.ancestor === void 0) {
        desc.ancestor = 1;
      }
      return new internals.Ref(desc);
    };
    internals.context = function(key, separator, prefix = {}) {
      key = key.trim();
      if (prefix) {
        const globalp = prefix.global === void 0 ? "$" : prefix.global;
        if (globalp !== separator && key.startsWith(globalp)) {
          return { key: key.slice(globalp.length), type: "global" };
        }
        const local = prefix.local === void 0 ? "#" : prefix.local;
        if (local !== separator && key.startsWith(local)) {
          return { key: key.slice(local.length), type: "local" };
        }
        const root = prefix.root === void 0 ? "/" : prefix.root;
        if (root !== separator && key.startsWith(root)) {
          return { key: key.slice(root.length), type: "value", root: true };
        }
      }
      return { key, type: "value" };
    };
    internals.ancestor = function(key, separator) {
      if (!separator) {
        return [1, 0];
      }
      if (key[0] !== separator) {
        return [1, 0];
      }
      if (key[1] !== separator) {
        return [0, 1];
      }
      let i = 2;
      while (key[i] === separator) {
        ++i;
      }
      return [i - 1, i];
    };
    exports.toSibling = 0;
    exports.toParent = 1;
    exports.Manager = class {
      constructor() {
        this.refs = [];
      }
      register(source, target) {
        if (!source) {
          return;
        }
        target = target === void 0 ? exports.toParent : target;
        if (Array.isArray(source)) {
          for (const ref of source) {
            this.register(ref, target);
          }
          return;
        }
        if (Common.isSchema(source)) {
          for (const item of source._refs.refs) {
            if (item.ancestor - target >= 0) {
              this.refs.push({ ancestor: item.ancestor - target, root: item.root });
            }
          }
          return;
        }
        if (exports.isRef(source) && source.type === "value" && source.ancestor - target >= 0) {
          this.refs.push({ ancestor: source.ancestor - target, root: source.root });
        }
        Template = Template || require_template();
        if (Template.isTemplate(source)) {
          this.register(source.refs(), target);
        }
      }
      get length() {
        return this.refs.length;
      }
      clone() {
        const copy = new exports.Manager();
        copy.refs = Clone(this.refs);
        return copy;
      }
      reset() {
        this.refs = [];
      }
      roots() {
        return this.refs.filter((ref) => !ref.ancestor).map((ref) => ref.root);
      }
    };
  }
});

// ../node_modules/@hapi/joi/lib/template.js
var require_template = __commonJS({
  "../node_modules/@hapi/joi/lib/template.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Clone = require_clone();
    var EscapeHtml = require_escapeHtml();
    var Formula = require_lib();
    var Common = require_common();
    var Errors = require_errors();
    var Ref = require_ref();
    var internals = {
      symbol: /* @__PURE__ */ Symbol("template"),
      opens: new Array(1e3).join("\0"),
      closes: new Array(1e3).join(""),
      dateFormat: {
        date: Date.prototype.toDateString,
        iso: Date.prototype.toISOString,
        string: Date.prototype.toString,
        time: Date.prototype.toTimeString,
        utc: Date.prototype.toUTCString
      }
    };
    module.exports = exports = internals.Template = class {
      constructor(source, options) {
        Assert(typeof source === "string", "Template source must be a string");
        Assert(!source.includes("\0") && !source.includes(""), "Template source cannot contain reserved control characters");
        this.source = source;
        this.rendered = source;
        this._template = null;
        this._settings = Clone(options);
        this._parse();
      }
      _parse() {
        if (!this.source.includes("{")) {
          return;
        }
        const encoded = internals.encode(this.source);
        const parts = internals.split(encoded);
        let refs = false;
        const processed = [];
        const head = parts.shift();
        if (head) {
          processed.push(head);
        }
        for (const part of parts) {
          const raw = part[0] !== "{";
          const ender = raw ? "}" : "}}";
          const end = part.indexOf(ender);
          if (end === -1 || // Ignore non-matching closing
          part[1] === "{") {
            processed.push(`{${internals.decode(part)}`);
            continue;
          }
          const variable = part.slice(raw ? 0 : 1, end);
          const dynamic = this._ref(internals.decode(variable), raw);
          processed.push(dynamic);
          if (typeof dynamic !== "string") {
            refs = true;
          }
          const rest = part.slice(end + ender.length);
          if (rest) {
            processed.push(internals.decode(rest));
          }
        }
        if (!refs) {
          this.rendered = processed.join("");
          return;
        }
        this._template = processed;
      }
      static date(date, prefs) {
        return internals.dateFormat[prefs.dateFormat].call(date);
      }
      describe(options = {}) {
        if (!this._settings && options.compact) {
          return this.source;
        }
        const desc = { template: this.source };
        if (this._settings) {
          desc.options = this._settings;
        }
        return desc;
      }
      static build(desc) {
        return new internals.Template(desc.template, desc.options);
      }
      isDynamic() {
        return !!this._template;
      }
      static isTemplate(template) {
        return template ? !!template[Common.symbols.template] : false;
      }
      refs() {
        if (!this._template) {
          return;
        }
        const refs = [];
        for (const part of this._template) {
          if (typeof part !== "string") {
            refs.push(...part.refs);
          }
        }
        return refs;
      }
      resolve(value, state, prefs, local) {
        if (this._template && this._template.length === 1) {
          return this._part(
            this._template[0],
            /* context -> [*/
            value,
            state,
            prefs,
            local,
            {}
            /*] */
          );
        }
        return this.render(value, state, prefs, local);
      }
      _part(part, ...args) {
        if (part.ref) {
          return part.ref.resolve(...args);
        }
        return part.formula.evaluate(args);
      }
      render(value, state, prefs, local, options = {}) {
        if (!this.isDynamic()) {
          return this.rendered;
        }
        const parts = [];
        for (const part of this._template) {
          if (typeof part === "string") {
            parts.push(part);
          } else {
            const rendered = this._part(
              part,
              /* context -> [*/
              value,
              state,
              prefs,
              local,
              options
              /*] */
            );
            const string2 = internals.stringify(rendered, prefs, options.errors);
            if (string2 !== void 0) {
              const result = part.raw || (options.errors && options.errors.escapeHtml) === false ? string2 : EscapeHtml(string2);
              const ends = part.ref && part.ref.type === "local" && part.ref.key === "label" && prefs.errors.wrap.label;
              parts.push(internals.wrap(result, ends));
            }
          }
        }
        return parts.join("");
      }
      _ref(content, raw) {
        const refs = [];
        const reference = /* @__PURE__ */ __name((variable) => {
          const ref = Ref.create(variable, this._settings);
          refs.push(ref);
          return (context) => ref.resolve(...context);
        }, "reference");
        try {
          var formula = new Formula.Parser(content, { reference, functions: internals.functions, constants: internals.constants });
        } catch (err) {
          err.message = `Invalid template variable "${content}" fails due to: ${err.message}`;
          throw err;
        }
        if (formula.single) {
          if (formula.single.type === "reference") {
            return { ref: refs[0], raw, refs };
          }
          return internals.stringify(formula.single.value);
        }
        return { formula, raw, refs };
      }
      toString() {
        return this.source;
      }
    };
    internals.Template.prototype[Common.symbols.template] = true;
    internals.Template.prototype.isImmutable = true;
    internals.encode = function(string2) {
      return string2.replace(/\\(\{+)/g, ($0, $1) => {
        return internals.opens.slice(0, $1.length);
      }).replace(/\\(\}+)/g, ($0, $1) => {
        return internals.closes.slice(0, $1.length);
      });
    };
    internals.decode = function(string2) {
      return string2.replace(/\u0000/g, "{").replace(/\u0001/g, "}");
    };
    internals.split = function(string2) {
      const parts = [];
      let current = "";
      for (let i = 0; i < string2.length; ++i) {
        const char = string2[i];
        if (char === "{") {
          let next = "";
          while (i + 1 < string2.length && string2[i + 1] === "{") {
            next += "{";
            ++i;
          }
          parts.push(current);
          current = next;
        } else {
          current += char;
        }
      }
      parts.push(current);
      return parts;
    };
    internals.wrap = function(value, ends) {
      if (!ends) {
        return value;
      }
      if (ends.length === 1) {
        return `${ends}${value}${ends}`;
      }
      return `${ends[0]}${value}${ends[1]}`;
    };
    internals.stringify = function(value, prefs, options) {
      const type = typeof value;
      if (value === null) {
        return "null";
      }
      if (type === "string") {
        return value;
      }
      if (type === "number" || type === "function" || type === "symbol") {
        return value.toString();
      }
      if (type !== "object") {
        return JSON.stringify(value);
      }
      if (value instanceof Date) {
        return internals.Template.date(value, prefs);
      }
      if (value instanceof Map) {
        const pairs = [];
        for (const [key, sym] of value.entries()) {
          pairs.push(`${key.toString()} -> ${sym.toString()}`);
        }
        value = pairs;
      }
      if (!Array.isArray(value)) {
        return value.toString();
      }
      let partial = "";
      for (const item of value) {
        partial = partial + (partial.length ? ", " : "") + internals.stringify(item, prefs, options);
      }
      return internals.wrap(partial, prefs.errors.wrap.array);
    };
    internals.constants = {
      true: true,
      false: false,
      null: null,
      second: 1e3,
      minute: 60 * 1e3,
      hour: 60 * 60 * 1e3,
      day: 24 * 60 * 60 * 1e3
    };
    internals.functions = {
      if(condition, then, otherwise) {
        return condition ? then : otherwise;
      },
      msg(code) {
        const [value, state, prefs, local, options] = this;
        const messages = options.messages;
        if (!messages) {
          return "";
        }
        const template = Errors.template(value, messages[0], code, state, prefs) || Errors.template(value, messages[1], code, state, prefs);
        if (!template) {
          return "";
        }
        return template.render(value, state, prefs, local, options);
      },
      number(value) {
        if (typeof value === "number") {
          return value;
        }
        if (typeof value === "string") {
          return parseFloat(value);
        }
        if (typeof value === "boolean") {
          return value ? 1 : 0;
        }
        if (value instanceof Date) {
          return value.getTime();
        }
        return null;
      }
    };
  }
});

// ../node_modules/@hapi/joi/lib/messages.js
var require_messages = __commonJS({
  "../node_modules/@hapi/joi/lib/messages.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Clone = require_clone();
    var Template = require_template();
    exports.compile = function(messages, target) {
      if (typeof messages === "string") {
        Assert(!target, "Cannot set single message string");
        return new Template(messages);
      }
      if (Template.isTemplate(messages)) {
        Assert(!target, "Cannot set single message template");
        return messages;
      }
      Assert(typeof messages === "object" && !Array.isArray(messages), "Invalid message options");
      target = target ? Clone(target) : {};
      for (let code in messages) {
        const message = messages[code];
        if (code === "root" || Template.isTemplate(message)) {
          target[code] = message;
          continue;
        }
        if (typeof message === "string") {
          target[code] = new Template(message);
          continue;
        }
        Assert(typeof message === "object" && !Array.isArray(message), "Invalid message for", code);
        const language = code;
        target[language] = target[language] || {};
        for (code in message) {
          const localized = message[code];
          if (code === "root" || Template.isTemplate(localized)) {
            target[language][code] = localized;
            continue;
          }
          Assert(typeof localized === "string", "Invalid message for", code, "in", language);
          target[language][code] = new Template(localized);
        }
      }
      return target;
    };
    exports.decompile = function(messages) {
      const target = {};
      for (let code in messages) {
        const message = messages[code];
        if (code === "root") {
          target[code] = message;
          continue;
        }
        if (Template.isTemplate(message)) {
          target[code] = message.describe({ compact: true });
          continue;
        }
        const language = code;
        target[language] = {};
        for (code in message) {
          const localized = message[code];
          if (code === "root") {
            target[language][code] = localized;
            continue;
          }
          target[language][code] = localized.describe({ compact: true });
        }
      }
      return target;
    };
    exports.merge = function(base, extended) {
      if (!base) {
        return exports.compile(extended);
      }
      if (!extended) {
        return base;
      }
      if (typeof extended === "string") {
        return new Template(extended);
      }
      if (Template.isTemplate(extended)) {
        return extended;
      }
      const target = Clone(base);
      for (let code in extended) {
        const message = extended[code];
        if (code === "root" || Template.isTemplate(message)) {
          target[code] = message;
          continue;
        }
        if (typeof message === "string") {
          target[code] = new Template(message);
          continue;
        }
        Assert(typeof message === "object" && !Array.isArray(message), "Invalid message for", code);
        const language = code;
        target[language] = target[language] || {};
        for (code in message) {
          const localized = message[code];
          if (code === "root" || Template.isTemplate(localized)) {
            target[language][code] = localized;
            continue;
          }
          Assert(typeof localized === "string", "Invalid message for", code, "in", language);
          target[language][code] = new Template(localized);
        }
      }
      return target;
    };
  }
});

// ../node_modules/@hapi/joi/lib/common.js
var require_common = __commonJS({
  "../node_modules/@hapi/joi/lib/common.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var AssertError = require_error();
    var Pkg = require_package();
    var Messages;
    var Schemas;
    var internals = {
      isoDate: /^(?:[-+]\d{2})?(?:\d{4}(?!\d{2}\b))(?:(-?)(?:(?:0[1-9]|1[0-2])(?:\1(?:[12]\d|0[1-9]|3[01]))?|W(?:[0-4]\d|5[0-2])(?:-?[1-7])?|(?:00[1-9]|0[1-9]\d|[12]\d{2}|3(?:[0-5]\d|6[1-6])))(?![T]$|[T][\d]+Z$)(?:[T\s](?:(?:(?:[01]\d|2[0-3])(?:(:?)[0-5]\d)?|24\:?00)(?:[.,]\d+(?!:))?)(?:\2[0-5]\d(?:[.,]\d+)?)?(?:[Z]|(?:[+-])(?:[01]\d|2[0-3])(?::?[0-5]\d)?)?)?)?$/
    };
    exports.version = Pkg.version;
    exports.defaults = {
      abortEarly: true,
      allowUnknown: false,
      cache: true,
      context: null,
      convert: true,
      dateFormat: "iso",
      errors: {
        escapeHtml: false,
        label: "path",
        language: null,
        render: true,
        stack: false,
        wrap: {
          label: '"',
          array: "[]"
        }
      },
      externals: true,
      messages: {},
      nonEnumerables: false,
      noDefaults: false,
      presence: "optional",
      skipFunctions: false,
      stripUnknown: false,
      warnings: false
    };
    exports.symbols = {
      any: /* @__PURE__ */ Symbol.for("@hapi/joi/schema"),
      // Used to internally identify any-based types (shared with other joi versions)
      arraySingle: /* @__PURE__ */ Symbol("arraySingle"),
      deepDefault: /* @__PURE__ */ Symbol("deepDefault"),
      literal: /* @__PURE__ */ Symbol("literal"),
      override: /* @__PURE__ */ Symbol("override"),
      prefs: /* @__PURE__ */ Symbol("prefs"),
      ref: /* @__PURE__ */ Symbol("ref"),
      values: /* @__PURE__ */ Symbol("values"),
      template: /* @__PURE__ */ Symbol("template")
    };
    exports.assertOptions = function(options, keys, name = "Options") {
      Assert(options && typeof options === "object" && !Array.isArray(options), "Options must be of type object");
      const unknownKeys = Object.keys(options).filter((k) => !keys.includes(k));
      Assert(unknownKeys.length === 0, `${name} contain unknown keys: ${unknownKeys}`);
    };
    exports.checkPreferences = function(prefs) {
      Schemas = Schemas || require_schemas();
      const result = Schemas.preferences.validate(prefs);
      if (result.error) {
        throw new AssertError([result.error.details[0].message]);
      }
    };
    exports.compare = function(a, b, operator) {
      switch (operator) {
        case "=":
          return a === b;
        case ">":
          return a > b;
        case "<":
          return a < b;
        case ">=":
          return a >= b;
        case "<=":
          return a <= b;
      }
    };
    exports.default = function(value, defaultValue) {
      return value === void 0 ? defaultValue : value;
    };
    exports.isIsoDate = function(date) {
      return internals.isoDate.test(date);
    };
    exports.isNumber = function(value) {
      return typeof value === "number" && !isNaN(value);
    };
    exports.isResolvable = function(obj) {
      if (!obj) {
        return false;
      }
      return obj[exports.symbols.ref] || obj[exports.symbols.template];
    };
    exports.isSchema = function(schema, options = {}) {
      const any = schema && schema[exports.symbols.any];
      if (!any) {
        return false;
      }
      Assert(options.legacy || any.version === exports.version, "Cannot mix different versions of joi schemas");
      return true;
    };
    exports.isValues = function(obj) {
      return obj[exports.symbols.values];
    };
    exports.limit = function(value) {
      return Number.isSafeInteger(value) && value >= 0;
    };
    exports.preferences = function(target, source) {
      Messages = Messages || require_messages();
      target = target || {};
      source = source || {};
      const merged = Object.assign({}, target, source);
      if (source.errors && target.errors) {
        merged.errors = Object.assign({}, target.errors, source.errors);
        merged.errors.wrap = Object.assign({}, target.errors.wrap, source.errors.wrap);
      }
      if (source.messages) {
        merged.messages = Messages.compile(source.messages, target.messages);
      }
      delete merged[exports.symbols.prefs];
      return merged;
    };
    exports.tryWithPath = function(fn, key, options = {}) {
      try {
        return fn();
      } catch (err) {
        if (err.path !== void 0) {
          err.path = key + "." + err.path;
        } else {
          err.path = key;
        }
        if (options.append) {
          err.message = `${err.message} (${err.path})`;
        }
        throw err;
      }
    };
    exports.validateArg = function(value, label, { assert, message }) {
      if (exports.isSchema(assert)) {
        const result = assert.validate(value);
        if (!result.error) {
          return;
        }
        return result.error.message;
      } else if (!assert(value)) {
        return label ? `${label} ${message}` : message;
      }
    };
    exports.verifyFlat = function(args, method) {
      for (const arg of args) {
        Assert(!Array.isArray(arg), "Method no longer accepts array arguments:", method);
      }
    };
  }
});

// ../node_modules/@hapi/joi/lib/cache.js
var require_cache = __commonJS({
  "../node_modules/@hapi/joi/lib/cache.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Clone = require_clone();
    var Common = require_common();
    var internals = {
      max: 1e3,
      supported: /* @__PURE__ */ new Set(["undefined", "boolean", "number", "string"])
    };
    exports.provider = {
      provision(options) {
        return new internals.Cache(options);
      }
    };
    internals.Cache = class {
      constructor(options = {}) {
        Common.assertOptions(options, ["max"]);
        Assert(options.max === void 0 || options.max && options.max > 0 && isFinite(options.max), "Invalid max cache size");
        this._max = options.max || internals.max;
        this._map = /* @__PURE__ */ new Map();
        this._list = new internals.List();
      }
      get length() {
        return this._map.size;
      }
      set(key, value) {
        if (key !== null && !internals.supported.has(typeof key)) {
          return;
        }
        let node = this._map.get(key);
        if (node) {
          node.value = value;
          this._list.first(node);
          return;
        }
        node = this._list.unshift({ key, value });
        this._map.set(key, node);
        this._compact();
      }
      get(key) {
        const node = this._map.get(key);
        if (node) {
          this._list.first(node);
          return Clone(node.value);
        }
      }
      _compact() {
        if (this._map.size > this._max) {
          const node = this._list.pop();
          this._map.delete(node.key);
        }
      }
    };
    internals.List = class {
      constructor() {
        this.tail = null;
        this.head = null;
      }
      unshift(node) {
        node.next = null;
        node.prev = this.head;
        if (this.head) {
          this.head.next = node;
        }
        this.head = node;
        if (!this.tail) {
          this.tail = node;
        }
        return node;
      }
      first(node) {
        if (node === this.head) {
          return;
        }
        this._remove(node);
        this.unshift(node);
      }
      pop() {
        return this._remove(this.tail);
      }
      _remove(node) {
        const { next, prev } = node;
        next.prev = prev;
        if (prev) {
          prev.next = next;
        }
        if (node === this.tail) {
          this.tail = next;
        }
        node.prev = null;
        node.next = null;
        return node;
      }
    };
  }
});

// ../node_modules/@hapi/joi/lib/compile.js
var require_compile = __commonJS({
  "../node_modules/@hapi/joi/lib/compile.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Common = require_common();
    var Ref = require_ref();
    var internals = {};
    exports.schema = function(Joi2, config2, options = {}) {
      Common.assertOptions(options, ["appendPath", "override"]);
      try {
        return internals.schema(Joi2, config2, options);
      } catch (err) {
        if (options.appendPath && err.path !== void 0) {
          err.message = `${err.message} (${err.path})`;
        }
        throw err;
      }
    };
    internals.schema = function(Joi2, config2, options) {
      Assert(config2 !== void 0, "Invalid undefined schema");
      if (Array.isArray(config2)) {
        Assert(config2.length, "Invalid empty array schema");
        if (config2.length === 1) {
          config2 = config2[0];
        }
      }
      const valid = /* @__PURE__ */ __name((base, ...values) => {
        if (options.override !== false) {
          return base.valid(Joi2.override, ...values);
        }
        return base.valid(...values);
      }, "valid");
      if (internals.simple(config2)) {
        return valid(Joi2, config2);
      }
      if (typeof config2 === "function") {
        return Joi2.custom(config2);
      }
      Assert(typeof config2 === "object", "Invalid schema content:", typeof config2);
      if (Common.isResolvable(config2)) {
        return valid(Joi2, config2);
      }
      if (Common.isSchema(config2)) {
        return config2;
      }
      if (Array.isArray(config2)) {
        for (const item of config2) {
          if (!internals.simple(item)) {
            return Joi2.alternatives().try(...config2);
          }
        }
        return valid(Joi2, ...config2);
      }
      if (config2 instanceof RegExp) {
        return Joi2.string().regex(config2);
      }
      if (config2 instanceof Date) {
        return valid(Joi2.date(), config2);
      }
      Assert(Object.getPrototypeOf(config2) === Object.getPrototypeOf({}), "Schema can only contain plain objects");
      return Joi2.object().keys(config2);
    };
    exports.ref = function(id, options) {
      return Ref.isRef(id) ? id : Ref.create(id, options);
    };
    exports.compile = function(root, schema, options = {}) {
      Common.assertOptions(options, ["legacy"]);
      const any = schema && schema[Common.symbols.any];
      if (any) {
        Assert(options.legacy || any.version === Common.version, "Cannot mix different versions of joi schemas:", any.version, Common.version);
        return schema;
      }
      if (typeof schema !== "object" || !options.legacy) {
        return exports.schema(root, schema, { appendPath: true });
      }
      const compiler = internals.walk(schema);
      if (!compiler) {
        return exports.schema(root, schema, { appendPath: true });
      }
      return compiler.compile(compiler.root, schema);
    };
    internals.walk = function(schema) {
      if (typeof schema !== "object") {
        return null;
      }
      if (Array.isArray(schema)) {
        for (const item of schema) {
          const compiler = internals.walk(item);
          if (compiler) {
            return compiler;
          }
        }
        return null;
      }
      const any = schema[Common.symbols.any];
      if (any) {
        return { root: schema[any.root], compile: any.compile };
      }
      Assert(Object.getPrototypeOf(schema) === Object.getPrototypeOf({}), "Schema can only contain plain objects");
      for (const key in schema) {
        const compiler = internals.walk(schema[key]);
        if (compiler) {
          return compiler;
        }
      }
      return null;
    };
    internals.simple = function(value) {
      return value === null || ["boolean", "string", "number"].includes(typeof value);
    };
    exports.when = function(schema, condition, options) {
      if (options === void 0) {
        Assert(condition && typeof condition === "object", "Missing options");
        options = condition;
        condition = Ref.create(".");
      }
      if (Array.isArray(options)) {
        options = { switch: options };
      }
      Common.assertOptions(options, ["is", "not", "then", "otherwise", "switch", "break"]);
      if (Common.isSchema(condition)) {
        Assert(options.is === void 0, '"is" can not be used with a schema condition');
        Assert(options.not === void 0, '"not" can not be used with a schema condition');
        Assert(options.switch === void 0, '"switch" can not be used with a schema condition');
        return internals.condition(schema, { is: condition, then: options.then, otherwise: options.otherwise, break: options.break });
      }
      Assert(Ref.isRef(condition) || typeof condition === "string", "Invalid condition:", condition);
      Assert(options.not === void 0 || options.is === void 0, 'Cannot combine "is" with "not"');
      if (options.switch === void 0) {
        let rule2 = options;
        if (options.not !== void 0) {
          rule2 = { is: options.not, then: options.otherwise, otherwise: options.then, break: options.break };
        }
        let is = rule2.is !== void 0 ? schema.$_compile(rule2.is) : schema.$_root.invalid(null, false, 0, "").required();
        Assert(rule2.then !== void 0 || rule2.otherwise !== void 0, 'options must have at least one of "then", "otherwise", or "switch"');
        Assert(rule2.break === void 0 || rule2.then === void 0 || rule2.otherwise === void 0, "Cannot specify then, otherwise, and break all together");
        if (options.is !== void 0 && !Ref.isRef(options.is) && !Common.isSchema(options.is)) {
          is = is.required();
        }
        return internals.condition(schema, { ref: exports.ref(condition), is, then: rule2.then, otherwise: rule2.otherwise, break: rule2.break });
      }
      Assert(Array.isArray(options.switch), '"switch" must be an array');
      Assert(options.is === void 0, 'Cannot combine "switch" with "is"');
      Assert(options.not === void 0, 'Cannot combine "switch" with "not"');
      Assert(options.then === void 0, 'Cannot combine "switch" with "then"');
      const rule = {
        ref: exports.ref(condition),
        switch: [],
        break: options.break
      };
      for (let i = 0; i < options.switch.length; ++i) {
        const test = options.switch[i];
        const last = i === options.switch.length - 1;
        Common.assertOptions(test, last ? ["is", "then", "otherwise"] : ["is", "then"]);
        Assert(test.is !== void 0, 'Switch statement missing "is"');
        Assert(test.then !== void 0, 'Switch statement missing "then"');
        const item = {
          is: schema.$_compile(test.is),
          then: schema.$_compile(test.then)
        };
        if (!Ref.isRef(test.is) && !Common.isSchema(test.is)) {
          item.is = item.is.required();
        }
        if (last) {
          Assert(options.otherwise === void 0 || test.otherwise === void 0, 'Cannot specify "otherwise" inside and outside a "switch"');
          const otherwise = options.otherwise !== void 0 ? options.otherwise : test.otherwise;
          if (otherwise !== void 0) {
            Assert(rule.break === void 0, "Cannot specify both otherwise and break");
            item.otherwise = schema.$_compile(otherwise);
          }
        }
        rule.switch.push(item);
      }
      return rule;
    };
    internals.condition = function(schema, condition) {
      for (const key of ["then", "otherwise"]) {
        if (condition[key] === void 0) {
          delete condition[key];
        } else {
          condition[key] = schema.$_compile(condition[key]);
        }
      }
      return condition;
    };
  }
});

// ../node_modules/@hapi/joi/lib/extend.js
var require_extend = __commonJS({
  "../node_modules/@hapi/joi/lib/extend.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Clone = require_clone();
    var Common = require_common();
    var Messages = require_messages();
    var internals = {};
    exports.type = function(from, options) {
      const base = Object.getPrototypeOf(from);
      const prototype = Clone(base);
      const schema = from._assign(Object.create(prototype));
      const def = Object.assign({}, options);
      delete def.base;
      prototype._definition = def;
      const parent = base._definition || {};
      def.messages = Messages.merge(parent.messages, def.messages);
      def.properties = Object.assign({}, parent.properties, def.properties);
      schema.type = def.type;
      def.flags = Object.assign({}, parent.flags, def.flags);
      const terms = Object.assign({}, parent.terms);
      if (def.terms) {
        for (const name in def.terms) {
          const term = def.terms[name];
          Assert(schema.$_terms[name] === void 0, "Invalid term override for", def.type, name);
          schema.$_terms[name] = term.init;
          terms[name] = term;
        }
      }
      def.terms = terms;
      if (!def.args) {
        def.args = parent.args;
      }
      def.prepare = internals.prepare(def.prepare, parent.prepare);
      if (def.coerce) {
        if (typeof def.coerce === "function") {
          def.coerce = { method: def.coerce };
        }
        if (def.coerce.from && !Array.isArray(def.coerce.from)) {
          def.coerce = { method: def.coerce.method, from: [].concat(def.coerce.from) };
        }
      }
      def.coerce = internals.coerce(def.coerce, parent.coerce);
      def.validate = internals.validate(def.validate, parent.validate);
      const rules = Object.assign({}, parent.rules);
      if (def.rules) {
        for (const name in def.rules) {
          const rule = def.rules[name];
          Assert(typeof rule === "object", "Invalid rule definition for", def.type, name);
          let method = rule.method;
          if (method === void 0) {
            method = /* @__PURE__ */ __name(function() {
              return this.$_addRule(name);
            }, "method");
          }
          if (method) {
            Assert(!prototype[name], "Rule conflict in", def.type, name);
            prototype[name] = method;
          }
          Assert(!rules[name], "Rule conflict in", def.type, name);
          rules[name] = rule;
          if (rule.alias) {
            const aliases = [].concat(rule.alias);
            for (const alias of aliases) {
              prototype[alias] = rule.method;
            }
          }
          if (rule.args) {
            rule.argsByName = /* @__PURE__ */ new Map();
            rule.args = rule.args.map((arg) => {
              if (typeof arg === "string") {
                arg = { name: arg };
              }
              Assert(!rule.argsByName.has(arg.name), "Duplicated argument name", arg.name);
              if (Common.isSchema(arg.assert)) {
                arg.assert = arg.assert.strict().label(arg.name);
              }
              rule.argsByName.set(arg.name, arg);
              return arg;
            });
          }
        }
      }
      def.rules = rules;
      const modifiers = Object.assign({}, parent.modifiers);
      if (def.modifiers) {
        for (const name in def.modifiers) {
          Assert(!prototype[name], "Rule conflict in", def.type, name);
          const modifier = def.modifiers[name];
          Assert(typeof modifier === "function", "Invalid modifier definition for", def.type, name);
          const method = /* @__PURE__ */ __name(function(arg) {
            return this.rule({ [name]: arg });
          }, "method");
          prototype[name] = method;
          modifiers[name] = modifier;
        }
      }
      def.modifiers = modifiers;
      if (def.overrides) {
        prototype._super = base;
        schema.$_super = {};
        for (const override in def.overrides) {
          Assert(base[override], "Cannot override missing", override);
          schema.$_super[override] = base[override].bind(schema);
        }
        Object.assign(prototype, def.overrides);
      }
      def.cast = Object.assign({}, parent.cast, def.cast);
      const manifest = Object.assign({}, parent.manifest, def.manifest);
      manifest.build = internals.build(def.manifest && def.manifest.build, parent.manifest && parent.manifest.build);
      def.manifest = manifest;
      def.rebuild = internals.rebuild(def.rebuild, parent.rebuild);
      return schema;
    };
    internals.build = function(child, parent) {
      if (!child || !parent) {
        return child || parent;
      }
      return function(obj, desc) {
        return parent(child(obj, desc), desc);
      };
    };
    internals.coerce = function(child, parent) {
      if (!child || !parent) {
        return child || parent;
      }
      return {
        from: child.from && parent.from ? [.../* @__PURE__ */ new Set([...child.from, ...parent.from])] : null,
        method(value, helpers) {
          let coerced;
          if (!parent.from || parent.from.includes(typeof value)) {
            coerced = parent.method(value, helpers);
            if (coerced) {
              if (coerced.errors || coerced.value === void 0) {
                return coerced;
              }
              value = coerced.value;
            }
          }
          if (!child.from || child.from.includes(typeof value)) {
            const own = child.method(value, helpers);
            if (own) {
              return own;
            }
          }
          return coerced;
        }
      };
    };
    internals.prepare = function(child, parent) {
      if (!child || !parent) {
        return child || parent;
      }
      return function(value, helpers) {
        const prepared = child(value, helpers);
        if (prepared) {
          if (prepared.errors || prepared.value === void 0) {
            return prepared;
          }
          value = prepared.value;
        }
        return parent(value, helpers) || prepared;
      };
    };
    internals.rebuild = function(child, parent) {
      if (!child || !parent) {
        return child || parent;
      }
      return function(schema) {
        parent(schema);
        child(schema);
      };
    };
    internals.validate = function(child, parent) {
      if (!child || !parent) {
        return child || parent;
      }
      return function(value, helpers) {
        const result = parent(value, helpers);
        if (result) {
          if (result.errors && (!Array.isArray(result.errors) || result.errors.length)) {
            return result;
          }
          value = result.value;
        }
        return child(value, helpers) || result;
      };
    };
  }
});

// ../node_modules/@hapi/joi/lib/manifest.js
var require_manifest = __commonJS({
  "../node_modules/@hapi/joi/lib/manifest.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Clone = require_clone();
    var Common = require_common();
    var Messages = require_messages();
    var Ref = require_ref();
    var Template = require_template();
    var Schemas;
    var internals = {};
    exports.describe = function(schema) {
      const def = schema._definition;
      const desc = {
        type: schema.type,
        flags: {},
        rules: []
      };
      for (const flag in schema._flags) {
        if (flag[0] !== "_") {
          desc.flags[flag] = internals.describe(schema._flags[flag]);
        }
      }
      if (!Object.keys(desc.flags).length) {
        delete desc.flags;
      }
      if (schema._preferences) {
        desc.preferences = Clone(schema._preferences, { shallow: ["messages"] });
        delete desc.preferences[Common.symbols.prefs];
        if (desc.preferences.messages) {
          desc.preferences.messages = Messages.decompile(desc.preferences.messages);
        }
      }
      if (schema._valids) {
        desc.allow = schema._valids.describe();
      }
      if (schema._invalids) {
        desc.invalid = schema._invalids.describe();
      }
      for (const rule of schema._rules) {
        const ruleDef = def.rules[rule.name];
        if (ruleDef.manifest === false) {
          continue;
        }
        const item = { name: rule.name };
        for (const custom in def.modifiers) {
          if (rule[custom] !== void 0) {
            item[custom] = internals.describe(rule[custom]);
          }
        }
        if (rule.args) {
          item.args = {};
          for (const key in rule.args) {
            const arg = rule.args[key];
            if (key === "options" && !Object.keys(arg).length) {
              continue;
            }
            item.args[key] = internals.describe(arg, { assign: key });
          }
          if (!Object.keys(item.args).length) {
            delete item.args;
          }
        }
        desc.rules.push(item);
      }
      if (!desc.rules.length) {
        delete desc.rules;
      }
      for (const term in schema.$_terms) {
        if (term[0] === "_") {
          continue;
        }
        Assert(!desc[term], "Cannot describe schema due to internal name conflict with", term);
        const items = schema.$_terms[term];
        if (!items) {
          continue;
        }
        if (items instanceof Map) {
          if (items.size) {
            desc[term] = [...items.entries()];
          }
          continue;
        }
        if (Common.isValues(items)) {
          desc[term] = items.describe();
          continue;
        }
        Assert(def.terms[term], "Term", term, "missing configuration");
        const manifest = def.terms[term].manifest;
        const mapped = typeof manifest === "object";
        if (!items.length && !mapped) {
          continue;
        }
        const normalized = [];
        for (const item of items) {
          normalized.push(internals.describe(item));
        }
        if (mapped) {
          const { from, to } = manifest.mapped;
          desc[term] = {};
          for (const item of normalized) {
            desc[term][item[to]] = item[from];
          }
          continue;
        }
        if (manifest === "single") {
          Assert(normalized.length === 1, "Term", term, "contains more than one item");
          desc[term] = normalized[0];
          continue;
        }
        desc[term] = normalized;
      }
      internals.validate(schema.$_root, desc);
      return desc;
    };
    internals.describe = function(item, options = {}) {
      if (Array.isArray(item)) {
        return item.map(internals.describe);
      }
      if (item === Common.symbols.deepDefault) {
        return { special: "deep" };
      }
      if (typeof item !== "object" || item === null) {
        return item;
      }
      if (options.assign === "options") {
        return Clone(item);
      }
      if (Buffer && Buffer.isBuffer(item)) {
        return { buffer: item.toString("binary") };
      }
      if (item instanceof Date) {
        return item.toISOString();
      }
      if (item instanceof Error) {
        return item;
      }
      if (item instanceof RegExp) {
        if (options.assign === "regex") {
          return item.toString();
        }
        return { regex: item.toString() };
      }
      if (item[Common.symbols.literal]) {
        return { function: item.literal };
      }
      if (typeof item.describe === "function") {
        if (options.assign === "ref") {
          return item.describe().ref;
        }
        return item.describe();
      }
      const normalized = {};
      for (const key in item) {
        const value = item[key];
        if (value === void 0) {
          continue;
        }
        normalized[key] = internals.describe(value, { assign: key });
      }
      return normalized;
    };
    exports.build = function(joi, desc) {
      const builder = new internals.Builder(joi);
      return builder.parse(desc);
    };
    internals.Builder = class {
      constructor(joi) {
        this.joi = joi;
      }
      parse(desc) {
        internals.validate(this.joi, desc);
        let schema = this.joi[desc.type]();
        const def = schema._definition;
        if (desc.flags) {
          for (const flag in desc.flags) {
            const setter = def.flags[flag] && def.flags[flag].setter || flag;
            Assert(typeof schema[setter] === "function", "Invalid flag", flag, "for type", desc.type);
            schema = schema[setter](this.build(desc.flags[flag]));
          }
        }
        if (desc.preferences) {
          schema = schema.preferences(this.build(desc.preferences));
        }
        if (desc.allow) {
          schema = schema.allow(...this.build(desc.allow));
        }
        if (desc.invalid) {
          schema = schema.invalid(...this.build(desc.invalid));
        }
        if (desc.rules) {
          for (const rule of desc.rules) {
            Assert(typeof schema[rule.name] === "function", "Invalid rule", rule.name, "for type", desc.type);
            const args = [];
            if (rule.args) {
              const built = {};
              for (const key in rule.args) {
                built[key] = this.build(rule.args[key], { assign: key });
              }
              const keys = Object.keys(built);
              const definition = def.rules[rule.name].args;
              if (definition) {
                Assert(keys.length <= definition.length, "Invalid number of arguments for", desc.type, rule.name, "(expected up to", definition.length, ", found", keys.length, ")");
                for (const { name } of definition) {
                  args.push(built[name]);
                }
              } else {
                Assert(keys.length === 1, "Invalid number of arguments for", desc.type, rule.name, "(expected up to 1, found", keys.length, ")");
                args.push(built[keys[0]]);
              }
            }
            schema = schema[rule.name](...args);
            const options = {};
            for (const custom in def.modifiers) {
              if (rule[custom] !== void 0) {
                options[custom] = this.build(rule[custom]);
              }
            }
            if (Object.keys(options).length) {
              schema = schema.rule(options);
            }
          }
        }
        const terms = {};
        for (const key in desc) {
          if (["allow", "flags", "invalid", "whens", "preferences", "rules", "type"].includes(key)) {
            continue;
          }
          Assert(def.terms[key], "Term", key, "missing configuration");
          const manifest = def.terms[key].manifest;
          if (manifest === "schema") {
            terms[key] = desc[key].map((item) => this.parse(item));
            continue;
          }
          if (manifest === "values") {
            terms[key] = desc[key].map((item) => this.build(item));
            continue;
          }
          if (manifest === "single") {
            terms[key] = this.build(desc[key]);
            continue;
          }
          if (typeof manifest === "object") {
            terms[key] = {};
            for (const name in desc[key]) {
              const value = desc[key][name];
              terms[key][name] = this.parse(value);
            }
            continue;
          }
          terms[key] = this.build(desc[key]);
        }
        if (desc.whens) {
          terms.whens = desc.whens.map((when) => this.build(when));
        }
        schema = def.manifest.build(schema, terms);
        schema.$_temp.ruleset = false;
        return schema;
      }
      build(desc, options = {}) {
        if (desc === null) {
          return null;
        }
        if (Array.isArray(desc)) {
          return desc.map((item) => this.build(item));
        }
        if (desc instanceof Error) {
          return desc;
        }
        if (options.assign === "options") {
          return Clone(desc);
        }
        if (options.assign === "regex") {
          return internals.regex(desc);
        }
        if (options.assign === "ref") {
          return Ref.build(desc);
        }
        if (typeof desc !== "object") {
          return desc;
        }
        if (Object.keys(desc).length === 1) {
          if (desc.buffer) {
            Assert(Buffer, "Buffers are not supported");
            return Buffer && Buffer.from(desc.buffer, "binary");
          }
          if (desc.function) {
            return { [Common.symbols.literal]: true, literal: desc.function };
          }
          if (desc.override) {
            return Common.symbols.override;
          }
          if (desc.ref) {
            return Ref.build(desc.ref);
          }
          if (desc.regex) {
            return internals.regex(desc.regex);
          }
          if (desc.special) {
            Assert(["deep"].includes(desc.special), "Unknown special value", desc.special);
            return Common.symbols.deepDefault;
          }
          if (desc.value) {
            return Clone(desc.value);
          }
        }
        if (desc.type) {
          return this.parse(desc);
        }
        if (desc.template) {
          return Template.build(desc);
        }
        const normalized = {};
        for (const key in desc) {
          normalized[key] = this.build(desc[key], { assign: key });
        }
        return normalized;
      }
    };
    internals.regex = function(string2) {
      const end = string2.lastIndexOf("/");
      const exp = string2.slice(1, end);
      const flags = string2.slice(end + 1);
      return new RegExp(exp, flags);
    };
    internals.validate = function(joi, desc) {
      Schemas = Schemas || require_schemas();
      joi.assert(desc, Schemas.description);
    };
  }
});

// ../node_modules/@hapi/hoek/lib/deepEqual.js
var require_deepEqual = __commonJS({
  "../node_modules/@hapi/hoek/lib/deepEqual.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Types = require_types();
    var internals = {
      mismatched: null
    };
    module.exports = function(obj, ref, options) {
      options = Object.assign({ prototype: true }, options);
      return !!internals.isDeepEqual(obj, ref, options, []);
    };
    internals.isDeepEqual = function(obj, ref, options, seen) {
      if (obj === ref) {
        return obj !== 0 || 1 / obj === 1 / ref;
      }
      const type = typeof obj;
      if (type !== typeof ref) {
        return false;
      }
      if (obj === null || ref === null) {
        return false;
      }
      if (type === "function") {
        if (!options.deepFunction || obj.toString() !== ref.toString()) {
          return false;
        }
      } else if (type !== "object") {
        return obj !== obj && ref !== ref;
      }
      const instanceType = internals.getSharedType(obj, ref, !!options.prototype);
      switch (instanceType) {
        case Types.buffer:
          return Buffer && Buffer.prototype.equals.call(obj, ref);
        // $lab:coverage:ignore$
        case Types.promise:
          return obj === ref;
        case Types.regex:
          return obj.toString() === ref.toString();
        case internals.mismatched:
          return false;
      }
      for (let i = seen.length - 1; i >= 0; --i) {
        if (seen[i].isSame(obj, ref)) {
          return true;
        }
      }
      seen.push(new internals.SeenEntry(obj, ref));
      try {
        return !!internals.isDeepEqualObj(instanceType, obj, ref, options, seen);
      } finally {
        seen.pop();
      }
    };
    internals.getSharedType = function(obj, ref, checkPrototype) {
      if (checkPrototype) {
        if (Object.getPrototypeOf(obj) !== Object.getPrototypeOf(ref)) {
          return internals.mismatched;
        }
        return Types.getInternalProto(obj);
      }
      const type = Types.getInternalProto(obj);
      if (type !== Types.getInternalProto(ref)) {
        return internals.mismatched;
      }
      return type;
    };
    internals.valueOf = function(obj) {
      const objValueOf = obj.valueOf;
      if (objValueOf === void 0) {
        return obj;
      }
      try {
        return objValueOf.call(obj);
      } catch (err) {
        return err;
      }
    };
    internals.hasOwnEnumerableProperty = function(obj, key) {
      return Object.prototype.propertyIsEnumerable.call(obj, key);
    };
    internals.isSetSimpleEqual = function(obj, ref) {
      for (const entry of obj) {
        if (!ref.has(entry)) {
          return false;
        }
      }
      return true;
    };
    internals.isDeepEqualObj = function(instanceType, obj, ref, options, seen) {
      const { isDeepEqual, valueOf, hasOwnEnumerableProperty } = internals;
      const { keys, getOwnPropertySymbols } = Object;
      if (instanceType === Types.array) {
        if (options.part) {
          for (const objValue of obj) {
            for (const refValue of ref) {
              if (isDeepEqual(objValue, refValue, options, seen)) {
                return true;
              }
            }
          }
        } else {
          if (obj.length !== ref.length) {
            return false;
          }
          for (let i = 0; i < obj.length; ++i) {
            if (!isDeepEqual(obj[i], ref[i], options, seen)) {
              return false;
            }
          }
          return true;
        }
      } else if (instanceType === Types.set) {
        if (obj.size !== ref.size) {
          return false;
        }
        if (!internals.isSetSimpleEqual(obj, ref)) {
          const ref2 = new Set(ref);
          for (const objEntry of obj) {
            if (ref2.delete(objEntry)) {
              continue;
            }
            let found = false;
            for (const refEntry of ref2) {
              if (isDeepEqual(objEntry, refEntry, options, seen)) {
                ref2.delete(refEntry);
                found = true;
                break;
              }
            }
            if (!found) {
              return false;
            }
          }
        }
      } else if (instanceType === Types.map) {
        if (obj.size !== ref.size) {
          return false;
        }
        for (const [key, value] of obj) {
          if (value === void 0 && !ref.has(key)) {
            return false;
          }
          if (!isDeepEqual(value, ref.get(key), options, seen)) {
            return false;
          }
        }
      } else if (instanceType === Types.error) {
        if (obj.name !== ref.name || obj.message !== ref.message) {
          return false;
        }
      }
      const valueOfObj = valueOf(obj);
      const valueOfRef = valueOf(ref);
      if ((obj !== valueOfObj || ref !== valueOfRef) && !isDeepEqual(valueOfObj, valueOfRef, options, seen)) {
        return false;
      }
      const objKeys = keys(obj);
      if (!options.part && objKeys.length !== keys(ref).length && !options.skip) {
        return false;
      }
      let skipped = 0;
      for (const key of objKeys) {
        if (options.skip && options.skip.includes(key)) {
          if (ref[key] === void 0) {
            ++skipped;
          }
          continue;
        }
        if (!hasOwnEnumerableProperty(ref, key)) {
          return false;
        }
        if (!isDeepEqual(obj[key], ref[key], options, seen)) {
          return false;
        }
      }
      if (!options.part && objKeys.length - skipped !== keys(ref).length) {
        return false;
      }
      if (options.symbols !== false) {
        const objSymbols = getOwnPropertySymbols(obj);
        const refSymbols = new Set(getOwnPropertySymbols(ref));
        for (const key of objSymbols) {
          if (!options.skip || !options.skip.includes(key)) {
            if (hasOwnEnumerableProperty(obj, key)) {
              if (!hasOwnEnumerableProperty(ref, key)) {
                return false;
              }
              if (!isDeepEqual(obj[key], ref[key], options, seen)) {
                return false;
              }
            } else if (hasOwnEnumerableProperty(ref, key)) {
              return false;
            }
          }
          refSymbols.delete(key);
        }
        for (const key of refSymbols) {
          if (hasOwnEnumerableProperty(ref, key)) {
            return false;
          }
        }
      }
      return true;
    };
    internals.SeenEntry = class {
      constructor(obj, ref) {
        this.obj = obj;
        this.ref = ref;
      }
      isSame(obj, ref) {
        return this.obj === obj && this.ref === ref;
      }
    };
  }
});

// ../node_modules/@hapi/pinpoint/lib/index.js
var require_lib2 = __commonJS({
  "../node_modules/@hapi/pinpoint/lib/index.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    exports.location = function(depth = 0) {
      const orig = Error.prepareStackTrace;
      Error.prepareStackTrace = (ignore, stack) => stack;
      const capture = {};
      Error.captureStackTrace(capture, this);
      const line = capture.stack[depth + 1];
      Error.prepareStackTrace = orig;
      return {
        filename: line.getFileName(),
        line: line.getLineNumber()
      };
    };
  }
});

// ../node_modules/@hapi/joi/lib/trace.js
var require_trace = __commonJS({
  "../node_modules/@hapi/joi/lib/trace.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var DeepEqual = require_deepEqual();
    var Pinpoint = require_lib2();
    var Errors = require_errors();
    var internals = {
      codes: {
        error: 1,
        pass: 2,
        full: 3
      },
      labels: {
        0: "never used",
        1: "always error",
        2: "always pass"
      }
    };
    exports.setup = function(root) {
      const trace = /* @__PURE__ */ __name(function() {
        root._tracer = root._tracer || new internals.Tracer();
        return root._tracer;
      }, "trace");
      root.trace = trace;
      root[/* @__PURE__ */ Symbol.for("@hapi/lab/coverage/initialize")] = trace;
      root.untrace = () => {
        root._tracer = null;
      };
    };
    exports.location = function(schema) {
      return schema.$_setFlag("_tracerLocation", Pinpoint.location(2));
    };
    internals.Tracer = class {
      constructor() {
        this.name = "Joi";
        this._schemas = /* @__PURE__ */ new Map();
      }
      _register(schema) {
        const existing = this._schemas.get(schema);
        if (existing) {
          return existing.store;
        }
        const store = new internals.Store(schema);
        const { filename, line } = schema._flags._tracerLocation || Pinpoint.location(5);
        this._schemas.set(schema, { filename, line, store });
        return store;
      }
      _combine(merged, sources) {
        for (const { store } of this._schemas.values()) {
          store._combine(merged, sources);
        }
      }
      report(file) {
        const coverage = [];
        for (const { filename, line, store } of this._schemas.values()) {
          if (file && file !== filename) {
            continue;
          }
          const missing = [];
          const skipped = [];
          for (const [schema, log] of store._sources.entries()) {
            if (internals.sub(log.paths, skipped)) {
              continue;
            }
            if (!log.entry) {
              missing.push({
                status: "never reached",
                paths: [...log.paths]
              });
              skipped.push(...log.paths);
              continue;
            }
            for (const type of ["valid", "invalid"]) {
              const set = schema[`_${type}s`];
              if (!set) {
                continue;
              }
              const values = new Set(set._values);
              const refs = new Set(set._refs);
              for (const { value, ref } of log[type]) {
                values.delete(value);
                refs.delete(ref);
              }
              if (values.size || refs.size) {
                missing.push({
                  status: [...values, ...[...refs].map((ref) => ref.display)],
                  rule: `${type}s`
                });
              }
            }
            const rules = schema._rules.map((rule) => rule.name);
            for (const type of ["default", "failover"]) {
              if (schema._flags[type] !== void 0) {
                rules.push(type);
              }
            }
            for (const name of rules) {
              const status = internals.labels[log.rule[name] || 0];
              if (status) {
                const report = { rule: name, status };
                if (log.paths.size) {
                  report.paths = [...log.paths];
                }
                missing.push(report);
              }
            }
          }
          if (missing.length) {
            coverage.push({
              filename,
              line,
              missing,
              severity: "error",
              message: `Schema missing tests for ${missing.map(internals.message).join(", ")}`
            });
          }
        }
        return coverage.length ? coverage : null;
      }
    };
    internals.Store = class {
      constructor(schema) {
        this.active = true;
        this._sources = /* @__PURE__ */ new Map();
        this._combos = /* @__PURE__ */ new Map();
        this._scan(schema);
      }
      debug(state, source, name, result) {
        state.mainstay.debug && state.mainstay.debug.push({ type: source, name, result, path: state.path });
      }
      entry(schema, state) {
        internals.debug(state, { type: "entry" });
        this._record(schema, (log) => {
          log.entry = true;
        });
      }
      filter(schema, state, source, value) {
        internals.debug(state, { type: source, ...value });
        this._record(schema, (log) => {
          log[source].add(value);
        });
      }
      log(schema, state, source, name, result) {
        internals.debug(state, { type: source, name, result: result === "full" ? "pass" : result });
        this._record(schema, (log) => {
          log[source][name] = log[source][name] || 0;
          log[source][name] |= internals.codes[result];
        });
      }
      resolve(state, ref, to) {
        if (!state.mainstay.debug) {
          return;
        }
        const log = { type: "resolve", ref: ref.display, to, path: state.path };
        state.mainstay.debug.push(log);
      }
      value(state, by, from, to, name) {
        if (!state.mainstay.debug || DeepEqual(from, to)) {
          return;
        }
        const log = { type: "value", by, from, to, path: state.path };
        if (name) {
          log.name = name;
        }
        state.mainstay.debug.push(log);
      }
      _record(schema, each) {
        const log = this._sources.get(schema);
        if (log) {
          each(log);
          return;
        }
        const sources = this._combos.get(schema);
        for (const source of sources) {
          this._record(source, each);
        }
      }
      _scan(schema, _path) {
        const path = _path || [];
        let log = this._sources.get(schema);
        if (!log) {
          log = {
            paths: /* @__PURE__ */ new Set(),
            entry: false,
            rule: {},
            valid: /* @__PURE__ */ new Set(),
            invalid: /* @__PURE__ */ new Set()
          };
          this._sources.set(schema, log);
        }
        if (path.length) {
          log.paths.add(path);
        }
        const each = /* @__PURE__ */ __name((sub, source) => {
          const subId = internals.id(sub, source);
          this._scan(sub, path.concat(subId));
        }, "each");
        schema.$_modify({ each, ref: false });
      }
      _combine(merged, sources) {
        this._combos.set(merged, sources);
      }
    };
    internals.message = function(item) {
      const path = item.paths ? Errors.path(item.paths[0]) + (item.rule ? ":" : "") : "";
      return `${path}${item.rule || ""} (${item.status})`;
    };
    internals.id = function(schema, { source, name, path, key }) {
      if (schema._flags.id) {
        return schema._flags.id;
      }
      if (key) {
        return key;
      }
      name = `@${name}`;
      if (source === "terms") {
        return [name, path[Math.min(path.length - 1, 1)]];
      }
      return name;
    };
    internals.sub = function(paths, skipped) {
      for (const path of paths) {
        for (const skip of skipped) {
          if (DeepEqual(path.slice(0, skip.length), skip)) {
            return true;
          }
        }
      }
      return false;
    };
    internals.debug = function(state, event) {
      if (state.mainstay.debug) {
        event.path = state.debug ? [...state.path, state.debug] : state.path;
        state.mainstay.debug.push(event);
      }
    };
  }
});

// ../node_modules/@hapi/hoek/lib/merge.js
var require_merge = __commonJS({
  "../node_modules/@hapi/hoek/lib/merge.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Clone = require_clone();
    var Utils = require_utils();
    var internals = {};
    module.exports = internals.merge = function(target, source, options) {
      Assert(target && typeof target === "object", "Invalid target value: must be an object");
      Assert(source === null || source === void 0 || typeof source === "object", "Invalid source value: must be null, undefined, or an object");
      if (!source) {
        return target;
      }
      options = Object.assign({ nullOverride: true, mergeArrays: true }, options);
      if (Array.isArray(source)) {
        Assert(Array.isArray(target), "Cannot merge array onto an object");
        if (!options.mergeArrays) {
          target.length = 0;
        }
        for (let i = 0; i < source.length; ++i) {
          target.push(Clone(source[i], { symbols: options.symbols }));
        }
        return target;
      }
      const keys = Utils.keys(source, options);
      for (let i = 0; i < keys.length; ++i) {
        const key = keys[i];
        if (key === "__proto__" || !Object.prototype.propertyIsEnumerable.call(source, key)) {
          continue;
        }
        const value = source[key];
        if (value && typeof value === "object") {
          if (target[key] === value) {
            continue;
          }
          if (!target[key] || typeof target[key] !== "object" || Array.isArray(target[key]) !== Array.isArray(value) || value instanceof Date || Buffer && Buffer.isBuffer(value) || // $lab:coverage:ignore$
          value instanceof RegExp) {
            target[key] = Clone(value, { symbols: options.symbols });
          } else {
            internals.merge(target[key], value, options);
          }
        } else {
          if (value !== null && value !== void 0) {
            target[key] = value;
          } else if (options.nullOverride) {
            target[key] = value;
          }
        }
      }
      return target;
    };
  }
});

// ../node_modules/@hapi/joi/lib/modify.js
var require_modify = __commonJS({
  "../node_modules/@hapi/joi/lib/modify.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Common = require_common();
    var Ref = require_ref();
    var internals = {};
    exports.Ids = internals.Ids = class {
      constructor() {
        this._byId = /* @__PURE__ */ new Map();
        this._byKey = /* @__PURE__ */ new Map();
        this._schemaChain = false;
      }
      clone() {
        const clone = new internals.Ids();
        clone._byId = new Map(this._byId);
        clone._byKey = new Map(this._byKey);
        clone._schemaChain = this._schemaChain;
        return clone;
      }
      concat(source) {
        if (source._schemaChain) {
          this._schemaChain = true;
        }
        for (const [id, value] of source._byId.entries()) {
          Assert(!this._byKey.has(id), "Schema id conflicts with existing key:", id);
          this._byId.set(id, value);
        }
        for (const [key, value] of source._byKey.entries()) {
          Assert(!this._byId.has(key), "Schema key conflicts with existing id:", key);
          this._byKey.set(key, value);
        }
      }
      fork(path, adjuster, root) {
        const chain = this._collect(path);
        chain.push({ schema: root });
        const tail = chain.shift();
        let adjusted = { id: tail.id, schema: adjuster(tail.schema) };
        Assert(Common.isSchema(adjusted.schema), "adjuster function failed to return a joi schema type");
        for (const node of chain) {
          adjusted = { id: node.id, schema: internals.fork(node.schema, adjusted.id, adjusted.schema) };
        }
        return adjusted.schema;
      }
      labels(path, behind = []) {
        const current = path[0];
        const node = this._get(current);
        if (!node) {
          return [...behind, ...path].join(".");
        }
        const forward = path.slice(1);
        behind = [...behind, node.schema._flags.label || current];
        if (!forward.length) {
          return behind.join(".");
        }
        return node.schema._ids.labels(forward, behind);
      }
      reach(path, behind = []) {
        const current = path[0];
        const node = this._get(current);
        Assert(node, "Schema does not contain path", [...behind, ...path].join("."));
        const forward = path.slice(1);
        if (!forward.length) {
          return node.schema;
        }
        return node.schema._ids.reach(forward, [...behind, current]);
      }
      register(schema, { key } = {}) {
        if (!schema || !Common.isSchema(schema)) {
          return;
        }
        if (schema.$_property("schemaChain") || schema._ids._schemaChain) {
          this._schemaChain = true;
        }
        const id = schema._flags.id;
        if (id) {
          const existing = this._byId.get(id);
          Assert(!existing || existing.schema === schema, "Cannot add different schemas with the same id:", id);
          Assert(!this._byKey.has(id), "Schema id conflicts with existing key:", id);
          this._byId.set(id, { schema, id });
        }
        if (key) {
          Assert(!this._byKey.has(key), "Schema already contains key:", key);
          Assert(!this._byId.has(key), "Schema key conflicts with existing id:", key);
          this._byKey.set(key, { schema, id: key });
        }
      }
      reset() {
        this._byId = /* @__PURE__ */ new Map();
        this._byKey = /* @__PURE__ */ new Map();
        this._schemaChain = false;
      }
      _collect(path, behind = [], nodes = []) {
        const current = path[0];
        const node = this._get(current);
        Assert(node, "Schema does not contain path", [...behind, ...path].join("."));
        nodes = [node, ...nodes];
        const forward = path.slice(1);
        if (!forward.length) {
          return nodes;
        }
        return node.schema._ids._collect(forward, [...behind, current], nodes);
      }
      _get(id) {
        return this._byId.get(id) || this._byKey.get(id);
      }
    };
    internals.fork = function(schema, id, replacement) {
      const each = /* @__PURE__ */ __name((item, { key }) => {
        if (id === (item._flags.id || key)) {
          return replacement;
        }
      }, "each");
      const obj = exports.schema(schema, { each, ref: false });
      return obj.$_mutateRebuild();
    };
    exports.schema = function(schema, options) {
      let obj;
      for (const name in schema._flags) {
        if (name[0] === "_") {
          continue;
        }
        const result = internals.scan(schema._flags[name], { source: "flags", name }, options);
        if (result !== void 0) {
          obj = obj || schema.clone();
          obj._flags[name] = result;
        }
      }
      for (let i = 0; i < schema._rules.length; ++i) {
        const rule = schema._rules[i];
        const result = internals.scan(rule.args, { source: "rules", name: rule.name }, options);
        if (result !== void 0) {
          obj = obj || schema.clone();
          const clone = Object.assign({}, rule);
          clone.args = result;
          obj._rules[i] = clone;
          const existingUnique = obj._singleRules.get(rule.name);
          if (existingUnique === rule) {
            obj._singleRules.set(rule.name, clone);
          }
        }
      }
      for (const name in schema.$_terms) {
        if (name[0] === "_") {
          continue;
        }
        const result = internals.scan(schema.$_terms[name], { source: "terms", name }, options);
        if (result !== void 0) {
          obj = obj || schema.clone();
          obj.$_terms[name] = result;
        }
      }
      return obj;
    };
    internals.scan = function(item, source, options, _path, _key) {
      const path = _path || [];
      if (item === null || typeof item !== "object") {
        return;
      }
      let clone;
      if (Array.isArray(item)) {
        for (let i = 0; i < item.length; ++i) {
          const key = source.source === "terms" && source.name === "keys" && item[i].key;
          const result = internals.scan(item[i], source, options, [i, ...path], key);
          if (result !== void 0) {
            clone = clone || item.slice();
            clone[i] = result;
          }
        }
        return clone;
      }
      if (options.schema !== false && Common.isSchema(item) || options.ref !== false && Ref.isRef(item)) {
        const result = options.each(item, { ...source, path, key: _key });
        if (result === item) {
          return;
        }
        return result;
      }
      for (const key in item) {
        if (key[0] === "_") {
          continue;
        }
        const result = internals.scan(item[key], source, options, [key, ...path], _key);
        if (result !== void 0) {
          clone = clone || Object.assign({}, item);
          clone[key] = result;
        }
      }
      return clone;
    };
  }
});

// ../node_modules/@hapi/hoek/lib/ignore.js
var require_ignore = __commonJS({
  "../node_modules/@hapi/hoek/lib/ignore.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    module.exports = function() {
    };
  }
});

// ../node_modules/@hapi/joi/lib/state.js
var require_state = __commonJS({
  "../node_modules/@hapi/joi/lib/state.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Clone = require_clone();
    var Reach = require_reach();
    var Common = require_common();
    var internals = {
      value: /* @__PURE__ */ Symbol("value")
    };
    module.exports = internals.State = class {
      constructor(path, ancestors, state) {
        this.path = path;
        this.ancestors = ancestors;
        this.mainstay = state.mainstay;
        this.schemas = state.schemas;
        this.debug = null;
      }
      localize(path, ancestors = null, schema = null) {
        const state = new internals.State(path, ancestors, this);
        if (schema && state.schemas) {
          state.schemas = [internals.schemas(schema), ...state.schemas];
        }
        return state;
      }
      nest(schema, debug) {
        const state = new internals.State(this.path, this.ancestors, this);
        state.schemas = state.schemas && [internals.schemas(schema), ...state.schemas];
        state.debug = debug;
        return state;
      }
      shadow(value, reason) {
        this.mainstay.shadow = this.mainstay.shadow || new internals.Shadow();
        this.mainstay.shadow.set(this.path, value, reason);
      }
      snapshot() {
        if (this.mainstay.shadow) {
          this._snapshot = Clone(this.mainstay.shadow.node(this.path));
        }
      }
      restore() {
        if (this.mainstay.shadow) {
          this.mainstay.shadow.override(this.path, this._snapshot);
          this._snapshot = void 0;
        }
      }
    };
    internals.schemas = function(schema) {
      if (Common.isSchema(schema)) {
        return { schema };
      }
      return schema;
    };
    internals.Shadow = class {
      constructor() {
        this._values = null;
      }
      set(path, value, reason) {
        if (!path.length) {
          return;
        }
        if (reason === "strip" && typeof path[path.length - 1] === "number") {
          return;
        }
        this._values = this._values || /* @__PURE__ */ new Map();
        let node = this._values;
        for (let i = 0; i < path.length; ++i) {
          const segment = path[i];
          let next = node.get(segment);
          if (!next) {
            next = /* @__PURE__ */ new Map();
            node.set(segment, next);
          }
          node = next;
        }
        node[internals.value] = value;
      }
      get(path) {
        const node = this.node(path);
        if (node) {
          return node[internals.value];
        }
      }
      node(path) {
        if (!this._values) {
          return;
        }
        return Reach(this._values, path, { iterables: true });
      }
      override(path, node) {
        if (!this._values) {
          return;
        }
        const parents = path.slice(0, -1);
        const own = path[path.length - 1];
        const parent = Reach(this._values, parents, { iterables: true });
        if (node) {
          parent.set(own, node);
          return;
        }
        if (parent) {
          parent.delete(own);
        }
      }
    };
  }
});

// ../node_modules/@hapi/joi/lib/validator.js
var require_validator = __commonJS({
  "../node_modules/@hapi/joi/lib/validator.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Clone = require_clone();
    var Ignore = require_ignore();
    var Reach = require_reach();
    var Common = require_common();
    var Errors = require_errors();
    var State2 = require_state();
    var internals = {
      result: /* @__PURE__ */ Symbol("result")
    };
    exports.entry = function(value, schema, prefs) {
      let settings = Common.defaults;
      if (prefs) {
        Assert(prefs.warnings === void 0, "Cannot override warnings preference in synchronous validation");
        settings = Common.preferences(Common.defaults, prefs);
      }
      const result = internals.entry(value, schema, settings);
      Assert(!result.mainstay.externals.length, "Schema with external rules must use validateAsync()");
      const outcome = { value: result.value };
      if (result.error) {
        outcome.error = result.error;
      }
      if (result.mainstay.warnings.length) {
        outcome.warning = Errors.details(result.mainstay.warnings);
      }
      if (result.mainstay.debug) {
        outcome.debug = result.mainstay.debug;
      }
      return outcome;
    };
    exports.entryAsync = async function(value, schema, prefs) {
      let settings = Common.defaults;
      if (prefs) {
        settings = Common.preferences(Common.defaults, prefs);
      }
      const result = internals.entry(value, schema, settings);
      const mainstay = result.mainstay;
      if (result.error) {
        if (mainstay.debug) {
          result.error.debug = mainstay.debug;
        }
        throw result.error;
      }
      if (mainstay.externals.length) {
        let root = result.value;
        for (const { method, path, label } of mainstay.externals) {
          let node = root;
          let key;
          let parent;
          if (path.length) {
            key = path[path.length - 1];
            parent = Reach(root, path.slice(0, -1));
            node = parent[key];
          }
          try {
            const output = await method(node);
            if (output === void 0 || output === node) {
              continue;
            }
            if (parent) {
              parent[key] = output;
            } else {
              root = output;
            }
          } catch (err) {
            err.message += ` (${label})`;
            throw err;
          }
        }
        result.value = root;
      }
      if (!settings.warnings && !settings.debug) {
        return result.value;
      }
      const outcome = { value: result.value };
      if (mainstay.warnings.length) {
        outcome.warning = Errors.details(mainstay.warnings);
      }
      if (mainstay.debug) {
        outcome.debug = mainstay.debug;
      }
      return outcome;
    };
    internals.entry = function(value, schema, prefs) {
      const { tracer, cleanup } = internals.tracer(schema, prefs);
      const debug = prefs.debug ? [] : null;
      const links = schema._ids._schemaChain ? /* @__PURE__ */ new Map() : null;
      const mainstay = { externals: [], warnings: [], tracer, debug, links };
      const schemas = schema._ids._schemaChain ? [{ schema }] : null;
      const state = new State2([], [], { mainstay, schemas });
      const result = exports.validate(value, schema, state, prefs);
      if (cleanup) {
        schema.$_root.untrace();
      }
      const error = Errors.process(result.errors, value, prefs);
      return { value: result.value, error, mainstay };
    };
    internals.tracer = function(schema, prefs) {
      if (schema.$_root._tracer) {
        return { tracer: schema.$_root._tracer._register(schema) };
      }
      if (prefs.debug) {
        Assert(schema.$_root.trace, "Debug mode not supported");
        return { tracer: schema.$_root.trace()._register(schema), cleanup: true };
      }
      return { tracer: internals.ignore };
    };
    exports.validate = function(value, schema, state, prefs, overrides = {}) {
      if (schema.$_terms.whens) {
        schema = schema._generate(value, state, prefs).schema;
      }
      if (schema._preferences) {
        prefs = internals.prefs(schema, prefs);
      }
      if (schema._cache && prefs.cache) {
        const result = schema._cache.get(value);
        state.mainstay.tracer.debug(state, "validate", "cached", !!result);
        if (result) {
          return result;
        }
      }
      const createError = /* @__PURE__ */ __name((code, local, localState) => schema.$_createError(code, value, local, localState || state, prefs), "createError");
      const helpers = {
        original: value,
        prefs,
        schema,
        state,
        error: createError,
        warn: /* @__PURE__ */ __name((code, local, localState) => state.mainstay.warnings.push(createError(code, local, localState)), "warn"),
        message: /* @__PURE__ */ __name((messages, local) => schema.$_createError("custom", value, local, state, prefs, { messages }), "message")
      };
      state.mainstay.tracer.entry(schema, state);
      const def = schema._definition;
      if (def.prepare && value !== void 0 && prefs.convert) {
        const prepared = def.prepare(value, helpers);
        if (prepared) {
          state.mainstay.tracer.value(state, "prepare", value, prepared.value);
          if (prepared.errors) {
            return internals.finalize(prepared.value, [].concat(prepared.errors), helpers);
          }
          value = prepared.value;
        }
      }
      if (def.coerce && value !== void 0 && prefs.convert && (!def.coerce.from || def.coerce.from.includes(typeof value))) {
        const coerced = def.coerce.method(value, helpers);
        if (coerced) {
          state.mainstay.tracer.value(state, "coerced", value, coerced.value);
          if (coerced.errors) {
            return internals.finalize(coerced.value, [].concat(coerced.errors), helpers);
          }
          value = coerced.value;
        }
      }
      const empty = schema._flags.empty;
      if (empty && empty.$_match(internals.trim(value, schema), state.nest(empty), Common.defaults)) {
        state.mainstay.tracer.value(state, "empty", value, void 0);
        value = void 0;
      }
      const presence = overrides.presence || schema._flags.presence || (schema._flags._endedSwitch ? "ignore" : prefs.presence);
      if (value === void 0) {
        if (presence === "forbidden") {
          return internals.finalize(value, null, helpers);
        }
        if (presence === "required") {
          return internals.finalize(value, [schema.$_createError("any.required", value, null, state, prefs)], helpers);
        }
        if (presence === "optional") {
          if (schema._flags.default !== Common.symbols.deepDefault) {
            return internals.finalize(value, null, helpers);
          }
          state.mainstay.tracer.value(state, "default", value, {});
          value = {};
        }
      } else if (presence === "forbidden") {
        return internals.finalize(value, [schema.$_createError("any.unknown", value, null, state, prefs)], helpers);
      }
      const errors = [];
      if (schema._valids) {
        const match = schema._valids.get(value, state, prefs, schema._flags.insensitive);
        if (match) {
          if (prefs.convert) {
            state.mainstay.tracer.value(state, "valids", value, match.value);
            value = match.value;
          }
          state.mainstay.tracer.filter(schema, state, "valid", match);
          return internals.finalize(value, null, helpers);
        }
        if (schema._flags.only) {
          const report = schema.$_createError("any.only", value, { valids: schema._valids.values({ display: true }) }, state, prefs);
          if (prefs.abortEarly) {
            return internals.finalize(value, [report], helpers);
          }
          errors.push(report);
        }
      }
      if (schema._invalids) {
        const match = schema._invalids.get(value, state, prefs, schema._flags.insensitive);
        if (match) {
          state.mainstay.tracer.filter(schema, state, "invalid", match);
          const report = schema.$_createError("any.invalid", value, { invalids: schema._invalids.values({ display: true }) }, state, prefs);
          if (prefs.abortEarly) {
            return internals.finalize(value, [report], helpers);
          }
          errors.push(report);
        }
      }
      if (def.validate) {
        const base = def.validate(value, helpers);
        if (base) {
          state.mainstay.tracer.value(state, "base", value, base.value);
          value = base.value;
          if (base.errors) {
            if (!Array.isArray(base.errors)) {
              errors.push(base.errors);
              return internals.finalize(value, errors, helpers);
            }
            if (base.errors.length) {
              errors.push(...base.errors);
              return internals.finalize(value, errors, helpers);
            }
          }
        }
      }
      if (!schema._rules.length) {
        return internals.finalize(value, errors, helpers);
      }
      return internals.rules(value, errors, helpers);
    };
    internals.rules = function(value, errors, helpers) {
      const { schema, state, prefs } = helpers;
      for (const rule of schema._rules) {
        const definition = schema._definition.rules[rule.method];
        if (definition.convert && prefs.convert) {
          state.mainstay.tracer.log(schema, state, "rule", rule.name, "full");
          continue;
        }
        let ret;
        let args = rule.args;
        if (rule._resolve.length) {
          args = Object.assign({}, args);
          for (const key of rule._resolve) {
            const resolver = definition.argsByName.get(key);
            const resolved = args[key].resolve(value, state, prefs);
            const normalized = resolver.normalize ? resolver.normalize(resolved) : resolved;
            const invalid = Common.validateArg(normalized, null, resolver);
            if (invalid) {
              ret = schema.$_createError("any.ref", resolved, { arg: key, ref: args[key], reason: invalid }, state, prefs);
              break;
            }
            args[key] = normalized;
          }
        }
        ret = ret || definition.validate(value, helpers, args, rule);
        const result = internals.rule(ret, rule);
        if (result.errors) {
          state.mainstay.tracer.log(schema, state, "rule", rule.name, "error");
          if (rule.warn) {
            state.mainstay.warnings.push(...result.errors);
            continue;
          }
          if (prefs.abortEarly) {
            return internals.finalize(value, result.errors, helpers);
          }
          errors.push(...result.errors);
        } else {
          state.mainstay.tracer.log(schema, state, "rule", rule.name, "pass");
          state.mainstay.tracer.value(state, "rule", value, result.value, rule.name);
          value = result.value;
        }
      }
      return internals.finalize(value, errors, helpers);
    };
    internals.rule = function(ret, rule) {
      if (ret instanceof Errors.Report) {
        internals.error(ret, rule);
        return { errors: [ret], value: null };
      }
      if (Array.isArray(ret) && (ret[0] instanceof Errors.Report || ret[0] instanceof Error)) {
        ret.forEach((report) => internals.error(report, rule));
        return { errors: ret, value: null };
      }
      return { errors: null, value: ret };
    };
    internals.error = function(report, rule) {
      if (rule.message) {
        report._setTemplate(rule.message);
      }
      return report;
    };
    internals.finalize = function(value, errors, helpers) {
      errors = errors || [];
      const { schema, state, prefs } = helpers;
      if (errors.length) {
        const failover = internals.default("failover", void 0, errors, helpers);
        if (failover !== void 0) {
          state.mainstay.tracer.value(state, "failover", value, failover);
          value = failover;
          errors = [];
        }
      }
      if (errors.length && schema._flags.error) {
        if (typeof schema._flags.error === "function") {
          errors = schema._flags.error(errors);
          if (!Array.isArray(errors)) {
            errors = [errors];
          }
          for (const error of errors) {
            Assert(error instanceof Error || error instanceof Errors.Report, "error() must return an Error object");
          }
        } else {
          errors = [schema._flags.error];
        }
      }
      if (value === void 0) {
        const defaulted = internals.default("default", value, errors, helpers);
        state.mainstay.tracer.value(state, "default", value, defaulted);
        value = defaulted;
      }
      if (schema._flags.cast && value !== void 0) {
        const caster = schema._definition.cast[schema._flags.cast];
        if (caster.from(value)) {
          const casted = caster.to(value, helpers);
          state.mainstay.tracer.value(state, "cast", value, casted, schema._flags.cast);
          value = casted;
        }
      }
      if (schema.$_terms.externals && prefs.externals && prefs._externals !== false) {
        for (const { method } of schema.$_terms.externals) {
          state.mainstay.externals.push({ method, path: state.path, label: Errors.label(schema._flags, state, prefs) });
        }
      }
      const result = { value, errors: errors.length ? errors : null };
      if (schema._flags.result) {
        result.value = schema._flags.result === "strip" ? void 0 : (
          /* raw */
          helpers.original
        );
        state.mainstay.tracer.value(state, schema._flags.result, value, result.value);
        state.shadow(value, schema._flags.result);
      }
      if (schema._cache && prefs.cache !== false && !schema._refs.length) {
        schema._cache.set(helpers.original, result);
      }
      return result;
    };
    internals.prefs = function(schema, prefs) {
      const isDefaultOptions = prefs === Common.defaults;
      if (isDefaultOptions && schema._preferences[Common.symbols.prefs]) {
        return schema._preferences[Common.symbols.prefs];
      }
      prefs = Common.preferences(prefs, schema._preferences);
      if (isDefaultOptions) {
        schema._preferences[Common.symbols.prefs] = prefs;
      }
      return prefs;
    };
    internals.default = function(flag, value, errors, helpers) {
      const { schema, state, prefs } = helpers;
      const source = schema._flags[flag];
      if (prefs.noDefaults || source === void 0) {
        return value;
      }
      state.mainstay.tracer.log(schema, state, "rule", flag, "full");
      if (!source) {
        return source;
      }
      if (typeof source === "function") {
        const args = source.length ? [Clone(state.ancestors[0]), helpers] : [];
        try {
          return source(...args);
        } catch (err) {
          errors.push(schema.$_createError(`any.${flag}`, null, { error: err }, state, prefs));
          return;
        }
      }
      if (typeof source !== "object") {
        return source;
      }
      if (source[Common.symbols.literal]) {
        return source.literal;
      }
      if (Common.isResolvable(source)) {
        return source.resolve(value, state, prefs);
      }
      return Clone(source);
    };
    internals.trim = function(value, schema) {
      if (typeof value !== "string") {
        return value;
      }
      const trim = schema.$_getRule("trim");
      if (!trim || !trim.args.enabled) {
        return value;
      }
      return value.trim();
    };
    internals.ignore = {
      active: false,
      debug: Ignore,
      entry: Ignore,
      filter: Ignore,
      log: Ignore,
      resolve: Ignore,
      value: Ignore
    };
  }
});

// ../node_modules/@hapi/joi/lib/values.js
var require_values = __commonJS({
  "../node_modules/@hapi/joi/lib/values.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var DeepEqual = require_deepEqual();
    var Common = require_common();
    var internals = {};
    module.exports = internals.Values = class {
      constructor(values, refs) {
        this._values = new Set(values);
        this._refs = new Set(refs);
        this._lowercase = internals.lowercases(values);
        this._override = false;
      }
      get length() {
        return this._values.size + this._refs.size;
      }
      add(value, refs) {
        if (Common.isResolvable(value)) {
          if (!this._refs.has(value)) {
            this._refs.add(value);
            if (refs) {
              refs.register(value);
            }
          }
          return;
        }
        if (!this.has(value, null, null, false)) {
          this._values.add(value);
          if (typeof value === "string") {
            this._lowercase.set(value.toLowerCase(), value);
          }
        }
      }
      static merge(target, source, remove) {
        target = target || new internals.Values();
        if (source) {
          if (source._override) {
            return source.clone();
          }
          for (const item of [...source._values, ...source._refs]) {
            target.add(item);
          }
        }
        if (remove) {
          for (const item of [...remove._values, ...remove._refs]) {
            target.remove(item);
          }
        }
        return target.length ? target : null;
      }
      remove(value) {
        if (Common.isResolvable(value)) {
          this._refs.delete(value);
          return;
        }
        this._values.delete(value);
        if (typeof value === "string") {
          this._lowercase.delete(value.toLowerCase());
        }
      }
      has(value, state, prefs, insensitive) {
        return !!this.get(value, state, prefs, insensitive);
      }
      get(value, state, prefs, insensitive) {
        if (!this.length) {
          return false;
        }
        if (this._values.has(value)) {
          return { value };
        }
        if (typeof value === "string" && value && insensitive) {
          const found = this._lowercase.get(value.toLowerCase());
          if (found) {
            return { value: found };
          }
        }
        if (!this._refs.size && typeof value !== "object") {
          return false;
        }
        if (typeof value === "object") {
          for (const item of this._values) {
            if (DeepEqual(item, value)) {
              return { value: item };
            }
          }
        }
        if (state) {
          for (const ref of this._refs) {
            const resolved = ref.resolve(value, state, prefs, null, { in: true });
            if (resolved === void 0) {
              continue;
            }
            const items = !ref.in || typeof resolved !== "object" ? [resolved] : Array.isArray(resolved) ? resolved : Object.keys(resolved);
            for (const item of items) {
              if (typeof item !== typeof value) {
                continue;
              }
              if (insensitive && value && typeof value === "string") {
                if (item.toLowerCase() === value.toLowerCase()) {
                  return { value: item, ref };
                }
              } else {
                if (DeepEqual(item, value)) {
                  return { value: item, ref };
                }
              }
            }
          }
        }
        return false;
      }
      override() {
        this._override = true;
      }
      values(options) {
        if (options && options.display) {
          const values = [];
          for (const item of [...this._values, ...this._refs]) {
            if (item !== void 0) {
              values.push(item);
            }
          }
          return values;
        }
        return Array.from([...this._values, ...this._refs]);
      }
      clone() {
        const set = new internals.Values(this._values, this._refs);
        set._override = this._override;
        return set;
      }
      concat(source) {
        Assert(!source._override, "Cannot concat override set of values");
        const set = new internals.Values([...this._values, ...source._values], [...this._refs, ...source._refs]);
        set._override = this._override;
        return set;
      }
      describe() {
        const normalized = [];
        if (this._override) {
          normalized.push({ override: true });
        }
        for (const value of this._values.values()) {
          normalized.push(value && typeof value === "object" ? { value } : value);
        }
        for (const value of this._refs.values()) {
          normalized.push(value.describe());
        }
        return normalized;
      }
    };
    internals.Values.prototype[Common.symbols.values] = true;
    internals.Values.prototype.slice = internals.Values.prototype.clone;
    internals.lowercases = function(from) {
      const map = /* @__PURE__ */ new Map();
      if (from) {
        for (const value of from) {
          if (typeof value === "string") {
            map.set(value.toLowerCase(), value);
          }
        }
      }
      return map;
    };
  }
});

// ../node_modules/@hapi/joi/lib/base.js
var require_base = __commonJS({
  "../node_modules/@hapi/joi/lib/base.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Clone = require_clone();
    var DeepEqual = require_deepEqual();
    var Merge = require_merge();
    var Cache = require_cache();
    var Common = require_common();
    var Compile = require_compile();
    var Errors = require_errors();
    var Extend = require_extend();
    var Manifest = require_manifest();
    var Messages = require_messages();
    var Modify = require_modify();
    var Ref = require_ref();
    var Trace = require_trace();
    var Validator = require_validator();
    var Values = require_values();
    var internals = {};
    internals.Base = class {
      constructor(type) {
        this.type = type;
        this.$_root = null;
        this._definition = {};
        this._ids = new Modify.Ids();
        this._preferences = null;
        this._refs = new Ref.Manager();
        this._cache = null;
        this._valids = null;
        this._invalids = null;
        this._flags = {};
        this._rules = [];
        this._singleRules = /* @__PURE__ */ new Map();
        this.$_terms = {};
        this.$_temp = {
          // Runtime state (not cloned)
          ruleset: null,
          // null: use last, false: error, number: start position
          whens: {}
          // Runtime cache of generated whens
        };
      }
      // Manifest
      describe() {
        Assert(typeof Manifest.describe === "function", "Manifest functionality disabled");
        return Manifest.describe(this);
      }
      // Rules
      allow(...values) {
        Common.verifyFlat(values, "allow");
        return this._values(values, "_valids");
      }
      alter(targets) {
        Assert(targets && typeof targets === "object" && !Array.isArray(targets), "Invalid targets argument");
        Assert(!this._inRuleset(), "Cannot set alterations inside a ruleset");
        const obj = this.clone();
        obj.$_terms.alterations = obj.$_terms.alterations || [];
        for (const target in targets) {
          const adjuster = targets[target];
          Assert(typeof adjuster === "function", "Alteration adjuster for", target, "must be a function");
          obj.$_terms.alterations.push({ target, adjuster });
        }
        obj.$_temp.ruleset = false;
        return obj;
      }
      cast(to) {
        Assert(to === false || typeof to === "string", "Invalid to value");
        Assert(to === false || this._definition.cast[to], "Type", this.type, "does not support casting to", to);
        return this.$_setFlag("cast", to === false ? void 0 : to);
      }
      default(value, options) {
        return this._default("default", value, options);
      }
      description(desc) {
        Assert(desc && typeof desc === "string", "Description must be a non-empty string");
        return this.$_setFlag("description", desc);
      }
      empty(schema) {
        const obj = this.clone();
        if (schema !== void 0) {
          schema = obj.$_compile(schema, { override: false });
        }
        return obj.$_setFlag("empty", schema, { clone: false });
      }
      error(err) {
        Assert(err, "Missing error");
        Assert(err instanceof Error || typeof err === "function", "Must provide a valid Error object or a function");
        return this.$_setFlag("error", err);
      }
      example(example, options = {}) {
        Assert(example !== void 0, "Missing example");
        Common.assertOptions(options, ["override"]);
        return this._inner("examples", example, { single: true, override: options.override });
      }
      external(method, description) {
        if (typeof method === "object") {
          Assert(!description, "Cannot combine options with description");
          description = method.description;
          method = method.method;
        }
        Assert(typeof method === "function", "Method must be a function");
        Assert(description === void 0 || description && typeof description === "string", "Description must be a non-empty string");
        return this._inner("externals", { method, description }, { single: true });
      }
      failover(value, options) {
        return this._default("failover", value, options);
      }
      forbidden() {
        return this.presence("forbidden");
      }
      id(id) {
        if (!id) {
          return this.$_setFlag("id", void 0);
        }
        Assert(typeof id === "string", "id must be a non-empty string");
        Assert(/^[^\.]+$/.test(id), "id cannot contain period character");
        return this.$_setFlag("id", id);
      }
      invalid(...values) {
        return this._values(values, "_invalids");
      }
      label(name) {
        Assert(name && typeof name === "string", "Label name must be a non-empty string");
        return this.$_setFlag("label", name);
      }
      meta(meta) {
        Assert(meta !== void 0, "Meta cannot be undefined");
        return this._inner("metas", meta, { single: true });
      }
      note(...notes) {
        Assert(notes.length, "Missing notes");
        for (const note of notes) {
          Assert(note && typeof note === "string", "Notes must be non-empty strings");
        }
        return this._inner("notes", notes);
      }
      only(mode = true) {
        Assert(typeof mode === "boolean", "Invalid mode:", mode);
        return this.$_setFlag("only", mode);
      }
      optional() {
        return this.presence("optional");
      }
      prefs(prefs) {
        Assert(prefs, "Missing preferences");
        Assert(prefs.context === void 0, "Cannot override context");
        Assert(prefs.externals === void 0, "Cannot override externals");
        Assert(prefs.warnings === void 0, "Cannot override warnings");
        Assert(prefs.debug === void 0, "Cannot override debug");
        Common.checkPreferences(prefs);
        const obj = this.clone();
        obj._preferences = Common.preferences(obj._preferences, prefs);
        return obj;
      }
      presence(mode) {
        Assert(["optional", "required", "forbidden"].includes(mode), "Unknown presence mode", mode);
        return this.$_setFlag("presence", mode);
      }
      raw(enabled = true) {
        return this.$_setFlag("result", enabled ? "raw" : void 0);
      }
      result(mode) {
        Assert(["raw", "strip"].includes(mode), "Unknown result mode", mode);
        return this.$_setFlag("result", mode);
      }
      required() {
        return this.presence("required");
      }
      strict(enabled) {
        const obj = this.clone();
        const convert = enabled === void 0 ? false : !enabled;
        obj._preferences = Common.preferences(obj._preferences, { convert });
        return obj;
      }
      strip(enabled = true) {
        return this.$_setFlag("result", enabled ? "strip" : void 0);
      }
      tag(...tags) {
        Assert(tags.length, "Missing tags");
        for (const tag of tags) {
          Assert(tag && typeof tag === "string", "Tags must be non-empty strings");
        }
        return this._inner("tags", tags);
      }
      unit(name) {
        Assert(name && typeof name === "string", "Unit name must be a non-empty string");
        return this.$_setFlag("unit", name);
      }
      valid(...values) {
        Common.verifyFlat(values, "valid");
        const obj = this.allow(...values);
        obj.$_setFlag("only", !!obj._valids, { clone: false });
        return obj;
      }
      when(condition, options) {
        const obj = this.clone();
        if (!obj.$_terms.whens) {
          obj.$_terms.whens = [];
        }
        const when = Compile.when(obj, condition, options);
        if (!["any", "link"].includes(obj.type)) {
          const conditions = when.is ? [when] : when.switch;
          for (const item of conditions) {
            Assert(!item.then || item.then.type === "any" || item.then.type === obj.type, "Cannot combine", obj.type, "with", item.then && item.then.type);
            Assert(!item.otherwise || item.otherwise.type === "any" || item.otherwise.type === obj.type, "Cannot combine", obj.type, "with", item.otherwise && item.otherwise.type);
          }
        }
        obj.$_terms.whens.push(when);
        return obj.$_mutateRebuild();
      }
      // Helpers
      cache(cache) {
        Assert(!this._inRuleset(), "Cannot set caching inside a ruleset");
        Assert(!this._cache, "Cannot override schema cache");
        const obj = this.clone();
        obj._cache = cache || Cache.provider.provision();
        obj.$_temp.ruleset = false;
        return obj;
      }
      clone() {
        const obj = Object.create(Object.getPrototypeOf(this));
        return this._assign(obj);
      }
      concat(source) {
        Assert(Common.isSchema(source), "Invalid schema object");
        Assert(this.type === "any" || source.type === "any" || source.type === this.type, "Cannot merge type", this.type, "with another type:", source.type);
        Assert(!this._inRuleset(), "Cannot concatenate onto a schema with open ruleset");
        Assert(!source._inRuleset(), "Cannot concatenate a schema with open ruleset");
        let obj = this.clone();
        if (this.type === "any" && source.type !== "any") {
          const tmpObj = source.clone();
          for (const key of Object.keys(obj)) {
            if (key !== "type") {
              tmpObj[key] = obj[key];
            }
          }
          obj = tmpObj;
        }
        obj._ids.concat(source._ids);
        obj._refs.register(source, Ref.toSibling);
        obj._preferences = obj._preferences ? Common.preferences(obj._preferences, source._preferences) : source._preferences;
        obj._valids = Values.merge(obj._valids, source._valids, source._invalids);
        obj._invalids = Values.merge(obj._invalids, source._invalids, source._valids);
        for (const name of source._singleRules.keys()) {
          if (obj._singleRules.has(name)) {
            obj._rules = obj._rules.filter((target) => target.keep || target.name !== name);
            obj._singleRules.delete(name);
          }
        }
        for (const test of source._rules) {
          if (!source._definition.rules[test.method].multi) {
            obj._singleRules.set(test.name, test);
          }
          obj._rules.push(test);
        }
        if (obj._flags.empty && source._flags.empty) {
          obj._flags.empty = obj._flags.empty.concat(source._flags.empty);
          const flags = Object.assign({}, source._flags);
          delete flags.empty;
          Merge(obj._flags, flags);
        } else if (source._flags.empty) {
          obj._flags.empty = source._flags.empty;
          const flags = Object.assign({}, source._flags);
          delete flags.empty;
          Merge(obj._flags, flags);
        } else {
          Merge(obj._flags, source._flags);
        }
        for (const key in source.$_terms) {
          const terms = source.$_terms[key];
          if (!terms) {
            if (!obj.$_terms[key]) {
              obj.$_terms[key] = terms;
            }
            continue;
          }
          if (!obj.$_terms[key]) {
            obj.$_terms[key] = terms.slice();
            continue;
          }
          obj.$_terms[key] = obj.$_terms[key].concat(terms);
        }
        if (this.$_root._tracer) {
          this.$_root._tracer._combine(obj, [this, source]);
        }
        return obj.$_mutateRebuild();
      }
      extend(options) {
        Assert(!options.base, "Cannot extend type with another base");
        return Extend.type(this, options);
      }
      extract(path) {
        path = Array.isArray(path) ? path : path.split(".");
        return this._ids.reach(path);
      }
      fork(paths, adjuster) {
        Assert(!this._inRuleset(), "Cannot fork inside a ruleset");
        let obj = this;
        for (let path of [].concat(paths)) {
          path = Array.isArray(path) ? path : path.split(".");
          obj = obj._ids.fork(path, adjuster, obj);
        }
        obj.$_temp.ruleset = false;
        return obj;
      }
      rule(options) {
        const def = this._definition;
        Common.assertOptions(options, Object.keys(def.modifiers));
        Assert(this.$_temp.ruleset !== false, "Cannot apply rules to empty ruleset or the last rule added does not support rule properties");
        const start = this.$_temp.ruleset === null ? this._rules.length - 1 : this.$_temp.ruleset;
        Assert(start >= 0 && start < this._rules.length, "Cannot apply rules to empty ruleset");
        const obj = this.clone();
        for (let i = start; i < obj._rules.length; ++i) {
          const original = obj._rules[i];
          const rule = Clone(original);
          for (const name in options) {
            def.modifiers[name](rule, options[name]);
            Assert(rule.name === original.name, "Cannot change rule name");
          }
          obj._rules[i] = rule;
          if (obj._singleRules.get(rule.name) === original) {
            obj._singleRules.set(rule.name, rule);
          }
        }
        obj.$_temp.ruleset = false;
        return obj.$_mutateRebuild();
      }
      get ruleset() {
        Assert(!this._inRuleset(), "Cannot start a new ruleset without closing the previous one");
        const obj = this.clone();
        obj.$_temp.ruleset = obj._rules.length;
        return obj;
      }
      get $() {
        return this.ruleset;
      }
      tailor(targets) {
        targets = [].concat(targets);
        Assert(!this._inRuleset(), "Cannot tailor inside a ruleset");
        let obj = this;
        if (this.$_terms.alterations) {
          for (const { target, adjuster } of this.$_terms.alterations) {
            if (targets.includes(target)) {
              obj = adjuster(obj);
              Assert(Common.isSchema(obj), "Alteration adjuster for", target, "failed to return a schema object");
            }
          }
        }
        obj = obj.$_modify({ each: /* @__PURE__ */ __name((item) => item.tailor(targets), "each"), ref: false });
        obj.$_temp.ruleset = false;
        return obj.$_mutateRebuild();
      }
      tracer() {
        return Trace.location ? Trace.location(this) : this;
      }
      validate(value, options) {
        return Validator.entry(value, this, options);
      }
      validateAsync(value, options) {
        return Validator.entryAsync(value, this, options);
      }
      // Extensions
      $_addRule(options) {
        if (typeof options === "string") {
          options = { name: options };
        }
        Assert(options && typeof options === "object", "Invalid options");
        Assert(options.name && typeof options.name === "string", "Invalid rule name");
        for (const key in options) {
          Assert(key[0] !== "_", "Cannot set private rule properties");
        }
        const rule = Object.assign({}, options);
        rule._resolve = [];
        rule.method = rule.method || rule.name;
        const definition = this._definition.rules[rule.method];
        const args = rule.args;
        Assert(definition, "Unknown rule", rule.method);
        const obj = this.clone();
        if (args) {
          Assert(Object.keys(args).length === 1 || Object.keys(args).length === this._definition.rules[rule.name].args.length, "Invalid rule definition for", this.type, rule.name);
          for (const key in args) {
            let arg = args[key];
            if (arg === void 0) {
              delete args[key];
              continue;
            }
            if (definition.argsByName) {
              const resolver = definition.argsByName.get(key);
              if (resolver.ref && Common.isResolvable(arg)) {
                rule._resolve.push(key);
                obj.$_mutateRegister(arg);
              } else {
                if (resolver.normalize) {
                  arg = resolver.normalize(arg);
                  args[key] = arg;
                }
                if (resolver.assert) {
                  const error = Common.validateArg(arg, key, resolver);
                  Assert(!error, error, "or reference");
                }
              }
            }
            args[key] = arg;
          }
        }
        if (!definition.multi) {
          obj._ruleRemove(rule.name, { clone: false });
          obj._singleRules.set(rule.name, rule);
        }
        if (obj.$_temp.ruleset === false) {
          obj.$_temp.ruleset = null;
        }
        if (definition.priority) {
          obj._rules.unshift(rule);
        } else {
          obj._rules.push(rule);
        }
        return obj;
      }
      $_compile(schema, options) {
        return Compile.schema(this.$_root, schema, options);
      }
      $_createError(code, value, local, state, prefs, options = {}) {
        const flags = options.flags !== false ? this._flags : {};
        const messages = options.messages ? Messages.merge(this._definition.messages, options.messages) : this._definition.messages;
        return new Errors.Report(code, value, local, flags, messages, state, prefs);
      }
      $_getFlag(name) {
        return this._flags[name];
      }
      $_getRule(name) {
        return this._singleRules.get(name);
      }
      $_mapLabels(path) {
        path = Array.isArray(path) ? path : path.split(".");
        return this._ids.labels(path);
      }
      $_match(value, state, prefs, overrides) {
        prefs = Object.assign({}, prefs);
        prefs.abortEarly = true;
        prefs._externals = false;
        state.snapshot();
        const result = !Validator.validate(value, this, state, prefs, overrides).errors;
        state.restore();
        return result;
      }
      $_modify(options) {
        Common.assertOptions(options, ["each", "once", "ref", "schema"]);
        return Modify.schema(this, options) || this;
      }
      $_mutateRebuild() {
        Assert(!this._inRuleset(), "Cannot add this rule inside a ruleset");
        this._refs.reset();
        this._ids.reset();
        const each = /* @__PURE__ */ __name((item, { source, name, path, key }) => {
          const family = this._definition[source][name] && this._definition[source][name].register;
          if (family !== false) {
            this.$_mutateRegister(item, { family, key });
          }
        }, "each");
        this.$_modify({ each });
        if (this._definition.rebuild) {
          this._definition.rebuild(this);
        }
        this.$_temp.ruleset = false;
        return this;
      }
      $_mutateRegister(schema, { family, key } = {}) {
        this._refs.register(schema, family);
        this._ids.register(schema, { key });
      }
      $_property(name) {
        return this._definition.properties[name];
      }
      $_reach(path) {
        return this._ids.reach(path);
      }
      $_rootReferences() {
        return this._refs.roots();
      }
      $_setFlag(name, value, options = {}) {
        Assert(name[0] === "_" || !this._inRuleset(), "Cannot set flag inside a ruleset");
        const flag = this._definition.flags[name] || {};
        if (DeepEqual(value, flag.default)) {
          value = void 0;
        }
        if (DeepEqual(value, this._flags[name])) {
          return this;
        }
        const obj = options.clone !== false ? this.clone() : this;
        if (value !== void 0) {
          obj._flags[name] = value;
          obj.$_mutateRegister(value);
        } else {
          delete obj._flags[name];
        }
        if (name[0] !== "_") {
          obj.$_temp.ruleset = false;
        }
        return obj;
      }
      $_validate(value, state, prefs) {
        return Validator.validate(value, this, state, prefs);
      }
      // Internals
      _assign(target) {
        target.type = this.type;
        target.$_root = this.$_root;
        target.$_temp = Object.assign({}, this.$_temp);
        target.$_temp.whens = {};
        target._ids = this._ids.clone();
        target._preferences = this._preferences;
        target._valids = this._valids && this._valids.clone();
        target._invalids = this._invalids && this._invalids.clone();
        target._rules = this._rules.slice();
        target._singleRules = Clone(this._singleRules, { shallow: true });
        target._refs = this._refs.clone();
        target._flags = Object.assign({}, this._flags);
        target._cache = null;
        target.$_terms = {};
        for (const key in this.$_terms) {
          target.$_terms[key] = this.$_terms[key] ? this.$_terms[key].slice() : null;
        }
        target.$_super = {};
        for (const override in this.$_super) {
          target.$_super[override] = this._super[override].bind(target);
        }
        return target;
      }
      _default(flag, value, options = {}) {
        Common.assertOptions(options, "literal");
        Assert(value !== void 0, "Missing", flag, "value");
        Assert(typeof value === "function" || !options.literal, "Only function value supports literal option");
        if (typeof value === "function" && options.literal) {
          value = {
            [Common.symbols.literal]: true,
            literal: value
          };
        }
        const obj = this.$_setFlag(flag, value);
        return obj;
      }
      _generate(value, state, prefs) {
        if (!this.$_terms.whens) {
          return { schema: this };
        }
        const whens = [];
        const ids = [];
        for (let i = 0; i < this.$_terms.whens.length; ++i) {
          const when = this.$_terms.whens[i];
          if (when.concat) {
            whens.push(when.concat);
            ids.push(`${i}.concat`);
            continue;
          }
          const input = when.ref ? when.ref.resolve(value, state, prefs) : value;
          const tests = when.is ? [when] : when.switch;
          const before = ids.length;
          for (let j = 0; j < tests.length; ++j) {
            const { is, then, otherwise } = tests[j];
            const baseId = `${i}${when.switch ? "." + j : ""}`;
            if (is.$_match(input, state.nest(is, `${baseId}.is`), prefs)) {
              if (then) {
                const localState = state.localize([...state.path, `${baseId}.then`], state.ancestors, state.schemas);
                const { schema: generated, id: id2 } = then._generate(value, localState, prefs);
                whens.push(generated);
                ids.push(`${baseId}.then${id2 ? `(${id2})` : ""}`);
                break;
              }
            } else if (otherwise) {
              const localState = state.localize([...state.path, `${baseId}.otherwise`], state.ancestors, state.schemas);
              const { schema: generated, id: id2 } = otherwise._generate(value, localState, prefs);
              whens.push(generated);
              ids.push(`${baseId}.otherwise${id2 ? `(${id2})` : ""}`);
              break;
            }
          }
          if (when.break && ids.length > before) {
            break;
          }
        }
        const id = ids.join(", ");
        state.mainstay.tracer.debug(state, "rule", "when", id);
        if (!id) {
          return { schema: this };
        }
        if (!state.mainstay.tracer.active && this.$_temp.whens[id]) {
          return { schema: this.$_temp.whens[id], id };
        }
        let obj = this;
        if (this._definition.generate) {
          obj = this._definition.generate(this, value, state, prefs);
        }
        for (const when of whens) {
          obj = obj.concat(when);
        }
        if (this.$_root._tracer) {
          this.$_root._tracer._combine(obj, [this, ...whens]);
        }
        this.$_temp.whens[id] = obj;
        return { schema: obj, id };
      }
      _inner(type, values, options = {}) {
        Assert(!this._inRuleset(), `Cannot set ${type} inside a ruleset`);
        const obj = this.clone();
        if (!obj.$_terms[type] || options.override) {
          obj.$_terms[type] = [];
        }
        if (options.single) {
          obj.$_terms[type].push(values);
        } else {
          obj.$_terms[type].push(...values);
        }
        obj.$_temp.ruleset = false;
        return obj;
      }
      _inRuleset() {
        return this.$_temp.ruleset !== null && this.$_temp.ruleset !== false;
      }
      _ruleRemove(name, options = {}) {
        if (!this._singleRules.has(name)) {
          return this;
        }
        const obj = options.clone !== false ? this.clone() : this;
        obj._singleRules.delete(name);
        const filtered = [];
        for (let i = 0; i < obj._rules.length; ++i) {
          const test = obj._rules[i];
          if (test.name === name && !test.keep) {
            if (obj._inRuleset() && i < obj.$_temp.ruleset) {
              --obj.$_temp.ruleset;
            }
            continue;
          }
          filtered.push(test);
        }
        obj._rules = filtered;
        return obj;
      }
      _values(values, key) {
        Common.verifyFlat(values, key.slice(1, -1));
        const obj = this.clone();
        const override = values[0] === Common.symbols.override;
        if (override) {
          values = values.slice(1);
        }
        if (!obj[key] && values.length) {
          obj[key] = new Values();
        } else if (override) {
          obj[key] = values.length ? new Values() : null;
          obj.$_mutateRebuild();
        }
        if (!obj[key]) {
          return obj;
        }
        if (override) {
          obj[key].override();
        }
        for (const value of values) {
          Assert(value !== void 0, "Cannot call allow/valid/invalid with undefined");
          Assert(value !== Common.symbols.override, "Override must be the first value");
          const other = key === "_invalids" ? "_valids" : "_invalids";
          if (obj[other]) {
            obj[other].remove(value);
            if (!obj[other].length) {
              Assert(key === "_valids" || !obj._flags.only, "Setting invalid value", value, "leaves schema rejecting all values due to previous valid rule");
              obj[other] = null;
            }
          }
          obj[key].add(value, obj._refs);
        }
        return obj;
      }
    };
    internals.Base.prototype[Common.symbols.any] = {
      version: Common.version,
      compile: Compile.compile,
      root: "$_root"
    };
    internals.Base.prototype.isImmutable = true;
    internals.Base.prototype.deny = internals.Base.prototype.invalid;
    internals.Base.prototype.disallow = internals.Base.prototype.invalid;
    internals.Base.prototype.equal = internals.Base.prototype.valid;
    internals.Base.prototype.exist = internals.Base.prototype.required;
    internals.Base.prototype.not = internals.Base.prototype.invalid;
    internals.Base.prototype.options = internals.Base.prototype.prefs;
    internals.Base.prototype.preferences = internals.Base.prototype.prefs;
    module.exports = new internals.Base();
  }
});

// ../node_modules/@hapi/joi/lib/types/any.js
var require_any = __commonJS({
  "../node_modules/@hapi/joi/lib/types/any.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Base2 = require_base();
    var Common = require_common();
    var Messages = require_messages();
    module.exports = Base2.extend({
      type: "any",
      flags: {
        only: { default: false }
      },
      terms: {
        alterations: { init: null },
        examples: { init: null },
        externals: { init: null },
        metas: { init: [] },
        notes: { init: [] },
        shared: { init: null },
        tags: { init: [] },
        whens: { init: null }
      },
      rules: {
        custom: {
          method(method, description) {
            Assert(typeof method === "function", "Method must be a function");
            Assert(description === void 0 || description && typeof description === "string", "Description must be a non-empty string");
            return this.$_addRule({ name: "custom", args: { method, description } });
          },
          validate(value, helpers, { method }) {
            try {
              return method(value, helpers);
            } catch (err) {
              return helpers.error("any.custom", { error: err });
            }
          },
          args: ["method", "description"],
          multi: true
        },
        messages: {
          method(messages) {
            return this.prefs({ messages });
          }
        },
        shared: {
          method(schema) {
            Assert(Common.isSchema(schema) && schema._flags.id, "Schema must be a schema with an id");
            const obj = this.clone();
            obj.$_terms.shared = obj.$_terms.shared || [];
            obj.$_terms.shared.push(schema);
            obj.$_mutateRegister(schema);
            return obj;
          }
        },
        warning: {
          method(code, local) {
            Assert(code && typeof code === "string", "Invalid warning code");
            return this.$_addRule({ name: "warning", args: { code, local }, warn: true });
          },
          validate(value, helpers, { code, local }) {
            return helpers.error(code, local);
          },
          args: ["code", "local"],
          multi: true
        }
      },
      modifiers: {
        keep(rule, enabled = true) {
          rule.keep = enabled;
        },
        message(rule, message) {
          rule.message = Messages.compile(message);
        },
        warn(rule, enabled = true) {
          rule.warn = enabled;
        }
      },
      manifest: {
        build(obj, desc) {
          for (const key in desc) {
            const values = desc[key];
            if (["examples", "externals", "metas", "notes", "tags"].includes(key)) {
              for (const value of values) {
                obj = obj[key.slice(0, -1)](value);
              }
              continue;
            }
            if (key === "alterations") {
              const alter = {};
              for (const { target, adjuster } of values) {
                alter[target] = adjuster;
              }
              obj = obj.alter(alter);
              continue;
            }
            if (key === "whens") {
              for (const value of values) {
                const { ref, is, not, then, otherwise, concat } = value;
                if (concat) {
                  obj = obj.concat(concat);
                } else if (ref) {
                  obj = obj.when(ref, { is, not, then, otherwise, switch: value.switch, break: value.break });
                } else {
                  obj = obj.when(is, { then, otherwise, break: value.break });
                }
              }
              continue;
            }
            if (key === "shared") {
              for (const value of values) {
                obj = obj.shared(value);
              }
            }
          }
          return obj;
        }
      },
      messages: {
        "any.custom": "{{#label}} failed custom validation because {{#error.message}}",
        "any.default": "{{#label}} threw an error when running default method",
        "any.failover": "{{#label}} threw an error when running failover method",
        "any.invalid": "{{#label}} contains an invalid value",
        "any.only": '{{#label}} must be {if(#valids.length == 1, "", "one of ")}{{#valids}}',
        "any.ref": '{{#label}} {{#arg}} references "{{#ref}}" which {{#reason}}',
        "any.required": "{{#label}} is required",
        "any.unknown": "{{#label}} is not allowed"
      }
    });
  }
});

// ../node_modules/@hapi/joi/lib/types/alternatives.js
var require_alternatives = __commonJS({
  "../node_modules/@hapi/joi/lib/types/alternatives.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Any = require_any();
    var Common = require_common();
    var Compile = require_compile();
    var Errors = require_errors();
    var Ref = require_ref();
    var internals = {};
    module.exports = Any.extend({
      type: "alternatives",
      flags: {
        match: { default: "any" }
        // 'any', 'one', 'all'
      },
      terms: {
        matches: { init: [], register: Ref.toSibling }
      },
      args(schema, ...schemas) {
        if (schemas.length === 1) {
          if (Array.isArray(schemas[0])) {
            return schema.try(...schemas[0]);
          }
        }
        return schema.try(...schemas);
      },
      validate(value, helpers) {
        const { schema, error, state, prefs } = helpers;
        if (schema._flags.match) {
          let hits = 0;
          let matched;
          for (let i = 0; i < schema.$_terms.matches.length; ++i) {
            const item = schema.$_terms.matches[i];
            const localState = state.nest(item.schema, `match.${i}`);
            localState.snapshot();
            const result = item.schema.$_validate(value, localState, prefs);
            if (!result.errors) {
              ++hits;
              matched = result.value;
            } else {
              localState.restore();
            }
          }
          if (!hits) {
            return { errors: error("alternatives.any") };
          }
          if (schema._flags.match === "one") {
            return hits === 1 ? { value: matched } : { errors: error("alternatives.one") };
          }
          return hits === schema.$_terms.matches.length ? { value } : { errors: error("alternatives.all") };
        }
        const errors = [];
        for (let i = 0; i < schema.$_terms.matches.length; ++i) {
          const item = schema.$_terms.matches[i];
          if (item.schema) {
            const localState = state.nest(item.schema, `match.${i}`);
            localState.snapshot();
            const result = item.schema.$_validate(value, localState, prefs);
            if (!result.errors) {
              return result;
            }
            localState.restore();
            errors.push({ schema: item.schema, reports: result.errors });
            continue;
          }
          const input = item.ref ? item.ref.resolve(value, state, prefs) : value;
          const tests = item.is ? [item] : item.switch;
          for (let j = 0; j < tests.length; ++j) {
            const test = tests[j];
            const { is, then, otherwise } = test;
            const id = `match.${i}${item.switch ? "." + j : ""}`;
            if (!is.$_match(input, state.nest(is, `${id}.is`), prefs)) {
              if (otherwise) {
                return otherwise.$_validate(value, state.nest(otherwise, `${id}.otherwise`), prefs);
              }
            } else if (then) {
              return then.$_validate(value, state.nest(then, `${id}.then`), prefs);
            }
          }
        }
        return internals.errors(errors, helpers);
      },
      rules: {
        conditional: {
          method(condition, options) {
            Assert(!this._flags._endedSwitch, "Unreachable condition");
            Assert(!this._flags.match, "Cannot combine match mode", this._flags.match, "with conditional rule");
            Assert(options.break === void 0, "Cannot use break option with alternatives conditional");
            const obj = this.clone();
            const match = Compile.when(obj, condition, options);
            const conditions = match.is ? [match] : match.switch;
            for (const item of conditions) {
              if (item.then && item.otherwise) {
                obj.$_setFlag("_endedSwitch", true, { clone: false });
                break;
              }
            }
            obj.$_terms.matches.push(match);
            return obj.$_mutateRebuild();
          }
        },
        match: {
          method(mode) {
            Assert(["any", "one", "all"].includes(mode), "Invalid alternatives match mode", mode);
            if (mode !== "any") {
              for (const match of this.$_terms.matches) {
                Assert(match.schema, "Cannot combine match mode", mode, "with conditional rules");
              }
            }
            return this.$_setFlag("match", mode);
          }
        },
        try: {
          method(...schemas) {
            Assert(schemas.length, "Missing alternative schemas");
            Common.verifyFlat(schemas, "try");
            Assert(!this._flags._endedSwitch, "Unreachable condition");
            const obj = this.clone();
            for (const schema of schemas) {
              obj.$_terms.matches.push({ schema: obj.$_compile(schema) });
            }
            return obj.$_mutateRebuild();
          }
        }
      },
      overrides: {
        label(name) {
          const obj = this.$_super.label(name);
          const each = /* @__PURE__ */ __name((item, source) => source.path[0] !== "is" ? item.label(name) : void 0, "each");
          return obj.$_modify({ each, ref: false });
        }
      },
      rebuild(schema) {
        const each = /* @__PURE__ */ __name((item) => {
          if (Common.isSchema(item) && item.type === "array") {
            schema.$_setFlag("_arrayItems", true, { clone: false });
          }
        }, "each");
        schema.$_modify({ each });
      },
      manifest: {
        build(obj, desc) {
          if (desc.matches) {
            for (const match of desc.matches) {
              const { schema, ref, is, not, then, otherwise } = match;
              if (schema) {
                obj = obj.try(schema);
              } else if (ref) {
                obj = obj.conditional(ref, { is, then, not, otherwise, switch: match.switch });
              } else {
                obj = obj.conditional(is, { then, otherwise });
              }
            }
          }
          return obj;
        }
      },
      messages: {
        "alternatives.all": "{{#label}} does not match all of the required types",
        "alternatives.any": "{{#label}} does not match any of the allowed types",
        "alternatives.match": "{{#label}} does not match any of the allowed types",
        "alternatives.one": "{{#label}} matches more than one allowed type",
        "alternatives.types": "{{#label}} must be one of {{#types}}"
      }
    });
    internals.errors = function(failures, { error, state }) {
      if (!failures.length) {
        return { errors: error("alternatives.any") };
      }
      if (failures.length === 1) {
        return { errors: failures[0].reports };
      }
      const valids = /* @__PURE__ */ new Set();
      const complex = [];
      for (const { reports, schema } of failures) {
        if (reports.length > 1) {
          return internals.unmatched(failures, error);
        }
        const report = reports[0];
        if (report instanceof Errors.Report === false) {
          return internals.unmatched(failures, error);
        }
        if (report.state.path.length !== state.path.length) {
          complex.push({ type: schema.type, report });
          continue;
        }
        if (report.code === "any.only") {
          for (const valid of report.local.valids) {
            valids.add(valid);
          }
          continue;
        }
        const [type, code] = report.code.split(".");
        if (code !== "base") {
          complex.push({ type: schema.type, report });
          continue;
        }
        valids.add(type);
      }
      if (!complex.length) {
        return { errors: error("alternatives.types", { types: [...valids] }) };
      }
      if (complex.length === 1) {
        return { errors: complex[0].report };
      }
      return internals.unmatched(failures, error);
    };
    internals.unmatched = function(failures, error) {
      const errors = [];
      for (const failure of failures) {
        errors.push(...failure.reports);
      }
      return { errors: error("alternatives.match", Errors.details(errors, { override: false })) };
    };
  }
});

// ../node_modules/@hapi/joi/lib/types/array.js
var require_array = __commonJS({
  "../node_modules/@hapi/joi/lib/types/array.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var DeepEqual = require_deepEqual();
    var Reach = require_reach();
    var Any = require_any();
    var Common = require_common();
    var Compile = require_compile();
    var internals = {};
    module.exports = Any.extend({
      type: "array",
      flags: {
        single: { default: false },
        sparse: { default: false }
      },
      terms: {
        items: { init: [], manifest: "schema" },
        ordered: { init: [], manifest: "schema" },
        _exclusions: { init: [] },
        _inclusions: { init: [] },
        _requireds: { init: [] }
      },
      coerce: {
        from: "object",
        method(value, { schema, state, prefs }) {
          if (!Array.isArray(value)) {
            return;
          }
          const sort = schema.$_getRule("sort");
          if (!sort) {
            return;
          }
          return internals.sort(schema, value, sort.args.options, state, prefs);
        }
      },
      validate(value, { schema, error }) {
        if (!Array.isArray(value)) {
          if (schema._flags.single) {
            const single = [value];
            single[Common.symbols.arraySingle] = true;
            return { value: single };
          }
          return { errors: error("array.base") };
        }
        if (!schema.$_getRule("items") && !schema.$_terms.externals) {
          return;
        }
        return { value: value.slice() };
      },
      rules: {
        has: {
          method(schema) {
            schema = this.$_compile(schema, { appendPath: true });
            const obj = this.$_addRule({ name: "has", args: { schema } });
            obj.$_mutateRegister(schema);
            return obj;
          },
          validate(value, { state, prefs, error }, { schema: has }) {
            const ancestors = [value, ...state.ancestors];
            for (let i = 0; i < value.length; ++i) {
              const localState = state.localize([...state.path, i], ancestors, has);
              if (has.$_match(value[i], localState, prefs)) {
                return value;
              }
            }
            const patternLabel = has._flags.label;
            if (patternLabel) {
              return error("array.hasKnown", { patternLabel });
            }
            return error("array.hasUnknown", null);
          },
          multi: true
        },
        items: {
          method(...schemas) {
            Common.verifyFlat(schemas, "items");
            const obj = this.$_addRule("items");
            for (let i = 0; i < schemas.length; ++i) {
              const type = Common.tryWithPath(() => this.$_compile(schemas[i]), i, { append: true });
              obj.$_terms.items.push(type);
            }
            return obj.$_mutateRebuild();
          },
          validate(value, { schema, error, state, prefs }) {
            const requireds = schema.$_terms._requireds.slice();
            const ordereds = schema.$_terms.ordered.slice();
            const inclusions = [...schema.$_terms._inclusions, ...requireds];
            const wasArray = !value[Common.symbols.arraySingle];
            delete value[Common.symbols.arraySingle];
            const errors = [];
            let il = value.length;
            for (let i = 0; i < il; ++i) {
              const item = value[i];
              let errored = false;
              let isValid = false;
              const key = wasArray ? i : new Number(i);
              const path = [...state.path, key];
              if (!schema._flags.sparse && item === void 0) {
                errors.push(error("array.sparse", { key, path, pos: i, value: void 0 }, state.localize(path)));
                if (prefs.abortEarly) {
                  return errors;
                }
                ordereds.shift();
                continue;
              }
              const ancestors = [value, ...state.ancestors];
              for (const exclusion of schema.$_terms._exclusions) {
                if (!exclusion.$_match(item, state.localize(path, ancestors, exclusion), prefs, { presence: "ignore" })) {
                  continue;
                }
                errors.push(error("array.excludes", { pos: i, value: item }, state.localize(path)));
                if (prefs.abortEarly) {
                  return errors;
                }
                errored = true;
                ordereds.shift();
                break;
              }
              if (errored) {
                continue;
              }
              if (schema.$_terms.ordered.length) {
                if (ordereds.length) {
                  const ordered = ordereds.shift();
                  const res = ordered.$_validate(item, state.localize(path, ancestors, ordered), prefs);
                  if (!res.errors) {
                    if (ordered._flags.result === "strip") {
                      internals.fastSplice(value, i);
                      --i;
                      --il;
                    } else if (!schema._flags.sparse && res.value === void 0) {
                      errors.push(error("array.sparse", { key, path, pos: i, value: void 0 }, state.localize(path)));
                      if (prefs.abortEarly) {
                        return errors;
                      }
                      continue;
                    } else {
                      value[i] = res.value;
                    }
                  } else {
                    errors.push(...res.errors);
                    if (prefs.abortEarly) {
                      return errors;
                    }
                  }
                  continue;
                } else if (!schema.$_terms.items.length) {
                  errors.push(error("array.orderedLength", { pos: i, limit: schema.$_terms.ordered.length }));
                  if (prefs.abortEarly) {
                    return errors;
                  }
                  break;
                }
              }
              const requiredChecks = [];
              let jl = requireds.length;
              for (let j = 0; j < jl; ++j) {
                const localState = state.localize(path, ancestors, requireds[j]);
                localState.snapshot();
                const res = requireds[j].$_validate(item, localState, prefs);
                requiredChecks[j] = res;
                if (!res.errors) {
                  value[i] = res.value;
                  isValid = true;
                  internals.fastSplice(requireds, j);
                  --j;
                  --jl;
                  if (!schema._flags.sparse && res.value === void 0) {
                    errors.push(error("array.sparse", { key, path, pos: i, value: void 0 }, state.localize(path)));
                    if (prefs.abortEarly) {
                      return errors;
                    }
                  }
                  break;
                }
                localState.restore();
              }
              if (isValid) {
                continue;
              }
              const stripUnknown = prefs.stripUnknown && !!prefs.stripUnknown.arrays || false;
              jl = inclusions.length;
              for (const inclusion of inclusions) {
                let res;
                const previousCheck = requireds.indexOf(inclusion);
                if (previousCheck !== -1) {
                  res = requiredChecks[previousCheck];
                } else {
                  const localState = state.localize(path, ancestors, inclusion);
                  localState.snapshot();
                  res = inclusion.$_validate(item, localState, prefs);
                  if (!res.errors) {
                    if (inclusion._flags.result === "strip") {
                      internals.fastSplice(value, i);
                      --i;
                      --il;
                    } else if (!schema._flags.sparse && res.value === void 0) {
                      errors.push(error("array.sparse", { key, path, pos: i, value: void 0 }, state.localize(path)));
                      errored = true;
                    } else {
                      value[i] = res.value;
                    }
                    isValid = true;
                    break;
                  }
                  localState.restore();
                }
                if (jl === 1) {
                  if (stripUnknown) {
                    internals.fastSplice(value, i);
                    --i;
                    --il;
                    isValid = true;
                    break;
                  }
                  errors.push(...res.errors);
                  if (prefs.abortEarly) {
                    return errors;
                  }
                  errored = true;
                  break;
                }
              }
              if (errored) {
                continue;
              }
              if (schema.$_terms._inclusions.length && !isValid) {
                if (stripUnknown) {
                  internals.fastSplice(value, i);
                  --i;
                  --il;
                  continue;
                }
                errors.push(error("array.includes", { pos: i, value: item }, state.localize(path)));
                if (prefs.abortEarly) {
                  return errors;
                }
              }
            }
            if (requireds.length) {
              internals.fillMissedErrors(schema, errors, requireds, value, state, prefs);
            }
            if (ordereds.length) {
              internals.fillOrderedErrors(schema, errors, ordereds, value, state, prefs);
            }
            return errors.length ? errors : value;
          },
          priority: true,
          manifest: false
        },
        length: {
          method(limit) {
            return this.$_addRule({ name: "length", args: { limit }, operator: "=" });
          },
          validate(value, helpers, { limit }, { name, operator, args }) {
            if (Common.compare(value.length, limit, operator)) {
              return value;
            }
            return helpers.error("array." + name, { limit: args.limit, value });
          },
          args: [
            {
              name: "limit",
              ref: true,
              assert: Common.limit,
              message: "must be a positive integer"
            }
          ]
        },
        max: {
          method(limit) {
            return this.$_addRule({ name: "max", method: "length", args: { limit }, operator: "<=" });
          }
        },
        min: {
          method(limit) {
            return this.$_addRule({ name: "min", method: "length", args: { limit }, operator: ">=" });
          }
        },
        ordered: {
          method(...schemas) {
            Common.verifyFlat(schemas, "ordered");
            const obj = this.$_addRule("items");
            for (let i = 0; i < schemas.length; ++i) {
              const type = Common.tryWithPath(() => this.$_compile(schemas[i]), i, { append: true });
              internals.validateSingle(type, obj);
              obj.$_mutateRegister(type);
              obj.$_terms.ordered.push(type);
            }
            return obj.$_mutateRebuild();
          }
        },
        single: {
          method(enabled) {
            const value = enabled === void 0 ? true : !!enabled;
            Assert(!value || !this._flags._arrayItems, "Cannot specify single rule when array has array items");
            return this.$_setFlag("single", value);
          }
        },
        sort: {
          method(options = {}) {
            Common.assertOptions(options, ["by", "order"]);
            const settings = {
              order: options.order || "ascending"
            };
            if (options.by) {
              settings.by = Compile.ref(options.by, { ancestor: 0 });
              Assert(!settings.by.ancestor, "Cannot sort by ancestor");
            }
            return this.$_addRule({ name: "sort", args: { options: settings } });
          },
          validate(value, { error, state, prefs, schema }, { options }) {
            const { value: sorted, errors } = internals.sort(schema, value, options, state, prefs);
            if (errors) {
              return errors;
            }
            for (let i = 0; i < value.length; ++i) {
              if (value[i] !== sorted[i]) {
                return error("array.sort", { order: options.order, by: options.by ? options.by.key : "value" });
              }
            }
            return value;
          },
          convert: true
        },
        sparse: {
          method(enabled) {
            const value = enabled === void 0 ? true : !!enabled;
            if (this._flags.sparse === value) {
              return this;
            }
            const obj = value ? this.clone() : this.$_addRule("items");
            return obj.$_setFlag("sparse", value, { clone: false });
          }
        },
        unique: {
          method(comparator, options = {}) {
            Assert(!comparator || typeof comparator === "function" || typeof comparator === "string", "comparator must be a function or a string");
            Common.assertOptions(options, ["ignoreUndefined", "separator"]);
            const rule = { name: "unique", args: { options, comparator } };
            if (comparator) {
              if (typeof comparator === "string") {
                const separator = Common.default(options.separator, ".");
                rule.path = separator ? comparator.split(separator) : [comparator];
              } else {
                rule.comparator = comparator;
              }
            }
            return this.$_addRule(rule);
          },
          validate(value, { state, error, schema }, { comparator: raw, options }, { comparator, path }) {
            const found = {
              string: /* @__PURE__ */ Object.create(null),
              number: /* @__PURE__ */ Object.create(null),
              undefined: /* @__PURE__ */ Object.create(null),
              boolean: /* @__PURE__ */ Object.create(null),
              object: /* @__PURE__ */ new Map(),
              function: /* @__PURE__ */ new Map(),
              custom: /* @__PURE__ */ new Map()
            };
            const compare = comparator || DeepEqual;
            const ignoreUndefined = options.ignoreUndefined;
            for (let i = 0; i < value.length; ++i) {
              const item = path ? Reach(value[i], path) : value[i];
              const records = comparator ? found.custom : found[typeof item];
              Assert(records, "Failed to find unique map container for type", typeof item);
              if (records instanceof Map) {
                const entries = records.entries();
                let current;
                while (!(current = entries.next()).done) {
                  if (compare(current.value[0], item)) {
                    const localState = state.localize([...state.path, i], [value, ...state.ancestors]);
                    const context = {
                      pos: i,
                      value: value[i],
                      dupePos: current.value[1],
                      dupeValue: value[current.value[1]]
                    };
                    if (path) {
                      context.path = raw;
                    }
                    return error("array.unique", context, localState);
                  }
                }
                records.set(item, i);
              } else {
                if ((!ignoreUndefined || item !== void 0) && records[item] !== void 0) {
                  const context = {
                    pos: i,
                    value: value[i],
                    dupePos: records[item],
                    dupeValue: value[records[item]]
                  };
                  if (path) {
                    context.path = raw;
                  }
                  const localState = state.localize([...state.path, i], [value, ...state.ancestors]);
                  return error("array.unique", context, localState);
                }
                records[item] = i;
              }
            }
            return value;
          },
          args: ["comparator", "options"],
          multi: true
        }
      },
      cast: {
        set: {
          from: Array.isArray,
          to(value, helpers) {
            return new Set(value);
          }
        }
      },
      rebuild(schema) {
        schema.$_terms._inclusions = [];
        schema.$_terms._exclusions = [];
        schema.$_terms._requireds = [];
        for (const type of schema.$_terms.items) {
          internals.validateSingle(type, schema);
          if (type._flags.presence === "required") {
            schema.$_terms._requireds.push(type);
          } else if (type._flags.presence === "forbidden") {
            schema.$_terms._exclusions.push(type);
          } else {
            schema.$_terms._inclusions.push(type);
          }
        }
        for (const type of schema.$_terms.ordered) {
          internals.validateSingle(type, schema);
        }
      },
      manifest: {
        build(obj, desc) {
          if (desc.items) {
            obj = obj.items(...desc.items);
          }
          if (desc.ordered) {
            obj = obj.ordered(...desc.ordered);
          }
          return obj;
        }
      },
      messages: {
        "array.base": "{{#label}} must be an array",
        "array.excludes": "{{#label}} contains an excluded value",
        "array.hasKnown": '{{#label}} does not contain at least one required match for type "{#patternLabel}"',
        "array.hasUnknown": "{{#label}} does not contain at least one required match",
        "array.includes": "{{#label}} does not match any of the allowed types",
        "array.includesRequiredBoth": "{{#label}} does not contain {{#knownMisses}} and {{#unknownMisses}} other required value(s)",
        "array.includesRequiredKnowns": "{{#label}} does not contain {{#knownMisses}}",
        "array.includesRequiredUnknowns": "{{#label}} does not contain {{#unknownMisses}} required value(s)",
        "array.length": "{{#label}} must contain {{#limit}} items",
        "array.max": "{{#label}} must contain less than or equal to {{#limit}} items",
        "array.min": "{{#label}} must contain at least {{#limit}} items",
        "array.orderedLength": "{{#label}} must contain at most {{#limit}} items",
        "array.sort": "{{#label}} must be sorted in {#order} order by {{#by}}",
        "array.sort.mismatching": "{{#label}} cannot be sorted due to mismatching types",
        "array.sort.unsupported": "{{#label}} cannot be sorted due to unsupported type {#type}",
        "array.sparse": "{{#label}} must not be a sparse array item",
        "array.unique": "{{#label}} contains a duplicate value"
      }
    });
    internals.fillMissedErrors = function(schema, errors, requireds, value, state, prefs) {
      const knownMisses = [];
      let unknownMisses = 0;
      for (const required of requireds) {
        const label = required._flags.label;
        if (label) {
          knownMisses.push(label);
        } else {
          ++unknownMisses;
        }
      }
      if (knownMisses.length) {
        if (unknownMisses) {
          errors.push(schema.$_createError("array.includesRequiredBoth", value, { knownMisses, unknownMisses }, state, prefs));
        } else {
          errors.push(schema.$_createError("array.includesRequiredKnowns", value, { knownMisses }, state, prefs));
        }
      } else {
        errors.push(schema.$_createError("array.includesRequiredUnknowns", value, { unknownMisses }, state, prefs));
      }
    };
    internals.fillOrderedErrors = function(schema, errors, ordereds, value, state, prefs) {
      const requiredOrdereds = [];
      for (const ordered of ordereds) {
        if (ordered._flags.presence === "required") {
          requiredOrdereds.push(ordered);
        }
      }
      if (requiredOrdereds.length) {
        internals.fillMissedErrors(schema, errors, requiredOrdereds, value, state, prefs);
      }
    };
    internals.fastSplice = function(arr, i) {
      let pos = i;
      while (pos < arr.length) {
        arr[pos++] = arr[pos];
      }
      --arr.length;
    };
    internals.validateSingle = function(type, obj) {
      if (type.type === "array" || type._flags._arrayItems) {
        Assert(!obj._flags.single, "Cannot specify array item with single rule enabled");
        obj.$_setFlag("_arrayItems", true, { clone: false });
      }
    };
    internals.sort = function(schema, value, settings, state, prefs) {
      const order = settings.order === "ascending" ? 1 : -1;
      const aFirst = -1 * order;
      const bFirst = order;
      const sort = /* @__PURE__ */ __name((a, b) => {
        let compare = internals.compare(a, b, aFirst, bFirst);
        if (compare !== null) {
          return compare;
        }
        if (settings.by) {
          a = settings.by.resolve(a, state, prefs);
          b = settings.by.resolve(b, state, prefs);
        }
        compare = internals.compare(a, b, aFirst, bFirst);
        if (compare !== null) {
          return compare;
        }
        const type = typeof a;
        if (type !== typeof b) {
          throw schema.$_createError("array.sort.mismatching", value, null, state, prefs);
        }
        if (type !== "number" && type !== "string") {
          throw schema.$_createError("array.sort.unsupported", value, { type }, state, prefs);
        }
        if (type === "number") {
          return (a - b) * order;
        }
        return a < b ? aFirst : bFirst;
      }, "sort");
      try {
        return { value: value.slice().sort(sort) };
      } catch (err) {
        return { errors: err };
      }
    };
    internals.compare = function(a, b, aFirst, bFirst) {
      if (a === b) {
        return 0;
      }
      if (a === void 0) {
        return 1;
      }
      if (b === void 0) {
        return -1;
      }
      if (a === null) {
        return bFirst;
      }
      if (b === null) {
        return aFirst;
      }
      return null;
    };
  }
});

// ../node_modules/@hapi/joi/lib/types/boolean.js
var require_boolean = __commonJS({
  "../node_modules/@hapi/joi/lib/types/boolean.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Any = require_any();
    var Common = require_common();
    var Values = require_values();
    var internals = {};
    internals.isBool = function(value) {
      return typeof value === "boolean";
    };
    module.exports = Any.extend({
      type: "boolean",
      flags: {
        sensitive: { default: false }
      },
      terms: {
        falsy: {
          init: null,
          manifest: "values"
        },
        truthy: {
          init: null,
          manifest: "values"
        }
      },
      coerce(value, { schema }) {
        if (typeof value === "boolean") {
          return;
        }
        if (typeof value === "string") {
          const normalized = schema._flags.sensitive ? value : value.toLowerCase();
          value = normalized === "true" ? true : normalized === "false" ? false : value;
        }
        if (typeof value !== "boolean") {
          value = schema.$_terms.truthy && schema.$_terms.truthy.has(value, null, null, !schema._flags.sensitive) || (schema.$_terms.falsy && schema.$_terms.falsy.has(value, null, null, !schema._flags.sensitive) ? false : value);
        }
        return { value };
      },
      validate(value, { error }) {
        if (typeof value !== "boolean") {
          return { value, errors: error("boolean.base") };
        }
      },
      rules: {
        truthy: {
          method(...values) {
            Common.verifyFlat(values, "truthy");
            const obj = this.clone();
            obj.$_terms.truthy = obj.$_terms.truthy || new Values();
            for (let i = 0; i < values.length; ++i) {
              const value = values[i];
              Assert(value !== void 0, "Cannot call truthy with undefined");
              obj.$_terms.truthy.add(value);
            }
            return obj;
          }
        },
        falsy: {
          method(...values) {
            Common.verifyFlat(values, "falsy");
            const obj = this.clone();
            obj.$_terms.falsy = obj.$_terms.falsy || new Values();
            for (let i = 0; i < values.length; ++i) {
              const value = values[i];
              Assert(value !== void 0, "Cannot call falsy with undefined");
              obj.$_terms.falsy.add(value);
            }
            return obj;
          }
        },
        sensitive: {
          method(enabled = true) {
            return this.$_setFlag("sensitive", enabled);
          }
        }
      },
      cast: {
        number: {
          from: internals.isBool,
          to(value, helpers) {
            return value ? 1 : 0;
          }
        },
        string: {
          from: internals.isBool,
          to(value, helpers) {
            return value ? "true" : "false";
          }
        }
      },
      manifest: {
        build(obj, desc) {
          if (desc.truthy) {
            obj = obj.truthy(...desc.truthy);
          }
          if (desc.falsy) {
            obj = obj.falsy(...desc.falsy);
          }
          return obj;
        }
      },
      messages: {
        "boolean.base": "{{#label}} must be a boolean"
      }
    });
  }
});

// ../node_modules/@hapi/joi/lib/types/date.js
var require_date = __commonJS({
  "../node_modules/@hapi/joi/lib/types/date.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Any = require_any();
    var Common = require_common();
    var Template = require_template();
    var internals = {};
    internals.isDate = function(value) {
      return value instanceof Date;
    };
    module.exports = Any.extend({
      type: "date",
      coerce: {
        from: ["number", "string"],
        method(value, { schema }) {
          return { value: internals.parse(value, schema._flags.format) || value };
        }
      },
      validate(value, { schema, error, prefs }) {
        if (value instanceof Date && !isNaN(value.getTime())) {
          return;
        }
        const format = schema._flags.format;
        if (!prefs.convert || !format || typeof value !== "string") {
          return { value, errors: error("date.base") };
        }
        return { value, errors: error("date.format", { format }) };
      },
      rules: {
        compare: {
          method: false,
          validate(value, helpers, { date }, { name, operator, args }) {
            const to = date === "now" ? Date.now() : date.getTime();
            if (Common.compare(value.getTime(), to, operator)) {
              return value;
            }
            return helpers.error("date." + name, { limit: args.date, value });
          },
          args: [
            {
              name: "date",
              ref: true,
              normalize: /* @__PURE__ */ __name((date) => {
                return date === "now" ? date : internals.parse(date);
              }, "normalize"),
              assert: /* @__PURE__ */ __name((date) => date !== null, "assert"),
              message: "must have a valid date format"
            }
          ]
        },
        format: {
          method(format) {
            Assert(["iso", "javascript", "unix"].includes(format), "Unknown date format", format);
            return this.$_setFlag("format", format);
          }
        },
        greater: {
          method(date) {
            return this.$_addRule({ name: "greater", method: "compare", args: { date }, operator: ">" });
          }
        },
        iso: {
          method() {
            return this.format("iso");
          }
        },
        less: {
          method(date) {
            return this.$_addRule({ name: "less", method: "compare", args: { date }, operator: "<" });
          }
        },
        max: {
          method(date) {
            return this.$_addRule({ name: "max", method: "compare", args: { date }, operator: "<=" });
          }
        },
        min: {
          method(date) {
            return this.$_addRule({ name: "min", method: "compare", args: { date }, operator: ">=" });
          }
        },
        timestamp: {
          method(type = "javascript") {
            Assert(["javascript", "unix"].includes(type), '"type" must be one of "javascript, unix"');
            return this.format(type);
          }
        }
      },
      cast: {
        number: {
          from: internals.isDate,
          to(value, helpers) {
            return value.getTime();
          }
        },
        string: {
          from: internals.isDate,
          to(value, { prefs }) {
            return Template.date(value, prefs);
          }
        }
      },
      messages: {
        "date.base": "{{#label}} must be a valid date",
        "date.format": '{{#label}} must be in {msg("date.format." + #format) || #format} format',
        "date.greater": '{{#label}} must be greater than "{{#limit}}"',
        "date.less": '{{#label}} must be less than "{{#limit}}"',
        "date.max": '{{#label}} must be less than or equal to "{{#limit}}"',
        "date.min": '{{#label}} must be larger than or equal to "{{#limit}}"',
        // Messages used in date.format
        "date.format.iso": "ISO 8601 date",
        "date.format.javascript": "timestamp or number of milliseconds",
        "date.format.unix": "timestamp or number of seconds"
      }
    });
    internals.parse = function(value, format) {
      if (value instanceof Date) {
        return value;
      }
      if (typeof value !== "string" && (isNaN(value) || !isFinite(value))) {
        return null;
      }
      if (/^\s*$/.test(value)) {
        return null;
      }
      if (format === "iso") {
        if (!Common.isIsoDate(value)) {
          return null;
        }
        return internals.date(value.toString());
      }
      const original = value;
      if (typeof value === "string" && /^[+-]?\d+(\.\d+)?$/.test(value)) {
        value = parseFloat(value);
      }
      if (format) {
        if (format === "javascript") {
          return internals.date(1 * value);
        }
        if (format === "unix") {
          return internals.date(1e3 * value);
        }
        if (typeof original === "string") {
          return null;
        }
      }
      return internals.date(value);
    };
    internals.date = function(value) {
      const date = new Date(value);
      if (!isNaN(date.getTime())) {
        return date;
      }
      return null;
    };
  }
});

// ../node_modules/@hapi/hoek/lib/applyToDefaults.js
var require_applyToDefaults = __commonJS({
  "../node_modules/@hapi/hoek/lib/applyToDefaults.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Clone = require_clone();
    var Merge = require_merge();
    var Reach = require_reach();
    var internals = {};
    module.exports = function(defaults, source, options = {}) {
      Assert(defaults && typeof defaults === "object", "Invalid defaults value: must be an object");
      Assert(!source || source === true || typeof source === "object", "Invalid source value: must be true, falsy or an object");
      Assert(typeof options === "object", "Invalid options: must be an object");
      if (!source) {
        return null;
      }
      if (options.shallow) {
        return internals.applyToDefaultsWithShallow(defaults, source, options);
      }
      const copy = Clone(defaults);
      if (source === true) {
        return copy;
      }
      const nullOverride = options.nullOverride !== void 0 ? options.nullOverride : false;
      return Merge(copy, source, { nullOverride, mergeArrays: false });
    };
    internals.applyToDefaultsWithShallow = function(defaults, source, options) {
      const keys = options.shallow;
      Assert(Array.isArray(keys), "Invalid keys");
      const seen = /* @__PURE__ */ new Map();
      const merge = source === true ? null : /* @__PURE__ */ new Set();
      for (let key of keys) {
        key = Array.isArray(key) ? key : key.split(".");
        const ref = Reach(defaults, key);
        if (ref && typeof ref === "object") {
          seen.set(ref, merge && Reach(source, key) || ref);
        } else if (merge) {
          merge.add(key);
        }
      }
      const copy = Clone(defaults, {}, seen);
      if (!merge) {
        return copy;
      }
      for (const key of merge) {
        internals.reachCopy(copy, source, key);
      }
      return Merge(copy, source, { mergeArrays: false, nullOverride: false });
    };
    internals.reachCopy = function(dst, src, path) {
      for (const segment of path) {
        if (!(segment in src)) {
          return;
        }
        src = src[segment];
      }
      const value = src;
      let ref = dst;
      for (let i = 0; i < path.length - 1; ++i) {
        const segment = path[i];
        if (typeof ref[segment] !== "object") {
          ref[segment] = {};
        }
        ref = ref[segment];
      }
      ref[path[path.length - 1]] = value;
    };
  }
});

// ../node_modules/@hapi/topo/lib/index.js
var require_lib3 = __commonJS({
  "../node_modules/@hapi/topo/lib/index.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var internals = {};
    exports.Sorter = class {
      constructor() {
        this._items = [];
        this.nodes = [];
      }
      add(nodes, options) {
        options = options || {};
        const before = [].concat(options.before || []);
        const after = [].concat(options.after || []);
        const group = options.group || "?";
        const sort = options.sort || 0;
        Assert(!before.includes(group), `Item cannot come before itself: ${group}`);
        Assert(!before.includes("?"), "Item cannot come before unassociated items");
        Assert(!after.includes(group), `Item cannot come after itself: ${group}`);
        Assert(!after.includes("?"), "Item cannot come after unassociated items");
        if (!Array.isArray(nodes)) {
          nodes = [nodes];
        }
        for (const node of nodes) {
          const item = {
            seq: this._items.length,
            sort,
            before,
            after,
            group,
            node
          };
          this._items.push(item);
        }
        const valid = this._sort();
        Assert(valid, "item", group !== "?" ? `added into group ${group}` : "", "created a dependencies error");
        return this.nodes;
      }
      merge(others) {
        if (!Array.isArray(others)) {
          others = [others];
        }
        for (const other of others) {
          if (other) {
            for (const item of other._items) {
              this._items.push(Object.assign({}, item));
            }
          }
        }
        this._items.sort(internals.mergeSort);
        for (let i = 0; i < this._items.length; ++i) {
          this._items[i].seq = i;
        }
        const valid = this._sort();
        Assert(valid, "merge created a dependencies error");
        return this.nodes;
      }
      _sort() {
        const graph = {};
        const graphAfters = /* @__PURE__ */ Object.create(null);
        const groups = /* @__PURE__ */ Object.create(null);
        for (const item of this._items) {
          const seq = item.seq;
          const group = item.group;
          groups[group] = groups[group] || [];
          groups[group].push(seq);
          graph[seq] = item.before;
          for (const after of item.after) {
            graphAfters[after] = graphAfters[after] || [];
            graphAfters[after].push(seq);
          }
        }
        for (const node in graph) {
          const expandedGroups = [];
          for (const graphNodeItem in graph[node]) {
            const group = graph[node][graphNodeItem];
            groups[group] = groups[group] || [];
            expandedGroups.push(...groups[group]);
          }
          graph[node] = expandedGroups;
        }
        for (const group in graphAfters) {
          if (groups[group]) {
            for (const node of groups[group]) {
              graph[node].push(...graphAfters[group]);
            }
          }
        }
        const ancestors = {};
        for (const node in graph) {
          const children = graph[node];
          for (const child of children) {
            ancestors[child] = ancestors[child] || [];
            ancestors[child].push(node);
          }
        }
        const visited = {};
        const sorted = [];
        for (let i = 0; i < this._items.length; ++i) {
          let next = i;
          if (ancestors[i]) {
            next = null;
            for (let j = 0; j < this._items.length; ++j) {
              if (visited[j] === true) {
                continue;
              }
              if (!ancestors[j]) {
                ancestors[j] = [];
              }
              const shouldSeeCount = ancestors[j].length;
              let seenCount = 0;
              for (let k = 0; k < shouldSeeCount; ++k) {
                if (visited[ancestors[j][k]]) {
                  ++seenCount;
                }
              }
              if (seenCount === shouldSeeCount) {
                next = j;
                break;
              }
            }
          }
          if (next !== null) {
            visited[next] = true;
            sorted.push(next);
          }
        }
        if (sorted.length !== this._items.length) {
          return false;
        }
        const seqIndex = {};
        for (const item of this._items) {
          seqIndex[item.seq] = item;
        }
        this._items = [];
        this.nodes = [];
        for (const value of sorted) {
          const sortedItem = seqIndex[value];
          this.nodes.push(sortedItem.node);
          this._items.push(sortedItem);
        }
        return true;
      }
    };
    internals.mergeSort = (a, b) => {
      return a.sort === b.sort ? 0 : a.sort < b.sort ? -1 : 1;
    };
  }
});

// ../node_modules/@hapi/joi/lib/types/keys.js
var require_keys = __commonJS({
  "../node_modules/@hapi/joi/lib/types/keys.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var ApplyToDefaults = require_applyToDefaults();
    var Assert = require_assert();
    var Clone = require_clone();
    var Topo = require_lib3();
    var Any = require_any();
    var Common = require_common();
    var Compile = require_compile();
    var Errors = require_errors();
    var Ref = require_ref();
    var Template = require_template();
    var internals = {
      renameDefaults: {
        alias: false,
        // Keep old value in place
        multiple: false,
        // Allow renaming multiple keys into the same target
        override: false
        // Overrides an existing key
      }
    };
    module.exports = Any.extend({
      type: "_keys",
      properties: {
        typeof: "object"
      },
      flags: {
        unknown: { default: false }
      },
      terms: {
        dependencies: { init: null },
        keys: { init: null, manifest: { mapped: { from: "schema", to: "key" } } },
        patterns: { init: null },
        renames: { init: null }
      },
      args(schema, keys) {
        return schema.keys(keys);
      },
      validate(value, { schema, error, state, prefs }) {
        if (!value || typeof value !== schema.$_property("typeof") || Array.isArray(value)) {
          return { value, errors: error("object.base", { type: schema.$_property("typeof") }) };
        }
        if (!schema.$_terms.renames && !schema.$_terms.dependencies && !schema.$_terms.keys && // null allows any keys
        !schema.$_terms.patterns && !schema.$_terms.externals) {
          return;
        }
        value = internals.clone(value, prefs);
        const errors = [];
        if (schema.$_terms.renames && !internals.rename(schema, value, state, prefs, errors)) {
          return { value, errors };
        }
        if (!schema.$_terms.keys && // null allows any keys
        !schema.$_terms.patterns && !schema.$_terms.dependencies) {
          return { value, errors };
        }
        const unprocessed = new Set(Object.keys(value));
        if (schema.$_terms.keys) {
          const ancestors = [value, ...state.ancestors];
          for (const child of schema.$_terms.keys) {
            const key = child.key;
            const item = value[key];
            unprocessed.delete(key);
            const localState = state.localize([...state.path, key], ancestors, child);
            const result = child.schema.$_validate(item, localState, prefs);
            if (result.errors) {
              if (prefs.abortEarly) {
                return { value, errors: result.errors };
              }
              errors.push(...result.errors);
            } else if (child.schema._flags.result === "strip" || result.value === void 0 && item !== void 0) {
              delete value[key];
            } else if (result.value !== void 0) {
              value[key] = result.value;
            }
          }
        }
        if (unprocessed.size || schema._flags._hasPatternMatch) {
          const early = internals.unknown(schema, value, unprocessed, errors, state, prefs);
          if (early) {
            return early;
          }
        }
        if (schema.$_terms.dependencies) {
          for (const dep of schema.$_terms.dependencies) {
            if (dep.key && dep.key.resolve(value, state, prefs, null, { shadow: false }) === void 0) {
              continue;
            }
            const failed = internals.dependencies[dep.rel](schema, dep, value, state, prefs);
            if (failed) {
              const report = schema.$_createError(failed.code, value, failed.context, state, prefs);
              if (prefs.abortEarly) {
                return { value, errors: report };
              }
              errors.push(report);
            }
          }
        }
        return { value, errors };
      },
      rules: {
        and: {
          method(...peers) {
            Common.verifyFlat(peers, "and");
            return internals.dependency(this, "and", null, peers);
          }
        },
        append: {
          method(schema) {
            if (schema === null || schema === void 0 || Object.keys(schema).length === 0) {
              return this;
            }
            return this.keys(schema);
          }
        },
        assert: {
          method(subject, schema, message) {
            if (!Template.isTemplate(subject)) {
              subject = Compile.ref(subject);
            }
            Assert(message === void 0 || typeof message === "string", "Message must be a string");
            schema = this.$_compile(schema, { appendPath: true });
            const obj = this.$_addRule({ name: "assert", args: { subject, schema, message } });
            obj.$_mutateRegister(subject);
            obj.$_mutateRegister(schema);
            return obj;
          },
          validate(value, { error, prefs, state }, { subject, schema, message }) {
            const about = subject.resolve(value, state, prefs);
            const path = Ref.isRef(subject) ? subject.absolute(state) : [];
            if (schema.$_match(about, state.localize(path, [value, ...state.ancestors], schema), prefs)) {
              return value;
            }
            return error("object.assert", { subject, message });
          },
          args: ["subject", "schema", "message"],
          multi: true
        },
        instance: {
          method(constructor, name) {
            Assert(typeof constructor === "function", "constructor must be a function");
            name = name || constructor.name;
            return this.$_addRule({ name: "instance", args: { constructor, name } });
          },
          validate(value, helpers, { constructor, name }) {
            if (value instanceof constructor) {
              return value;
            }
            return helpers.error("object.instance", { type: name, value });
          },
          args: ["constructor", "name"]
        },
        keys: {
          method(schema) {
            Assert(schema === void 0 || typeof schema === "object", "Object schema must be a valid object");
            Assert(!Common.isSchema(schema), "Object schema cannot be a joi schema");
            const obj = this.clone();
            if (!schema) {
              obj.$_terms.keys = null;
            } else if (!Object.keys(schema).length) {
              obj.$_terms.keys = new internals.Keys();
            } else {
              obj.$_terms.keys = obj.$_terms.keys ? obj.$_terms.keys.filter((child) => !schema.hasOwnProperty(child.key)) : new internals.Keys();
              for (const key in schema) {
                Common.tryWithPath(() => obj.$_terms.keys.push({ key, schema: this.$_compile(schema[key]) }), key);
              }
            }
            return obj.$_mutateRebuild();
          }
        },
        length: {
          method(limit) {
            return this.$_addRule({ name: "length", args: { limit }, operator: "=" });
          },
          validate(value, helpers, { limit }, { name, operator, args }) {
            if (Common.compare(Object.keys(value).length, limit, operator)) {
              return value;
            }
            return helpers.error("object." + name, { limit: args.limit, value });
          },
          args: [
            {
              name: "limit",
              ref: true,
              assert: Common.limit,
              message: "must be a positive integer"
            }
          ]
        },
        max: {
          method(limit) {
            return this.$_addRule({ name: "max", method: "length", args: { limit }, operator: "<=" });
          }
        },
        min: {
          method(limit) {
            return this.$_addRule({ name: "min", method: "length", args: { limit }, operator: ">=" });
          }
        },
        nand: {
          method(...peers) {
            Common.verifyFlat(peers, "nand");
            return internals.dependency(this, "nand", null, peers);
          }
        },
        or: {
          method(...peers) {
            Common.verifyFlat(peers, "or");
            return internals.dependency(this, "or", null, peers);
          }
        },
        oxor: {
          method(...peers) {
            return internals.dependency(this, "oxor", null, peers);
          }
        },
        pattern: {
          method(pattern, schema, options = {}) {
            const isRegExp = pattern instanceof RegExp;
            if (!isRegExp) {
              pattern = this.$_compile(pattern, { appendPath: true });
            }
            Assert(schema !== void 0, "Invalid rule");
            Common.assertOptions(options, ["fallthrough", "matches"]);
            if (isRegExp) {
              Assert(!pattern.flags.includes("g") && !pattern.flags.includes("y"), "pattern should not use global or sticky mode");
            }
            schema = this.$_compile(schema, { appendPath: true });
            const obj = this.clone();
            obj.$_terms.patterns = obj.$_terms.patterns || [];
            const config2 = { [isRegExp ? "regex" : "schema"]: pattern, rule: schema };
            if (options.matches) {
              config2.matches = this.$_compile(options.matches);
              if (config2.matches.type !== "array") {
                config2.matches = config2.matches.$_root.array().items(config2.matches);
              }
              obj.$_mutateRegister(config2.matches);
              obj.$_setFlag("_hasPatternMatch", true, { clone: false });
            }
            if (options.fallthrough) {
              config2.fallthrough = true;
            }
            obj.$_terms.patterns.push(config2);
            obj.$_mutateRegister(schema);
            return obj;
          }
        },
        ref: {
          method() {
            return this.$_addRule("ref");
          },
          validate(value, helpers) {
            if (Ref.isRef(value)) {
              return value;
            }
            return helpers.error("object.refType", { value });
          }
        },
        regex: {
          method() {
            return this.$_addRule("regex");
          },
          validate(value, helpers) {
            if (value instanceof RegExp) {
              return value;
            }
            return helpers.error("object.regex", { value });
          }
        },
        rename: {
          method(from, to, options = {}) {
            Assert(typeof from === "string" || from instanceof RegExp, "Rename missing the from argument");
            Assert(typeof to === "string" || to instanceof Template, "Invalid rename to argument");
            Assert(to !== from, "Cannot rename key to same name:", from);
            Common.assertOptions(options, ["alias", "ignoreUndefined", "override", "multiple"]);
            const obj = this.clone();
            obj.$_terms.renames = obj.$_terms.renames || [];
            for (const rename of obj.$_terms.renames) {
              Assert(rename.from !== from, "Cannot rename the same key multiple times");
            }
            if (to instanceof Template) {
              obj.$_mutateRegister(to);
            }
            obj.$_terms.renames.push({
              from,
              to,
              options: ApplyToDefaults(internals.renameDefaults, options)
            });
            return obj;
          }
        },
        schema: {
          method(type = "any") {
            return this.$_addRule({ name: "schema", args: { type } });
          },
          validate(value, helpers, { type }) {
            if (Common.isSchema(value) && (type === "any" || value.type === type)) {
              return value;
            }
            return helpers.error("object.schema", { type });
          }
        },
        unknown: {
          method(allow) {
            return this.$_setFlag("unknown", allow !== false);
          }
        },
        with: {
          method(key, peers, options = {}) {
            return internals.dependency(this, "with", key, peers, options);
          }
        },
        without: {
          method(key, peers, options = {}) {
            return internals.dependency(this, "without", key, peers, options);
          }
        },
        xor: {
          method(...peers) {
            Common.verifyFlat(peers, "xor");
            return internals.dependency(this, "xor", null, peers);
          }
        }
      },
      overrides: {
        default(value, options) {
          if (value === void 0) {
            value = Common.symbols.deepDefault;
          }
          return this.$_super.default(value, options);
        }
      },
      rebuild(schema) {
        if (schema.$_terms.keys) {
          const topo = new Topo.Sorter();
          for (const child of schema.$_terms.keys) {
            Common.tryWithPath(() => topo.add(child, { after: child.schema.$_rootReferences(), group: child.key }), child.key);
          }
          schema.$_terms.keys = new internals.Keys(...topo.nodes);
        }
      },
      manifest: {
        build(obj, desc) {
          if (desc.keys) {
            obj = obj.keys(desc.keys);
          }
          if (desc.dependencies) {
            for (const { rel, key = null, peers, options } of desc.dependencies) {
              obj = internals.dependency(obj, rel, key, peers, options);
            }
          }
          if (desc.patterns) {
            for (const { regex, schema, rule, fallthrough, matches } of desc.patterns) {
              obj = obj.pattern(regex || schema, rule, { fallthrough, matches });
            }
          }
          if (desc.renames) {
            for (const { from, to, options } of desc.renames) {
              obj = obj.rename(from, to, options);
            }
          }
          return obj;
        }
      },
      messages: {
        "object.and": "{{#label}} contains {{#presentWithLabels}} without its required peers {{#missingWithLabels}}",
        "object.assert": '{{#label}} is invalid because {if(#subject.key, `"` + #subject.key + `" failed to ` + (#message || "pass the assertion test"), #message || "the assertion failed")}',
        "object.base": "{{#label}} must be of type {{#type}}",
        "object.instance": '{{#label}} must be an instance of "{{#type}}"',
        "object.length": '{{#label}} must have {{#limit}} key{if(#limit == 1, "", "s")}',
        "object.max": '{{#label}} must have less than or equal to {{#limit}} key{if(#limit == 1, "", "s")}',
        "object.min": '{{#label}} must have at least {{#limit}} key{if(#limit == 1, "", "s")}',
        "object.missing": "{{#label}} must contain at least one of {{#peersWithLabels}}",
        "object.nand": '"{{#mainWithLabel}}" must not exist simultaneously with {{#peersWithLabels}}',
        "object.oxor": "{{#label}} contains a conflict between optional exclusive peers {{#peersWithLabels}}",
        "object.pattern.match": "{{#label}} keys failed to match pattern requirements",
        "object.refType": "{{#label}} must be a Joi reference",
        "object.regex": "{{#label}} must be a RegExp object",
        "object.rename.multiple": '{{#label}} cannot rename "{{#from}}" because multiple renames are disabled and another key was already renamed to "{{#to}}"',
        "object.rename.override": '{{#label}} cannot rename "{{#from}}" because override is disabled and target "{{#to}}" exists',
        "object.schema": "{{#label}} must be a Joi schema of {{#type}} type",
        "object.unknown": "{{#label}} is not allowed",
        "object.with": '"{{#mainWithLabel}}" missing required peer "{{#peerWithLabel}}"',
        "object.without": '"{{#mainWithLabel}}" conflict with forbidden peer "{{#peerWithLabel}}"',
        "object.xor": "{{#label}} contains a conflict between exclusive peers {{#peersWithLabels}}"
      }
    });
    internals.clone = function(value, prefs) {
      if (typeof value === "object") {
        if (prefs.nonEnumerables) {
          return Clone(value, { shallow: true });
        }
        const clone2 = Object.create(Object.getPrototypeOf(value));
        Object.assign(clone2, value);
        return clone2;
      }
      const clone = /* @__PURE__ */ __name(function(...args) {
        return value.apply(this, args);
      }, "clone");
      clone.prototype = Clone(value.prototype);
      Object.defineProperty(clone, "name", { value: value.name, writable: false });
      Object.defineProperty(clone, "length", { value: value.length, writable: false });
      Object.assign(clone, value);
      return clone;
    };
    internals.dependency = function(schema, rel, key, peers, options) {
      Assert(key === null || typeof key === "string", rel, "key must be a strings");
      if (!options) {
        options = peers.length > 1 && typeof peers[peers.length - 1] === "object" ? peers.pop() : {};
      }
      Common.assertOptions(options, ["separator"]);
      peers = [].concat(peers);
      const separator = Common.default(options.separator, ".");
      const paths = [];
      for (const peer of peers) {
        Assert(typeof peer === "string", rel, "peers must be a string or a reference");
        paths.push(Compile.ref(peer, { separator, ancestor: 0, prefix: false }));
      }
      if (key !== null) {
        key = Compile.ref(key, { separator, ancestor: 0, prefix: false });
      }
      const obj = schema.clone();
      obj.$_terms.dependencies = obj.$_terms.dependencies || [];
      obj.$_terms.dependencies.push(new internals.Dependency(rel, key, paths, peers));
      return obj;
    };
    internals.dependencies = {
      and(schema, dep, value, state, prefs) {
        const missing = [];
        const present = [];
        const count = dep.peers.length;
        for (const peer of dep.peers) {
          if (peer.resolve(value, state, prefs, null, { shadow: false }) === void 0) {
            missing.push(peer.key);
          } else {
            present.push(peer.key);
          }
        }
        if (missing.length !== count && present.length !== count) {
          return {
            code: "object.and",
            context: {
              present,
              presentWithLabels: internals.keysToLabels(schema, present),
              missing,
              missingWithLabels: internals.keysToLabels(schema, missing)
            }
          };
        }
      },
      nand(schema, dep, value, state, prefs) {
        const present = [];
        for (const peer of dep.peers) {
          if (peer.resolve(value, state, prefs, null, { shadow: false }) !== void 0) {
            present.push(peer.key);
          }
        }
        if (present.length !== dep.peers.length) {
          return;
        }
        const main = dep.paths[0];
        const values = dep.paths.slice(1);
        return {
          code: "object.nand",
          context: {
            main,
            mainWithLabel: internals.keysToLabels(schema, main),
            peers: values,
            peersWithLabels: internals.keysToLabels(schema, values)
          }
        };
      },
      or(schema, dep, value, state, prefs) {
        for (const peer of dep.peers) {
          if (peer.resolve(value, state, prefs, null, { shadow: false }) !== void 0) {
            return;
          }
        }
        return {
          code: "object.missing",
          context: {
            peers: dep.paths,
            peersWithLabels: internals.keysToLabels(schema, dep.paths)
          }
        };
      },
      oxor(schema, dep, value, state, prefs) {
        const present = [];
        for (const peer of dep.peers) {
          if (peer.resolve(value, state, prefs, null, { shadow: false }) !== void 0) {
            present.push(peer.key);
          }
        }
        if (!present.length || present.length === 1) {
          return;
        }
        const context = { peers: dep.paths, peersWithLabels: internals.keysToLabels(schema, dep.paths) };
        context.present = present;
        context.presentWithLabels = internals.keysToLabels(schema, present);
        return { code: "object.oxor", context };
      },
      with(schema, dep, value, state, prefs) {
        for (const peer of dep.peers) {
          if (peer.resolve(value, state, prefs, null, { shadow: false }) === void 0) {
            return {
              code: "object.with",
              context: {
                main: dep.key.key,
                mainWithLabel: internals.keysToLabels(schema, dep.key.key),
                peer: peer.key,
                peerWithLabel: internals.keysToLabels(schema, peer.key)
              }
            };
          }
        }
      },
      without(schema, dep, value, state, prefs) {
        for (const peer of dep.peers) {
          if (peer.resolve(value, state, prefs, null, { shadow: false }) !== void 0) {
            return {
              code: "object.without",
              context: {
                main: dep.key.key,
                mainWithLabel: internals.keysToLabels(schema, dep.key.key),
                peer: peer.key,
                peerWithLabel: internals.keysToLabels(schema, peer.key)
              }
            };
          }
        }
      },
      xor(schema, dep, value, state, prefs) {
        const present = [];
        for (const peer of dep.peers) {
          if (peer.resolve(value, state, prefs, null, { shadow: false }) !== void 0) {
            present.push(peer.key);
          }
        }
        if (present.length === 1) {
          return;
        }
        const context = { peers: dep.paths, peersWithLabels: internals.keysToLabels(schema, dep.paths) };
        if (present.length === 0) {
          return { code: "object.missing", context };
        }
        context.present = present;
        context.presentWithLabels = internals.keysToLabels(schema, present);
        return { code: "object.xor", context };
      }
    };
    internals.keysToLabels = function(schema, keys) {
      if (Array.isArray(keys)) {
        return keys.map((key) => schema.$_mapLabels(key));
      }
      return schema.$_mapLabels(keys);
    };
    internals.rename = function(schema, value, state, prefs, errors) {
      const renamed = {};
      for (const rename of schema.$_terms.renames) {
        const matches = [];
        const pattern = typeof rename.from !== "string";
        if (!pattern) {
          if (Object.prototype.hasOwnProperty.call(value, rename.from) && (value[rename.from] !== void 0 || !rename.options.ignoreUndefined)) {
            matches.push(rename);
          }
        } else {
          for (const from in value) {
            if (value[from] === void 0 && rename.options.ignoreUndefined) {
              continue;
            }
            if (from === rename.to) {
              continue;
            }
            const match = rename.from.exec(from);
            if (!match) {
              continue;
            }
            matches.push({ from, to: rename.to, match });
          }
        }
        for (const match of matches) {
          const from = match.from;
          let to = match.to;
          if (to instanceof Template) {
            to = to.render(value, state, prefs, match.match);
          }
          if (from === to) {
            continue;
          }
          if (!rename.options.multiple && renamed[to]) {
            errors.push(schema.$_createError("object.rename.multiple", value, { from, to, pattern }, state, prefs));
            if (prefs.abortEarly) {
              return false;
            }
          }
          if (Object.prototype.hasOwnProperty.call(value, to) && !rename.options.override && !renamed[to]) {
            errors.push(schema.$_createError("object.rename.override", value, { from, to, pattern }, state, prefs));
            if (prefs.abortEarly) {
              return false;
            }
          }
          if (value[from] === void 0) {
            delete value[to];
          } else {
            value[to] = value[from];
          }
          renamed[to] = true;
          if (!rename.options.alias) {
            delete value[from];
          }
        }
      }
      return true;
    };
    internals.unknown = function(schema, value, unprocessed, errors, state, prefs) {
      if (schema.$_terms.patterns) {
        let hasMatches = false;
        const matches = schema.$_terms.patterns.map((pattern) => {
          if (pattern.matches) {
            hasMatches = true;
            return [];
          }
        });
        const ancestors = [value, ...state.ancestors];
        for (const key of unprocessed) {
          const item = value[key];
          const path = [...state.path, key];
          for (let i = 0; i < schema.$_terms.patterns.length; ++i) {
            const pattern = schema.$_terms.patterns[i];
            if (pattern.regex) {
              const match = pattern.regex.test(key);
              state.mainstay.tracer.debug(state, "rule", `pattern.${i}`, match ? "pass" : "error");
              if (!match) {
                continue;
              }
            } else {
              if (!pattern.schema.$_match(key, state.nest(pattern.schema, `pattern.${i}`), prefs)) {
                continue;
              }
            }
            unprocessed.delete(key);
            const localState = state.localize(path, ancestors, { schema: pattern.rule, key });
            const result = pattern.rule.$_validate(item, localState, prefs);
            if (result.errors) {
              if (prefs.abortEarly) {
                return { value, errors: result.errors };
              }
              errors.push(...result.errors);
            }
            if (pattern.matches) {
              matches[i].push(key);
            }
            value[key] = result.value;
            if (!pattern.fallthrough) {
              break;
            }
          }
        }
        if (hasMatches) {
          for (let i = 0; i < matches.length; ++i) {
            const match = matches[i];
            if (!match) {
              continue;
            }
            const stpm = schema.$_terms.patterns[i].matches;
            const localState = state.localize(state.path, ancestors, stpm);
            const result = stpm.$_validate(match, localState, prefs);
            if (result.errors) {
              const details = Errors.details(result.errors, { override: false });
              details.matches = match;
              const report = schema.$_createError("object.pattern.match", value, details, state, prefs);
              if (prefs.abortEarly) {
                return { value, errors: report };
              }
              errors.push(report);
            }
          }
        }
      }
      if (!unprocessed.size || !schema.$_terms.keys && !schema.$_terms.patterns) {
        return;
      }
      if (prefs.stripUnknown && !schema._flags.unknown || prefs.skipFunctions) {
        const stripUnknown = prefs.stripUnknown ? prefs.stripUnknown === true ? true : !!prefs.stripUnknown.objects : false;
        for (const key of unprocessed) {
          if (stripUnknown) {
            delete value[key];
            unprocessed.delete(key);
          } else if (typeof value[key] === "function") {
            unprocessed.delete(key);
          }
        }
      }
      const forbidUnknown = !Common.default(schema._flags.unknown, prefs.allowUnknown);
      if (forbidUnknown) {
        for (const unprocessedKey of unprocessed) {
          const localState = state.localize([...state.path, unprocessedKey], []);
          const report = schema.$_createError("object.unknown", value[unprocessedKey], { child: unprocessedKey }, localState, prefs, { flags: false });
          if (prefs.abortEarly) {
            return { value, errors: report };
          }
          errors.push(report);
        }
      }
    };
    internals.Dependency = class {
      constructor(rel, key, peers, paths) {
        this.rel = rel;
        this.key = key;
        this.peers = peers;
        this.paths = paths;
      }
      describe() {
        const desc = {
          rel: this.rel,
          peers: this.paths
        };
        if (this.key !== null) {
          desc.key = this.key.key;
        }
        if (this.peers[0].separator !== ".") {
          desc.options = { separator: this.peers[0].separator };
        }
        return desc;
      }
    };
    internals.Keys = class extends Array {
      concat(source) {
        const result = this.slice();
        const keys = /* @__PURE__ */ new Map();
        for (let i = 0; i < result.length; ++i) {
          keys.set(result[i].key, i);
        }
        for (const item of source) {
          const key = item.key;
          const pos = keys.get(key);
          if (pos !== void 0) {
            result[pos] = { key, schema: result[pos].schema.concat(item.schema) };
          } else {
            result.push(item);
          }
        }
        return result;
      }
    };
  }
});

// ../node_modules/@hapi/joi/lib/types/function.js
var require_function = __commonJS({
  "../node_modules/@hapi/joi/lib/types/function.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Keys = require_keys();
    module.exports = Keys.extend({
      type: "function",
      properties: {
        typeof: "function"
      },
      rules: {
        arity: {
          method(n) {
            Assert(Number.isSafeInteger(n) && n >= 0, "n must be a positive integer");
            return this.$_addRule({ name: "arity", args: { n } });
          },
          validate(value, helpers, { n }) {
            if (value.length === n) {
              return value;
            }
            return helpers.error("function.arity", { n });
          }
        },
        class: {
          method() {
            return this.$_addRule("class");
          },
          validate(value, helpers) {
            if (/^\s*class\s/.test(value.toString())) {
              return value;
            }
            return helpers.error("function.class", { value });
          }
        },
        minArity: {
          method(n) {
            Assert(Number.isSafeInteger(n) && n > 0, "n must be a strict positive integer");
            return this.$_addRule({ name: "minArity", args: { n } });
          },
          validate(value, helpers, { n }) {
            if (value.length >= n) {
              return value;
            }
            return helpers.error("function.minArity", { n });
          }
        },
        maxArity: {
          method(n) {
            Assert(Number.isSafeInteger(n) && n >= 0, "n must be a positive integer");
            return this.$_addRule({ name: "maxArity", args: { n } });
          },
          validate(value, helpers, { n }) {
            if (value.length <= n) {
              return value;
            }
            return helpers.error("function.maxArity", { n });
          }
        }
      },
      messages: {
        "function.arity": "{{#label}} must have an arity of {{#n}}",
        "function.class": "{{#label}} must be a class",
        "function.maxArity": "{{#label}} must have an arity lesser or equal to {{#n}}",
        "function.minArity": "{{#label}} must have an arity greater or equal to {{#n}}"
      }
    });
  }
});

// ../node_modules/@hapi/joi/lib/types/link.js
var require_link = __commonJS({
  "../node_modules/@hapi/joi/lib/types/link.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Any = require_any();
    var Common = require_common();
    var Compile = require_compile();
    var Errors = require_errors();
    var internals = {};
    module.exports = Any.extend({
      type: "link",
      properties: {
        schemaChain: true
      },
      terms: {
        link: { init: null, manifest: "single", register: false }
      },
      args(schema, ref) {
        return schema.ref(ref);
      },
      validate(value, { schema, state, prefs }) {
        Assert(schema.$_terms.link, "Uninitialized link schema");
        const linked = internals.generate(schema, value, state, prefs);
        const ref = schema.$_terms.link[0].ref;
        return linked.$_validate(value, state.nest(linked, `link:${ref.display}:${linked.type}`), prefs);
      },
      generate(schema, value, state, prefs) {
        return internals.generate(schema, value, state, prefs);
      },
      rules: {
        ref: {
          method(ref) {
            Assert(!this.$_terms.link, "Cannot reinitialize schema");
            ref = Compile.ref(ref);
            Assert(ref.type === "value" || ref.type === "local", "Invalid reference type:", ref.type);
            Assert(ref.type === "local" || ref.ancestor === "root" || ref.ancestor > 0, "Link cannot reference itself");
            const obj = this.clone();
            obj.$_terms.link = [{ ref }];
            return obj;
          }
        },
        relative: {
          method(enabled = true) {
            return this.$_setFlag("relative", enabled);
          }
        }
      },
      overrides: {
        concat(source) {
          Assert(this.$_terms.link, "Uninitialized link schema");
          Assert(Common.isSchema(source), "Invalid schema object");
          Assert(source.type !== "link", "Cannot merge type link with another link");
          const obj = this.clone();
          if (!obj.$_terms.whens) {
            obj.$_terms.whens = [];
          }
          obj.$_terms.whens.push({ concat: source });
          return obj.$_mutateRebuild();
        }
      },
      manifest: {
        build(obj, desc) {
          Assert(desc.link, "Invalid link description missing link");
          return obj.ref(desc.link);
        }
      }
    });
    internals.generate = function(schema, value, state, prefs) {
      let linked = state.mainstay.links.get(schema);
      if (linked) {
        return linked._generate(value, state, prefs).schema;
      }
      const ref = schema.$_terms.link[0].ref;
      const { perspective, path } = internals.perspective(ref, state);
      internals.assert(perspective, "which is outside of schema boundaries", ref, schema, state, prefs);
      try {
        linked = path.length ? perspective.$_reach(path) : perspective;
      } catch (ignoreErr) {
        internals.assert(false, "to non-existing schema", ref, schema, state, prefs);
      }
      internals.assert(linked.type !== "link", "which is another link", ref, schema, state, prefs);
      if (!schema._flags.relative) {
        state.mainstay.links.set(schema, linked);
      }
      return linked._generate(value, state, prefs).schema;
    };
    internals.perspective = function(ref, state) {
      if (ref.type === "local") {
        for (const { schema, key } of state.schemas) {
          const id = schema._flags.id || key;
          if (id === ref.path[0]) {
            return { perspective: schema, path: ref.path.slice(1) };
          }
          if (schema.$_terms.shared) {
            for (const shared of schema.$_terms.shared) {
              if (shared._flags.id === ref.path[0]) {
                return { perspective: shared, path: ref.path.slice(1) };
              }
            }
          }
        }
        return { perspective: null, path: null };
      }
      if (ref.ancestor === "root") {
        return { perspective: state.schemas[state.schemas.length - 1].schema, path: ref.path };
      }
      return { perspective: state.schemas[ref.ancestor] && state.schemas[ref.ancestor].schema, path: ref.path };
    };
    internals.assert = function(condition, message, ref, schema, state, prefs) {
      if (condition) {
        return;
      }
      Assert(false, `"${Errors.label(schema._flags, state, prefs)}" contains link reference "${ref.display}" ${message}`);
    };
  }
});

// ../node_modules/@hapi/joi/lib/types/number.js
var require_number = __commonJS({
  "../node_modules/@hapi/joi/lib/types/number.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Any = require_any();
    var Common = require_common();
    var internals = {
      numberRx: /^\s*[+-]?(?:(?:\d+(?:\.\d*)?)|(?:\.\d+))(?:e([+-]?\d+))?\s*$/i,
      precisionRx: /(?:\.(\d+))?(?:[eE]([+-]?\d+))?$/
    };
    module.exports = Any.extend({
      type: "number",
      flags: {
        unsafe: { default: false }
      },
      coerce: {
        from: "string",
        method(value, { schema, error }) {
          const matches = value.match(internals.numberRx);
          if (!matches) {
            return;
          }
          value = value.trim();
          const result = { value: parseFloat(value) };
          if (result.value === 0) {
            result.value = 0;
          }
          if (!schema._flags.unsafe) {
            if (value.match(/e/i)) {
              const constructed = internals.normalizeExponent(`${result.value / Math.pow(10, matches[1])}e${matches[1]}`);
              if (constructed !== internals.normalizeExponent(value)) {
                result.errors = error("number.unsafe");
                return result;
              }
            } else {
              const string2 = result.value.toString();
              if (string2.match(/e/i)) {
                return result;
              }
              if (string2 !== internals.normalizeDecimal(value)) {
                result.errors = error("number.unsafe");
                return result;
              }
            }
          }
          return result;
        }
      },
      validate(value, { schema, error, prefs }) {
        if (value === Infinity || value === -Infinity) {
          return { value, errors: error("number.infinity") };
        }
        if (!Common.isNumber(value)) {
          return { value, errors: error("number.base") };
        }
        const result = { value };
        if (prefs.convert) {
          const rule = schema.$_getRule("precision");
          if (rule) {
            const precision = Math.pow(10, rule.args.limit);
            result.value = Math.round(result.value * precision) / precision;
          }
        }
        if (result.value === 0) {
          result.value = 0;
        }
        if (!schema._flags.unsafe && (value > Number.MAX_SAFE_INTEGER || value < Number.MIN_SAFE_INTEGER)) {
          result.errors = error("number.unsafe");
        }
        return result;
      },
      rules: {
        compare: {
          method: false,
          validate(value, helpers, { limit }, { name, operator, args }) {
            if (Common.compare(value, limit, operator)) {
              return value;
            }
            return helpers.error("number." + name, { limit: args.limit, value });
          },
          args: [
            {
              name: "limit",
              ref: true,
              assert: Common.isNumber,
              message: "must be a number"
            }
          ]
        },
        greater: {
          method(limit) {
            return this.$_addRule({ name: "greater", method: "compare", args: { limit }, operator: ">" });
          }
        },
        integer: {
          method() {
            return this.$_addRule("integer");
          },
          validate(value, helpers) {
            if (Math.trunc(value) - value === 0) {
              return value;
            }
            return helpers.error("number.integer");
          }
        },
        less: {
          method(limit) {
            return this.$_addRule({ name: "less", method: "compare", args: { limit }, operator: "<" });
          }
        },
        max: {
          method(limit) {
            return this.$_addRule({ name: "max", method: "compare", args: { limit }, operator: "<=" });
          }
        },
        min: {
          method(limit) {
            return this.$_addRule({ name: "min", method: "compare", args: { limit }, operator: ">=" });
          }
        },
        multiple: {
          method(base) {
            return this.$_addRule({ name: "multiple", args: { base } });
          },
          validate(value, helpers, { base }, options) {
            if (value % base === 0) {
              return value;
            }
            return helpers.error("number.multiple", { multiple: options.args.base, value });
          },
          args: [
            {
              name: "base",
              ref: true,
              assert: /* @__PURE__ */ __name((value) => typeof value === "number" && isFinite(value) && value > 0, "assert"),
              message: "must be a positive number"
            }
          ],
          multi: true
        },
        negative: {
          method() {
            return this.sign("negative");
          }
        },
        port: {
          method() {
            return this.$_addRule("port");
          },
          validate(value, helpers) {
            if (Number.isSafeInteger(value) && value >= 0 && value <= 65535) {
              return value;
            }
            return helpers.error("number.port");
          }
        },
        positive: {
          method() {
            return this.sign("positive");
          }
        },
        precision: {
          method(limit) {
            Assert(Number.isSafeInteger(limit), "limit must be an integer");
            return this.$_addRule({ name: "precision", args: { limit } });
          },
          validate(value, helpers, { limit }) {
            const places = value.toString().match(internals.precisionRx);
            const decimals = Math.max((places[1] ? places[1].length : 0) - (places[2] ? parseInt(places[2], 10) : 0), 0);
            if (decimals <= limit) {
              return value;
            }
            return helpers.error("number.precision", { limit, value });
          },
          convert: true
        },
        sign: {
          method(sign) {
            Assert(["negative", "positive"].includes(sign), "Invalid sign", sign);
            return this.$_addRule({ name: "sign", args: { sign } });
          },
          validate(value, helpers, { sign }) {
            if (sign === "negative" && value < 0 || sign === "positive" && value > 0) {
              return value;
            }
            return helpers.error(`number.${sign}`);
          }
        },
        unsafe: {
          method(enabled = true) {
            Assert(typeof enabled === "boolean", "enabled must be a boolean");
            return this.$_setFlag("unsafe", enabled);
          }
        }
      },
      cast: {
        string: {
          from: /* @__PURE__ */ __name((value) => typeof value === "number", "from"),
          to(value, helpers) {
            return value.toString();
          }
        }
      },
      messages: {
        "number.base": "{{#label}} must be a number",
        "number.greater": "{{#label}} must be greater than {{#limit}}",
        "number.infinity": "{{#label}} cannot be infinity",
        "number.integer": "{{#label}} must be an integer",
        "number.less": "{{#label}} must be less than {{#limit}}",
        "number.max": "{{#label}} must be less than or equal to {{#limit}}",
        "number.min": "{{#label}} must be larger than or equal to {{#limit}}",
        "number.multiple": "{{#label}} must be a multiple of {{#multiple}}",
        "number.negative": "{{#label}} must be a negative number",
        "number.port": "{{#label}} must be a valid port",
        "number.positive": "{{#label}} must be a positive number",
        "number.precision": "{{#label}} must have no more than {{#limit}} decimal places",
        "number.unsafe": "{{#label}} must be a safe number"
      }
    });
    internals.normalizeExponent = function(str) {
      return str.replace(/E/, "e").replace(/\.(\d*[1-9])?0+e/, ".$1e").replace(/\.e/, "e").replace(/e\+/, "e").replace(/^\+/, "").replace(/^(-?)0+([1-9])/, "$1$2");
    };
    internals.normalizeDecimal = function(str) {
      str = str.replace(/^\+/, "").replace(/\.0+$/, "").replace(/^(-?)\.([^\.]*)$/, "$10.$2").replace(/^(-?)0+([1-9])/, "$1$2");
      if (str.includes(".") && str.endsWith("0")) {
        str = str.replace(/0+$/, "");
      }
      if (str === "-0") {
        return "0";
      }
      return str;
    };
  }
});

// ../node_modules/@hapi/joi/lib/types/object.js
var require_object = __commonJS({
  "../node_modules/@hapi/joi/lib/types/object.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Keys = require_keys();
    module.exports = Keys.extend({
      type: "object",
      cast: {
        map: {
          from: /* @__PURE__ */ __name((value) => value && typeof value === "object", "from"),
          to(value, helpers) {
            return new Map(Object.entries(value));
          }
        }
      }
    });
  }
});

// node-built-in-modules:url
import libDefault from "url";
var require_url = __commonJS({
  "node-built-in-modules:url"(exports, module) {
    init_modules_watch_stub();
    module.exports = libDefault;
  }
});

// ../node_modules/@hapi/address/lib/errors.js
var require_errors2 = __commonJS({
  "../node_modules/@hapi/address/lib/errors.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    exports.codes = {
      EMPTY_STRING: "Address must be a non-empty string",
      FORBIDDEN_UNICODE: "Address contains forbidden Unicode characters",
      MULTIPLE_AT_CHAR: "Address cannot contain more than one @ character",
      MISSING_AT_CHAR: "Address must contain one @ character",
      EMPTY_LOCAL: "Address local part cannot be empty",
      ADDRESS_TOO_LONG: "Address too long",
      LOCAL_TOO_LONG: "Address local part too long",
      EMPTY_LOCAL_SEGMENT: "Address local part contains empty dot-separated segment",
      INVALID_LOCAL_CHARS: "Address local part contains invalid character",
      DOMAIN_NON_EMPTY_STRING: "Domain must be a non-empty string",
      DOMAIN_TOO_LONG: "Domain too long",
      DOMAIN_INVALID_UNICODE_CHARS: "Domain contains forbidden Unicode characters",
      DOMAIN_INVALID_CHARS: "Domain contains invalid character",
      DOMAIN_INVALID_TLDS_CHARS: "Domain contains invalid tld character",
      DOMAIN_SEGMENTS_COUNT: "Domain lacks the minimum required number of segments",
      DOMAIN_FORBIDDEN_TLDS: "Domain uses forbidden TLD",
      DOMAIN_EMPTY_SEGMENT: "Domain contains empty dot-separated segment",
      DOMAIN_LONG_SEGMENT: "Domain contains dot-separated segment that is too long"
    };
    exports.code = function(code) {
      return { code, error: exports.codes[code] };
    };
  }
});

// ../node_modules/@hapi/address/lib/domain.js
var require_domain = __commonJS({
  "../node_modules/@hapi/address/lib/domain.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Url = require_url();
    var Errors = require_errors2();
    var internals = {
      minDomainSegments: 2,
      nonAsciiRx: /[^\x00-\x7f]/,
      domainControlRx: /[\x00-\x20@\:\/]/,
      // Control + space + separators
      tldSegmentRx: /^[a-zA-Z](?:[a-zA-Z0-9\-]*[a-zA-Z0-9])?$/,
      domainSegmentRx: /^[a-zA-Z0-9](?:[a-zA-Z0-9\-]*[a-zA-Z0-9])?$/,
      URL: Url.URL || URL
      // $lab:coverage:ignore$
    };
    exports.analyze = function(domain, options = {}) {
      if (typeof domain !== "string") {
        throw new Error("Invalid input: domain must be a string");
      }
      if (!domain) {
        return Errors.code("DOMAIN_NON_EMPTY_STRING");
      }
      if (domain.length > 256) {
        return Errors.code("DOMAIN_TOO_LONG");
      }
      const ascii = !internals.nonAsciiRx.test(domain);
      if (!ascii) {
        if (options.allowUnicode === false) {
          return Errors.code("DOMAIN_INVALID_UNICODE_CHARS");
        }
        domain = domain.normalize("NFC");
      }
      if (internals.domainControlRx.test(domain)) {
        return Errors.code("DOMAIN_INVALID_CHARS");
      }
      domain = internals.punycode(domain);
      const minDomainSegments = options.minDomainSegments || internals.minDomainSegments;
      const segments = domain.split(".");
      if (segments.length < minDomainSegments) {
        return Errors.code("DOMAIN_SEGMENTS_COUNT");
      }
      const tlds = options.tlds;
      if (tlds) {
        const tld = segments[segments.length - 1].toLowerCase();
        if (tlds.deny && tlds.deny.has(tld) || tlds.allow && !tlds.allow.has(tld)) {
          return Errors.code("DOMAIN_FORBIDDEN_TLDS");
        }
      }
      for (let i = 0; i < segments.length; ++i) {
        const segment = segments[i];
        if (!segment.length) {
          return Errors.code("DOMAIN_EMPTY_SEGMENT");
        }
        if (segment.length > 63) {
          return Errors.code("DOMAIN_LONG_SEGMENT");
        }
        if (i < segments.length - 1) {
          if (!internals.domainSegmentRx.test(segment)) {
            return Errors.code("DOMAIN_INVALID_CHARS");
          }
        } else {
          if (!internals.tldSegmentRx.test(segment)) {
            return Errors.code("DOMAIN_INVALID_TLDS_CHARS");
          }
        }
      }
      return null;
    };
    exports.isValid = function(domain, options) {
      return !exports.analyze(domain, options);
    };
    internals.punycode = function(domain) {
      try {
        return new internals.URL(`http://${domain}`).host;
      } catch (err) {
        return domain;
      }
    };
  }
});

// node-built-in-modules:util
import libDefault2 from "util";
var require_util = __commonJS({
  "node-built-in-modules:util"(exports, module) {
    init_modules_watch_stub();
    module.exports = libDefault2;
  }
});

// ../node_modules/@hapi/address/lib/email.js
var require_email = __commonJS({
  "../node_modules/@hapi/address/lib/email.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Util = require_util();
    var Domain = require_domain();
    var Errors = require_errors2();
    var internals = {
      nonAsciiRx: /[^\x00-\x7f]/,
      encoder: new (Util.TextEncoder || TextEncoder)()
      // $lab:coverage:ignore$
    };
    exports.analyze = function(email, options) {
      return internals.email(email, options);
    };
    exports.isValid = function(email, options) {
      return !internals.email(email, options);
    };
    internals.email = function(email, options = {}) {
      if (typeof email !== "string") {
        throw new Error("Invalid input: email must be a string");
      }
      if (!email) {
        return Errors.code("EMPTY_STRING");
      }
      const ascii = !internals.nonAsciiRx.test(email);
      if (!ascii) {
        if (options.allowUnicode === false) {
          return Errors.code("FORBIDDEN_UNICODE");
        }
        email = email.normalize("NFC");
      }
      const parts = email.split("@");
      if (parts.length !== 2) {
        return parts.length > 2 ? Errors.code("MULTIPLE_AT_CHAR") : Errors.code("MISSING_AT_CHAR");
      }
      const [local, domain] = parts;
      if (!local) {
        return Errors.code("EMPTY_LOCAL");
      }
      if (!options.ignoreLength) {
        if (email.length > 254) {
          return Errors.code("ADDRESS_TOO_LONG");
        }
        if (internals.encoder.encode(local).length > 64) {
          return Errors.code("LOCAL_TOO_LONG");
        }
      }
      return internals.local(local, ascii) || Domain.analyze(domain, options);
    };
    internals.local = function(local, ascii) {
      const segments = local.split(".");
      for (const segment of segments) {
        if (!segment.length) {
          return Errors.code("EMPTY_LOCAL_SEGMENT");
        }
        if (ascii) {
          if (!internals.atextRx.test(segment)) {
            return Errors.code("INVALID_LOCAL_CHARS");
          }
          continue;
        }
        for (const char of segment) {
          if (internals.atextRx.test(char)) {
            continue;
          }
          const binary = internals.binary(char);
          if (!internals.atomRx.test(binary)) {
            return Errors.code("INVALID_LOCAL_CHARS");
          }
        }
      }
    };
    internals.binary = function(char) {
      return Array.from(internals.encoder.encode(char)).map((v) => String.fromCharCode(v)).join("");
    };
    internals.atextRx = /^[\w!#\$%&'\*\+\-/=\?\^`\{\|\}~]+$/;
    internals.atomRx = new RegExp([
      //  %xC2-DF UTF8-tail
      "(?:[\\xc2-\\xdf][\\x80-\\xbf])",
      //  %xE0 %xA0-BF UTF8-tail              %xE1-EC 2( UTF8-tail )            %xED %x80-9F UTF8-tail              %xEE-EF 2( UTF8-tail )
      "(?:\\xe0[\\xa0-\\xbf][\\x80-\\xbf])|(?:[\\xe1-\\xec][\\x80-\\xbf]{2})|(?:\\xed[\\x80-\\x9f][\\x80-\\xbf])|(?:[\\xee-\\xef][\\x80-\\xbf]{2})",
      //  %xF0 %x90-BF 2( UTF8-tail )            %xF1-F3 3( UTF8-tail )            %xF4 %x80-8F 2( UTF8-tail )
      "(?:\\xf0[\\x90-\\xbf][\\x80-\\xbf]{2})|(?:[\\xf1-\\xf3][\\x80-\\xbf]{3})|(?:\\xf4[\\x80-\\x8f][\\x80-\\xbf]{2})"
    ].join("|"));
  }
});

// ../node_modules/@hapi/hoek/lib/escapeRegex.js
var require_escapeRegex = __commonJS({
  "../node_modules/@hapi/hoek/lib/escapeRegex.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    module.exports = function(string2) {
      return string2.replace(/[\^\$\.\*\+\-\?\=\!\:\|\\\/\(\)\[\]\{\}\,]/g, "\\$&");
    };
  }
});

// ../node_modules/@hapi/address/lib/uri.js
var require_uri = __commonJS({
  "../node_modules/@hapi/address/lib/uri.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var EscapeRegex = require_escapeRegex();
    var internals = {};
    internals.generate = function() {
      const rfc3986 = {};
      const hexDigit = "\\dA-Fa-f";
      const hexDigitOnly = "[" + hexDigit + "]";
      const unreserved = "\\w-\\.~";
      const subDelims = "!\\$&'\\(\\)\\*\\+,;=";
      const pctEncoded = "%" + hexDigit;
      const pchar = unreserved + pctEncoded + subDelims + ":@";
      const pcharOnly = "[" + pchar + "]";
      const decOctect = "(?:0{0,2}\\d|0?[1-9]\\d|1\\d\\d|2[0-4]\\d|25[0-5])";
      rfc3986.ipv4address = "(?:" + decOctect + "\\.){3}" + decOctect;
      const h16 = hexDigitOnly + "{1,4}";
      const ls32 = "(?:" + h16 + ":" + h16 + "|" + rfc3986.ipv4address + ")";
      const IPv6SixHex = "(?:" + h16 + ":){6}" + ls32;
      const IPv6FiveHex = "::(?:" + h16 + ":){5}" + ls32;
      const IPv6FourHex = "(?:" + h16 + ")?::(?:" + h16 + ":){4}" + ls32;
      const IPv6ThreeHex = "(?:(?:" + h16 + ":){0,1}" + h16 + ")?::(?:" + h16 + ":){3}" + ls32;
      const IPv6TwoHex = "(?:(?:" + h16 + ":){0,2}" + h16 + ")?::(?:" + h16 + ":){2}" + ls32;
      const IPv6OneHex = "(?:(?:" + h16 + ":){0,3}" + h16 + ")?::" + h16 + ":" + ls32;
      const IPv6NoneHex = "(?:(?:" + h16 + ":){0,4}" + h16 + ")?::" + ls32;
      const IPv6NoneHex2 = "(?:(?:" + h16 + ":){0,5}" + h16 + ")?::" + h16;
      const IPv6NoneHex3 = "(?:(?:" + h16 + ":){0,6}" + h16 + ")?::";
      rfc3986.ipv4Cidr = "(?:\\d|[1-2]\\d|3[0-2])";
      rfc3986.ipv6Cidr = "(?:0{0,2}\\d|0?[1-9]\\d|1[01]\\d|12[0-8])";
      rfc3986.ipv6address = "(?:" + IPv6SixHex + "|" + IPv6FiveHex + "|" + IPv6FourHex + "|" + IPv6ThreeHex + "|" + IPv6TwoHex + "|" + IPv6OneHex + "|" + IPv6NoneHex + "|" + IPv6NoneHex2 + "|" + IPv6NoneHex3 + ")";
      rfc3986.ipvFuture = "v" + hexDigitOnly + "+\\.[" + unreserved + subDelims + ":]+";
      rfc3986.scheme = "[a-zA-Z][a-zA-Z\\d+-\\.]*";
      rfc3986.schemeRegex = new RegExp(rfc3986.scheme);
      const userinfo = "[" + unreserved + pctEncoded + subDelims + ":]*";
      const IPLiteral = "\\[(?:" + rfc3986.ipv6address + "|" + rfc3986.ipvFuture + ")\\]";
      const regName = "[" + unreserved + pctEncoded + subDelims + "]{1,255}";
      const host = "(?:" + IPLiteral + "|" + rfc3986.ipv4address + "|" + regName + ")";
      const port = "\\d*";
      const authority = "(?:" + userinfo + "@)?" + host + "(?::" + port + ")?";
      const authorityCapture = "(?:" + userinfo + "@)?(" + host + ")(?::" + port + ")?";
      const segment = pcharOnly + "*";
      const segmentNz = pcharOnly + "+";
      const segmentNzNc = "[" + unreserved + pctEncoded + subDelims + "@]+";
      const pathEmpty = "";
      const pathAbEmpty = "(?:\\/" + segment + ")*";
      const pathAbsolute = "\\/(?:" + segmentNz + pathAbEmpty + ")?";
      const pathRootless = segmentNz + pathAbEmpty;
      const pathNoScheme = segmentNzNc + pathAbEmpty;
      const pathAbNoAuthority = "(?:\\/\\/\\/" + segment + pathAbEmpty + ")";
      rfc3986.hierPart = "(?:(?:\\/\\/" + authority + pathAbEmpty + ")|" + pathAbsolute + "|" + pathRootless + "|" + pathAbNoAuthority + ")";
      rfc3986.hierPartCapture = "(?:(?:\\/\\/" + authorityCapture + pathAbEmpty + ")|" + pathAbsolute + "|" + pathRootless + ")";
      rfc3986.relativeRef = "(?:(?:\\/\\/" + authority + pathAbEmpty + ")|" + pathAbsolute + "|" + pathNoScheme + "|" + pathEmpty + ")";
      rfc3986.query = "[" + pchar + "\\/\\?]*(?=#|$)";
      rfc3986.queryWithSquareBrackets = "[" + pchar + "\\[\\]\\/\\?]*(?=#|$)";
      rfc3986.fragment = "[" + pchar + "\\/\\?]*";
      return rfc3986;
    };
    internals.rfc3986 = internals.generate();
    exports.ip = {
      v4Cidr: internals.rfc3986.ipv4Cidr,
      v6Cidr: internals.rfc3986.ipv6Cidr,
      ipv4: internals.rfc3986.ipv4address,
      ipv6: internals.rfc3986.ipv6address,
      ipvfuture: internals.rfc3986.ipvFuture
    };
    internals.createRegex = function(options) {
      const rfc = internals.rfc3986;
      const query = options.allowQuerySquareBrackets ? rfc.queryWithSquareBrackets : rfc.query;
      const suffix = "(?:\\?" + query + ")?(?:#" + rfc.fragment + ")?";
      if (options.relativeOnly) {
        return internals.wrap(rfc.relativeRef + suffix);
      }
      let customScheme = "";
      if (options.scheme) {
        Assert(options.scheme instanceof RegExp || typeof options.scheme === "string" || Array.isArray(options.scheme), "scheme must be a RegExp, String, or Array");
        const schemes = [].concat(options.scheme);
        Assert(schemes.length >= 1, "scheme must have at least 1 scheme specified");
        const selections = [];
        for (let i = 0; i < schemes.length; ++i) {
          const scheme2 = schemes[i];
          Assert(scheme2 instanceof RegExp || typeof scheme2 === "string", "scheme at position " + i + " must be a RegExp or String");
          if (scheme2 instanceof RegExp) {
            selections.push(scheme2.source.toString());
          } else {
            Assert(rfc.schemeRegex.test(scheme2), "scheme at position " + i + " must be a valid scheme");
            selections.push(EscapeRegex(scheme2));
          }
        }
        customScheme = selections.join("|");
      }
      const scheme = customScheme ? "(?:" + customScheme + ")" : rfc.scheme;
      const absolute = "(?:" + scheme + ":" + (options.domain ? rfc.hierPartCapture : rfc.hierPart) + ")";
      const prefix = options.allowRelative ? "(?:" + absolute + "|" + rfc.relativeRef + ")" : absolute;
      return internals.wrap(prefix + suffix, customScheme);
    };
    internals.wrap = function(raw, scheme) {
      raw = `(?=.)(?!https?:/$)${raw}`;
      return {
        raw,
        regex: new RegExp(`^${raw}$`),
        scheme
      };
    };
    internals.uriRegex = internals.createRegex({});
    exports.regex = function(options = {}) {
      if (options.scheme || options.allowRelative || options.relativeOnly || options.allowQuerySquareBrackets || options.domain) {
        return internals.createRegex(options);
      }
      return internals.uriRegex;
    };
  }
});

// ../node_modules/@hapi/address/lib/ip.js
var require_ip = __commonJS({
  "../node_modules/@hapi/address/lib/ip.js"(exports) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Uri = require_uri();
    exports.regex = function(options = {}) {
      Assert(options.cidr === void 0 || typeof options.cidr === "string", "options.cidr must be a string");
      const cidr = options.cidr ? options.cidr.toLowerCase() : "optional";
      Assert(["required", "optional", "forbidden"].includes(cidr), "options.cidr must be one of required, optional, forbidden");
      Assert(options.version === void 0 || typeof options.version === "string" || Array.isArray(options.version), "options.version must be a string or an array of string");
      let versions = options.version || ["ipv4", "ipv6", "ipvfuture"];
      if (!Array.isArray(versions)) {
        versions = [versions];
      }
      Assert(versions.length >= 1, "options.version must have at least 1 version specified");
      for (let i = 0; i < versions.length; ++i) {
        Assert(typeof versions[i] === "string", "options.version must only contain strings");
        versions[i] = versions[i].toLowerCase();
        Assert(["ipv4", "ipv6", "ipvfuture"].includes(versions[i]), "options.version contains unknown version " + versions[i] + " - must be one of ipv4, ipv6, ipvfuture");
      }
      versions = Array.from(new Set(versions));
      const parts = versions.map((version) => {
        if (cidr === "forbidden") {
          return Uri.ip[version];
        }
        const cidrpart = `\\/${version === "ipv4" ? Uri.ip.v4Cidr : Uri.ip.v6Cidr}`;
        if (cidr === "required") {
          return `${Uri.ip[version]}${cidrpart}`;
        }
        return `${Uri.ip[version]}(?:${cidrpart})?`;
      });
      const raw = `(?:${parts.join("|")})`;
      const regex = new RegExp(`^${raw}$`);
      return { cidr, versions, regex, raw };
    };
  }
});

// ../node_modules/@hapi/address/lib/tlds.js
var require_tlds = __commonJS({
  "../node_modules/@hapi/address/lib/tlds.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var internals = {};
    internals.tlds = [
      "AAA",
      "AARP",
      "ABARTH",
      "ABB",
      "ABBOTT",
      "ABBVIE",
      "ABC",
      "ABLE",
      "ABOGADO",
      "ABUDHABI",
      "AC",
      "ACADEMY",
      "ACCENTURE",
      "ACCOUNTANT",
      "ACCOUNTANTS",
      "ACO",
      "ACTOR",
      "AD",
      "ADAC",
      "ADS",
      "ADULT",
      "AE",
      "AEG",
      "AERO",
      "AETNA",
      "AF",
      "AFAMILYCOMPANY",
      "AFL",
      "AFRICA",
      "AG",
      "AGAKHAN",
      "AGENCY",
      "AI",
      "AIG",
      "AIGO",
      "AIRBUS",
      "AIRFORCE",
      "AIRTEL",
      "AKDN",
      "AL",
      "ALFAROMEO",
      "ALIBABA",
      "ALIPAY",
      "ALLFINANZ",
      "ALLSTATE",
      "ALLY",
      "ALSACE",
      "ALSTOM",
      "AM",
      "AMERICANEXPRESS",
      "AMERICANFAMILY",
      "AMEX",
      "AMFAM",
      "AMICA",
      "AMSTERDAM",
      "ANALYTICS",
      "ANDROID",
      "ANQUAN",
      "ANZ",
      "AO",
      "AOL",
      "APARTMENTS",
      "APP",
      "APPLE",
      "AQ",
      "AQUARELLE",
      "AR",
      "ARAB",
      "ARAMCO",
      "ARCHI",
      "ARMY",
      "ARPA",
      "ART",
      "ARTE",
      "AS",
      "ASDA",
      "ASIA",
      "ASSOCIATES",
      "AT",
      "ATHLETA",
      "ATTORNEY",
      "AU",
      "AUCTION",
      "AUDI",
      "AUDIBLE",
      "AUDIO",
      "AUSPOST",
      "AUTHOR",
      "AUTO",
      "AUTOS",
      "AVIANCA",
      "AW",
      "AWS",
      "AX",
      "AXA",
      "AZ",
      "AZURE",
      "BA",
      "BABY",
      "BAIDU",
      "BANAMEX",
      "BANANAREPUBLIC",
      "BAND",
      "BANK",
      "BAR",
      "BARCELONA",
      "BARCLAYCARD",
      "BARCLAYS",
      "BAREFOOT",
      "BARGAINS",
      "BASEBALL",
      "BASKETBALL",
      "BAUHAUS",
      "BAYERN",
      "BB",
      "BBC",
      "BBT",
      "BBVA",
      "BCG",
      "BCN",
      "BD",
      "BE",
      "BEATS",
      "BEAUTY",
      "BEER",
      "BENTLEY",
      "BERLIN",
      "BEST",
      "BESTBUY",
      "BET",
      "BF",
      "BG",
      "BH",
      "BHARTI",
      "BI",
      "BIBLE",
      "BID",
      "BIKE",
      "BING",
      "BINGO",
      "BIO",
      "BIZ",
      "BJ",
      "BLACK",
      "BLACKFRIDAY",
      "BLOCKBUSTER",
      "BLOG",
      "BLOOMBERG",
      "BLUE",
      "BM",
      "BMS",
      "BMW",
      "BN",
      "BNPPARIBAS",
      "BO",
      "BOATS",
      "BOEHRINGER",
      "BOFA",
      "BOM",
      "BOND",
      "BOO",
      "BOOK",
      "BOOKING",
      "BOSCH",
      "BOSTIK",
      "BOSTON",
      "BOT",
      "BOUTIQUE",
      "BOX",
      "BR",
      "BRADESCO",
      "BRIDGESTONE",
      "BROADWAY",
      "BROKER",
      "BROTHER",
      "BRUSSELS",
      "BS",
      "BT",
      "BUDAPEST",
      "BUGATTI",
      "BUILD",
      "BUILDERS",
      "BUSINESS",
      "BUY",
      "BUZZ",
      "BV",
      "BW",
      "BY",
      "BZ",
      "BZH",
      "CA",
      "CAB",
      "CAFE",
      "CAL",
      "CALL",
      "CALVINKLEIN",
      "CAM",
      "CAMERA",
      "CAMP",
      "CANCERRESEARCH",
      "CANON",
      "CAPETOWN",
      "CAPITAL",
      "CAPITALONE",
      "CAR",
      "CARAVAN",
      "CARDS",
      "CARE",
      "CAREER",
      "CAREERS",
      "CARS",
      "CARTIER",
      "CASA",
      "CASE",
      "CASEIH",
      "CASH",
      "CASINO",
      "CAT",
      "CATERING",
      "CATHOLIC",
      "CBA",
      "CBN",
      "CBRE",
      "CBS",
      "CC",
      "CD",
      "CEB",
      "CENTER",
      "CEO",
      "CERN",
      "CF",
      "CFA",
      "CFD",
      "CG",
      "CH",
      "CHANEL",
      "CHANNEL",
      "CHARITY",
      "CHASE",
      "CHAT",
      "CHEAP",
      "CHINTAI",
      "CHRISTMAS",
      "CHROME",
      "CHRYSLER",
      "CHURCH",
      "CI",
      "CIPRIANI",
      "CIRCLE",
      "CISCO",
      "CITADEL",
      "CITI",
      "CITIC",
      "CITY",
      "CITYEATS",
      "CK",
      "CL",
      "CLAIMS",
      "CLEANING",
      "CLICK",
      "CLINIC",
      "CLINIQUE",
      "CLOTHING",
      "CLOUD",
      "CLUB",
      "CLUBMED",
      "CM",
      "CN",
      "CO",
      "COACH",
      "CODES",
      "COFFEE",
      "COLLEGE",
      "COLOGNE",
      "COM",
      "COMCAST",
      "COMMBANK",
      "COMMUNITY",
      "COMPANY",
      "COMPARE",
      "COMPUTER",
      "COMSEC",
      "CONDOS",
      "CONSTRUCTION",
      "CONSULTING",
      "CONTACT",
      "CONTRACTORS",
      "COOKING",
      "COOKINGCHANNEL",
      "COOL",
      "COOP",
      "CORSICA",
      "COUNTRY",
      "COUPON",
      "COUPONS",
      "COURSES",
      "CR",
      "CREDIT",
      "CREDITCARD",
      "CREDITUNION",
      "CRICKET",
      "CROWN",
      "CRS",
      "CRUISE",
      "CRUISES",
      "CSC",
      "CU",
      "CUISINELLA",
      "CV",
      "CW",
      "CX",
      "CY",
      "CYMRU",
      "CYOU",
      "CZ",
      "DABUR",
      "DAD",
      "DANCE",
      "DATA",
      "DATE",
      "DATING",
      "DATSUN",
      "DAY",
      "DCLK",
      "DDS",
      "DE",
      "DEAL",
      "DEALER",
      "DEALS",
      "DEGREE",
      "DELIVERY",
      "DELL",
      "DELOITTE",
      "DELTA",
      "DEMOCRAT",
      "DENTAL",
      "DENTIST",
      "DESI",
      "DESIGN",
      "DEV",
      "DHL",
      "DIAMONDS",
      "DIET",
      "DIGITAL",
      "DIRECT",
      "DIRECTORY",
      "DISCOUNT",
      "DISCOVER",
      "DISH",
      "DIY",
      "DJ",
      "DK",
      "DM",
      "DNP",
      "DO",
      "DOCS",
      "DOCTOR",
      "DODGE",
      "DOG",
      "DOMAINS",
      "DOT",
      "DOWNLOAD",
      "DRIVE",
      "DTV",
      "DUBAI",
      "DUCK",
      "DUNLOP",
      "DUPONT",
      "DURBAN",
      "DVAG",
      "DVR",
      "DZ",
      "EARTH",
      "EAT",
      "EC",
      "ECO",
      "EDEKA",
      "EDU",
      "EDUCATION",
      "EE",
      "EG",
      "EMAIL",
      "EMERCK",
      "ENERGY",
      "ENGINEER",
      "ENGINEERING",
      "ENTERPRISES",
      "EPSON",
      "EQUIPMENT",
      "ER",
      "ERICSSON",
      "ERNI",
      "ES",
      "ESQ",
      "ESTATE",
      "ESURANCE",
      "ET",
      "ETISALAT",
      "EU",
      "EUROVISION",
      "EUS",
      "EVENTS",
      "EVERBANK",
      "EXCHANGE",
      "EXPERT",
      "EXPOSED",
      "EXPRESS",
      "EXTRASPACE",
      "FAGE",
      "FAIL",
      "FAIRWINDS",
      "FAITH",
      "FAMILY",
      "FAN",
      "FANS",
      "FARM",
      "FARMERS",
      "FASHION",
      "FAST",
      "FEDEX",
      "FEEDBACK",
      "FERRARI",
      "FERRERO",
      "FI",
      "FIAT",
      "FIDELITY",
      "FIDO",
      "FILM",
      "FINAL",
      "FINANCE",
      "FINANCIAL",
      "FIRE",
      "FIRESTONE",
      "FIRMDALE",
      "FISH",
      "FISHING",
      "FIT",
      "FITNESS",
      "FJ",
      "FK",
      "FLICKR",
      "FLIGHTS",
      "FLIR",
      "FLORIST",
      "FLOWERS",
      "FLY",
      "FM",
      "FO",
      "FOO",
      "FOOD",
      "FOODNETWORK",
      "FOOTBALL",
      "FORD",
      "FOREX",
      "FORSALE",
      "FORUM",
      "FOUNDATION",
      "FOX",
      "FR",
      "FREE",
      "FRESENIUS",
      "FRL",
      "FROGANS",
      "FRONTDOOR",
      "FRONTIER",
      "FTR",
      "FUJITSU",
      "FUJIXEROX",
      "FUN",
      "FUND",
      "FURNITURE",
      "FUTBOL",
      "FYI",
      "GA",
      "GAL",
      "GALLERY",
      "GALLO",
      "GALLUP",
      "GAME",
      "GAMES",
      "GAP",
      "GARDEN",
      "GAY",
      "GB",
      "GBIZ",
      "GD",
      "GDN",
      "GE",
      "GEA",
      "GENT",
      "GENTING",
      "GEORGE",
      "GF",
      "GG",
      "GGEE",
      "GH",
      "GI",
      "GIFT",
      "GIFTS",
      "GIVES",
      "GIVING",
      "GL",
      "GLADE",
      "GLASS",
      "GLE",
      "GLOBAL",
      "GLOBO",
      "GM",
      "GMAIL",
      "GMBH",
      "GMO",
      "GMX",
      "GN",
      "GODADDY",
      "GOLD",
      "GOLDPOINT",
      "GOLF",
      "GOO",
      "GOODYEAR",
      "GOOG",
      "GOOGLE",
      "GOP",
      "GOT",
      "GOV",
      "GP",
      "GQ",
      "GR",
      "GRAINGER",
      "GRAPHICS",
      "GRATIS",
      "GREEN",
      "GRIPE",
      "GROCERY",
      "GROUP",
      "GS",
      "GT",
      "GU",
      "GUARDIAN",
      "GUCCI",
      "GUGE",
      "GUIDE",
      "GUITARS",
      "GURU",
      "GW",
      "GY",
      "HAIR",
      "HAMBURG",
      "HANGOUT",
      "HAUS",
      "HBO",
      "HDFC",
      "HDFCBANK",
      "HEALTH",
      "HEALTHCARE",
      "HELP",
      "HELSINKI",
      "HERE",
      "HERMES",
      "HGTV",
      "HIPHOP",
      "HISAMITSU",
      "HITACHI",
      "HIV",
      "HK",
      "HKT",
      "HM",
      "HN",
      "HOCKEY",
      "HOLDINGS",
      "HOLIDAY",
      "HOMEDEPOT",
      "HOMEGOODS",
      "HOMES",
      "HOMESENSE",
      "HONDA",
      "HORSE",
      "HOSPITAL",
      "HOST",
      "HOSTING",
      "HOT",
      "HOTELES",
      "HOTELS",
      "HOTMAIL",
      "HOUSE",
      "HOW",
      "HR",
      "HSBC",
      "HT",
      "HU",
      "HUGHES",
      "HYATT",
      "HYUNDAI",
      "IBM",
      "ICBC",
      "ICE",
      "ICU",
      "ID",
      "IE",
      "IEEE",
      "IFM",
      "IKANO",
      "IL",
      "IM",
      "IMAMAT",
      "IMDB",
      "IMMO",
      "IMMOBILIEN",
      "IN",
      "INC",
      "INDUSTRIES",
      "INFINITI",
      "INFO",
      "ING",
      "INK",
      "INSTITUTE",
      "INSURANCE",
      "INSURE",
      "INT",
      "INTEL",
      "INTERNATIONAL",
      "INTUIT",
      "INVESTMENTS",
      "IO",
      "IPIRANGA",
      "IQ",
      "IR",
      "IRISH",
      "IS",
      "ISMAILI",
      "IST",
      "ISTANBUL",
      "IT",
      "ITAU",
      "ITV",
      "IVECO",
      "JAGUAR",
      "JAVA",
      "JCB",
      "JCP",
      "JE",
      "JEEP",
      "JETZT",
      "JEWELRY",
      "JIO",
      "JLL",
      "JM",
      "JMP",
      "JNJ",
      "JO",
      "JOBS",
      "JOBURG",
      "JOT",
      "JOY",
      "JP",
      "JPMORGAN",
      "JPRS",
      "JUEGOS",
      "JUNIPER",
      "KAUFEN",
      "KDDI",
      "KE",
      "KERRYHOTELS",
      "KERRYLOGISTICS",
      "KERRYPROPERTIES",
      "KFH",
      "KG",
      "KH",
      "KI",
      "KIA",
      "KIM",
      "KINDER",
      "KINDLE",
      "KITCHEN",
      "KIWI",
      "KM",
      "KN",
      "KOELN",
      "KOMATSU",
      "KOSHER",
      "KP",
      "KPMG",
      "KPN",
      "KR",
      "KRD",
      "KRED",
      "KUOKGROUP",
      "KW",
      "KY",
      "KYOTO",
      "KZ",
      "LA",
      "LACAIXA",
      "LADBROKES",
      "LAMBORGHINI",
      "LAMER",
      "LANCASTER",
      "LANCIA",
      "LANCOME",
      "LAND",
      "LANDROVER",
      "LANXESS",
      "LASALLE",
      "LAT",
      "LATINO",
      "LATROBE",
      "LAW",
      "LAWYER",
      "LB",
      "LC",
      "LDS",
      "LEASE",
      "LECLERC",
      "LEFRAK",
      "LEGAL",
      "LEGO",
      "LEXUS",
      "LGBT",
      "LI",
      "LIAISON",
      "LIDL",
      "LIFE",
      "LIFEINSURANCE",
      "LIFESTYLE",
      "LIGHTING",
      "LIKE",
      "LILLY",
      "LIMITED",
      "LIMO",
      "LINCOLN",
      "LINDE",
      "LINK",
      "LIPSY",
      "LIVE",
      "LIVING",
      "LIXIL",
      "LK",
      "LLC",
      "LOAN",
      "LOANS",
      "LOCKER",
      "LOCUS",
      "LOFT",
      "LOL",
      "LONDON",
      "LOTTE",
      "LOTTO",
      "LOVE",
      "LPL",
      "LPLFINANCIAL",
      "LR",
      "LS",
      "LT",
      "LTD",
      "LTDA",
      "LU",
      "LUNDBECK",
      "LUPIN",
      "LUXE",
      "LUXURY",
      "LV",
      "LY",
      "MA",
      "MACYS",
      "MADRID",
      "MAIF",
      "MAISON",
      "MAKEUP",
      "MAN",
      "MANAGEMENT",
      "MANGO",
      "MAP",
      "MARKET",
      "MARKETING",
      "MARKETS",
      "MARRIOTT",
      "MARSHALLS",
      "MASERATI",
      "MATTEL",
      "MBA",
      "MC",
      "MCKINSEY",
      "MD",
      "ME",
      "MED",
      "MEDIA",
      "MEET",
      "MELBOURNE",
      "MEME",
      "MEMORIAL",
      "MEN",
      "MENU",
      "MERCKMSD",
      "METLIFE",
      "MG",
      "MH",
      "MIAMI",
      "MICROSOFT",
      "MIL",
      "MINI",
      "MINT",
      "MIT",
      "MITSUBISHI",
      "MK",
      "ML",
      "MLB",
      "MLS",
      "MM",
      "MMA",
      "MN",
      "MO",
      "MOBI",
      "MOBILE",
      "MODA",
      "MOE",
      "MOI",
      "MOM",
      "MONASH",
      "MONEY",
      "MONSTER",
      "MOPAR",
      "MORMON",
      "MORTGAGE",
      "MOSCOW",
      "MOTO",
      "MOTORCYCLES",
      "MOV",
      "MOVIE",
      "MOVISTAR",
      "MP",
      "MQ",
      "MR",
      "MS",
      "MSD",
      "MT",
      "MTN",
      "MTR",
      "MU",
      "MUSEUM",
      "MUTUAL",
      "MV",
      "MW",
      "MX",
      "MY",
      "MZ",
      "NA",
      "NAB",
      "NADEX",
      "NAGOYA",
      "NAME",
      "NATIONWIDE",
      "NATURA",
      "NAVY",
      "NBA",
      "NC",
      "NE",
      "NEC",
      "NET",
      "NETBANK",
      "NETFLIX",
      "NETWORK",
      "NEUSTAR",
      "NEW",
      "NEWHOLLAND",
      "NEWS",
      "NEXT",
      "NEXTDIRECT",
      "NEXUS",
      "NF",
      "NFL",
      "NG",
      "NGO",
      "NHK",
      "NI",
      "NICO",
      "NIKE",
      "NIKON",
      "NINJA",
      "NISSAN",
      "NISSAY",
      "NL",
      "NO",
      "NOKIA",
      "NORTHWESTERNMUTUAL",
      "NORTON",
      "NOW",
      "NOWRUZ",
      "NOWTV",
      "NP",
      "NR",
      "NRA",
      "NRW",
      "NTT",
      "NU",
      "NYC",
      "NZ",
      "OBI",
      "OBSERVER",
      "OFF",
      "OFFICE",
      "OKINAWA",
      "OLAYAN",
      "OLAYANGROUP",
      "OLDNAVY",
      "OLLO",
      "OM",
      "OMEGA",
      "ONE",
      "ONG",
      "ONL",
      "ONLINE",
      "ONYOURSIDE",
      "OOO",
      "OPEN",
      "ORACLE",
      "ORANGE",
      "ORG",
      "ORGANIC",
      "ORIGINS",
      "OSAKA",
      "OTSUKA",
      "OTT",
      "OVH",
      "PA",
      "PAGE",
      "PANASONIC",
      "PARIS",
      "PARS",
      "PARTNERS",
      "PARTS",
      "PARTY",
      "PASSAGENS",
      "PAY",
      "PCCW",
      "PE",
      "PET",
      "PF",
      "PFIZER",
      "PG",
      "PH",
      "PHARMACY",
      "PHD",
      "PHILIPS",
      "PHONE",
      "PHOTO",
      "PHOTOGRAPHY",
      "PHOTOS",
      "PHYSIO",
      "PIAGET",
      "PICS",
      "PICTET",
      "PICTURES",
      "PID",
      "PIN",
      "PING",
      "PINK",
      "PIONEER",
      "PIZZA",
      "PK",
      "PL",
      "PLACE",
      "PLAY",
      "PLAYSTATION",
      "PLUMBING",
      "PLUS",
      "PM",
      "PN",
      "PNC",
      "POHL",
      "POKER",
      "POLITIE",
      "PORN",
      "POST",
      "PR",
      "PRAMERICA",
      "PRAXI",
      "PRESS",
      "PRIME",
      "PRO",
      "PROD",
      "PRODUCTIONS",
      "PROF",
      "PROGRESSIVE",
      "PROMO",
      "PROPERTIES",
      "PROPERTY",
      "PROTECTION",
      "PRU",
      "PRUDENTIAL",
      "PS",
      "PT",
      "PUB",
      "PW",
      "PWC",
      "PY",
      "QA",
      "QPON",
      "QUEBEC",
      "QUEST",
      "QVC",
      "RACING",
      "RADIO",
      "RAID",
      "RE",
      "READ",
      "REALESTATE",
      "REALTOR",
      "REALTY",
      "RECIPES",
      "RED",
      "REDSTONE",
      "REDUMBRELLA",
      "REHAB",
      "REISE",
      "REISEN",
      "REIT",
      "RELIANCE",
      "REN",
      "RENT",
      "RENTALS",
      "REPAIR",
      "REPORT",
      "REPUBLICAN",
      "REST",
      "RESTAURANT",
      "REVIEW",
      "REVIEWS",
      "REXROTH",
      "RICH",
      "RICHARDLI",
      "RICOH",
      "RIGHTATHOME",
      "RIL",
      "RIO",
      "RIP",
      "RMIT",
      "RO",
      "ROCHER",
      "ROCKS",
      "RODEO",
      "ROGERS",
      "ROOM",
      "RS",
      "RSVP",
      "RU",
      "RUGBY",
      "RUHR",
      "RUN",
      "RW",
      "RWE",
      "RYUKYU",
      "SA",
      "SAARLAND",
      "SAFE",
      "SAFETY",
      "SAKURA",
      "SALE",
      "SALON",
      "SAMSCLUB",
      "SAMSUNG",
      "SANDVIK",
      "SANDVIKCOROMANT",
      "SANOFI",
      "SAP",
      "SARL",
      "SAS",
      "SAVE",
      "SAXO",
      "SB",
      "SBI",
      "SBS",
      "SC",
      "SCA",
      "SCB",
      "SCHAEFFLER",
      "SCHMIDT",
      "SCHOLARSHIPS",
      "SCHOOL",
      "SCHULE",
      "SCHWARZ",
      "SCIENCE",
      "SCJOHNSON",
      "SCOR",
      "SCOT",
      "SD",
      "SE",
      "SEARCH",
      "SEAT",
      "SECURE",
      "SECURITY",
      "SEEK",
      "SELECT",
      "SENER",
      "SERVICES",
      "SES",
      "SEVEN",
      "SEW",
      "SEX",
      "SEXY",
      "SFR",
      "SG",
      "SH",
      "SHANGRILA",
      "SHARP",
      "SHAW",
      "SHELL",
      "SHIA",
      "SHIKSHA",
      "SHOES",
      "SHOP",
      "SHOPPING",
      "SHOUJI",
      "SHOW",
      "SHOWTIME",
      "SHRIRAM",
      "SI",
      "SILK",
      "SINA",
      "SINGLES",
      "SITE",
      "SJ",
      "SK",
      "SKI",
      "SKIN",
      "SKY",
      "SKYPE",
      "SL",
      "SLING",
      "SM",
      "SMART",
      "SMILE",
      "SN",
      "SNCF",
      "SO",
      "SOCCER",
      "SOCIAL",
      "SOFTBANK",
      "SOFTWARE",
      "SOHU",
      "SOLAR",
      "SOLUTIONS",
      "SONG",
      "SONY",
      "SOY",
      "SPACE",
      "SPORT",
      "SPOT",
      "SPREADBETTING",
      "SR",
      "SRL",
      "SRT",
      "SS",
      "ST",
      "STADA",
      "STAPLES",
      "STAR",
      "STATEBANK",
      "STATEFARM",
      "STC",
      "STCGROUP",
      "STOCKHOLM",
      "STORAGE",
      "STORE",
      "STREAM",
      "STUDIO",
      "STUDY",
      "STYLE",
      "SU",
      "SUCKS",
      "SUPPLIES",
      "SUPPLY",
      "SUPPORT",
      "SURF",
      "SURGERY",
      "SUZUKI",
      "SV",
      "SWATCH",
      "SWIFTCOVER",
      "SWISS",
      "SX",
      "SY",
      "SYDNEY",
      "SYMANTEC",
      "SYSTEMS",
      "SZ",
      "TAB",
      "TAIPEI",
      "TALK",
      "TAOBAO",
      "TARGET",
      "TATAMOTORS",
      "TATAR",
      "TATTOO",
      "TAX",
      "TAXI",
      "TC",
      "TCI",
      "TD",
      "TDK",
      "TEAM",
      "TECH",
      "TECHNOLOGY",
      "TEL",
      "TELEFONICA",
      "TEMASEK",
      "TENNIS",
      "TEVA",
      "TF",
      "TG",
      "TH",
      "THD",
      "THEATER",
      "THEATRE",
      "TIAA",
      "TICKETS",
      "TIENDA",
      "TIFFANY",
      "TIPS",
      "TIRES",
      "TIROL",
      "TJ",
      "TJMAXX",
      "TJX",
      "TK",
      "TKMAXX",
      "TL",
      "TM",
      "TMALL",
      "TN",
      "TO",
      "TODAY",
      "TOKYO",
      "TOOLS",
      "TOP",
      "TORAY",
      "TOSHIBA",
      "TOTAL",
      "TOURS",
      "TOWN",
      "TOYOTA",
      "TOYS",
      "TR",
      "TRADE",
      "TRADING",
      "TRAINING",
      "TRAVEL",
      "TRAVELCHANNEL",
      "TRAVELERS",
      "TRAVELERSINSURANCE",
      "TRUST",
      "TRV",
      "TT",
      "TUBE",
      "TUI",
      "TUNES",
      "TUSHU",
      "TV",
      "TVS",
      "TW",
      "TZ",
      "UA",
      "UBANK",
      "UBS",
      "UCONNECT",
      "UG",
      "UK",
      "UNICOM",
      "UNIVERSITY",
      "UNO",
      "UOL",
      "UPS",
      "US",
      "UY",
      "UZ",
      "VA",
      "VACATIONS",
      "VANA",
      "VANGUARD",
      "VC",
      "VE",
      "VEGAS",
      "VENTURES",
      "VERISIGN",
      "VERSICHERUNG",
      "VET",
      "VG",
      "VI",
      "VIAJES",
      "VIDEO",
      "VIG",
      "VIKING",
      "VILLAS",
      "VIN",
      "VIP",
      "VIRGIN",
      "VISA",
      "VISION",
      "VISTAPRINT",
      "VIVA",
      "VIVO",
      "VLAANDEREN",
      "VN",
      "VODKA",
      "VOLKSWAGEN",
      "VOLVO",
      "VOTE",
      "VOTING",
      "VOTO",
      "VOYAGE",
      "VU",
      "VUELOS",
      "WALES",
      "WALMART",
      "WALTER",
      "WANG",
      "WANGGOU",
      "WARMAN",
      "WATCH",
      "WATCHES",
      "WEATHER",
      "WEATHERCHANNEL",
      "WEBCAM",
      "WEBER",
      "WEBSITE",
      "WED",
      "WEDDING",
      "WEIBO",
      "WEIR",
      "WF",
      "WHOSWHO",
      "WIEN",
      "WIKI",
      "WILLIAMHILL",
      "WIN",
      "WINDOWS",
      "WINE",
      "WINNERS",
      "WME",
      "WOLTERSKLUWER",
      "WOODSIDE",
      "WORK",
      "WORKS",
      "WORLD",
      "WOW",
      "WS",
      "WTC",
      "WTF",
      "XBOX",
      "XEROX",
      "XFINITY",
      "XIHUAN",
      "XIN",
      "XN--11B4C3D",
      "XN--1CK2E1B",
      "XN--1QQW23A",
      "XN--2SCRJ9C",
      "XN--30RR7Y",
      "XN--3BST00M",
      "XN--3DS443G",
      "XN--3E0B707E",
      "XN--3HCRJ9C",
      "XN--3OQ18VL8PN36A",
      "XN--3PXU8K",
      "XN--42C2D9A",
      "XN--45BR5CYL",
      "XN--45BRJ9C",
      "XN--45Q11C",
      "XN--4GBRIM",
      "XN--54B7FTA0CC",
      "XN--55QW42G",
      "XN--55QX5D",
      "XN--5SU34J936BGSG",
      "XN--5TZM5G",
      "XN--6FRZ82G",
      "XN--6QQ986B3XL",
      "XN--80ADXHKS",
      "XN--80AO21A",
      "XN--80AQECDR1A",
      "XN--80ASEHDB",
      "XN--80ASWG",
      "XN--8Y0A063A",
      "XN--90A3AC",
      "XN--90AE",
      "XN--90AIS",
      "XN--9DBQ2A",
      "XN--9ET52U",
      "XN--9KRT00A",
      "XN--B4W605FERD",
      "XN--BCK1B9A5DRE4C",
      "XN--C1AVG",
      "XN--C2BR7G",
      "XN--CCK2B3B",
      "XN--CG4BKI",
      "XN--CLCHC0EA0B2G2A9GCD",
      "XN--CZR694B",
      "XN--CZRS0T",
      "XN--CZRU2D",
      "XN--D1ACJ3B",
      "XN--D1ALF",
      "XN--E1A4C",
      "XN--ECKVDTC9D",
      "XN--EFVY88H",
      "XN--ESTV75G",
      "XN--FCT429K",
      "XN--FHBEI",
      "XN--FIQ228C5HS",
      "XN--FIQ64B",
      "XN--FIQS8S",
      "XN--FIQZ9S",
      "XN--FJQ720A",
      "XN--FLW351E",
      "XN--FPCRJ9C3D",
      "XN--FZC2C9E2C",
      "XN--FZYS8D69UVGM",
      "XN--G2XX48C",
      "XN--GCKR3F0F",
      "XN--GECRJ9C",
      "XN--GK3AT1E",
      "XN--H2BREG3EVE",
      "XN--H2BRJ9C",
      "XN--H2BRJ9C8C",
      "XN--HXT814E",
      "XN--I1B6B1A6A2E",
      "XN--IMR513N",
      "XN--IO0A7I",
      "XN--J1AEF",
      "XN--J1AMH",
      "XN--J6W193G",
      "XN--JLQ61U9W7B",
      "XN--JVR189M",
      "XN--KCRX77D1X4A",
      "XN--KPRW13D",
      "XN--KPRY57D",
      "XN--KPU716F",
      "XN--KPUT3I",
      "XN--L1ACC",
      "XN--LGBBAT1AD8J",
      "XN--MGB9AWBF",
      "XN--MGBA3A3EJT",
      "XN--MGBA3A4F16A",
      "XN--MGBA7C0BBN0A",
      "XN--MGBAAKC7DVF",
      "XN--MGBAAM7A8H",
      "XN--MGBAB2BD",
      "XN--MGBAH1A3HJKRD",
      "XN--MGBAI9AZGQP6J",
      "XN--MGBAYH7GPA",
      "XN--MGBBH1A",
      "XN--MGBBH1A71E",
      "XN--MGBC0A9AZCG",
      "XN--MGBCA7DZDO",
      "XN--MGBERP4A5D4AR",
      "XN--MGBGU82A",
      "XN--MGBI4ECEXP",
      "XN--MGBPL2FH",
      "XN--MGBT3DHD",
      "XN--MGBTX2B",
      "XN--MGBX4CD0AB",
      "XN--MIX891F",
      "XN--MK1BU44C",
      "XN--MXTQ1M",
      "XN--NGBC5AZD",
      "XN--NGBE9E0A",
      "XN--NGBRX",
      "XN--NODE",
      "XN--NQV7F",
      "XN--NQV7FS00EMA",
      "XN--NYQY26A",
      "XN--O3CW4H",
      "XN--OGBPF8FL",
      "XN--OTU796D",
      "XN--P1ACF",
      "XN--P1AI",
      "XN--PBT977C",
      "XN--PGBS0DH",
      "XN--PSSY2U",
      "XN--Q9JYB4C",
      "XN--QCKA1PMC",
      "XN--QXA6A",
      "XN--QXAM",
      "XN--RHQV96G",
      "XN--ROVU88B",
      "XN--RVC1E0AM3E",
      "XN--S9BRJ9C",
      "XN--SES554G",
      "XN--T60B56A",
      "XN--TCKWE",
      "XN--TIQ49XQYJ",
      "XN--UNUP4Y",
      "XN--VERMGENSBERATER-CTB",
      "XN--VERMGENSBERATUNG-PWB",
      "XN--VHQUV",
      "XN--VUQ861B",
      "XN--W4R85EL8FHU5DNRA",
      "XN--W4RS40L",
      "XN--WGBH1C",
      "XN--WGBL6A",
      "XN--XHQ521B",
      "XN--XKC2AL3HYE2A",
      "XN--XKC2DL3A5EE0H",
      "XN--Y9A3AQ",
      "XN--YFRO4I67O",
      "XN--YGBI2AMMX",
      "XN--ZFR164B",
      "XXX",
      "XYZ",
      "YACHTS",
      "YAHOO",
      "YAMAXUN",
      "YANDEX",
      "YE",
      "YODOBASHI",
      "YOGA",
      "YOKOHAMA",
      "YOU",
      "YOUTUBE",
      "YT",
      "YUN",
      "ZA",
      "ZAPPOS",
      "ZARA",
      "ZERO",
      "ZIP",
      "ZM",
      "ZONE",
      "ZUERICH",
      "ZW"
    ];
    module.exports = new Set(internals.tlds.map((tld) => tld.toLowerCase()));
  }
});

// ../node_modules/@hapi/joi/lib/types/string.js
var require_string = __commonJS({
  "../node_modules/@hapi/joi/lib/types/string.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Domain = require_domain();
    var Email = require_email();
    var Ip = require_ip();
    var EscapeRegex = require_escapeRegex();
    var Tlds = require_tlds();
    var Uri = require_uri();
    var Any = require_any();
    var Common = require_common();
    var internals = {
      tlds: Tlds instanceof Set ? { tlds: { allow: Tlds, deny: null } } : false,
      // $lab:coverage:ignore$
      base64Regex: {
        // paddingRequired
        true: {
          // urlSafe
          true: /^(?:[\w\-]{2}[\w\-]{2})*(?:[\w\-]{2}==|[\w\-]{3}=)?$/,
          false: /^(?:[A-Za-z0-9+\/]{2}[A-Za-z0-9+\/]{2})*(?:[A-Za-z0-9+\/]{2}==|[A-Za-z0-9+\/]{3}=)?$/
        },
        false: {
          true: /^(?:[\w\-]{2}[\w\-]{2})*(?:[\w\-]{2}(==)?|[\w\-]{3}=?)?$/,
          false: /^(?:[A-Za-z0-9+\/]{2}[A-Za-z0-9+\/]{2})*(?:[A-Za-z0-9+\/]{2}(==)?|[A-Za-z0-9+\/]{3}=?)?$/
        }
      },
      dataUriRegex: /^data:[\w+.-]+\/[\w+.-]+;((charset=[\w-]+|base64),)?(.*)$/,
      hexRegex: /^[a-f0-9]+$/i,
      ipRegex: Ip.regex().regex,
      isoDurationRegex: /^P(?!$)(\d+Y)?(\d+M)?(\d+W)?(\d+D)?(T(?=\d)(\d+H)?(\d+M)?(\d+S)?)?$/,
      guidBrackets: {
        "{": "}",
        "[": "]",
        "(": ")",
        "": ""
      },
      guidVersions: {
        uuidv1: "1",
        uuidv2: "2",
        uuidv3: "3",
        uuidv4: "4",
        uuidv5: "5"
      },
      cidrPresences: ["required", "optional", "forbidden"],
      normalizationForms: ["NFC", "NFD", "NFKC", "NFKD"]
    };
    module.exports = Any.extend({
      type: "string",
      flags: {
        insensitive: { default: false },
        truncate: { default: false }
      },
      terms: {
        replacements: { init: null }
      },
      coerce: {
        from: "string",
        method(value, { schema, state, prefs }) {
          const normalize = schema.$_getRule("normalize");
          if (normalize) {
            value = value.normalize(normalize.args.form);
          }
          const casing = schema.$_getRule("case");
          if (casing) {
            value = casing.args.direction === "upper" ? value.toLocaleUpperCase() : value.toLocaleLowerCase();
          }
          const trim = schema.$_getRule("trim");
          if (trim && trim.args.enabled) {
            value = value.trim();
          }
          if (schema.$_terms.replacements) {
            for (const replacement of schema.$_terms.replacements) {
              value = value.replace(replacement.pattern, replacement.replacement);
            }
          }
          const hex = schema.$_getRule("hex");
          if (hex && hex.args.options.byteAligned && value.length % 2 !== 0) {
            value = `0${value}`;
          }
          if (schema.$_getRule("isoDate")) {
            const iso = internals.isoDate(value);
            if (iso) {
              value = iso;
            }
          }
          if (schema._flags.truncate) {
            const rule = schema.$_getRule("max");
            if (rule) {
              let limit = rule.args.limit;
              if (Common.isResolvable(limit)) {
                limit = limit.resolve(value, state, prefs);
                if (!Common.limit(limit)) {
                  return { value, errors: schema.$_createError("any.ref", limit, { ref: rule.args.limit, arg: "limit", reason: "must be a positive integer" }, state, prefs) };
                }
              }
              value = value.slice(0, limit);
            }
          }
          return { value };
        }
      },
      validate(value, { error }) {
        if (typeof value !== "string") {
          return { value, errors: error("string.base") };
        }
        if (value === "") {
          return { value, errors: error("string.empty") };
        }
      },
      rules: {
        alphanum: {
          method() {
            return this.$_addRule("alphanum");
          },
          validate(value, helpers) {
            if (/^[a-zA-Z0-9]+$/.test(value)) {
              return value;
            }
            return helpers.error("string.alphanum");
          }
        },
        base64: {
          method(options = {}) {
            Common.assertOptions(options, ["paddingRequired", "urlSafe"]);
            options = { urlSafe: false, paddingRequired: true, ...options };
            Assert(typeof options.paddingRequired === "boolean", "paddingRequired must be boolean");
            Assert(typeof options.urlSafe === "boolean", "urlSafe must be boolean");
            return this.$_addRule({ name: "base64", args: { options } });
          },
          validate(value, helpers, { options }) {
            const regex = internals.base64Regex[options.paddingRequired][options.urlSafe];
            if (regex.test(value)) {
              return value;
            }
            return helpers.error("string.base64");
          }
        },
        case: {
          method(direction) {
            Assert(["lower", "upper"].includes(direction), "Invalid case:", direction);
            return this.$_addRule({ name: "case", args: { direction } });
          },
          validate(value, helpers, { direction }) {
            if (direction === "lower" && value === value.toLocaleLowerCase() || direction === "upper" && value === value.toLocaleUpperCase()) {
              return value;
            }
            return helpers.error(`string.${direction}case`);
          },
          convert: true
        },
        creditCard: {
          method() {
            return this.$_addRule("creditCard");
          },
          validate(value, helpers) {
            let i = value.length;
            let sum = 0;
            let mul = 1;
            while (i--) {
              const char = value.charAt(i) * mul;
              sum = sum + (char - (char > 9) * 9);
              mul = mul ^ 3;
            }
            if (sum > 0 && sum % 10 === 0) {
              return value;
            }
            return helpers.error("string.creditCard");
          }
        },
        dataUri: {
          method(options = {}) {
            Common.assertOptions(options, ["paddingRequired"]);
            options = { paddingRequired: true, ...options };
            Assert(typeof options.paddingRequired === "boolean", "paddingRequired must be boolean");
            return this.$_addRule({ name: "dataUri", args: { options } });
          },
          validate(value, helpers, { options }) {
            const matches = value.match(internals.dataUriRegex);
            if (matches) {
              if (!matches[2]) {
                return value;
              }
              if (matches[2] !== "base64") {
                return value;
              }
              const base64regex = internals.base64Regex[options.paddingRequired].false;
              if (base64regex.test(matches[3])) {
                return value;
              }
            }
            return helpers.error("string.dataUri");
          }
        },
        domain: {
          method(options) {
            if (options) {
              Common.assertOptions(options, ["allowUnicode", "minDomainSegments", "tlds"]);
            }
            const address = internals.addressOptions(options);
            return this.$_addRule({ name: "domain", args: { options }, address });
          },
          validate(value, helpers, args, { address }) {
            if (Domain.isValid(value, address)) {
              return value;
            }
            return helpers.error("string.domain");
          }
        },
        email: {
          method(options = {}) {
            Common.assertOptions(options, ["allowUnicode", "ignoreLength", "minDomainSegments", "multiple", "separator", "tlds"]);
            Assert(options.multiple === void 0 || typeof options.multiple === "boolean", "multiple option must be an boolean");
            const address = internals.addressOptions(options);
            const regex = new RegExp(`\\s*[${options.separator ? EscapeRegex(options.separator) : ","}]\\s*`);
            return this.$_addRule({ name: "email", args: { options }, regex, address });
          },
          validate(value, helpers, { options }, { regex, address }) {
            const emails = options.multiple ? value.split(regex) : [value];
            const invalids = [];
            for (const email of emails) {
              if (!Email.isValid(email, address)) {
                invalids.push(email);
              }
            }
            if (!invalids.length) {
              return value;
            }
            return helpers.error("string.email", { value, invalids });
          }
        },
        guid: {
          alias: "uuid",
          method(options = {}) {
            Common.assertOptions(options, ["version"]);
            let versionNumbers = "";
            if (options.version) {
              const versions = [].concat(options.version);
              Assert(versions.length >= 1, "version must have at least 1 valid version specified");
              const set = /* @__PURE__ */ new Set();
              for (let i = 0; i < versions.length; ++i) {
                const version = versions[i];
                Assert(typeof version === "string", "version at position " + i + " must be a string");
                const versionNumber = internals.guidVersions[version.toLowerCase()];
                Assert(versionNumber, "version at position " + i + " must be one of " + Object.keys(internals.guidVersions).join(", "));
                Assert(!set.has(versionNumber), "version at position " + i + " must not be a duplicate");
                versionNumbers += versionNumber;
                set.add(versionNumber);
              }
            }
            const regex = new RegExp(`^([\\[{\\(]?)[0-9A-F]{8}([:-]?)[0-9A-F]{4}\\2?[${versionNumbers || "0-9A-F"}][0-9A-F]{3}\\2?[${versionNumbers ? "89AB" : "0-9A-F"}][0-9A-F]{3}\\2?[0-9A-F]{12}([\\]}\\)]?)$`, "i");
            return this.$_addRule({ name: "guid", args: { options }, regex });
          },
          validate(value, helpers, args, { regex }) {
            const results = regex.exec(value);
            if (!results) {
              return helpers.error("string.guid");
            }
            if (internals.guidBrackets[results[1]] !== results[results.length - 1]) {
              return helpers.error("string.guid");
            }
            return value;
          }
        },
        hex: {
          method(options = {}) {
            Common.assertOptions(options, ["byteAligned"]);
            options = { byteAligned: false, ...options };
            Assert(typeof options.byteAligned === "boolean", "byteAligned must be boolean");
            return this.$_addRule({ name: "hex", args: { options } });
          },
          validate(value, helpers, { options }) {
            if (!internals.hexRegex.test(value)) {
              return helpers.error("string.hex");
            }
            if (options.byteAligned && value.length % 2 !== 0) {
              return helpers.error("string.hexAlign");
            }
            return value;
          }
        },
        hostname: {
          method() {
            return this.$_addRule("hostname");
          },
          validate(value, helpers) {
            if (Domain.isValid(value, { minDomainSegments: 1 }) || internals.ipRegex.test(value)) {
              return value;
            }
            return helpers.error("string.hostname");
          }
        },
        insensitive: {
          method() {
            return this.$_setFlag("insensitive", true);
          }
        },
        ip: {
          method(options = {}) {
            Common.assertOptions(options, ["cidr", "version"]);
            const { cidr, versions, regex } = Ip.regex(options);
            const version = options.version ? versions : void 0;
            return this.$_addRule({ name: "ip", args: { options: { cidr, version } }, regex });
          },
          validate(value, helpers, { options }, { regex }) {
            if (regex.test(value)) {
              return value;
            }
            if (options.version) {
              return helpers.error("string.ipVersion", { value, cidr: options.cidr, version: options.version });
            }
            return helpers.error("string.ip", { value, cidr: options.cidr });
          }
        },
        isoDate: {
          method() {
            return this.$_addRule("isoDate");
          },
          validate(value, { error }) {
            if (internals.isoDate(value)) {
              return value;
            }
            return error("string.isoDate");
          }
        },
        isoDuration: {
          method() {
            return this.$_addRule("isoDuration");
          },
          validate(value, helpers) {
            if (internals.isoDurationRegex.test(value)) {
              return value;
            }
            return helpers.error("string.isoDuration");
          }
        },
        length: {
          method(limit, encoding) {
            return internals.length(this, "length", limit, "=", encoding);
          },
          validate(value, helpers, { limit, encoding }, { name, operator, args }) {
            const length = encoding ? Buffer && Buffer.byteLength(value, encoding) : value.length;
            if (Common.compare(length, limit, operator)) {
              return value;
            }
            return helpers.error("string." + name, { limit: args.limit, value, encoding });
          },
          args: [
            {
              name: "limit",
              ref: true,
              assert: Common.limit,
              message: "must be a positive integer"
            },
            "encoding"
          ]
        },
        lowercase: {
          method() {
            return this.case("lower");
          }
        },
        max: {
          method(limit, encoding) {
            return internals.length(this, "max", limit, "<=", encoding);
          },
          args: ["limit", "encoding"]
        },
        min: {
          method(limit, encoding) {
            return internals.length(this, "min", limit, ">=", encoding);
          },
          args: ["limit", "encoding"]
        },
        normalize: {
          method(form = "NFC") {
            Assert(internals.normalizationForms.includes(form), "normalization form must be one of " + internals.normalizationForms.join(", "));
            return this.$_addRule({ name: "normalize", args: { form } });
          },
          validate(value, { error }, { form }) {
            if (value === value.normalize(form)) {
              return value;
            }
            return error("string.normalize", { value, form });
          },
          convert: true
        },
        pattern: {
          alias: "regex",
          method(regex, options = {}) {
            Assert(regex instanceof RegExp, "regex must be a RegExp");
            Assert(!regex.flags.includes("g") && !regex.flags.includes("y"), "regex should not use global or sticky mode");
            if (typeof options === "string") {
              options = { name: options };
            }
            Common.assertOptions(options, ["invert", "name"]);
            const errorCode = ["string.pattern", options.invert ? ".invert" : "", options.name ? ".name" : ".base"].join("");
            return this.$_addRule({ name: "pattern", args: { regex, options }, errorCode });
          },
          validate(value, helpers, { regex, options }, { errorCode }) {
            const patternMatch = regex.test(value);
            if (patternMatch ^ options.invert) {
              return value;
            }
            return helpers.error(errorCode, { name: options.name, regex, value });
          },
          args: ["regex", "options"],
          multi: true
        },
        replace: {
          method(pattern, replacement) {
            if (typeof pattern === "string") {
              pattern = new RegExp(EscapeRegex(pattern), "g");
            }
            Assert(pattern instanceof RegExp, "pattern must be a RegExp");
            Assert(typeof replacement === "string", "replacement must be a String");
            const obj = this.clone();
            if (!obj.$_terms.replacements) {
              obj.$_terms.replacements = [];
            }
            obj.$_terms.replacements.push({ pattern, replacement });
            return obj;
          }
        },
        token: {
          method() {
            return this.$_addRule("token");
          },
          validate(value, helpers) {
            if (/^\w+$/.test(value)) {
              return value;
            }
            return helpers.error("string.token");
          }
        },
        trim: {
          method(enabled = true) {
            Assert(typeof enabled === "boolean", "enabled must be a boolean");
            return this.$_addRule({ name: "trim", args: { enabled } });
          },
          validate(value, helpers, { enabled }) {
            if (!enabled || value === value.trim()) {
              return value;
            }
            return helpers.error("string.trim");
          },
          convert: true
        },
        truncate: {
          method(enabled = true) {
            Assert(typeof enabled === "boolean", "enabled must be a boolean");
            return this.$_setFlag("truncate", enabled);
          }
        },
        uppercase: {
          method() {
            return this.case("upper");
          }
        },
        uri: {
          method(options = {}) {
            Common.assertOptions(options, ["allowRelative", "allowQuerySquareBrackets", "domain", "relativeOnly", "scheme"]);
            if (options.domain) {
              Common.assertOptions(options.domain, ["allowUnicode", "minDomainSegments", "tlds"]);
            }
            const { regex, scheme } = Uri.regex(options);
            const domain = options.domain ? internals.addressOptions(options.domain) : null;
            return this.$_addRule({ name: "uri", args: { options }, regex, domain, scheme });
          },
          validate(value, helpers, { options }, { regex, domain, scheme }) {
            if (["http:/", "https:/"].includes(value)) {
              return helpers.error("string.uri");
            }
            const match = regex.exec(value);
            if (match) {
              if (domain && !Domain.isValid(match[1], domain)) {
                return helpers.error("string.domain", { value: match[1] });
              }
              return value;
            }
            if (options.relativeOnly) {
              return helpers.error("string.uriRelativeOnly");
            }
            if (options.scheme) {
              return helpers.error("string.uriCustomScheme", { scheme, value });
            }
            return helpers.error("string.uri");
          }
        }
      },
      manifest: {
        build(obj, desc) {
          if (desc.replacements) {
            for (const { pattern, replacement } of desc.replacements) {
              obj = obj.replace(pattern, replacement);
            }
          }
          return obj;
        }
      },
      messages: {
        "string.alphanum": "{{#label}} must only contain alpha-numeric characters",
        "string.base": "{{#label}} must be a string",
        "string.base64": "{{#label}} must be a valid base64 string",
        "string.creditCard": "{{#label}} must be a credit card",
        "string.dataUri": "{{#label}} must be a valid dataUri string",
        "string.domain": "{{#label}} must contain a valid domain name",
        "string.email": "{{#label}} must be a valid email",
        "string.empty": "{{#label}} is not allowed to be empty",
        "string.guid": "{{#label}} must be a valid GUID",
        "string.hex": "{{#label}} must only contain hexadecimal characters",
        "string.hexAlign": "{{#label}} hex decoded representation must be byte aligned",
        "string.hostname": "{{#label}} must be a valid hostname",
        "string.ip": "{{#label}} must be a valid ip address with a {{#cidr}} CIDR",
        "string.ipVersion": "{{#label}} must be a valid ip address of one of the following versions {{#version}} with a {{#cidr}} CIDR",
        "string.isoDate": "{{#label}} must be in iso format",
        "string.isoDuration": "{{#label}} must be a valid ISO 8601 duration",
        "string.length": "{{#label}} length must be {{#limit}} characters long",
        "string.lowercase": "{{#label}} must only contain lowercase characters",
        "string.max": "{{#label}} length must be less than or equal to {{#limit}} characters long",
        "string.min": "{{#label}} length must be at least {{#limit}} characters long",
        "string.normalize": "{{#label}} must be unicode normalized in the {{#form}} form",
        "string.token": "{{#label}} must only contain alpha-numeric and underscore characters",
        "string.pattern.base": '{{#label}} with value "{[.]}" fails to match the required pattern: {{#regex}}',
        "string.pattern.name": '{{#label}} with value "{[.]}" fails to match the {{#name}} pattern',
        "string.pattern.invert.base": '{{#label}} with value "{[.]}" matches the inverted pattern: {{#regex}}',
        "string.pattern.invert.name": '{{#label}} with value "{[.]}" matches the inverted {{#name}} pattern',
        "string.trim": "{{#label}} must not have leading or trailing whitespace",
        "string.uri": "{{#label}} must be a valid uri",
        "string.uriCustomScheme": "{{#label}} must be a valid uri with a scheme matching the {{#scheme}} pattern",
        "string.uriRelativeOnly": "{{#label}} must be a valid relative uri",
        "string.uppercase": "{{#label}} must only contain uppercase characters"
      }
    });
    internals.addressOptions = function(options) {
      if (!options) {
        return options;
      }
      Assert(options.minDomainSegments === void 0 || Number.isSafeInteger(options.minDomainSegments) && options.minDomainSegments > 0, "minDomainSegments must be a positive integer");
      if (options.tlds === false) {
        return options;
      }
      if (options.tlds === true || options.tlds === void 0) {
        Assert(internals.tlds, "Built-in TLD list disabled");
        return Object.assign({}, options, internals.tlds);
      }
      Assert(typeof options.tlds === "object", "tlds must be true, false, or an object");
      const deny = options.tlds.deny;
      if (deny) {
        if (Array.isArray(deny)) {
          options = Object.assign({}, options, { tlds: { deny: new Set(deny) } });
        }
        Assert(options.tlds.deny instanceof Set, "tlds.deny must be an array, Set, or boolean");
        Assert(!options.tlds.allow, "Cannot specify both tlds.allow and tlds.deny lists");
        return options;
      }
      const allow = options.tlds.allow;
      if (!allow) {
        return options;
      }
      if (allow === true) {
        Assert(internals.tlds, "Built-in TLD list disabled");
        return Object.assign({}, options, internals.tlds);
      }
      if (Array.isArray(allow)) {
        options = Object.assign({}, options, { tlds: { allow: new Set(allow) } });
      }
      Assert(options.tlds.allow instanceof Set, "tlds.allow must be an array, Set, or boolean");
      return options;
    };
    internals.isoDate = function(value) {
      if (!Common.isIsoDate(value)) {
        return null;
      }
      const date = new Date(value);
      if (isNaN(date.getTime())) {
        return null;
      }
      return date.toISOString();
    };
    internals.length = function(schema, name, limit, operator, encoding) {
      Assert(!encoding || Buffer && Buffer.isEncoding(encoding), "Invalid encoding:", encoding);
      return schema.$_addRule({ name, method: "length", args: { limit, encoding }, operator });
    };
  }
});

// ../node_modules/@hapi/joi/lib/types/symbol.js
var require_symbol = __commonJS({
  "../node_modules/@hapi/joi/lib/types/symbol.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Any = require_any();
    var internals = {};
    internals.Map = class extends Map {
      slice() {
        return new internals.Map(this);
      }
    };
    module.exports = Any.extend({
      type: "symbol",
      terms: {
        map: { init: new internals.Map() }
      },
      coerce: {
        method(value, { schema, error }) {
          const lookup = schema.$_terms.map.get(value);
          if (lookup) {
            value = lookup;
          }
          if (!schema._flags.only || typeof value === "symbol") {
            return { value };
          }
          return { value, errors: error("symbol.map", { map: schema.$_terms.map }) };
        }
      },
      validate(value, { error }) {
        if (typeof value !== "symbol") {
          return { value, errors: error("symbol.base") };
        }
      },
      rules: {
        map: {
          method(iterable) {
            if (iterable && !iterable[Symbol.iterator] && typeof iterable === "object") {
              iterable = Object.entries(iterable);
            }
            Assert(iterable && iterable[Symbol.iterator], "Iterable must be an iterable or object");
            const obj = this.clone();
            const symbols = [];
            for (const entry of iterable) {
              Assert(entry && entry[Symbol.iterator], "Entry must be an iterable");
              const [key, value] = entry;
              Assert(typeof key !== "object" && typeof key !== "function" && typeof key !== "symbol", "Key must not be of type object, function, or Symbol");
              Assert(typeof value === "symbol", "Value must be a Symbol");
              obj.$_terms.map.set(key, value);
              symbols.push(value);
            }
            return obj.valid(...symbols);
          }
        }
      },
      manifest: {
        build(obj, desc) {
          if (desc.map) {
            obj = obj.map(desc.map);
          }
          return obj;
        }
      },
      messages: {
        "symbol.base": "{{#label}} must be a symbol",
        "symbol.map": "{{#label}} must be one of {{#map}}"
      }
    });
  }
});

// ../node_modules/@hapi/joi/lib/types/binary.js
var require_binary = __commonJS({
  "../node_modules/@hapi/joi/lib/types/binary.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Any = require_any();
    var Common = require_common();
    module.exports = Any.extend({
      type: "binary",
      coerce: {
        from: "string",
        method(value, { schema }) {
          try {
            return { value: Buffer.from(value, schema._flags.encoding) };
          } catch (ignoreErr) {
          }
        }
      },
      validate(value, { error }) {
        if (!Buffer.isBuffer(value)) {
          return { value, errors: error("binary.base") };
        }
      },
      rules: {
        encoding: {
          method(encoding) {
            Assert(Buffer.isEncoding(encoding), "Invalid encoding:", encoding);
            return this.$_setFlag("encoding", encoding);
          }
        },
        length: {
          method(limit) {
            return this.$_addRule({ name: "length", method: "length", args: { limit }, operator: "=" });
          },
          validate(value, helpers, { limit }, { name, operator, args }) {
            if (Common.compare(value.length, limit, operator)) {
              return value;
            }
            return helpers.error("binary." + name, { limit: args.limit, value });
          },
          args: [
            {
              name: "limit",
              ref: true,
              assert: Common.limit,
              message: "must be a positive integer"
            }
          ]
        },
        max: {
          method(limit) {
            return this.$_addRule({ name: "max", method: "length", args: { limit }, operator: "<=" });
          }
        },
        min: {
          method(limit) {
            return this.$_addRule({ name: "min", method: "length", args: { limit }, operator: ">=" });
          }
        }
      },
      cast: {
        string: {
          from: /* @__PURE__ */ __name((value) => Buffer.isBuffer(value), "from"),
          to(value, helpers) {
            return value.toString();
          }
        }
      },
      messages: {
        "binary.base": "{{#label}} must be a buffer or a string",
        "binary.length": "{{#label}} must be {{#limit}} bytes",
        "binary.max": "{{#label}} must be less than or equal to {{#limit}} bytes",
        "binary.min": "{{#label}} must be at least {{#limit}} bytes"
      }
    });
  }
});

// ../node_modules/@hapi/joi/lib/index.js
var require_lib4 = __commonJS({
  "../node_modules/@hapi/joi/lib/index.js"(exports, module) {
    "use strict";
    init_modules_watch_stub();
    var Assert = require_assert();
    var Clone = require_clone();
    var Cache = require_cache();
    var Common = require_common();
    var Compile = require_compile();
    var Errors = require_errors();
    var Extend = require_extend();
    var Manifest = require_manifest();
    var Ref = require_ref();
    var Template = require_template();
    var Trace = require_trace();
    var Schemas;
    var internals = {
      types: {
        alternatives: require_alternatives(),
        any: require_any(),
        array: require_array(),
        boolean: require_boolean(),
        date: require_date(),
        function: require_function(),
        link: require_link(),
        number: require_number(),
        object: require_object(),
        string: require_string(),
        symbol: require_symbol()
      },
      aliases: {
        alt: "alternatives",
        bool: "boolean",
        func: "function"
      }
    };
    if (Buffer) {
      internals.types.binary = require_binary();
    }
    internals.root = function() {
      const root = {
        _types: new Set(Object.keys(internals.types))
      };
      for (const type of root._types) {
        root[type] = function(...args) {
          Assert(!args.length || ["alternatives", "link", "object"].includes(type), "The", type, "type does not allow arguments");
          return internals.generate(this, internals.types[type], args);
        };
      }
      for (const method of ["allow", "custom", "disallow", "equal", "exist", "forbidden", "invalid", "not", "only", "optional", "options", "prefs", "preferences", "required", "strip", "valid", "when"]) {
        root[method] = function(...args) {
          return this.any()[method](...args);
        };
      }
      Object.assign(root, internals.methods);
      for (const alias in internals.aliases) {
        const target = internals.aliases[alias];
        root[alias] = root[target];
      }
      root.x = root.expression;
      if (Trace.setup) {
        Trace.setup(root);
      }
      return root;
    };
    internals.methods = {
      ValidationError: Errors.ValidationError,
      version: Common.version,
      cache: Cache.provider,
      assert(value, schema, ...args) {
        internals.assert(value, schema, true, args);
      },
      attempt(value, schema, ...args) {
        return internals.assert(value, schema, false, args);
      },
      build(desc) {
        Assert(typeof Manifest.build === "function", "Manifest functionality disabled");
        return Manifest.build(this, desc);
      },
      checkPreferences(prefs) {
        Common.checkPreferences(prefs);
      },
      compile(schema, options) {
        return Compile.compile(this, schema, options);
      },
      defaults(modifier) {
        Assert(typeof modifier === "function", "modifier must be a function");
        const joi = Object.assign({}, this);
        for (const type of joi._types) {
          const schema = modifier(joi[type]());
          Assert(Common.isSchema(schema), "modifier must return a valid schema object");
          joi[type] = function(...args) {
            return internals.generate(this, schema, args);
          };
        }
        return joi;
      },
      expression(...args) {
        return new Template(...args);
      },
      extend(...extensions) {
        Common.verifyFlat(extensions, "extend");
        Schemas = Schemas || require_schemas();
        Assert(extensions.length, "You need to provide at least one extension");
        this.assert(extensions, Schemas.extensions);
        const joi = Object.assign({}, this);
        joi._types = new Set(joi._types);
        for (let extension of extensions) {
          if (typeof extension === "function") {
            extension = extension(joi);
          }
          this.assert(extension, Schemas.extension);
          const expanded = internals.expandExtension(extension, joi);
          for (const item of expanded) {
            Assert(joi[item.type] === void 0 || joi._types.has(item.type), "Cannot override name", item.type);
            const base = item.base || this.any();
            const schema = Extend.type(base, item);
            joi._types.add(item.type);
            joi[item.type] = function(...args) {
              return internals.generate(this, schema, args);
            };
          }
        }
        return joi;
      },
      isError: Errors.ValidationError.isError,
      isExpression: Template.isTemplate,
      isRef: Ref.isRef,
      isSchema: Common.isSchema,
      in(...args) {
        return Ref.in(...args);
      },
      override: Common.symbols.override,
      ref(...args) {
        return Ref.create(...args);
      },
      types() {
        const types = {};
        for (const type of this._types) {
          types[type] = this[type]();
        }
        for (const target in internals.aliases) {
          types[target] = this[target]();
        }
        return types;
      }
    };
    internals.assert = function(value, schema, annotate, args) {
      const message = args[0] instanceof Error || typeof args[0] === "string" ? args[0] : null;
      const options = message ? args[1] : args[0];
      const result = schema.validate(value, Common.preferences({ errors: { stack: true } }, options || {}));
      let error = result.error;
      if (!error) {
        return result.value;
      }
      if (message instanceof Error) {
        throw message;
      }
      const display = annotate && typeof error.annotate === "function" ? error.annotate() : error.message;
      if (error instanceof Errors.ValidationError === false) {
        error = Clone(error);
      }
      error.message = message ? `${message} ${display}` : display;
      throw error;
    };
    internals.generate = function(root, schema, args) {
      Assert(root, "Must be invoked on a Joi instance.");
      schema.$_root = root;
      if (!schema._definition.args || !args.length) {
        return schema;
      }
      return schema._definition.args(schema, ...args);
    };
    internals.expandExtension = function(extension, joi) {
      if (typeof extension.type === "string") {
        return [extension];
      }
      const extended = [];
      for (const type of joi._types) {
        if (extension.type.test(type)) {
          const item = Object.assign({}, extension);
          item.type = type;
          item.base = joi[type]();
          extended.push(item);
        }
      }
      return extended;
    };
    module.exports = internals.root();
  }
});

// .wrangler/tmp/bundle-i7Md8O/middleware-loader.entry.ts
init_modules_watch_stub();

// .wrangler/tmp/bundle-i7Md8O/middleware-insertion-facade.js
init_modules_watch_stub();

// src/index.ts
init_modules_watch_stub();
import { DurableObject } from "cloudflare:workers";

// ../scripts/engine-battle-city-simulation.ts
init_modules_watch_stub();

// ../src/core/collision/CollisionSystem.ts
init_modules_watch_stub();

// ../src/core/collision/Collision.ts
init_modules_watch_stub();
var Collision = class {
  constructor(collider, box) {
    this.contacts = [];
    this.collider = collider;
    this.box = box;
  }
  static {
    __name(this, "Collision");
  }
  addContact(contact) {
    this.contacts.push(contact);
  }
};

// ../src/core/collision/CollisionContact.ts
init_modules_watch_stub();
var CollisionContact = class {
  static {
    __name(this, "CollisionContact");
  }
  constructor(collider, box) {
    this.collider = collider;
    this.box = box;
  }
};

// ../src/core/collision/CollisionSystem.ts
var CollisionSystem = class {
  constructor() {
    this.dynamicColliders = [];
    this.staticColliders = [];
    this.collisions = [];
  }
  static {
    __name(this, "CollisionSystem");
  }
  register(collider) {
    if (collider.dynamic) {
      this.dynamicColliders.push(collider);
    } else {
      this.staticColliders.push(collider);
    }
    collider.unregisterRequested.addListenerOnce(() => {
      this.unregister(collider);
    });
    collider.init();
  }
  unregister(collider) {
    const list = collider.dynamic ? this.dynamicColliders : this.staticColliders;
    const index = list.indexOf(collider);
    if (index !== -1) {
      list.splice(index, 1);
    }
  }
  update() {
    const bothColliders = this.dynamicColliders.concat(this.staticColliders);
    this.collisions = [];
    for (const selfCollider of this.dynamicColliders) {
      let collision = null;
      for (const otherCollider of bothColliders) {
        if (otherCollider === selfCollider) {
          continue;
        }
        if (otherCollider.object.hasParent(selfCollider.object)) {
          continue;
        }
        if (selfCollider.object.hasParent(otherCollider.object)) {
          continue;
        }
        const selfBox = selfCollider.getBox();
        const otherBox = otherCollider.getBox();
        if (selfBox.intersectsBox(otherBox)) {
          if (collision === null) {
            collision = new Collision(selfCollider, selfBox);
          }
          const contact = new CollisionContact(otherCollider, otherBox);
          collision.addContact(contact);
        }
      }
      if (collision !== null) {
        this.collisions.push(collision);
      }
    }
  }
  getCollisionByCollider(collider) {
    for (const collision of this.collisions) {
      if (collision.collider === collider) {
        return collision;
      }
    }
    return null;
  }
  collide() {
    this.collisions.forEach((collision) => {
      collision.collider.object.invokeCollide(collision);
    });
  }
  getCollisions() {
    return this.collisions;
  }
  // Read-only access to the registered static colliders, for queries that
  // need to look at world geometry beyond a collision's own contact list
  // (e.g. line-of-sight / cover checks).
  getStaticColliders() {
    return this.staticColliders;
  }
  reset() {
    this.dynamicColliders = [];
    this.staticColliders = [];
    this.collisions = [];
  }
};

// ../src/core/GameObject.ts
init_modules_watch_stub();

// ../src/core/RenderObject.ts
init_modules_watch_stub();

// ../src/core/Transform.ts
init_modules_watch_stub();

// ../src/core/BoundingBox.ts
init_modules_watch_stub();

// ../src/core/Rect.ts
init_modules_watch_stub();

// ../src/core/Vector.ts
init_modules_watch_stub();
var Vector = class _Vector {
  static {
    __name(this, "Vector");
  }
  constructor(x = 0, y = 0) {
    this.x = x;
    this.y = y;
  }
  set(x, y) {
    this.x = x;
    this.y = y;
    return this;
  }
  setX(x) {
    this.x = x;
    return this;
  }
  setY(y) {
    this.y = y;
    return this;
  }
  add(v) {
    this.x += v.x;
    this.y += v.y;
    return this;
  }
  addX(x) {
    this.x += x;
    return this;
  }
  addY(y) {
    this.y += y;
    return this;
  }
  addScalar(s) {
    this.x += s;
    this.y += s;
    return this;
  }
  sub(v) {
    this.x -= v.x;
    this.y -= v.y;
    return this;
  }
  subX(x) {
    this.x -= x;
    return this;
  }
  subY(y) {
    this.y -= y;
    return this;
  }
  divideScalar(s) {
    this.x /= s;
    this.y /= s;
    return this;
  }
  mult(v) {
    this.x *= v.x;
    this.y *= v.y;
    return this;
  }
  multScalar(s) {
    this.x *= s;
    this.y *= s;
    return this;
  }
  round() {
    this.x = Math.round(this.x);
    this.y = Math.round(this.y);
    return this;
  }
  length() {
    return Math.sqrt(this.x ** 2 + this.y ** 2);
  }
  normalize() {
    const length = this.length() || 1;
    this.divideScalar(length);
    return this;
  }
  distanceTo(v) {
    const dx = this.x - v.x;
    const dy = this.y - v.y;
    const distance = Math.sqrt(dx ** 2 + dy ** 2);
    return distance;
  }
  negate() {
    this.x = -this.x;
    this.y = -this.y;
    return this;
  }
  copyFrom(v) {
    this.x = v.x;
    this.y = v.y;
    return this;
  }
  snapX(step = 1) {
    this.x = Math.round(this.x / step) * step;
    return this;
  }
  snapY(step = 1) {
    this.y = Math.round(this.y / step) * step;
    return this;
  }
  dot(v) {
    return this.x * v.x + this.y * v.y;
  }
  cross(v) {
    return this.x * v.y - this.y * v.x;
  }
  equals(v) {
    return this.x === v.x && this.y === v.y;
  }
  clone() {
    return new _Vector(this.x, this.y);
  }
  static fromArray(array2) {
    return new _Vector(array2[0], array2[1]);
  }
};

// ../src/core/Rect.ts
var Rect = class _Rect {
  static {
    __name(this, "Rect");
  }
  constructor(x = 0, y = 0, width = 0, height = 0) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
  }
  setWidth(width) {
    this.width = width;
    return this;
  }
  setHeight(height) {
    this.height = height;
    return this;
  }
  getCenter() {
    return new Vector(this.x + this.width / 2, this.y + this.height / 2);
  }
  intersectsRect(other) {
    return this.x < other.x + other.width && this.x + this.width > other.x && this.y < other.y + other.height && this.y + this.height > other.y;
  }
  clone() {
    return new _Rect(this.x, this.y, this.width, this.height);
  }
  toString() {
    return `[x: ${this.x}, y: ${this.y}, width: ${this.width}, height: ${this.height}]`;
  }
};

// ../src/core/Size.ts
init_modules_watch_stub();
var Size = class _Size {
  static {
    __name(this, "Size");
  }
  constructor(width = 0, height = 0) {
    this.width = width;
    this.height = height;
  }
  set(width, height) {
    this.width = width;
    this.height = height;
    return this;
  }
  setWidth(width) {
    this.width = width;
    return this;
  }
  setHeight(height) {
    this.height = height;
    return this;
  }
  copyFrom(size) {
    this.width = size.width;
    this.height = size.height;
    return this;
  }
  flip() {
    const tmp = this.width;
    this.width = this.height;
    this.height = tmp;
    return this;
  }
  clone() {
    return new _Size(this.width, this.height);
  }
  toVector() {
    return new Vector(this.width, this.height);
  }
};

// ../src/core/BoundingBox.ts
var BoundingBox = class _BoundingBox {
  static {
    __name(this, "BoundingBox");
  }
  constructor(min = new Vector(), max = new Vector()) {
    this.min = min;
    this.max = max;
  }
  getCenter() {
    return this.min.clone().add(this.max).divideScalar(2);
  }
  getSize() {
    const width = this.max.x - this.min.x;
    const height = this.max.y - this.min.y;
    const size = new Size(width, height);
    return size;
  }
  round() {
    this.min.round();
    this.max.round();
    return this;
  }
  equals(other) {
    return this.min.equals(other.min) && this.max.equals(other.max);
  }
  distanceCenterToCenter(other) {
    return this.getCenter().distanceTo(other.getCenter());
  }
  minkowskiSum(other) {
    const otherWidth = other.max.x - other.min.x;
    const otherHeight = other.max.y - other.min.y;
    const minX = this.min.x - otherWidth / 2;
    const maxX = this.max.x + otherWidth / 2;
    const minY = this.min.y - otherHeight / 2;
    const maxY = this.max.y + otherHeight / 2;
    this.min.set(minX, minY);
    this.max.set(maxX, maxY);
    return this;
  }
  intersectWith(other) {
    const minX = Math.max(this.min.x, other.min.x);
    const maxX = Math.min(this.max.x, other.max.x);
    const minY = Math.max(this.min.y, other.min.y);
    const maxY = Math.min(this.max.y, other.max.y);
    this.min.set(minX, minY);
    this.max.set(maxX, maxY);
    return this;
  }
  unionWith(...others) {
    const boxes = [this, ...others];
    let minX = null;
    let maxX = null;
    let minY = null;
    let maxY = null;
    for (const box of boxes) {
      if (minX === null || box.min.x < minX) {
        minX = box.min.x;
      }
      if (maxX === null || box.max.x > maxX) {
        maxX = box.max.x;
      }
      if (minY === null || box.min.y < minY) {
        minY = box.min.y;
      }
      if (maxY === null || box.max.y > maxY) {
        maxY = box.max.y;
      }
    }
    this.min.set(minX, minY);
    this.max.set(maxX, maxY);
    return this;
  }
  intersectsBox(other) {
    return this.min.x < other.max.x && this.max.x > other.min.x && this.min.y < other.max.y && this.max.y > other.min.y;
  }
  containsPoint(p) {
    const isOutside = this.max.x <= p.x || this.min.x >= p.x || this.max.y <= p.y || this.min.y >= p.y;
    return !isOutside;
  }
  clone() {
    return new _BoundingBox(this.min.clone(), this.max.clone());
  }
  toRect() {
    return new Rect(
      this.min.x,
      this.min.y,
      this.max.x - this.min.x,
      this.max.y - this.min.y
    );
  }
  fromPoints(points) {
    if (points.length === 0) {
      return this;
    }
    this.min.copyFrom(points[0]);
    this.max.copyFrom(points[0]);
    for (const point of points) {
      if (point.x < this.min.x) {
        this.min.x = point.x;
      }
      if (point.x > this.max.x) {
        this.max.x = point.x;
      }
      if (point.y < this.min.y) {
        this.min.y = point.y;
      }
      if (point.y > this.max.y) {
        this.max.y = point.y;
      }
    }
    return this;
  }
};

// ../src/core/Matrix3.ts
init_modules_watch_stub();

// ../src/core/utils/index.ts
init_modules_watch_stub();

// ../src/core/utils/ArrayUtils.ts
init_modules_watch_stub();

// ../src/core/utils/MathUtils.ts
init_modules_watch_stub();
var MathUtils = class {
  static {
    __name(this, "MathUtils");
  }
  // Note: floatinig point errors can be inroduced when calculating.
  // For example:
  //
  //   Math.sin(180 * Math.PI / 180) = 1.2246467991473532e-16
  //
  // which is supposed to be a zero and is very close to zero, but is not
  // exactly a zero. Later on when changing a sign, for zero it would not matter
  // but for this value - it carries on the sign and can influence the
  // calculations when multiplying by this number.
  // Use this method to round possible floating-point errors on the early stages
  // so this errors don't propagate to higher-level code.
  static round(value, precisionExp = 10) {
    const precision = 10 * precisionExp;
    return Math.round(value * precision) / precision;
  }
  static degreesToRadians(degrees) {
    return degrees * Math.PI / 180;
  }
  static radiansToDegrees(radians) {
    return radians * 180 / Math.PI;
  }
  static cosDegrees(degrees) {
    const radians = this.degreesToRadians(degrees);
    const cos = Math.cos(radians);
    const rounded = this.round(cos);
    return rounded;
  }
  static sinDegrees(degrees) {
    const radians = this.degreesToRadians(degrees);
    const sin = Math.sin(radians);
    const rounded = this.round(sin);
    return rounded;
  }
  static atan2Degrees(sin, cos) {
    const radians = Math.atan2(sin, cos);
    const degrees = this.radiansToDegrees(radians);
    return degrees;
  }
};

// ../src/core/utils/NumberUtils.ts
init_modules_watch_stub();

// ../src/core/utils/Prng.ts
init_modules_watch_stub();
var Prng = class {
  static {
    __name(this, "Prng");
  }
  constructor(seed = 1) {
    this.seedValue = seed >>> 0 || 1;
    this.state = this.seedValue;
  }
  // Restart the sequence from a (new) seed.
  reseed(seed) {
    this.seedValue = seed >>> 0 || 1;
    this.state = this.seedValue;
  }
  // The seed this generator was (re)started with. Record it to reproduce a run.
  getSeed() {
    return this.seedValue;
  }
  // Raw float in [0, 1).
  next() {
    this.state = this.state + 1831565813 >>> 0;
    let t = this.state;
    t = Math.imul(t ^ t >>> 15, t | 1);
    t ^= t + Math.imul(t ^ t >>> 7, t | 61);
    return ((t ^ t >>> 14) >>> 0) / 4294967296;
  }
  // Integer in [min, max) - min inclusive, max exclusive.
  number(min = 0, max = 100) {
    return min + Math.floor(this.next() * (max - min));
  }
  arrayElement(values) {
    const index = this.number(0, values.length);
    return values[index];
  }
  probability(chancePercent) {
    const num = this.number(1, 100);
    return num <= chancePercent;
  }
};

// ../src/core/utils/RandomUtils.ts
init_modules_watch_stub();

// ../src/core/Matrix3.ts
var Matrix3 = class _Matrix3 {
  constructor() {
    this.elements = [1, 0, 0, 0, 1, 0, 0, 0, 1];
  }
  static {
    __name(this, "Matrix3");
  }
  set(e11, e12, e13, e21, e22, e23, e31, e32, e33) {
    const e = this.elements;
    e[0] = e11;
    e[1] = e12;
    e[2] = e13;
    e[3] = e21;
    e[4] = e22;
    e[5] = e23;
    e[6] = e31;
    e[7] = e32;
    e[8] = e33;
    return this;
  }
  applyToVector(v) {
    const x = v.x;
    const y = v.y;
    const z = 1;
    const e = this.elements;
    v.x = x * e[0] + y * e[3] + z * e[6];
    v.y = x * e[1] + y * e[4] + z * e[7];
    return v;
  }
  premultiply(m) {
    return this.multiplyMatrices(m, this);
  }
  multiply(m) {
    return this.multiplyMatrices(this, m);
  }
  multiplyMatrices(a, b) {
    const ae = a.elements;
    const a11 = ae[0];
    const a12 = ae[1];
    const a13 = ae[2];
    const a21 = ae[3];
    const a22 = ae[4];
    const a23 = ae[5];
    const a31 = ae[6];
    const a32 = ae[7];
    const a33 = ae[8];
    const be = b.elements;
    const b11 = be[0];
    const b12 = be[1];
    const b13 = be[2];
    const b21 = be[3];
    const b22 = be[4];
    const b23 = be[5];
    const b31 = be[6];
    const b32 = be[7];
    const b33 = be[8];
    const e = this.elements;
    e[0] = a11 * b11 + a12 * b21 + a13 * b31;
    e[1] = a11 * b12 + a12 * b22 + a13 * b32;
    e[2] = a11 * b13 + a12 * b23 + a13 * b33;
    e[3] = a21 * b11 + a22 * b21 + a23 * b31;
    e[4] = a21 * b12 + a22 * b22 + a23 * b32;
    e[5] = a21 * b13 + a22 * b23 + a23 * b33;
    e[6] = a31 * b11 + a32 * b21 + a33 * b31;
    e[7] = a31 * b12 + a32 * b22 + a33 * b32;
    e[8] = a31 * b13 + a32 * b23 + a33 * b33;
    return this;
  }
  multiplyScalar(s) {
    const e = this.elements;
    e[0] *= s;
    e[1] *= s;
    e[2] *= s;
    e[3] *= s;
    e[4] *= s;
    e[5] *= s;
    e[6] *= s;
    e[7] *= s;
    e[8] *= s;
    return this;
  }
  invert() {
    const d = this.getDeterminant();
    if (d === 0) {
      throw new Error("Determinant is 0, cant invert");
    }
    const i = this.getCofactors().transpose().multiplyScalar(1 / d);
    this.copyFrom(i);
    return this;
  }
  getMinors() {
    const e = this.elements;
    const e11 = e[0];
    const e12 = e[1];
    const e13 = e[2];
    const e21 = e[3];
    const e22 = e[4];
    const e23 = e[5];
    const e31 = e[6];
    const e32 = e[7];
    const e33 = e[8];
    const m11 = e22 * e33 - e23 * e32;
    const m12 = e21 * e33 - e23 * e31;
    const m13 = e21 * e32 - e22 * e31;
    const m21 = e12 * e33 - e13 * e32;
    const m22 = e11 * e33 - e13 * e31;
    const m23 = e11 * e32 - e12 * e31;
    const m31 = e12 * e23 - e13 * e22;
    const m32 = e11 * e23 - e13 * e21;
    const m33 = e11 * e22 - e12 * e21;
    const m = new _Matrix3().set(m11, m12, m13, m21, m22, m23, m31, m32, m33);
    return m;
  }
  getCofactors() {
    const m = this.getMinors();
    const me = m.elements;
    const c = new _Matrix3().set(
      me[0],
      me[1] * -1,
      me[2],
      me[3] * -1,
      me[4],
      me[5] * -1,
      me[6],
      me[7] * -1,
      me[8]
    );
    return c;
  }
  getDeterminant() {
    const e = this.elements;
    const e11 = e[0];
    const e12 = e[1];
    const e13 = e[2];
    const e21 = e[3];
    const e22 = e[4];
    const e23 = e[5];
    const e31 = e[6];
    const e32 = e[7];
    const e33 = e[8];
    const d1 = e11 * (e22 * e33 - e23 * e32);
    const d2 = e12 * (e21 * e33 - e23 * e31);
    const d3 = e13 * (e21 * e32 - e22 * e31);
    const d = d1 - d2 + d3;
    return d;
  }
  transpose() {
    const e = this.elements;
    const a11 = e[0];
    const a12 = e[1];
    const a13 = e[2];
    const a21 = e[3];
    const a22 = e[4];
    const a23 = e[5];
    const a31 = e[6];
    const a32 = e[7];
    const a33 = e[8];
    e[0] = a11;
    e[1] = a21;
    e[2] = a31;
    e[3] = a12;
    e[4] = a22;
    e[5] = a32;
    e[6] = a13;
    e[7] = a23;
    e[8] = a33;
    return this;
  }
  copyFrom(m) {
    this.fromArray(m.elements);
    return this;
  }
  fromArray(array2 = []) {
    for (let i = 0; i < 9; i += 1) {
      this.elements[i] = array2[i];
    }
    return this;
  }
  static createRotation(rotation) {
    const cos = MathUtils.cosDegrees(rotation);
    const sin = MathUtils.sinDegrees(rotation);
    return new _Matrix3().set(cos, sin, 0, -sin, cos, 0, 0, 0, 1);
  }
  clone() {
    return new _Matrix3().fromArray(this.elements);
  }
};

// ../src/core/Node.ts
init_modules_watch_stub();
var Node = class {
  constructor() {
    this.children = [];
    this.parent = null;
    this.removedChildren = [];
    this.isRemoved = false;
  }
  static {
    __name(this, "Node");
  }
  // TODO: figure out input type for a child
  add(...childrenToAdd) {
    for (const childToAdd of childrenToAdd) {
      if (childToAdd.parent !== null) {
        childToAdd.parent.remove(childToAdd, false);
      }
      childToAdd.parent = this;
      this.children.push(childToAdd);
    }
    return this;
  }
  replaceSelf(replacement) {
    if (this.parent === null) {
      return this;
    }
    this.parent.remove(this);
    this.parent.add(replacement);
    return this;
  }
  remove(childToRemove, addToRemoved = true) {
    const index = this.children.indexOf(childToRemove);
    if (index === -1) {
      return false;
    }
    if (addToRemoved) {
      childToRemove.isRemoved = true;
      this.removedChildren.push(childToRemove);
    }
    this.children.splice(index, 1);
    return true;
  }
  removeSelf() {
    if (this.parent === null) {
      return this;
    }
    this.parent.remove(this);
    return this;
  }
  removeAllChildren() {
    this.children = [];
    return this;
  }
  cleanupRemoved() {
    this.removedChildren = [];
  }
  traverse(callback) {
    callback(this);
    for (const child of this.children) {
      child.traverse(callback);
    }
    return this;
  }
  traverseDescedants(callback) {
    for (const child of this.children) {
      child.traverse(callback);
    }
    return this;
  }
  hasParent(parentToFind) {
    let parent = this.parent;
    while (parent !== null) {
      if (parent === parentToFind) {
        return true;
      }
      parent = parent.parent;
    }
    return false;
  }
  traverseParents(callback) {
    const parent = this.parent;
    if (parent !== null) {
      callback(parent);
      parent.traverseParents(callback);
    }
    return this;
  }
  flatten() {
    const nodes = [];
    this.traverse((node) => {
      nodes.push(node);
    });
    return nodes;
  }
};

// ../src/core/Transform.ts
var X_AXIS = new Vector(1, 0);
var Y_AXIS = new Vector(0, -1);
var Transform = class extends Node {
  constructor(width = 0, height = 0) {
    super();
    this.size = new Size();
    this.position = new Vector(0, 0);
    // Values in range [0,1] based on size.
    this.origin = new Vector(0, 0);
    // Degrees; clockwise
    this.rotation = 0;
    // Pivot for rotation. Values in range [0,1] based on size.
    this.pivot = new Vector(0, 0);
    this.matrix = new Matrix3();
    this.worldMatrix = new Matrix3();
    this.boundingBox = new BoundingBox();
    this.worldBoundingBox = new BoundingBox();
    this.matrixAutoUpdate = false;
    this.worldMatrixNeedsUpdate = false;
    // Render interpolation: position at the start of the current sim step
    // (interpPrev) and the real position (interpActual) saved while the object is
    // temporarily moved to its interpolated position for drawing. Presentation
    // only — the sim always runs on the real position.
    this.interpPrev = new Vector(0, 0);
    this.interpActual = new Vector(0, 0);
    this.interpHasPrev = false;
    this.interpApplied = false;
    this.size = new Size(width, height);
  }
  static {
    __name(this, "Transform");
  }
  // Snapshot the current position as the "previous" step state. Called once per
  // fixed sim step (before the step moves anything) so the renderer can
  // interpolate between steps.
  interpCapture() {
    this.interpPrev.copyFrom(this.position);
    this.interpHasPrev = true;
  }
  // Temporarily move the object forward along last step's velocity for
  // rendering: current + (current - prev) * alpha, where alpha is the fraction
  // into the not-yet-simulated next step. Extrapolation (not interpolation) is
  // used so there is NO added latency when the display is matched to the sim
  // rate (alpha ~ 0 -> draw the real position), while still smoothing motion on
  // higher-refresh displays. Only position is extrapolated (rotation is
  // discrete). Marks the subtree dirty so world matrices recompute. No-op for
  // objects that didn't move (prev == current) or have no captured previous.
  interpApply(alpha) {
    this.interpApplied = false;
    if (!this.interpHasPrev) {
      return;
    }
    if (this.interpPrev.x === this.position.x && this.interpPrev.y === this.position.y) {
      return;
    }
    this.interpActual.copyFrom(this.position);
    this.position.set(
      this.interpActual.x + (this.interpActual.x - this.interpPrev.x) * alpha,
      this.interpActual.y + (this.interpActual.y - this.interpPrev.y) * alpha
    );
    this.updateMatrix(true);
    this.interpApplied = true;
  }
  // Restore the real position after drawing an interpolated frame.
  interpRestore() {
    if (!this.interpApplied) {
      return;
    }
    this.position.copyFrom(this.interpActual);
    this.updateMatrix(true);
    this.interpApplied = false;
  }
  rotate(rotation) {
    this.rotation = rotation;
  }
  // Add child while keeping it's world position.
  // Modifies attachable object local matrix, position and rotation.
  attach(target) {
    this.updateWorldMatrix(true);
    const invSelfWorldTransformMatrix = this.worldMatrix.clone().invert();
    if (target.parent !== null) {
      target.parent.updateWorldMatrix(true);
      invSelfWorldTransformMatrix.premultiply(target.parent.worldMatrix);
    }
    target.updateMatrix();
    target.applyMatrix3(invSelfWorldTransformMatrix);
    this.add(target);
    return this;
  }
  applyMatrix3(transformMatrix) {
    if (this.matrixAutoUpdate) {
      this.updateMatrix();
    }
    this.matrix.multiply(transformMatrix);
    const { rotation, position } = this.decomposeTransformMatrix(
      this.matrix,
      this.getPivotOffset(),
      this.getOriginOffset()
    );
    this.rotation = rotation;
    this.position = position;
  }
  translateX(distance) {
    this.translateOnAxis(X_AXIS, distance);
    return this;
  }
  translateY(distance) {
    this.translateOnAxis(Y_AXIS, distance);
    return this;
  }
  translateOnAxis(axis, distance) {
    const d = Matrix3.createRotation(this.rotation).applyToVector(axis.clone()).multScalar(distance);
    this.position.add(d);
    return this;
  }
  getWorldPosition() {
    const { position: worldPosition } = this.decomposeTransformMatrix(
      this.worldMatrix,
      this.getPivotOffset(),
      this.getOriginOffset()
    );
    return worldPosition;
  }
  getWorldRotation() {
    const { rotation: worldRotation } = this.decomposeTransformMatrix(
      this.worldMatrix,
      this.getPivotOffset(),
      this.getOriginOffset()
    );
    return worldRotation;
  }
  getCenter() {
    return this.getBoundingBox().getCenter();
  }
  setCenter(v) {
    const size = this.getBoundingBox().getSize();
    this.position.set(v.x - size.width / 2, v.y - size.height / 2);
  }
  setCenterX(x) {
    const size = this.getBoundingBox().getSize();
    this.position.setX(x - size.width / 2);
  }
  setCenterY(y) {
    const size = this.getBoundingBox().getSize();
    this.position.setY(y - size.height / 2);
  }
  getSelfCenter() {
    return this.size.toVector().divideScalar(2);
  }
  getBoundingBox() {
    if (this.matrixAutoUpdate) {
      this.updateMatrix();
    }
    return this.boundingBox;
  }
  getWorldBoundingBox() {
    this.updateWorldMatrix(true);
    return this.worldBoundingBox;
  }
  getSelfPoints() {
    const { width, height } = this.size;
    const points = [
      new Vector(0, 0),
      new Vector(width, 0),
      new Vector(width, height),
      new Vector(0, height)
    ];
    return points;
  }
  getPoints() {
    const selfPoints = this.getSelfPoints();
    const points = selfPoints.map((point) => {
      return this.matrix.applyToVector(point);
    });
    return points;
  }
  getWorldPoints() {
    const selfPoints = this.getSelfPoints();
    const points = selfPoints.map((point) => {
      return this.worldMatrix.applyToVector(point);
    });
    return points;
  }
  updateMatrix(childrenNeedUpdate = false) {
    const width = this.size.width;
    const height = this.size.height;
    const pivX = -this.pivot.x * width;
    const pivY = -this.pivot.y * height;
    const orgX = -this.origin.x * width;
    const orgY = -this.origin.y * height;
    const cos = MathUtils.cosDegrees(this.rotation);
    const sin = MathUtils.sinDegrees(this.rotation);
    const tx = pivX * cos - pivY * sin - pivX + orgX + this.position.x;
    const ty = pivX * sin + pivY * cos - pivY + orgY + this.position.y;
    this.matrix.set(cos, sin, 0, -sin, cos, 0, tx, ty, 1);
    this.writeTransformedBox(this.boundingBox, this.matrix, width, height);
    this.setWorldMatrixNeedsUpdate(childrenNeedUpdate);
  }
  // Writes the axis-aligned bounding box of this transform's local rect
  // (0,0,width,height) under the given matrix, corner by corner, without
  // allocating point vectors (allocation-free twin of
  // boundingBox.fromPoints(getPoints())).
  writeTransformedBox(box, m, width, height) {
    const e = m.elements;
    const e0 = e[0];
    const e1 = e[1];
    const e3 = e[3];
    const e4 = e[4];
    const e6 = e[6];
    const e7 = e[7];
    const x0 = e6;
    const y0 = e7;
    const x1 = width * e0 + e6;
    const y1 = width * e1 + e7;
    const x2 = width * e0 + height * e3 + e6;
    const y2 = width * e1 + height * e4 + e7;
    const x3 = height * e3 + e6;
    const y3 = height * e4 + e7;
    let minX = x0;
    let maxX = x0;
    let minY = y0;
    let maxY = y0;
    if (x1 < minX) minX = x1;
    if (x1 > maxX) maxX = x1;
    if (y1 < minY) minY = y1;
    if (y1 > maxY) maxY = y1;
    if (x2 < minX) minX = x2;
    if (x2 > maxX) maxX = x2;
    if (y2 < minY) minY = y2;
    if (y2 > maxY) maxY = y2;
    if (x3 < minX) minX = x3;
    if (x3 > maxX) maxX = x3;
    if (y3 < minY) minY = y3;
    if (y3 > maxY) maxY = y3;
    box.min.set(minX, minY);
    box.max.set(maxX, maxY);
  }
  setWorldMatrixNeedsUpdate(updateChildren = false) {
    this.worldMatrixNeedsUpdate = true;
    if (updateChildren) {
      for (const child of this.children) {
        child.setWorldMatrixNeedsUpdate(updateChildren);
      }
    }
  }
  updateWorldMatrix(updateParents = false, updateChildren = false) {
    if (updateParents === true && this.parent !== null) {
      this.parent.updateWorldMatrix(true, false);
    }
    if (this.matrixAutoUpdate) {
      this.updateMatrix();
    }
    if (this.worldMatrixNeedsUpdate) {
      if (this.parent === null) {
        this.worldMatrix.copyFrom(this.matrix);
      } else {
        this.worldMatrix.multiplyMatrices(this.matrix, this.parent.worldMatrix);
      }
      this.writeTransformedBox(
        this.worldBoundingBox,
        this.worldMatrix,
        this.size.width,
        this.size.height
      );
      this.worldMatrixNeedsUpdate = false;
    }
    if (updateChildren === true) {
      for (const child of this.children) {
        child.updateWorldMatrix(false, true);
      }
    }
  }
  getPivotOffset() {
    const pivotOffset = new Vector(
      -this.pivot.x * this.size.width,
      -this.pivot.y * this.size.height
    );
    return pivotOffset;
  }
  getOriginOffset() {
    const originOffset = new Vector(
      -this.origin.x * this.size.width,
      -this.origin.y * this.size.height
    );
    return originOffset;
  }
  /**
     * P (Pivot offset for rotation around it)
     *   [1  0  0]
     *   [0  1  0]
     *   [px py 1]
  
     * R (Rotation)
     *   [cos -sin 0]
     *   [sin  cos 0]
     *   [ 0    0  1]
     *
     * -P (Pivot offset cancellation)
     *   [1    0  0]
     *   [0    1  0]
     *   [-px -py 1]
     *
     * O (Origin offset for translation around it)
     *   [1  0  0]
     *   [0  1  0]
     *   [ox oy 1]
     *
     * T (Translation):
     *   [1  0  0]
     *   [0  1  0]
     *   [tx ty 1]
     *
     * All combined into a single transform matrix
     *
     *   TM = P * R * (-P) * O * T
     *
     * which can be applied to a vector V to transform it:
     *
     *   V' = V * TM
     */
  composeTransformMatrix(pivotOffset, originOffset, rotation, position) {
    const pivX = pivotOffset.x;
    const pivY = pivotOffset.y;
    const orgX = originOffset.x;
    const orgY = originOffset.y;
    const posX = position.x;
    const posY = position.y;
    const cos = MathUtils.cosDegrees(rotation);
    const sin = MathUtils.sinDegrees(rotation);
    const tx = pivX * cos - pivY * sin - pivX + orgX + posX;
    const ty = pivX * sin + pivY * cos - pivY + orgY + posY;
    const transformMatrix = new Matrix3().set(
      cos,
      sin,
      0,
      -sin,
      cos,
      0,
      tx,
      ty,
      1
    );
    return transformMatrix;
  }
  decomposeTransformMatrix(transformMatrix, pivotOffset, originOffset) {
    const pivX = pivotOffset.x;
    const pivY = pivotOffset.y;
    const orgX = originOffset.x;
    const orgY = originOffset.y;
    const cos = transformMatrix.elements[0];
    const sin = transformMatrix.elements[1];
    const tx = transformMatrix.elements[6];
    const ty = transformMatrix.elements[7];
    let rotation = MathUtils.atan2Degrees(sin, cos);
    if (rotation < 0) {
      rotation += 360;
    }
    const posX = tx - (pivX * cos - pivY * sin - pivX + orgX);
    const posY = ty - (pivX * sin + pivY * cos - pivY + orgY);
    const position = new Vector(posX, posY);
    return {
      rotation,
      position
    };
  }
};

// ../src/core/RenderObject.ts
var RenderObject = class extends Transform {
  constructor() {
    super(...arguments);
    // TODO: circular reference
    this.painter = null;
    // Render-time camera zoom for this object's subtree (does NOT touch the
    // transform tree or collision). 1 = no zoom. Set on the world/field root; the
    // renderer scales that subtree around (cameraPivotX, cameraPivotY) in screen
    // space, so gameplay zooms while the HUD/menus stay at their normal size.
    this.cameraZoom = 1;
    this.cameraPivotX = 0;
    this.cameraPivotY = 0;
    // Optional culling camera for dev visual zoom-out. Null means cull with the
    // rendered camera; set it on the camera root to keep culling on gameplay zoom.
    this.cameraCullZoom = null;
    this.cameraCullPivotX = 0;
    this.cameraCullPivotY = 0;
    // 0 by default
    // If null - will inherit from parent
    this.zIndex = null;
    // Computed, don't change
    this.worldZIndex = null;
    // Visible by default
    // If null - will inherit from parent
    this.visible = null;
    // Computed, don't change
    this.worldVisible = null;
    this.prevDirtyBox = null;
    this.needsPaint = true;
  }
  static {
    __name(this, "RenderObject");
  }
  canRender() {
    if (this.painter === null) {
      return false;
    }
    if (this.getWorldVisible() === false) {
      return false;
    }
    return true;
  }
  // Visibility
  setVisible(visible) {
    this.visible = visible;
    this.updateWorldVisible(true);
  }
  getVisible() {
    return this.visible;
  }
  getWorldVisible() {
    return this.worldVisible;
  }
  updateWorldVisible(updateParents = false) {
    if (this.parent !== null && updateParents === true) {
      this.parent.updateWorldVisible(true);
    }
    if (this.parent === null) {
      this.worldVisible = this.visible ?? true;
    } else {
      this.worldVisible = this.visible ?? this.parent.worldVisible;
    }
    for (const child of this.children) {
      child.updateWorldVisible();
    }
  }
  // Z-index
  setZIndex(zIndex) {
    this.zIndex = zIndex;
    this.updateWorldZIndex(true);
  }
  getZIndex() {
    return this.zIndex;
  }
  getWorldZIndex() {
    return this.worldZIndex;
  }
  updateWorldZIndex(updateParents = false) {
    if (this.parent !== null && updateParents === true) {
      this.parent.updateWorldZIndex(true);
    }
    if (this.parent === null) {
      this.worldZIndex = this.zIndex ?? 0;
    } else {
      this.worldZIndex = this.zIndex ?? this.parent.worldZIndex;
    }
    for (const child of this.children) {
      child.updateWorldZIndex();
    }
  }
  // Dirty box
  dirtyPaintBox() {
    this.prevDirtyBox = this.getWorldBoundingBox().clone();
    this.needsPaint = true;
    for (const child of this.children) {
      child.dirtyPaintBox();
    }
  }
  getPrevDirtyBox() {
    return this.prevDirtyBox;
  }
  resetPrevDirtyBox() {
    this.prevDirtyBox = null;
  }
  // Paint flag
  setNeedsPaint() {
    this.needsPaint = true;
    for (const child of this.children) {
      child.setNeedsPaint();
    }
  }
  doesNeedPaint() {
    return this.needsPaint;
  }
  resetNeedsPaint() {
    this.needsPaint = false;
  }
};

// ../src/core/GameObject.ts
var GameObject = class extends RenderObject {
  constructor() {
    super(...arguments);
    this.collider = null;
    this.ignorePause = false;
    this.tags = [];
    this.needsSetup = true;
  }
  static {
    __name(this, "GameObject");
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any
  invokeUpdate(...args) {
    if (this.needsSetup === true) {
      this.needsSetup = false;
      this.setup(...args);
      this.updateMatrix();
      this.updateWorldVisible(true);
      this.updateWorldZIndex(true);
    }
    this.update(...args);
  }
  invokeCollide(collision) {
    if (this.needsSetup === true) {
      return;
    }
    this.collide(collision);
  }
  hasBeenSetup() {
    return !this.needsSetup;
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any
  setup(...args) {
    return void 0;
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any
  update(...args) {
    return void 0;
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  collide(collision) {
    return void 0;
  }
  resetNeedsPaint() {
    if (this.needsSetup === true) {
      return;
    }
    super.resetNeedsPaint();
  }
};

// ../src/core/State.ts
init_modules_watch_stub();
var State = class {
  static {
    __name(this, "State");
  }
  constructor(initialValue) {
    this.value = initialValue;
    this.previousValue = null;
  }
  get() {
    return this.value;
  }
  set(newValue) {
    this.previousValue = this.value;
    this.value = newValue;
    return this;
  }
  update() {
    this.previousValue = this.value;
    this.value = this.value;
    return this;
  }
  is(value) {
    return this.value === value;
  }
  not(value) {
    return this.value !== value;
  }
  hasChanged() {
    return this.value !== this.previousValue;
  }
  hasChangedTo(toValue) {
    return this.value !== this.previousValue && this.value === toValue;
  }
  hasChangedFrom(fromValue) {
    return this.previousValue === fromValue;
  }
  hasChangedFromTo(fromValue, toValue) {
    return this.previousValue === fromValue && this.value === toValue;
  }
};

// ../src/game/GameState.ts
init_modules_watch_stub();

// ../src/game/Rotation.ts
init_modules_watch_stub();

// ../src/game/Session.ts
init_modules_watch_stub();

// ../src/game/SessionPlayer.ts
init_modules_watch_stub();

// ../src/core/index.ts
init_modules_watch_stub();

// ../src/core/AudioMixer.ts
init_modules_watch_stub();

// ../src/core/collision/index.ts
init_modules_watch_stub();

// ../src/core/collision/colliders/index.ts
init_modules_watch_stub();

// ../src/core/collision/colliders/BoxCollider.ts
init_modules_watch_stub();

// ../src/core/collision/Collider.ts
init_modules_watch_stub();

// ../src/core/Subject.ts
init_modules_watch_stub();
var Subject = class {
  constructor() {
    this.listeners = [];
    this.notify = /* @__PURE__ */ __name((event) => {
      this.listeners.forEach((listener) => {
        listener(event);
      });
      return this;
    }, "notify");
  }
  static {
    __name(this, "Subject");
  }
  addListener(listenerToAdd) {
    this.listeners.push(listenerToAdd);
    const unsubscribe = /* @__PURE__ */ __name(() => {
      this.removeListener(listenerToAdd);
    }, "unsubscribe");
    return unsubscribe;
  }
  addListenerOnce(listenerToAdd) {
    const wrappedListener = /* @__PURE__ */ __name((event) => {
      this.removeListener(wrappedListener);
      listenerToAdd(event);
    }, "wrappedListener");
    const unsubscribe = this.addListener(wrappedListener);
    return unsubscribe;
  }
  removeListener(listenerToRemove) {
    this.listeners = this.listeners.filter((listener) => {
      return listener !== listenerToRemove;
    });
    return this;
  }
};

// ../src/core/collision/Collider.ts
var Collider = class {
  constructor(object2, dynamic = false) {
    this.unregisterRequested = new Subject();
    this.object = object2;
    this.dynamic = dynamic;
  }
  static {
    __name(this, "Collider");
  }
  isInitialized() {
    return this.getCurrentBox() !== void 0;
  }
  unregister() {
    this.unregisterRequested.notify(null);
  }
};

// ../src/core/collision/colliders/BoxCollider.ts
var BoxCollider = class extends Collider {
  static {
    __name(this, "BoxCollider");
  }
  init() {
    const box = this.object.getWorldBoundingBox();
    this.prevBox = box.clone();
    this.currentBox = box.clone();
  }
  update() {
    const box = this.object.getWorldBoundingBox();
    this.prevBox = this.currentBox;
    this.currentBox = box.clone();
  }
  getBox() {
    return this.currentBox;
  }
  getPrevBox() {
    return this.prevBox;
  }
  getCurrentBox() {
    return this.currentBox;
  }
  getDirection() {
    const prevCenter = this.prevBox.getCenter();
    const currentCenter = this.currentBox.getCenter();
    const dx = currentCenter.x - prevCenter.x;
    const dy = currentCenter.y - prevCenter.y;
    const direction = new Vector(dx, dy);
    return direction;
  }
};

// ../src/core/collision/colliders/SweptBoxCollider.ts
init_modules_watch_stub();
var SweptBoxCollider = class extends Collider {
  static {
    __name(this, "SweptBoxCollider");
  }
  init() {
    const box = this.object.getWorldBoundingBox();
    this.prevBox = box.clone();
    this.currentBox = box.clone();
    this.sumBox = this.prevBox.clone().unionWith(this.currentBox);
  }
  update() {
    const box = this.object.getWorldBoundingBox();
    this.prevBox = this.currentBox;
    this.currentBox = box.clone();
    this.sumBox = this.prevBox.clone().unionWith(this.currentBox);
  }
  getBox() {
    return this.sumBox;
  }
  getCurrentBox() {
    return this.currentBox;
  }
  getPrevBox() {
    return this.prevBox;
  }
  getDirection() {
    const prevCenter = this.prevBox.getCenter();
    const currentCenter = this.currentBox.getCenter();
    const dx = currentCenter.x - prevCenter.x;
    const dy = currentCenter.y - prevCenter.y;
    const direction = new Vector(dx, dy);
    return direction;
  }
};

// ../src/core/file/index.ts
init_modules_watch_stub();

// ../src/core/file/FileOpener.ts
init_modules_watch_stub();

// ../src/core/file/FileSaver.ts
init_modules_watch_stub();

// ../src/core/file/TextFileReader.ts
init_modules_watch_stub();

// ../src/core/Logger.ts
init_modules_watch_stub();
var LogLevel = /* @__PURE__ */ ((LogLevel2) => {
  LogLevel2[LogLevel2["Debug"] = 1] = "Debug";
  LogLevel2[LogLevel2["Info"] = 2] = "Info";
  LogLevel2[LogLevel2["Warn"] = 3] = "Warn";
  LogLevel2[LogLevel2["Error"] = 4] = "Error";
  LogLevel2[LogLevel2["None"] = 5] = "None";
  return LogLevel2;
})(LogLevel || {});
var Logger = class {
  constructor(tag = "", level = 4 /* Error */) {
    this.warn = /* @__PURE__ */ __name((...args) => {
      if (this.level > 3 /* Warn */) {
        return;
      }
      console.warn(...this.composeArgs(...args));
    }, "warn");
    this.error = /* @__PURE__ */ __name((...args) => {
      if (this.level > 4 /* Error */) {
        return;
      }
      console.error(...this.composeArgs(...args));
    }, "error");
    this.time = /* @__PURE__ */ __name((mark) => {
      console.time(`${this.tag} ${mark}`);
    }, "time");
    this.timeEnd = /* @__PURE__ */ __name((mark) => {
      console.timeEnd(`${this.tag} ${mark}`);
    }, "timeEnd");
    this.tag = tag ? `[${tag}]` : "";
    this.level = level;
  }
  static {
    __name(this, "Logger");
  }
  static {
    this.Level = LogLevel;
  }
  setLevel(level) {
    this.level = level;
    return this;
  }
  debug(...args) {
    if (this.level > 1 /* Debug */) {
      return;
    }
    console.log(...this.composeArgs(...args));
  }
  info(...args) {
    if (this.level > 2 /* Info */) {
      return;
    }
    console.log(...this.composeArgs(...args));
  }
  /**
   * First argument to console.log-like methods supports formatting like %s.
   * Check if it is a string and append custom tag to it.
   */
  composeArgs(message, ...optionalParams) {
    const args = [];
    if (typeof message === "string") {
      const taggedMessage = `${this.tag} ${message}`;
      args.push(taggedMessage);
    } else {
      args.push(this.tag, message);
    }
    args.push(...optionalParams);
    return args;
  }
};

// ../src/core/graphics/index.ts
init_modules_watch_stub();

// ../src/core/graphics/sources/index.ts
init_modules_watch_stub();

// ../src/core/graphics/sources/CanvasImage.ts
init_modules_watch_stub();

// ../src/core/graphics/ImageSource.ts
init_modules_watch_stub();

// ../src/core/graphics/sources/Image.ts
init_modules_watch_stub();

// ../src/core/graphics/ColorSpriteFontGenerator.ts
init_modules_watch_stub();

// ../src/core/text/index.ts
init_modules_watch_stub();

// ../src/core/text/fonts/index.ts
init_modules_watch_stub();

// ../src/core/text/fonts/NullFont.ts
init_modules_watch_stub();
var NullFont = class {
  static {
    __name(this, "NullFont");
  }
  buildCharacter() {
    return null;
  }
  getScale() {
    return new Vector(1, 1);
  }
  getCharacterWidth() {
    return 0;
  }
  getCharacterHeight() {
    return 0;
  }
};

// ../src/core/text/fonts/RectFont.ts
init_modules_watch_stub();

// ../src/core/text/fonts/SpriteFont.ts
init_modules_watch_stub();

// ../src/core/text/Font.ts
init_modules_watch_stub();

// ../src/core/text/Text.ts
init_modules_watch_stub();
var DEFAULT_OPTIONS = {
  alignment: 0 /* Left */,
  letterSpacing: 1,
  lineSpacing: 1
};
var TEXT_LINE_SEPARATOR = "\n";
var TEXT_WORD_SEPARATOR = " ";
var Text = class {
  constructor(text = "", options = {}) {
    this.text = "";
    this.font = new NullFont();
    this.text = text;
    this.options = Object.assign({}, DEFAULT_OPTIONS, options);
  }
  static {
    __name(this, "Text");
  }
  static {
    this.LINE_SEPARATOR = TEXT_LINE_SEPARATOR;
  }
  static {
    this.WORD_SEPARATOR = TEXT_WORD_SEPARATOR;
  }
  setText(text) {
    this.text = text;
    return this;
  }
  setFont(font) {
    this.font = font;
    return this;
  }
  setOptions(options = {}) {
    this.options = Object.assign({}, this.options, options);
    return this;
  }
  getFont() {
    return this.font;
  }
  build() {
    const textOffset = new Vector(0, 0);
    const items = this.buildLinesFromText(this.text, textOffset);
    return items;
  }
  getWidth() {
    const unscaledWidth = this.getUnscaledTextWidth(this.text);
    const scale = this.font.getScale();
    const width = unscaledWidth * scale.x;
    return width;
  }
  getHeight() {
    const { lineSpacing } = this.options;
    const lines = this.splitTextToLines(this.text);
    const characterHeight = this.font.getCharacterHeight();
    const scale = this.font.getScale();
    const charactersHeight = lines.length * characterHeight * scale.y;
    const spacingHeight = (lines.length - 1) * lineSpacing * scale.y;
    const linesHeight = charactersHeight + spacingHeight;
    return linesHeight;
  }
  getSize() {
    const size = new Size(this.getWidth(), this.getHeight());
    return size;
  }
  buildLinesFromText(text, textOffset = new Vector(0, 0)) {
    const textItems = [];
    const characterHeight = this.font.getCharacterHeight();
    const textWidth = this.getUnscaledTextWidth(this.text);
    const lines = this.splitTextToLines(text);
    lines.forEach((line, index) => {
      const lineSpacing = index > 0 ? this.options.lineSpacing : 0;
      const lineOffsetY = index * (characterHeight + lineSpacing);
      let lineOffsetX = 0;
      if (this.options.alignment === 1 /* Center */) {
        const lineWidth = this.getUnscaledLineWidth(line);
        lineOffsetX = (textWidth - lineWidth) / 2;
      } else if (this.options.alignment === 2 /* Right */) {
        const lineWidth = this.getUnscaledLineWidth(line);
        lineOffsetX = textWidth - lineWidth;
      }
      const lineOffset = textOffset.clone().addX(lineOffsetX).addY(lineOffsetY);
      const lineItems = this.buildWordsFromLine(line, lineOffset);
      textItems.push(...lineItems);
    });
    return textItems;
  }
  buildWordsFromLine(line, lineOffset = new Vector(0, 0)) {
    const lineItems = [];
    let wordOffsetX = 0;
    const words = this.splitLineToWords(line);
    words.forEach((word, index) => {
      const wordOffset = lineOffset.clone().addX(wordOffsetX);
      const wordWidth = this.getUnscaledWordWidth(word);
      const wordItems = this.buildCharactersFromWord(word, wordOffset);
      lineItems.push(...wordItems);
      const wordSpacing = index < words.length - 1 ? this.getWordSeparatorWidth() : 0;
      wordOffsetX += wordWidth + wordSpacing;
    });
    return lineItems;
  }
  buildCharactersFromWord(word, wordOffset = new Vector(0, 0)) {
    const wordItems = [];
    const scale = this.font.getScale();
    const characters = this.splitWordToCharacters(word);
    characters.forEach((character, index) => {
      const letterSpacing = index > 0 ? this.options.letterSpacing : 0;
      const characterOffset = wordOffset.clone().addX(index * (this.font.getCharacterWidth() + letterSpacing));
      const characterItem = this.font.buildCharacter(
        character,
        scale,
        characterOffset
      );
      wordItems.push(characterItem);
    });
    return wordItems;
  }
  getUnscaledTextWidth(text) {
    const lines = this.splitTextToLines(text);
    const lineWidths = lines.map((line) => {
      return this.getUnscaledLineWidth(line);
    });
    const maxLineWidth = Math.max(...lineWidths);
    return maxLineWidth;
  }
  getUnscaledLineWidth(line) {
    let lineWidth = 0;
    const words = this.splitLineToWords(line);
    words.forEach((word, index) => {
      const wordWidth = this.getUnscaledWordWidth(word);
      const wordSpacing = index > 0 ? this.getWordSeparatorWidth() : 0;
      const wordTotalWidth = wordWidth + wordSpacing;
      lineWidth += wordTotalWidth;
    });
    return lineWidth;
  }
  getUnscaledWordWidth(word) {
    const characterWidth = this.font.getCharacterWidth();
    const { letterSpacing } = this.options;
    const charactersWidth = word.length * characterWidth;
    const spacingWidth = (word.length - 1) * letterSpacing;
    const wordWidth = charactersWidth + spacingWidth;
    return wordWidth;
  }
  getWordSeparatorWidth() {
    const { letterSpacing } = this.options;
    const characterWidth = this.font.getCharacterWidth();
    const separatorWidth = characterWidth + letterSpacing * 2;
    return separatorWidth;
  }
  splitTextToLines(text) {
    const lines = text.split(TEXT_LINE_SEPARATOR);
    return lines;
  }
  splitLineToWords(line) {
    const words = line.split(TEXT_WORD_SEPARATOR);
    return words;
  }
  splitWordToCharacters(word) {
    const characters = Array.from(word);
    return characters;
  }
};

// ../src/core/graphics/Sprite.ts
init_modules_watch_stub();

// ../src/core/input/index.ts
init_modules_watch_stub();

// ../src/core/input/codes/index.ts
init_modules_watch_stub();

// ../src/core/input/codes/GamepadButtonCode.ts
init_modules_watch_stub();

// ../src/core/input/codes/KeyboardButtonCode.ts
init_modules_watch_stub();

// ../src/core/input/devices/index.ts
init_modules_watch_stub();

// ../src/core/input/devices/GamepadInputDevice.ts
init_modules_watch_stub();

// ../src/core/input/devices/InputRecorderDevice.ts
init_modules_watch_stub();

// ../src/core/input/devices/KeyboardInputDevice.ts
init_modules_watch_stub();

// ../src/core/input/devices/MobileGamepadInputDevice.ts
init_modules_watch_stub();

// ../src/core/input/devices/NullInputDevice.ts
init_modules_watch_stub();

// ../src/core/input/devices/RecordedInputDevice.ts
init_modules_watch_stub();
var RecordedInputDevice = class _RecordedInputDevice {
  constructor(frames) {
    this.frameIndex = 0;
    this.frames = frames;
  }
  static {
    __name(this, "RecordedInputDevice");
  }
  static {
    this.EMPTY_FRAME = {
      down: [],
      hold: [],
      up: []
    };
  }
  getFrameIndex() {
    return this.frameIndex;
  }
  isConnected() {
    return true;
  }
  listen() {
  }
  unlisten() {
  }
  update() {
    this.frameIndex += 1;
  }
  // Mirrors live devices resetting held state when entering gameplay. Also
  // rewinds playback to the start, so a replay lines up with the original
  // recording as long as both call reset() at the same point in the pipeline
  // (level start, before the first tick).
  reset() {
    this.frameIndex = 0;
  }
  getDownCodes() {
    return this.getCurrentFrame().down;
  }
  getHoldCodes() {
    return this.getCurrentFrame().hold;
  }
  getUpCodes() {
    return this.getCurrentFrame().up;
  }
  getCurrentFrame() {
    const frame = this.frames[this.frameIndex - 1];
    return frame ?? _RecordedInputDevice.EMPTY_FRAME;
  }
};

// ../src/core/input/DeviceInputFrame.ts
init_modules_watch_stub();

// ../src/core/input/InputBinding.ts
init_modules_watch_stub();

// ../src/core/input/InputDevice.ts
init_modules_watch_stub();

// ../src/core/input/InputMethod.ts
init_modules_watch_stub();

// ../src/core/loaders/index.ts
init_modules_watch_stub();

// ../src/core/loaders/AudioLoader.ts
init_modules_watch_stub();

// ../src/core/Sound.ts
init_modules_watch_stub();

// ../src/core/loaders/RectFontLoader.ts
init_modules_watch_stub();

// ../src/core/loaders/ImageLoader.ts
init_modules_watch_stub();

// ../src/core/loaders/SpriteFontLoader.ts
init_modules_watch_stub();

// ../src/core/loaders/SpriteLoader.ts
init_modules_watch_stub();

// ../src/core/painters/index.ts
init_modules_watch_stub();

// ../src/core/painters/LinePainter.ts
init_modules_watch_stub();

// ../src/core/Painter.ts
init_modules_watch_stub();
var Painter = class {
  static {
    __name(this, "Painter");
  }
};

// ../src/core/painters/LinePainter.ts
var LinePainter = class extends Painter {
  constructor() {
    super(...arguments);
    this.positions = [];
    this.strokeColor = "#000";
  }
  static {
    __name(this, "LinePainter");
  }
  paint(context, renderObject) {
    if (this.positions.length === 0) {
      return;
    }
    const { min } = renderObject.getWorldBoundingBox();
    const worldPositions = this.positions.map((position) => {
      return position.clone().add(min);
    });
    context.strokePath(worldPositions, this.strokeColor);
  }
};

// ../src/core/painters/RectPainter.ts
init_modules_watch_stub();
var RectPainter = class extends Painter {
  constructor(fillColor = null, strokeColor = null) {
    super();
    this.fillColor = null;
    this.strokeColor = null;
    this.lineWidth = 1;
    this.fillColor = fillColor;
    this.strokeColor = strokeColor;
  }
  static {
    __name(this, "RectPainter");
  }
  paint(context, renderObject) {
    const box = renderObject.getWorldBoundingBox();
    const rect = box.toRect();
    const x = rect.x + this.lineWidth;
    const y = rect.y + this.lineWidth;
    const width = rect.width - this.lineWidth * 2;
    const height = rect.height - this.lineWidth * 2;
    if (this.fillColor !== null) {
      context.fillRect(rect.x, rect.y, rect.width, rect.height, this.fillColor);
    }
    if (this.strokeColor !== null) {
      context.strokeRect(x, y, width, height, this.strokeColor, this.lineWidth);
    }
  }
};

// ../src/core/painters/SpritePainter.ts
init_modules_watch_stub();

// ../src/core/SpriteAlignment.ts
init_modules_watch_stub();

// ../src/core/painters/SpritePainter.ts
var SpritePainter = class extends Painter {
  constructor(sprite = null, alignment = 3 /* MiddleCenter */) {
    super();
    this.sprite = null;
    this.opacity = 1;
    // White-tint amount [0..1] for a hit flash. 0 = draw unmodified. Render-only;
    // set by the owning object (e.g. Tank on hit) and never read by the sim.
    this.flash = 0;
    this.tintColor = null;
    this.tintAlpha = 0;
    this.objectRect = new Rect();
    this.destinationRect = new Rect();
    this.sprite = sprite;
    this.alignment = alignment;
  }
  static {
    __name(this, "SpritePainter");
  }
  paint(context, renderObject) {
    if (this.sprite === null) {
      return;
    }
    if (!this.sprite.isImageLoaded()) {
      return;
    }
    const box = renderObject.getWorldBoundingBox();
    const objectRect = this.objectRect;
    objectRect.x = box.min.x;
    objectRect.y = box.min.y;
    objectRect.width = box.max.x - box.min.x;
    objectRect.height = box.max.y - box.min.y;
    let destinationRect = objectRect;
    if (this.alignment === 0 /* Stretch */) {
      destinationRect = objectRect;
    } else if (this.alignment === 1 /* AspectFit */) {
      const sourceWidth = this.sprite.destinationRect.width || this.sprite.sourceRect.width;
      const sourceHeight = this.sprite.destinationRect.height || this.sprite.sourceRect.height;
      const scale = Math.min(
        objectRect.width / sourceWidth,
        objectRect.height / sourceHeight
      );
      destinationRect = this.destinationRect;
      destinationRect.width = sourceWidth * scale;
      destinationRect.height = sourceHeight * scale;
      destinationRect.x = objectRect.x + objectRect.width / 2 - destinationRect.width / 2;
      destinationRect.y = objectRect.y + objectRect.height / 2 - destinationRect.height / 2;
    } else if (this.alignment === 2 /* AspectCover */) {
      const sourceWidth = this.sprite.destinationRect.width || this.sprite.sourceRect.width;
      const sourceHeight = this.sprite.destinationRect.height || this.sprite.sourceRect.height;
      const scale = Math.max(
        objectRect.width / sourceWidth,
        objectRect.height / sourceHeight
      );
      destinationRect = this.destinationRect;
      destinationRect.width = sourceWidth * scale;
      destinationRect.height = sourceHeight * scale;
      destinationRect.x = objectRect.x + objectRect.width / 2 - destinationRect.width / 2;
      destinationRect.y = objectRect.y + objectRect.height / 2 - destinationRect.height / 2;
    } else if (this.alignment === 5 /* TopLeft */) {
      destinationRect = this.destinationRect;
      destinationRect.x = objectRect.x;
      destinationRect.y = objectRect.y;
      destinationRect.width = this.sprite.destinationRect.width;
      destinationRect.height = this.sprite.destinationRect.height;
    } else if (this.alignment === 3 /* MiddleCenter */) {
      destinationRect = this.destinationRect;
      destinationRect.x = objectRect.x + objectRect.width / 2 - this.sprite.destinationRect.width / 2;
      destinationRect.y = objectRect.y + objectRect.height / 2 - this.sprite.destinationRect.height / 2;
      destinationRect.width = this.sprite.destinationRect.width;
      destinationRect.height = this.sprite.destinationRect.height;
    }
    const tmpGlobalAlpha = context.getGlobalAlpha();
    if (this.opacity !== 1) {
      context.setGlobalAlpha(this.opacity);
    }
    context.drawImage(
      this.sprite.image,
      this.sprite.sourceRect,
      destinationRect,
      this.flash,
      this.tintColor,
      this.tintAlpha
    );
    if (this.opacity !== 1) {
      context.setGlobalAlpha(tmpGlobalAlpha);
    }
  }
};

// ../src/core/painters/SpriteTextPainter.ts
init_modules_watch_stub();

// ../src/core/particles/index.ts
init_modules_watch_stub();

// ../src/core/particles/ParticleSystem.ts
init_modules_watch_stub();
var ParticleSystem = class _ParticleSystem {
  constructor(width, height, capacity = 2e3) {
    this.count = 0;
    // View transform mapping field-local coords to overlay (screen) pixels:
    // screen = local * scale + offset. Set each frame from the level camera.
    this.viewScale = 1;
    this.viewOffsetX = 0;
    this.viewOffsetY = 0;
    this.cullScale = 1;
    this.cullOffsetX = 0;
    this.cullOffsetY = 0;
    // Full-screen white flash [0..1], decays over FLASH_FADE_SECONDS. Impact pop.
    this.flashAlpha = 0;
    this.capacity = capacity;
    this.logicalWidth = width;
    this.logicalHeight = height;
    this.canvas = document.createElement("canvas");
    this.canvas.width = width;
    this.canvas.height = height;
    this.canvas.className = "particle-overlay";
    this.context = this.canvas.getContext("2d");
    this.posX = new Float32Array(capacity);
    this.posY = new Float32Array(capacity);
    this.velX = new Float32Array(capacity);
    this.velY = new Float32Array(capacity);
    this.life = new Float32Array(capacity);
    this.maxLife = new Float32Array(capacity);
    this.size = new Float32Array(capacity);
    this.gravity = new Float32Array(capacity);
    this.drag = new Float32Array(capacity);
    this.shrink = new Uint8Array(capacity);
    this.color = new Array(capacity).fill("rgb(255,255,255)");
  }
  static {
    __name(this, "ParticleSystem");
  }
  static {
    this.FLASH_FADE_SECONDS = 0.14;
  }
  getDomElement() {
    return this.canvas;
  }
  resizeBackingStore(width, height) {
    const backingWidth = Math.max(1, Math.round(width));
    const backingHeight = Math.max(1, Math.round(height));
    if (this.canvas.width === backingWidth && this.canvas.height === backingHeight) {
      return;
    }
    this.canvas.width = backingWidth;
    this.canvas.height = backingHeight;
    this.context.setTransform(
      backingWidth / this.logicalWidth,
      0,
      0,
      backingHeight / this.logicalHeight,
      0,
      0
    );
    this.context.imageSmoothingEnabled = false;
  }
  getCount() {
    return this.count;
  }
  // screen = local * scale + offset. The level scene feeds this from the
  // camera (zoom + field position) so particles stay anchored to the world.
  setView(scale, offsetX, offsetY) {
    this.viewScale = scale;
    this.viewOffsetX = offsetX;
    this.viewOffsetY = offsetY;
  }
  setCullView(scale, offsetX, offsetY) {
    this.cullScale = scale;
    this.cullOffsetX = offsetX;
    this.cullOffsetY = offsetY;
  }
  // Trigger a full-screen white flash (impact punch). Takes the strongest of
  // any overlapping requests. alpha in [0..1].
  flash(alpha) {
    const clamped = alpha > 1 ? 1 : alpha;
    if (clamped > this.flashAlpha) {
      this.flashAlpha = clamped;
    }
  }
  spawn(options) {
    if (this.count >= this.capacity) {
      return;
    }
    const index = this.count;
    this.count += 1;
    this.posX[index] = options.x;
    this.posY[index] = options.y;
    this.velX[index] = options.vx;
    this.velY[index] = options.vy;
    this.life[index] = options.life;
    this.maxLife[index] = options.life;
    this.size[index] = options.size;
    this.gravity[index] = options.gravity ?? 0;
    this.drag[index] = options.drag ?? 0;
    this.shrink[index] = options.shrink ? 1 : 0;
    this.color[index] = options.color;
  }
  update(deltaTime) {
    if (this.flashAlpha > 0) {
      this.flashAlpha = Math.max(
        0,
        this.flashAlpha - deltaTime / _ParticleSystem.FLASH_FADE_SECONDS
      );
    }
    let index = 0;
    while (index < this.count) {
      const nextLife = this.life[index] - deltaTime;
      if (nextLife <= 0) {
        this.swapRemove(index);
        continue;
      }
      this.life[index] = nextLife;
      let vx = this.velX[index];
      let vy = this.velY[index] + this.gravity[index] * deltaTime;
      const damping = 1 - this.drag[index] * deltaTime;
      if (damping < 1) {
        vx *= damping;
        vy *= damping;
      }
      this.velX[index] = vx;
      this.velY[index] = vy;
      this.posX[index] += vx * deltaTime;
      this.posY[index] += vy * deltaTime;
      index += 1;
    }
  }
  render() {
    const context = this.context;
    context.clearRect(0, 0, this.logicalWidth, this.logicalHeight);
    if (this.count === 0 && this.flashAlpha <= 0) {
      return;
    }
    const scale = this.viewScale;
    const cullScale = this.cullScale;
    const canvasWidth = this.logicalWidth;
    const canvasHeight = this.logicalHeight;
    for (let index = 0; index < this.count; index += 1) {
      const alpha = this.life[index] / this.maxLife[index];
      const cullX = this.posX[index] * cullScale + this.cullOffsetX;
      const cullY = this.posY[index] * cullScale + this.cullOffsetY;
      const cullSize = this.size[index] * cullScale;
      if (cullX + cullSize < 0 || cullX - cullSize > canvasWidth || cullY + cullSize < 0 || cullY - cullSize > canvasHeight) {
        continue;
      }
      const screenX = this.posX[index] * scale + this.viewOffsetX;
      const screenY = this.posY[index] * scale + this.viewOffsetY;
      let drawSize = this.size[index] * scale;
      if (this.shrink[index] === 1) {
        drawSize *= alpha;
      }
      if (drawSize < 1) {
        drawSize = 1;
      }
      context.globalAlpha = alpha < 0 ? 0 : alpha;
      context.fillStyle = this.color[index];
      context.fillRect(
        screenX - drawSize / 2,
        screenY - drawSize / 2,
        drawSize,
        drawSize
      );
    }
    context.globalAlpha = 1;
    if (this.flashAlpha > 0) {
      context.globalAlpha = this.flashAlpha;
      context.fillStyle = "rgb(255,255,255)";
      context.fillRect(0, 0, this.logicalWidth, this.logicalHeight);
      context.globalAlpha = 1;
    }
  }
  clear() {
    this.count = 0;
    this.flashAlpha = 0;
    this.context.clearRect(0, 0, this.logicalWidth, this.logicalHeight);
  }
  swapRemove(index) {
    const last = this.count - 1;
    if (index !== last) {
      this.posX[index] = this.posX[last];
      this.posY[index] = this.posY[last];
      this.velX[index] = this.velX[last];
      this.velY[index] = this.velY[last];
      this.life[index] = this.life[last];
      this.maxLife[index] = this.maxLife[last];
      this.size[index] = this.size[last];
      this.gravity[index] = this.gravity[last];
      this.drag[index] = this.drag[last];
      this.shrink[index] = this.shrink[last];
      this.color[index] = this.color[last];
    }
    this.count = last;
  }
};

// ../src/core/scene/index.ts
init_modules_watch_stub();

// ../src/core/scene/Scene.ts
init_modules_watch_stub();

// ../src/core/scene/SceneNavigator.ts
init_modules_watch_stub();

// ../src/core/scene/SceneRouter.ts
init_modules_watch_stub();

// ../src/core/scene/SceneType.ts
init_modules_watch_stub();

// ../src/core/storage/index.ts
init_modules_watch_stub();

// ../src/core/storage/Storage.ts
init_modules_watch_stub();

// ../src/core/storage/LocalStorage.ts
init_modules_watch_stub();

// ../src/core/Animation.ts
init_modules_watch_stub();
var DEFAULT_OPTIONS2 = {
  delay: 0,
  // requestAnimationFrame is usually 60 fps
  fps: 60,
  loop: false
};
var Animation = class {
  constructor(frames = [], options = {}) {
    this.time = 0;
    this.frames = frames;
    this.options = Object.assign({}, DEFAULT_OPTIONS2, options);
    this.frameIndex = 0;
    this.loopIndex = 0;
  }
  static {
    __name(this, "Animation");
  }
  getCurrentFrame() {
    return this.frames[this.frameIndex];
  }
  getCurrentFrameIndex() {
    return this.frameIndex;
  }
  isComplete() {
    if (this.isLoopInfinite()) {
      return false;
    }
    if (this.isLoopFinite() && !this.isLastLoop()) {
      return false;
    }
    if (!this.isLastFrame()) {
      return false;
    }
    const isComplete = this.isCurrentFrameComplete();
    return isComplete;
  }
  /**
   * @param {number} deltaTime time passed since last frame in seconds
   */
  update(deltaTime) {
    if (this.time === 0) {
      this.time = deltaTime;
      return;
    }
    if (this.isLoopDisabled() && this.isLastFrame() && this.isCurrentFrameComplete()) {
      return;
    }
    if (this.isLoopFinite() && this.isLastLoop() && this.isLastFrame() && this.isCurrentFrameComplete()) {
      return;
    }
    if (!this.isCurrentFrameComplete()) {
      this.time += deltaTime;
      return;
    }
    this.frameIndex += 1;
    if (this.frameIndex > this.frames.length - 1) {
      this.frameIndex = 0;
      this.loopIndex += 1;
    }
    this.time += deltaTime;
  }
  reset() {
    this.frameIndex = 0;
    this.loopIndex = 0;
    this.time = 0;
    return this;
  }
  resetWithFrames(frames) {
    this.reset();
    this.frames = frames;
    return this;
  }
  isCurrentFrameComplete() {
    const minFrameTime = 1 / this.options.fps;
    const singleFrameTime = minFrameTime + this.options.delay;
    const passedFramesTime = (this.loopIndex * this.frames.length + this.frameIndex + 1) * singleFrameTime - minFrameTime;
    const isComplete = this.time > passedFramesTime;
    return isComplete;
  }
  isLastFrame() {
    return this.frameIndex === this.frames.length - 1;
  }
  isLoopInfinite() {
    return this.options.loop === true;
  }
  isLoopFinite() {
    return typeof this.options.loop === "number";
  }
  isLoopDisabled() {
    return this.options.loop === false;
  }
  isLastLoop() {
    return this.loopIndex + 1 === this.options.loop;
  }
};

// ../src/core/Deferred.ts
init_modules_watch_stub();

// ../src/core/GameLoop.ts
init_modules_watch_stub();

// ../src/core/GameRenderer.ts
init_modules_watch_stub();

// ../src/core/render/index.ts
init_modules_watch_stub();

// ../src/core/render/contexts/index.ts
init_modules_watch_stub();

// ../src/core/render/contexts/CanvasRenderContext.ts
init_modules_watch_stub();

// ../src/core/text/CanvasText.ts
init_modules_watch_stub();
function measureTrackedTextWidth(context, text, letterSpacing) {
  const glyphs = Array.from(text);
  const glyphWidth = glyphs.reduce((width, glyph) => {
    return width + context.measureText(glyph).width;
  }, 0);
  return glyphWidth + Math.max(0, glyphs.length - 1) * letterSpacing;
}
__name(measureTrackedTextWidth, "measureTrackedTextWidth");

// ../src/core/render/RenderContext.ts
init_modules_watch_stub();

// ../src/core/render/contexts/WebglRenderContext.ts
init_modules_watch_stub();

// ../src/core/Matrix4.ts
init_modules_watch_stub();

// ../src/core/render/contexts/WebglRenderContext.ts
var FLOATS_PER_VERTEX = 13;
var VERTICES_PER_QUAD = 6;
var FLOATS_PER_QUAD = FLOATS_PER_VERTEX * VERTICES_PER_QUAD;

// ../src/core/Timer.ts
init_modules_watch_stub();
var Timer = class {
  constructor(timeLeft = null) {
    this.done = new Subject();
    this.timeLeft = null;
    this.timeLeft = timeLeft;
  }
  static {
    __name(this, "Timer");
  }
  reset(timeLeft) {
    this.timeLeft = timeLeft;
    return this;
  }
  stop() {
    this.timeLeft = null;
    return this;
  }
  update(deltaTime) {
    if (!this.isActive()) {
      return;
    }
    this.timeLeft -= deltaTime;
    if (this.timeLeft < 0) {
      this.timeLeft = null;
      this.done.notify(null);
    }
  }
  isActive() {
    return this.timeLeft !== null;
  }
  isDone() {
    return this.timeLeft === null;
  }
};

// ../src/points/index.ts
init_modules_watch_stub();

// ../src/points/PointsHighscoreManager.ts
init_modules_watch_stub();

// ../src/network/api.ts
init_modules_watch_stub();
function getApiBaseUrl() {
  const configuredBaseUrl = String(
    process.env.BATTLECITY_API_BASE_URL || ""
  ).trim();
  if (configuredBaseUrl !== "") {
    return configuredBaseUrl;
  }
  if (typeof window === "undefined") {
    return "http://127.0.0.1:3001";
  }
  return window.location.origin;
}
__name(getApiBaseUrl, "getApiBaseUrl");
function getApiUrl(path) {
  return new URL(path, getApiBaseUrl()).toString();
}
__name(getApiUrl, "getApiUrl");
function apiFetch(path, init = {}) {
  return fetch(getApiUrl(path), {
    credentials: "include",
    ...init
  });
}
__name(apiFetch, "apiFetch");

// ../src/config.ts
init_modules_watch_stub();
var IS_DEV = true;
var IS_PROD = false;
var TILE_SIZE_SMALL = 16;
var TILE_SIZE_MEDIUM = 32;
var TILE_SIZE_LARGE = 64;
var LEGACY_FIELD_TILE_COUNT = 13;
var FIELD_TILE_COUNT = 20;
var VIEWPORT_FIELD_TILE_COUNT = 16;
var LEGACY_FIELD_SIZE = LEGACY_FIELD_TILE_COUNT * TILE_SIZE_LARGE;
var FIELD_SIZE = FIELD_TILE_COUNT * TILE_SIZE_LARGE;
var VIEWPORT_FIELD_SIZE = VIEWPORT_FIELD_TILE_COUNT * TILE_SIZE_LARGE;
var FIELD_CONTENT_OFFSET_X = 0;
var FIELD_CONTENT_OFFSET_Y = FIELD_SIZE - LEGACY_FIELD_SIZE;
var BORDER_LEFT_WIDTH = 32;
var BORDER_RIGHT_WIDTH = 32;
var BORDER_TOP_BOTTOM_HEIGHT = 32;
var LEVEL_INFO_HEIGHT = 64;
var LEVEL_PLAY_TOP_OFFSET = LEVEL_INFO_HEIGHT;
var BORDER_RECTS = [
  // Top
  {
    x: 0,
    y: 0,
    width: BORDER_LEFT_WIDTH + FIELD_SIZE + BORDER_RIGHT_WIDTH,
    height: BORDER_TOP_BOTTOM_HEIGHT
  },
  // Bottom
  {
    x: 0,
    y: FIELD_SIZE + BORDER_TOP_BOTTOM_HEIGHT,
    width: BORDER_LEFT_WIDTH + FIELD_SIZE + BORDER_RIGHT_WIDTH,
    height: BORDER_TOP_BOTTOM_HEIGHT
  },
  // Left
  {
    x: 0,
    y: 0,
    width: BORDER_LEFT_WIDTH,
    height: FIELD_SIZE + BORDER_TOP_BOTTOM_HEIGHT * 2
  },
  // Right
  {
    x: BORDER_LEFT_WIDTH + FIELD_SIZE,
    y: 0,
    width: BORDER_RIGHT_WIDTH,
    height: FIELD_SIZE + BORDER_TOP_BOTTOM_HEIGHT * 2
  }
];
function getFieldPixelSize(tileCount) {
  return tileCount * TILE_SIZE_LARGE;
}
__name(getFieldPixelSize, "getFieldPixelSize");
var BASE_CANVAS_WIDTH = VIEWPORT_FIELD_SIZE + BORDER_LEFT_WIDTH + BORDER_RIGHT_WIDTH;
var BASE_CANVAS_HEIGHT = LEVEL_PLAY_TOP_OFFSET + VIEWPORT_FIELD_SIZE + BORDER_TOP_BOTTOM_HEIGHT * 2;
var MOBILE_UI_CANVAS_WIDTH = 1288;
var DESKTOP_UI_CANVAS_MIN_WIDTH = 1288;
function getViewportSize() {
  if (typeof window === "undefined") {
    return {
      width: BASE_CANVAS_WIDTH,
      height: BASE_CANVAS_HEIGHT
    };
  }
  const mobileTouchLayout = isMobileTouchViewport();
  return {
    width: Math.max(window.innerWidth, 1),
    height: Math.max(
      mobileTouchLayout ? Math.floor(window.innerHeight * 0.6) : window.innerHeight,
      1
    )
  };
}
__name(getViewportSize, "getViewportSize");
function isMobileTouchViewport() {
  return typeof window !== "undefined" && typeof window.matchMedia === "function" && window.matchMedia("(pointer: coarse) and (max-width: 900px)").matches;
}
__name(isMobileTouchViewport, "isMobileTouchViewport");
function getResponsiveCanvasSize() {
  const viewportSize = getViewportSize();
  const viewportAspectRatio = viewportSize.width / viewportSize.height;
  if (isMobileTouchViewport()) {
    return {
      width: MOBILE_UI_CANVAS_WIDTH,
      height: Math.ceil(MOBILE_UI_CANVAS_WIDTH / viewportAspectRatio)
    };
  }
  const desktopCanvasWidth = Math.max(
    DESKTOP_UI_CANVAS_MIN_WIDTH,
    Math.ceil(BASE_CANVAS_HEIGHT * viewportAspectRatio)
  );
  return {
    width: desktopCanvasWidth,
    height: Math.ceil(desktopCanvasWidth / viewportAspectRatio)
  };
}
__name(getResponsiveCanvasSize, "getResponsiveCanvasSize");
var RESPONSIVE_CANVAS_SIZE = getResponsiveCanvasSize();
var CANVAS_WIDTH = RESPONSIVE_CANVAS_SIZE.width;
var CANVAS_HEIGHT = RESPONSIVE_CANVAS_SIZE.height;
var TARGET_TILES_WIDE = 34.5;
var ZOOM_MIN = 0.5;
var ZOOM_MAX = 6;
function getResponsiveZoom(targetTilesWide = TARGET_TILES_WIDE) {
  const playWidth = CANVAS_WIDTH - BORDER_LEFT_WIDTH - BORDER_RIGHT_WIDTH;
  const zoom = playWidth / (targetTilesWide * TILE_SIZE_MEDIUM);
  return Math.max(ZOOM_MIN, Math.min(ZOOM_MAX, zoom));
}
__name(getResponsiveZoom, "getResponsiveZoom");
var GAMEPLAY_ZOOM = getResponsiveZoom();
var SPEED_POWERUP_MULTIPLIER = 1.45;
var SPEED_POWERUP_DURATION = 10;
var CAMERA_SHAKE_INTENSITY = 1;
var PARTICLE_INTENSITY = 1;
var HIT_STOP_KILL = 0.045;
var HIT_STOP_DEATH = 0.11;
var SPRITE_FLASH_HIT = 0.85;
var SPRITE_FLASH_DECAY_SECONDS = 0.11;
var ENEMY_DROP_BLINK_INTERVAL = 0.12;
var ENEMY_DROP_BLINK_COLOR = "#5a0505";
var ENEMY_DROP_BLINK_ALPHA = 0.5;
var SHOW_EXPLOSION_SPRITE = true;
var BRICK_TILE_SIZE = TILE_SIZE_SMALL;
var BRICK_SUPER_TILE_SIZE = TILE_SIZE_MEDIUM;
var STEEL_TILE_SIZE = TILE_SIZE_MEDIUM;
var JUNGLE_TILE_SIZE = TILE_SIZE_MEDIUM;
var WATER_TILE_SIZE = TILE_SIZE_MEDIUM;
var ICE_TILE_SIZE = TILE_SIZE_MEDIUM;
var BULLET_WIDTH = 12;
var PLAYER_FIRST_SPAWN_DELAY = 0;
var PLAYER_SPAWN_DELAY = 1.5;
var ENEMY_FIRST_SPAWN_DELAY = 0.16;
var ENEMY_SPAWN_DELAY = 3;
var ENEMY_MAX_TOTAL_COUNT = 20;
var ENEMY_MAX_ALIVE_COUNT = 4;
var ENEMY_MAX_ALIVE_COUNT_MULTIPLAYER = 6;
var POWERUP_DURATION = 30;
var SHIELD_SPAWN_DURATION = 3.5;
var SHIELD_POWERUP_DURATION = 10;
var BASE_DEFENCE_POWERUP_DURATION = 17;
var FREEZE_POWERUP_DURATION = 10;
var FRIENDLY_FIRE_STUN_DURATION = 5;
var ICE_SLIDE_DURATION = 0.5;
var POINTS_POWERUP_DURATION = 0.8;
var POINTS_ENEMY_TANK_DURATION = 0.16;
var LEVEL_START_DELAY = 2;
var PLAYER_INITIAL_LIVES = 3;
var PLAYER_EXTRA_LIVE_POINTS = 2e4;
var BONUS_POINTS = 1e3;
var COLOR_GRAY = "#636363";
var COLOR_BLACK = "#000";
var COLOR_WHITE = "#fff";
var COLOR_YELLOW = "#ffae0a";
var STORAGE_KEY_SHOP_WALLET_CONNECTED = "shop.wallet-connected";
var STORAGE_KEY_SHOP_WALLET_ADDRESS = "shop.wallet-address";
var STORAGE_KEY_SHOP_TOKEN_BALANCE = "shop.token-balance";
var STORAGE_KEY_SHOP_SOL_BALANCE = "shop.sol-balance";
var STORAGE_KEY_SHOP_FUEL_BALANCE = "shop.fuel-balance";
var STORAGE_KEY_SHOP_INVENTORY = "shop.inventory";
var STORAGE_KEY_SHOP_LOADOUT = "shop.loadout";
var STORAGE_KEY_SHOP_TX_INDEX = "shop.tx-index";
var SHOP_STARTING_TOKEN_BALANCE = 1e3;
var SHOP_STARTING_SOL_BALANCE = 1.25;
var SHOP_GUEST_FUEL_BALANCE = 9999;
var SHOP_GUEST_INVENTORY_COUNT = 99;
var SHOP_RUN_FUEL_COST = 1;
var PLAYER_DEFAULT_SPAWN_POSITIONS = [
  { x: FIELD_CONTENT_OFFSET_X + 256, y: FIELD_CONTENT_OFFSET_Y + 768 },
  { x: FIELD_CONTENT_OFFSET_X + 512, y: FIELD_CONTENT_OFFSET_Y + 768 }
];
var ENEMY_DEFAULT_SPAWN_POSITIONS = [
  { x: FIELD_CONTENT_OFFSET_X + 384, y: FIELD_CONTENT_OFFSET_Y + 0 },
  { x: FIELD_CONTENT_OFFSET_X + 768, y: FIELD_CONTENT_OFFSET_Y + 0 },
  { x: FIELD_CONTENT_OFFSET_X + 0, y: FIELD_CONTENT_OFFSET_Y + 0 }
];
var BASE_DEFAULT_POSITION = {
  x: FIELD_CONTENT_OFFSET_X + 352,
  y: FIELD_CONTENT_OFFSET_Y + 736
};
var BASE_DEFAULT_SIZE = { width: 128, height: 96 };
var GROUND_FIELD_Z_INDEX = -1e3;
var WALL_SHADOW_Z_INDEX = -900;
var WALL_SHADOW_OFFSET_X = 9;
var WALL_SHADOW_OFFSET_Y = 6;
var WALL_SHADOW_STEPS = 6;
var WALL_SHADOW_COLOR = "#000000";
var WALL_SHADOW_ALPHA = 0.14;
var BRICK_TILE_Z_INDEX = 0;
var STEEL_TILE_Z_INDEX = 0;
var WATER_TILE_Z_INDEX = 0;
var ICE_TILE_Z_INDEX = 0;
var BORDER_WALL_Z_INDEX = 0;
var BASE_HEART_Z_INDEX = 0;
var ENEMY_TANK_Z_INDEX = 1;
var PLAYER_TANK_Z_INDEX = 2;
var BULLET_Z_INDEX = 3;
var SHIELD_Z_INDEX = 3;
var SMALL_EXPLOSION_Z_INDEX = 4;
var SPAWN_Z_INDEX = 4;
var JUNGLE_TILE_Z_INDEX = 5;
var LARGE_EXPLOSION_Z_INDEX = 6;
var POWERUP_Z_INDEX = 7;
var POINTS_Z_INDEX = 7;
var GAME_OVER_NOTICE_Z_INDEX = 8;
var CURTAIN_Z_INDEX = 9;
var LEVEL_TITLE_Z_INDEX = 10;
var DEBUG_GRID_Z_INDEX = 21;

// ../src/points/PointsRecord.ts
init_modules_watch_stub();

// ../src/tank/index.ts
init_modules_watch_stub();

// ../src/tank/animations/index.ts
init_modules_watch_stub();

// ../src/tank/animations/TankIdleAnimation.ts
init_modules_watch_stub();

// ../src/tank/TankAnimationFrame.ts
init_modules_watch_stub();

// ../src/tank/TankSpriteId.ts
init_modules_watch_stub();
var SPRITE_TANK_PREFIX = "tank";
var SPRITE_ID_SEPARATOR = ".";
var TankSpriteId = class {
  static {
    __name(this, "TankSpriteId");
  }
  static create(type, color, rotation, frameNumber = 1) {
    const parts = [
      SPRITE_TANK_PREFIX,
      type.party.toString(),
      color.toString(),
      type.tier.toString(),
      this.getRotationString(rotation),
      frameNumber.toString()
    ];
    const spriteId = parts.join(SPRITE_ID_SEPARATOR);
    return spriteId;
  }
  static getRotationString(rotation) {
    switch (rotation) {
      case 0 /* Up */:
        return "up";
      case 180 /* Down */:
        return "down";
      case 270 /* Left */:
        return "left";
      case 90 /* Right */:
        return "right";
      default:
        return "unknown";
    }
  }
};

// ../src/tank/TankAnimationFrame.ts
var TankAnimationFrame = class {
  static {
    __name(this, "TankAnimationFrame");
  }
  constructor(spriteLoader, type, colors, rotation, frameNumber = 1) {
    this.sprites = colors.map((color) => {
      const spriteId = TankSpriteId.create(type, color, rotation, frameNumber);
      const sprite = spriteLoader.load(spriteId);
      return sprite;
    });
  }
  getSprite(index) {
    const sprite = this.sprites[index];
    if (sprite === void 0) {
      return null;
    }
    return sprite;
  }
};

// ../src/tank/animations/TankIdleAnimation.ts
var TankIdleAnimation = class extends Animation {
  constructor(spriteLoader, type, colors, rotation) {
    super([], { delay: 0.12, loop: true });
    this.regularFrames = [];
    this.regularFrames = [
      new TankAnimationFrame(spriteLoader, type, colors, rotation, 1)
    ];
    this.updateFrames();
  }
  static {
    __name(this, "TankIdleAnimation");
  }
  // Kept for callers that refresh tank skin state after gameplay flags change.
  updateFrames() {
    this.resetWithFrames(this.regularFrames);
  }
};

// ../src/tank/animations/TankMoveAnimation.ts
init_modules_watch_stub();
var TankMoveAnimation = class extends Animation {
  constructor(spriteLoader, type, colors, rotation) {
    super([], { delay: 0.02, loop: true });
    this.regularFrames = [];
    this.regularFrames = this.createRegularFrames(
      spriteLoader,
      type,
      colors,
      rotation
    );
    this.updateFrames();
  }
  static {
    __name(this, "TankMoveAnimation");
  }
  // Kept for callers that refresh tank skin state after gameplay flags change.
  updateFrames() {
    this.resetWithFrames(this.regularFrames);
  }
  createRegularFrames(spriteLoader, type, colors, rotation) {
    const numberedMoveFrames = this.createNumberedMoveFrames(
      spriteLoader,
      type,
      colors,
      rotation
    );
    if (numberedMoveFrames.length > 1) {
      return numberedMoveFrames;
    }
    return [
      new TankAnimationFrame(spriteLoader, type, colors, rotation, 1),
      new TankAnimationFrame(spriteLoader, type, colors, rotation, 2),
      new TankAnimationFrame(spriteLoader, type, colors, rotation, 1),
      new TankAnimationFrame(spriteLoader, type, colors, rotation, 2)
    ];
  }
  createNumberedMoveFrames(spriteLoader, type, colors, rotation) {
    const frames = [];
    for (let frameNumber = 2; ; frameNumber += 1) {
      const spriteId = TankSpriteId.create(
        type,
        colors[0],
        rotation,
        frameNumber
      );
      if (!spriteLoader.has(spriteId)) {
        break;
      }
      frames.push(
        new TankAnimationFrame(
          spriteLoader,
          type,
          colors,
          rotation,
          frameNumber
        )
      );
    }
    return frames;
  }
};

// ../src/tank/behaviors/index.ts
init_modules_watch_stub();

// ../src/tank/behaviors/AiTankBehavior.ts
init_modules_watch_stub();

// ../src/tank/TankBehavior.ts
init_modules_watch_stub();
var TankBehavior = class {
  static {
    __name(this, "TankBehavior");
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  setup(tank, updateArgs) {
  }
};

// ../src/tank/behaviors/AiTankBehavior.ts
var THINK_DURATION = 0.3;
var FIRE_MIN_DELAY = 0;
var FIRE_MAX_DELAY = 1.5;
var STUCK_FIRE_CHANCE = 30;
var UNSTUCK_THINK_CHANCE = 5;
var ROTATE_TOWARDS_BASE_CHANCE = 30;
var ROTATE_UP_CHANCE = 10;
var ROTATIONS = [0 /* Up */, 180 /* Down */, 270 /* Left */, 90 /* Right */];
var AiTankBehavior = class _AiTankBehavior extends TankBehavior {
  constructor() {
    super(...arguments);
    this.state = 0 /* Moving */;
    this.lastPosition = new Vector(-1, -1);
    this.thinkTimer = new Timer();
    this.fireTimer = new Timer();
    this.log = new Logger(_AiTankBehavior.name, Logger.Level.Info);
    this.basePosition = new Vector(
      BASE_DEFAULT_POSITION.x,
      BASE_DEFAULT_POSITION.y
    );
  }
  static {
    __name(this, "AiTankBehavior");
  }
  setBasePosition(basePosition) {
    this.basePosition = basePosition.clone();
    return this;
  }
  update(tank, updateArgs) {
    this.rng = updateArgs.rng;
    const shootingDisabled = updateArgs.webRtcMatch.shouldDisableEnemyShooting();
    if (shootingDisabled && this.state === 3 /* Firing */) {
      this.state = 0 /* Moving */;
    }
    if (this.fireTimer.isDone() && !shootingDisabled) {
      const hasFired = tank.fire();
      if (hasFired) {
        this.log.debug("Fire!");
        if (this.state === 3 /* Firing */) {
          this.state = 0 /* Moving */;
        }
      } else {
        this.log.debug("Could not fire :(");
      }
      this.attemptFire();
    } else if (this.fireTimer.isDone()) {
      this.attemptFire();
    } else {
      this.fireTimer.update(updateArgs.deltaTime);
    }
    if (this.state === 3 /* Firing */) {
      return;
    }
    if (this.state === 1 /* Thinking */ || this.state === 2 /* UnstuckThinking */) {
      if (this.thinkTimer.isDone()) {
        if (!shootingDisabled && this.state === 1 /* Thinking */ && this.shouldFireWhenStuck()) {
          this.log.debug("I am done thinking. I want to fire!");
          this.state = 3 /* Firing */;
          return;
        }
        this.state = 0 /* Moving */;
        const nextRotation = this.getNextRotation(tank);
        this.log.debug("I am done thinking. Rotating %s", nextRotation);
        tank.rotate(nextRotation);
        return;
      }
      this.thinkTimer.update(updateArgs.deltaTime);
      return;
    }
    tank.move(updateArgs.deltaTime);
    const tankPosition = tank.position.clone().round();
    const isStuck = this.lastPosition.equals(tankPosition) && this.state === 0 /* Moving */;
    if (isStuck) {
      this.log.debug("I am stuck. Thinking...");
      this.state = 1 /* Thinking */;
      this.thinkTimer.reset(THINK_DURATION);
      return;
    }
    if (this.shouldThinkWhenUnstuck(tank)) {
      this.log.debug("I changed my mind all of a sudden. Thinking...");
      this.state = 2 /* UnstuckThinking */;
      this.thinkTimer.reset(THINK_DURATION);
      return;
    }
    this.lastPosition = tankPosition;
  }
  attemptFire() {
    const min = FIRE_MIN_DELAY * 1e3;
    const max = FIRE_MAX_DELAY * 1e3;
    const delay = this.rng.number(min, max) / 1e3;
    this.log.debug("I will try to fire in %f seconds", delay);
    this.fireTimer.reset(delay);
  }
  shouldThinkWhenUnstuck(tank) {
    const num = this.rng.number(1, 100);
    const hasChance = num <= UNSTUCK_THINK_CHANCE;
    const { rotation, position } = tank;
    const isTankVertical = rotation === 0 /* Up */ || rotation === 180 /* Down */;
    const isTankHorizontal = rotation === 270 /* Left */ || rotation === 90 /* Right */;
    const tileSize = TILE_SIZE_MEDIUM;
    const isTankOnTileX = isTankHorizontal && position.x % tileSize === 0;
    const isTankOnTileY = isTankVertical && position.y % tileSize === 0;
    const shouldThink = hasChance && (isTankOnTileX || isTankOnTileY);
    return shouldThink;
  }
  shouldFireWhenStuck() {
    const shouldFire = this.rng.probability(STUCK_FIRE_CHANCE);
    return shouldFire;
  }
  getNextRotation(tank) {
    const shouldRotateTowardsBase = this.rng.probability(
      ROTATE_TOWARDS_BASE_CHANCE
    );
    if (shouldRotateTowardsBase) {
      this.log.debug("I want to go towards base");
      return this.getRotationTowardsBase(tank);
    }
    const shouldRotateUp = this.rng.probability(ROTATE_UP_CHANCE);
    if (shouldRotateUp) {
      this.log.debug("I want to go up");
      return 0 /* Up */;
    }
    return this.getRandomRotationExcept(0 /* Up */);
  }
  getRandomRotation() {
    return this.rng.arrayElement(ROTATIONS);
  }
  getRandomRotationExcept(prevRotation) {
    const rotations = ROTATIONS.slice();
    const prevIndex = rotations.indexOf(prevRotation);
    rotations.splice(prevIndex, 1);
    return this.rng.arrayElement(rotations);
  }
  getRotationTowardsBase(tank) {
    const tankPosition = tank.position;
    const direction = this.basePosition.clone().sub(tankPosition).normalize();
    const maxValue = Math.max(direction.x, direction.y);
    if (Math.abs(direction.x) === Math.abs(maxValue)) {
      if (direction.x > 0) {
        return 90 /* Right */;
      }
      if (direction.x < 0) {
        return 270 /* Left */;
      }
    }
    return 180 /* Down */;
  }
};

// ../src/tank/behaviors/PatrolTankBehavior.ts
init_modules_watch_stub();

// ../src/game/index.ts
init_modules_watch_stub();

// ../src/game/AudioManager.ts
init_modules_watch_stub();

// ../src/game/GameUpdateArgs.ts
init_modules_watch_stub();

// ../src/game/GameStorage.ts
init_modules_watch_stub();

// ../src/game/RotationMap.ts
init_modules_watch_stub();
var RotationMap = class {
  constructor() {
    this.map = /* @__PURE__ */ new Map();
  }
  static {
    __name(this, "RotationMap");
  }
  set(rotation, value) {
    this.map.set(this.round(rotation), value);
    return this;
  }
  get(rotation) {
    return this.map.get(this.round(rotation));
  }
  forEach(callbackFn) {
    this.map.forEach(callbackFn);
    return this;
  }
  // Tries to round rotation value to one of possible map values, because
  // calculations might introduce some error.
  round(rotation) {
    return Math.round(rotation) % 360;
  }
};

// ../src/game/Tag.ts
init_modules_watch_stub();

// ../src/tank/behaviors/PatrolFireTankBehavior.ts
init_modules_watch_stub();

// ../src/tank/behaviors/PlayerTankBehavior.ts
init_modules_watch_stub();

// ../src/gameObjects/index.ts
init_modules_watch_stub();

// ../src/gameObjects/editor/index.ts
init_modules_watch_stub();

// ../src/gameObjects/editor/EditorBaseBrush.ts
init_modules_watch_stub();

// ../src/gameObjects/Base.ts
init_modules_watch_stub();

// ../src/gameObjects/BaseHeart.ts
init_modules_watch_stub();

// ../src/gameObjects/Explosion.ts
init_modules_watch_stub();

// ../src/gameObjects/explosionEffect.ts
init_modules_watch_stub();
function emitMuzzleFlash(particles, x, y, dirX, dirY) {
  const intensity = PARTICLE_INTENSITY;
  if (intensity <= 0) {
    return;
  }
  const baseAngle = Math.atan2(dirY, dirX);
  const count = Math.round(6 * intensity);
  for (let i = 0; i < count; i += 1) {
    const angle = baseAngle + (Math.random() - 0.5) * 0.7;
    const speed = 70 + Math.random() * 110;
    particles.spawn({
      x,
      y,
      vx: Math.cos(angle) * speed,
      vy: Math.sin(angle) * speed,
      life: 0.06 + Math.random() * 0.1,
      size: 2 + Math.random() * 2,
      color: Math.random() < 0.5 ? "rgb(255,250,232)" : "rgb(255,214,92)",
      drag: 3,
      shrink: true
    });
  }
}
__name(emitMuzzleFlash, "emitMuzzleFlash");
function emitExplosion(particles, x, y, options = {}) {
  const intensity = PARTICLE_INTENSITY;
  if (intensity <= 0) {
    return;
  }
  const scale = options.scale ?? 1;
  const withSmoke = options.smoke ?? true;
  const flashCount = Math.max(1, Math.round(3 * scale * intensity));
  for (let i = 0; i < flashCount; i += 1) {
    const angle = Math.random() * Math.PI * 2;
    const speed = Math.random() * 30 * scale;
    particles.spawn({
      x,
      y,
      vx: Math.cos(angle) * speed,
      vy: Math.sin(angle) * speed,
      life: 0.1 + Math.random() * 0.08,
      size: (10 + Math.random() * 8) * scale,
      color: Math.random() < 0.5 ? "rgb(255,250,232)" : "rgb(255,224,150)",
      drag: 2,
      shrink: true
    });
  }
  const fireCount = Math.round(10 * scale * intensity);
  for (let i = 0; i < fireCount; i += 1) {
    const angle = Math.random() * Math.PI * 2;
    const speed = (30 + Math.random() * 90) * scale;
    const roll = Math.random();
    const color = roll < 0.5 ? "rgb(255,150,48)" : roll < 0.85 ? "rgb(242,96,32)" : "rgb(198,58,30)";
    particles.spawn({
      x,
      y,
      vx: Math.cos(angle) * speed,
      vy: Math.sin(angle) * speed,
      life: 0.28 + Math.random() * 0.32,
      size: (4 + Math.random() * 4) * scale,
      color,
      gravity: 120,
      drag: 1.5,
      shrink: true
    });
  }
  const sparkCount = Math.round(14 * scale * intensity);
  for (let i = 0; i < sparkCount; i += 1) {
    const angle = Math.random() * Math.PI * 2;
    const speed = (70 + Math.random() * 170) * scale;
    particles.spawn({
      x,
      y,
      vx: Math.cos(angle) * speed,
      vy: Math.sin(angle) * speed,
      life: 0.3 + Math.random() * 0.4,
      size: 2 + Math.random() * 2,
      color: Math.random() < 0.5 ? "rgb(255,214,92)" : "rgb(255,168,64)",
      gravity: 260,
      drag: 1.4,
      shrink: true
    });
  }
  if (withSmoke) {
    const smokeCount = Math.round(6 * scale * intensity);
    for (let i = 0; i < smokeCount; i += 1) {
      const angle = Math.random() * Math.PI * 2;
      const speed = (10 + Math.random() * 30) * scale;
      const grey = 60 + Math.floor(Math.random() * 40);
      particles.spawn({
        x,
        y,
        vx: Math.cos(angle) * speed,
        vy: Math.sin(angle) * speed - 30 * scale,
        life: 0.6 + Math.random() * 0.5,
        size: (6 + Math.random() * 6) * scale,
        color: `rgb(${grey},${grey},${grey})`,
        drag: 1.2
      });
    }
  }
}
__name(emitExplosion, "emitExplosion");

// ../src/gameObjects/Explosion.ts
var Explosion = class extends GameObject {
  constructor() {
    super(136, 136);
    this.zIndex = LARGE_EXPLOSION_Z_INDEX;
    this.painter = new SpritePainter();
    this.completed = new Subject();
    this.painter.alignment = 3 /* MiddleCenter */;
  }
  static {
    __name(this, "Explosion");
  }
  setup({ spriteLoader, particles }) {
    this.animation = new Animation(
      spriteLoader.loadList(["explosion.large.1", "explosion.large.2"]),
      { delay: 0.066, loop: false }
    );
    this.updateMatrix();
    const center = this.getCenter();
    emitExplosion(particles, center.x, center.y, { scale: 1.3, smoke: true });
  }
  update(updateArgs) {
    if (this.animation.isComplete()) {
      this.dirtyPaintBox();
      this.removeSelf();
      this.completed.notify(null);
      return;
    }
    this.animation.update(updateArgs.deltaTime);
    this.painter.sprite = SHOW_EXPLOSION_SPRITE ? this.animation.getCurrentFrame() : null;
    this.setNeedsPaint();
  }
};

// ../src/gameObjects/BaseHeart.ts
var BaseHeart = class extends GameObject {
  constructor() {
    super(64, 64);
    this.collider = new BoxCollider(this, true);
    this.tags = ["BlockMove" /* BlockMove */];
    this.painter = new SpritePainter();
    this.zIndex = BASE_HEART_Z_INDEX;
    this.died = new Subject();
    this.isDead = false;
  }
  static {
    __name(this, "BaseHeart");
  }
  explode() {
    if (this.isDead) {
      return;
    }
    this.isDead = true;
    const explosion = new Explosion();
    explosion.updateMatrix();
    explosion.setCenter(this.getSelfCenter());
    this.add(explosion);
    this.painter.sprite = this.deadSprite;
    this.died.notify(null);
  }
  setup({ collisionSystem, spriteLoader }) {
    collisionSystem.register(this.collider);
    this.aliveSprite = spriteLoader.load("base.heart.alive");
    this.deadSprite = spriteLoader.load("base.heart.dead");
    this.painter.sprite = this.aliveSprite;
  }
  update() {
    this.collider.update();
  }
  collide(collision) {
    if (this.isDead) {
      return;
    }
    const bulletContacts = collision.contacts.filter((contact) => {
      return contact.collider.object.tags.includes("Bullet" /* Bullet */);
    });
    if (bulletContacts.length > 0) {
      const firstBulletContact = bulletContacts[0];
      const bullet = firstBulletContact.collider.object;
      if (bullet.isLocalDamageDisabled()) {
        return;
      }
      bullet.explode();
      this.explode();
    }
  }
};

// ../src/gameObjects/Base.ts
var Base = class extends GameObject {
  constructor() {
    super(BASE_DEFAULT_SIZE.width, BASE_DEFAULT_SIZE.height);
    this.died = new Subject();
    this.heart = new BaseHeart();
  }
  static {
    __name(this, "Base");
  }
  activateDefence(_duration) {
  }
  setup() {
    this.heart.position.set(32, 32);
    this.heart.died.addListener(this.died.notify);
    this.add(this.heart);
  }
};

// ../src/gameObjects/editor/EditorBorder.ts
init_modules_watch_stub();

// ../src/gameObjects/BorderWall.ts
init_modules_watch_stub();
var BorderWall = class extends GameObject {
  constructor() {
    super(...arguments);
    this.collider = new BoxCollider(this);
    this.tags = ["Wall" /* Wall */, "Border" /* Border */, "BlockMove" /* BlockMove */];
    this.zIndex = BORDER_WALL_Z_INDEX;
  }
  static {
    __name(this, "BorderWall");
  }
  setup({ collisionSystem, spriteLoader }) {
    collisionSystem.register(this.collider);
    const sprite = spriteLoader.load("terrain.border-steel");
    const tileSize = STEEL_TILE_SIZE;
    const columns = Math.ceil(this.size.width / tileSize);
    const rows = Math.ceil(this.size.height / tileSize);
    for (let row = 0; row < rows; row += 1) {
      for (let column = 0; column < columns; column += 1) {
        const tile = new GameObject(tileSize, tileSize);
        tile.position.set(column * tileSize, row * tileSize);
        tile.painter = new SpritePainter(sprite);
        this.add(tile);
      }
    }
  }
  update() {
    this.collider.update();
  }
};

// ../src/gameObjects/editor/EditorBrush.ts
init_modules_watch_stub();

// ../src/terrain/index.ts
init_modules_watch_stub();

// ../src/terrain/TerrainFactory.ts
init_modules_watch_stub();

// ../src/gameObjects/terrain/BlueBrickTerrainTile.ts
init_modules_watch_stub();

// ../src/gameObjects/terrain/BrickTerrainTile.ts
init_modules_watch_stub();

// ../src/gameObjects/TerrainTile.ts
init_modules_watch_stub();
var TerrainTile = class extends GameObject {
  constructor() {
    super(...arguments);
    this.destroyed = new Subject();
  }
  static {
    __name(this, "TerrainTile");
  }
  destroy(notify = true) {
    this.removeSelf();
    if (notify) {
      this.destroyed.notify({
        centerPosition: this.getCenter()
      });
    }
  }
};

// ../src/gameObjects/terrain/BrickTerrainTile.ts
var BrickTerrainTile = class extends TerrainTile {
  constructor() {
    super(BRICK_TILE_SIZE, BRICK_TILE_SIZE);
    this.type = "brick" /* Brick */;
    this.collider = new BoxCollider(this);
    this.zIndex = BRICK_TILE_Z_INDEX;
    this.tags = ["Wall" /* Wall */, "Brick" /* Brick */];
    this.painter = new SpritePainter();
    // Set by TerrainFactory for bricks at the bottom edge of a wall cluster so
    // they render the darker base course. baseVariant picks the skirt to match
    // the surrounding terrain: 'grass' on land, 'moss' where the wall meets water.
    this.isBase = false;
    this.baseVariant = "grass";
  }
  static {
    __name(this, "BrickTerrainTile");
  }
  destroy(notify = true) {
    super.destroy(notify);
    this.collider.unregister();
  }
  setup({ collisionSystem, spriteLoader }) {
    collisionSystem.register(this.collider);
    this.sprites = spriteLoader.loadList(this.getSpriteIds());
    this.baseSprites = spriteLoader.loadList(this.getBaseSpriteIds());
    this.mossSprites = spriteLoader.loadList(this.getMossSpriteIds());
    this.painter.sprite = this.getSpriteByPosition();
  }
  update() {
    this.collider.update();
  }
  getSpriteIds() {
    return ["terrain.brick.1", "terrain.brick.2"];
  }
  getBaseSpriteIds() {
    return ["terrain.brick.base.1", "terrain.brick.base.2"];
  }
  getMossSpriteIds() {
    return ["terrain.brick.moss.1", "terrain.brick.moss.2"];
  }
  getSpriteByPosition() {
    const horizontalIndex = Math.floor(this.position.x / BRICK_TILE_SIZE) % 2;
    const verticalIndex = Math.floor(this.position.y / BRICK_TILE_SIZE) % 2;
    const index = (horizontalIndex + verticalIndex) % 2;
    let sprites = this.sprites;
    if (this.isBase) {
      sprites = this.baseVariant === "moss" ? this.mossSprites : this.baseSprites;
    }
    return sprites[index];
  }
};

// ../src/gameObjects/terrain/BlueBrickTerrainTile.ts
var BlueBrickTerrainTile = class extends BrickTerrainTile {
  static {
    __name(this, "BlueBrickTerrainTile");
  }
  getSpriteIds() {
    return ["terrain.blue-brick.1", "terrain.blue-brick.2"];
  }
};

// ../src/gameObjects/terrain/BrickSuperTerrainTile.ts
init_modules_watch_stub();
var BrickSuperTerrainTile = class extends TerrainTile {
  constructor(subTiles) {
    super(BRICK_SUPER_TILE_SIZE, BRICK_SUPER_TILE_SIZE);
    this.type = "brick-super" /* BrickSuper */;
    this.collider = new BoxCollider(this);
    this.tags = ["BlockMove" /* BlockMove */];
    // Fires for each individual sub-brick destroyed (not just when the whole
    // super-tile clears), carrying the sub-brick's field-local center. Purely
    // cosmetic hook — the level scene uses it to spawn destruction debris so
    // every chipped brick shows particles, not only the final one in a cell.
    this.subTileDestroyed = new Subject();
    this.subTiles = subTiles;
  }
  static {
    __name(this, "BrickSuperTerrainTile");
  }
  destroy(notify = true) {
    super.destroy(notify);
    this.collider.unregister();
    for (const subTile of this.subTiles) {
      subTile.destroy(notify);
    }
  }
  setup({ collisionSystem }) {
    collisionSystem.register(this.collider);
    for (const tile of this.subTiles) {
      tile.destroyed.addListenerOnce(() => {
        const index = this.subTiles.indexOf(tile);
        if (index === -1) {
          return;
        }
        this.subTiles.splice(index, 1);
        this.subTileDestroyed.notify(
          new Vector(
            this.position.x + tile.position.x + tile.size.width / 2,
            this.position.y + tile.position.y + tile.size.height / 2
          )
        );
        if (this.subTiles.length === 0) {
          this.destroy();
        }
      });
    }
    this.add(...this.subTiles);
  }
  update() {
    this.collider.update();
  }
};

// ../src/gameObjects/terrain/IceTerrainTile.ts
init_modules_watch_stub();
var IceTerrainTile = class extends TerrainTile {
  constructor() {
    super(ICE_TILE_SIZE, ICE_TILE_SIZE);
    this.collider = new BoxCollider(this);
    this.type = "ice" /* Ice */;
    this.painter = new SpritePainter();
    this.tags = ["Ice" /* Ice */];
    this.zIndex = ICE_TILE_Z_INDEX;
  }
  static {
    __name(this, "IceTerrainTile");
  }
  setup({ collisionSystem, spriteLoader }) {
    collisionSystem.register(this.collider);
    this.painter.sprite = spriteLoader.load("terrain.ice");
  }
  update() {
    this.collider.update();
  }
};

// ../src/gameObjects/terrain/InverseBrickTerrainTile.ts
init_modules_watch_stub();
var InverseBrickTerrainTile = class extends BrickTerrainTile {
  static {
    __name(this, "InverseBrickTerrainTile");
  }
  getSpriteIds() {
    return ["terrain.inverse-brick.1", "terrain.inverse-brick.2"];
  }
};

// ../src/gameObjects/terrain/JungleTerrainTile.ts
init_modules_watch_stub();
var BOTTOM_VARIANT_COUNT = 2;
var JungleTerrainTile = class extends TerrainTile {
  constructor() {
    super(JUNGLE_TILE_SIZE, JUNGLE_TILE_SIZE);
    this.type = "jungle" /* Jungle */;
    this.painter = new SpritePainter();
    this.zIndex = JUNGLE_TILE_Z_INDEX;
    // Set by TerrainFactory: true when there is no jungle in the cell directly
    // below, so this tile sits at the bottom edge of a jungle cluster.
    this.isBottom = false;
  }
  static {
    __name(this, "JungleTerrainTile");
  }
  setup({ spriteLoader }) {
    this.sprites = spriteLoader.loadSequence("terrain.jungle");
    this.painter.sprite = this.getSpriteByPosition();
  }
  // Picks a variant per cell so the foliage doesn't visibly repeat (same hash
  // approach as the grass ground), choosing from the bottom set on the bottom
  // edge and the top set everywhere else.
  getSpriteByPosition() {
    const size = JUNGLE_TILE_SIZE;
    const col = Math.floor(this.position.x / size);
    const row = Math.floor(this.position.y / size);
    const hash = Math.abs(col * 73856093 ^ row * 19349663);
    if (this.isBottom) {
      const count = Math.min(BOTTOM_VARIANT_COUNT, this.sprites.length);
      return this.sprites[hash % count];
    }
    const topCount = this.sprites.length - BOTTOM_VARIANT_COUNT;
    if (topCount <= 0) {
      return this.sprites[hash % this.sprites.length];
    }
    return this.sprites[BOTTOM_VARIANT_COUNT + hash % topCount];
  }
};

// ../src/gameObjects/terrain/MenuBrickTerrainTile.ts
init_modules_watch_stub();
var MenuBrickTerrainTile = class extends TerrainTile {
  constructor() {
    super(BRICK_TILE_SIZE, BRICK_TILE_SIZE);
    this.type = "menu-brick" /* MenuBrick */;
    this.painter = new SpritePainter();
  }
  static {
    __name(this, "MenuBrickTerrainTile");
  }
  setup({ spriteLoader }) {
    this.sprites = spriteLoader.loadList([
      "terrain.menu-brick.1",
      "terrain.menu-brick.2"
    ]);
    this.painter.sprite = this.getSpriteByPosition();
  }
  getSpriteIds() {
    return ["terrain.brick.1", "terrain.brick.2"];
  }
  getSpriteByPosition() {
    const horizontalIndex = Math.floor(this.position.x / BRICK_TILE_SIZE) % 2;
    const verticalIndex = Math.floor(this.position.y / BRICK_TILE_SIZE) % 2;
    const index = (horizontalIndex + verticalIndex) % 2;
    const sprite = this.sprites[index];
    return sprite;
  }
};

// ../src/gameObjects/terrain/SteelTerrainTile.ts
init_modules_watch_stub();
var SteelTerrainTile = class extends TerrainTile {
  constructor() {
    super(STEEL_TILE_SIZE, STEEL_TILE_SIZE);
    this.type = "steel" /* Steel */;
    this.collider = new BoxCollider(this);
    this.zIndex = STEEL_TILE_Z_INDEX;
    this.tags = ["Wall" /* Wall */, "Steel" /* Steel */, "BlockMove" /* BlockMove */];
    this.painter = new SpritePainter();
  }
  static {
    __name(this, "SteelTerrainTile");
  }
  destroy(notify = true) {
    super.destroy(notify);
    this.collider.unregister();
  }
  setup({ collisionSystem, spriteLoader }) {
    collisionSystem.register(this.collider);
    this.painter.sprite = spriteLoader.load("terrain.steel");
  }
  update() {
    this.collider.update();
  }
};

// ../src/gameObjects/terrain/WaterTerrainTile.ts
init_modules_watch_stub();
var WaterTerrainTile = class extends TerrainTile {
  constructor() {
    super(WATER_TILE_SIZE, WATER_TILE_SIZE);
    this.type = "water" /* Water */;
    this.collider = new BoxCollider(this);
    this.zIndex = WATER_TILE_Z_INDEX;
    this.tags = ["BlockMove" /* BlockMove */];
    this.painter = new SpritePainter();
  }
  static {
    __name(this, "WaterTerrainTile");
  }
  destroy(notify = true) {
    super.destroy(notify);
    this.collider.unregister();
  }
  setup({ collisionSystem, spriteLoader }) {
    collisionSystem.register(this.collider);
    this.animation = new Animation(spriteLoader.loadSequence("terrain.water"), {
      delay: 0.333,
      loop: true
    });
  }
  update(updateArgs) {
    this.collider.update();
    this.animation.update(updateArgs.deltaTime);
    this.painter.sprite = this.animation.getCurrentFrame();
    this.setNeedsPaint();
  }
};

// ../src/terrain/TerrainType.ts
init_modules_watch_stub();
var TerrainType = /* @__PURE__ */ ((TerrainType4) => {
  TerrainType4["Brick"] = "brick";
  TerrainType4["BrickSuper"] = "brick-super";
  TerrainType4["Steel"] = "steel";
  TerrainType4["Jungle"] = "jungle";
  TerrainType4["Water"] = "water";
  TerrainType4["Ice"] = "ice";
  TerrainType4["MenuBrick"] = "menu-brick";
  TerrainType4["InverseBrick"] = "inverse-brick";
  TerrainType4["BlueBrick"] = "blue-brick";
  return TerrainType4;
})(TerrainType || {});

// ../src/terrain/TerrainFactory.ts
var TerrainFactory = class {
  static {
    __name(this, "TerrainFactory");
  }
  // Pass in regions for entire map at once so collision blocks can be
  // calculated correctly.
  static createMapFromRegionConfigs(regionConfigs, fieldWidth, fieldHeight, occupiedRects = []) {
    const rectsByType = this.mapRegionConfigsByType(regionConfigs);
    const waterCells = this.computeOccupiedCells(
      rectsByType.get("water" /* Water */) || [],
      BRICK_TILE_SIZE
    );
    const allRects = [...occupiedRects];
    rectsByType.forEach((regionRects) => allRects.push(...regionRects));
    const solidCells = this.computeOccupiedCells(
      allRects,
      TILE_SIZE_SMALL
    );
    const tiles = [];
    rectsByType.forEach((regionRects, type) => {
      const regionTiles = this.createMapFromRegions(
        type,
        regionRects,
        fieldWidth,
        fieldHeight,
        waterCells,
        solidCells
      );
      tiles.push(...regionTiles);
    });
    return tiles;
  }
  // Pass in regions for entire map at once so collision blocks can be
  // calculated correctly.
  static createMapFromRegions(type, regionRects, fieldWidth, fieldHeight, waterCells, solidCells) {
    if (type === "brick" /* Brick */) {
      return this.createMapFromBrickRegions(
        regionRects,
        fieldWidth,
        fieldHeight,
        waterCells,
        solidCells
      );
    }
    if (type === "jungle" /* Jungle */) {
      return this.createMapFromJungleRegions(regionRects, solidCells);
    }
    const tiles = [];
    regionRects.forEach((regionRect) => {
      const regionTiles = this.createFromRegion(type, regionRect);
      tiles.push(...regionTiles);
    });
    return tiles;
  }
  // NOTE: skips super brick creation, which means that movement collision
  // will be disabled. To create collisions use createMapXXX.
  static createFromRegionConfigs(regionConfigs) {
    const rectsByType = this.mapRegionConfigsByType(regionConfigs);
    const tiles = [];
    rectsByType.forEach((regionRects, type) => {
      const regionTiles = this.createFromRegions(type, regionRects);
      tiles.push(...regionTiles);
    });
    return tiles;
  }
  // NOTE: skips super brick creation, which means that movement collision
  // will be disabled. To create collisions use createMapXXX.
  static createFromRegions(type, regionRects) {
    const tiles = [];
    regionRects.forEach((regionRect) => {
      const regionTiles = this.createFromRegion(type, regionRect);
      tiles.push(...regionTiles);
    });
    return tiles;
  }
  static validateRegion(type, regionRect) {
    const { x, y, width, height } = regionRect;
    const tileSize = this.getTileSize(type);
    if (x % tileSize.width !== 0) {
      return this.createTileSizeError(type, regionRect, "x", tileSize.width);
    }
    if (y % tileSize.height !== 0) {
      return this.createTileSizeError(type, regionRect, "y", tileSize.height);
    }
    if (width % tileSize.width !== 0) {
      return this.createTileSizeError(
        type,
        regionRect,
        "width",
        tileSize.width
      );
    }
    if (height % tileSize.height !== 0) {
      return this.createTileSizeError(
        type,
        regionRect,
        "height",
        tileSize.height
      );
    }
  }
  static validateRegionConfigs(regions) {
    for (const region of regions) {
      const regionRect = new Rect(
        region.x,
        region.y,
        region.width,
        region.height
      );
      const error = this.validateRegion(region.type, regionRect);
      if (error !== void 0) {
        return error;
      }
    }
  }
  static createFromRegion(type, regionRect) {
    const { x, y, width, height } = regionRect;
    const tileSize = this.getTileSize(type);
    const tiles = [];
    for (let i = x; i < x + width; i += tileSize.width) {
      for (let j = y; j < y + height; j += tileSize.height) {
        const tile = this.createTile(type);
        tile.position.set(i, j);
        tiles.push(tile);
      }
    }
    return tiles;
  }
  static createMapFromBrickRegions(regionRects, fieldWidth, fieldHeight, waterCells = /* @__PURE__ */ new Set(), solidCells) {
    const superTileSize = BRICK_SUPER_TILE_SIZE;
    const fieldBounds = this.getFieldBounds(
      regionRects,
      fieldWidth,
      fieldHeight
    );
    const superTileCols = Math.ceil(fieldBounds.width / superTileSize);
    const superTileRows = Math.ceil(fieldBounds.height / superTileSize);
    const superGrid = [];
    for (let rowIndex = 0; rowIndex < superTileRows; rowIndex += 1) {
      superGrid[rowIndex] = [];
      for (let colIndex = 0; colIndex < superTileCols; colIndex += 1) {
        superGrid[rowIndex][colIndex] = [];
      }
    }
    const subTileSize = BRICK_TILE_SIZE;
    const subTileCols = Math.ceil(fieldBounds.width / subTileSize);
    const subTileRows = Math.ceil(fieldBounds.height / subTileSize);
    const ratio = superTileSize / subTileSize;
    const occupied = /* @__PURE__ */ new Set();
    const cellKey = /* @__PURE__ */ __name((col, row) => `${col},${row}`, "cellKey");
    for (const regionRect of regionRects) {
      const minCol = Math.max(0, Math.floor(regionRect.x / subTileSize));
      const minRow = Math.max(0, Math.floor(regionRect.y / subTileSize));
      const maxCol = Math.min(
        subTileCols,
        Math.ceil((regionRect.x + regionRect.width) / subTileSize)
      );
      const maxRow = Math.min(
        subTileRows,
        Math.ceil((regionRect.y + regionRect.height) / subTileSize)
      );
      for (let row = minRow; row < maxRow; row += 1) {
        for (let col = minCol; col < maxCol; col += 1) {
          occupied.add(cellKey(col, row));
        }
      }
    }
    for (const regionRect of regionRects) {
      const minIndex = new Vector(
        Math.max(0, Math.floor(regionRect.x / subTileSize)),
        Math.max(0, Math.floor(regionRect.y / subTileSize))
      );
      const maxIndex = new Vector(
        Math.min(
          subTileCols,
          Math.ceil((regionRect.x + regionRect.width) / subTileSize)
        ),
        Math.min(
          subTileRows,
          Math.ceil((regionRect.y + regionRect.height) / subTileSize)
        )
      );
      for (let rowIndex = minIndex.y; rowIndex < maxIndex.y; rowIndex += 1) {
        for (let colIndex = minIndex.x; colIndex < maxIndex.x; colIndex += 1) {
          const superRowIndex = Math.floor(rowIndex / ratio);
          const superColIndex = Math.floor(colIndex / ratio);
          const localRowIndex = rowIndex % ratio;
          const localColIndex = colIndex % ratio;
          const x = localColIndex * subTileSize;
          const y = localRowIndex * subTileSize;
          const subTile = this.createTile(
            "brick" /* Brick */
          );
          subTile.position.set(x, y);
          const belowCells = solidCells ?? occupied;
          subTile.isBase = !belowCells.has(cellKey(colIndex, rowIndex + 1));
          if (subTile.isBase) {
            const touchesWater = waterCells.has(cellKey(colIndex, rowIndex + 1)) || waterCells.has(cellKey(colIndex - 1, rowIndex)) || waterCells.has(cellKey(colIndex + 1, rowIndex)) || waterCells.has(cellKey(colIndex, rowIndex - 1));
            subTile.baseVariant = touchesWater ? "moss" : "grass";
          }
          superGrid[superRowIndex][superColIndex].push(subTile);
        }
      }
    }
    const superTiles = [];
    for (let rowIndex = 0; rowIndex < superTileRows; rowIndex += 1) {
      for (let colIndex = 0; colIndex < superTileCols; colIndex += 1) {
        const subTiles = superGrid[rowIndex][colIndex];
        if (subTiles.length === 0) {
          continue;
        }
        const x = colIndex * superTileSize;
        const y = rowIndex * superTileSize;
        const superTile = new BrickSuperTerrainTile(subTiles);
        superTile.position.set(x, y);
        superTiles.push(superTile);
      }
    }
    return superTiles;
  }
  // Builds jungle tiles, flagging each tile that has nothing directly below it
  // as a bottom-edge tile (so it renders the bottom foliage variant). If any
  // element sits below — another jungle tile, a wall, etc. — it uses the top
  // variant. solidCells covers all terrain at the finest grid; falls back to
  // jungle-only occupancy when not provided (e.g. an editor preview).
  static createMapFromJungleRegions(regionRects, solidCells) {
    const tileSize = JUNGLE_TILE_SIZE;
    const belowCells = solidCells ?? this.computeOccupiedCells(regionRects, TILE_SIZE_SMALL);
    const tiles = [];
    for (const regionRect of regionRects) {
      const { x, y, width, height } = regionRect;
      for (let i = x; i < x + width; i += tileSize) {
        for (let j = y; j < y + height; j += tileSize) {
          const tile = this.createTile("jungle" /* Jungle */);
          tile.position.set(i, j);
          tile.isBottom = !this.hasSolidBelow(i, j, tileSize, belowCells);
          tiles.push(tile);
        }
      }
    }
    return tiles;
  }
  // True if any cell in the finest-grid row directly below a tile of the given
  // size (spanning its full width) is occupied.
  static hasSolidBelow(x, y, size, cells) {
    const small = TILE_SIZE_SMALL;
    const belowRow = Math.floor((y + size) / small);
    const startCol = Math.floor(x / small);
    const endCol = Math.floor((x + size - 1) / small);
    for (let col = startCol; col <= endCol; col += 1) {
      if (cells.has(`${col},${belowRow}`)) {
        return true;
      }
    }
    return false;
  }
  static computeOccupiedCells(regionRects, cellSize) {
    const cells = /* @__PURE__ */ new Set();
    for (const regionRect of regionRects) {
      const minCol = Math.floor(regionRect.x / cellSize);
      const minRow = Math.floor(regionRect.y / cellSize);
      const maxCol = Math.ceil((regionRect.x + regionRect.width) / cellSize);
      const maxRow = Math.ceil((regionRect.y + regionRect.height) / cellSize);
      for (let row = minRow; row < maxRow; row += 1) {
        for (let col = minCol; col < maxCol; col += 1) {
          cells.add(`${col},${row}`);
        }
      }
    }
    return cells;
  }
  static getFieldBounds(regionRects, fieldWidth, fieldHeight) {
    if (fieldWidth !== void 0 && fieldHeight !== void 0) {
      return new Rect(0, 0, fieldWidth, fieldHeight);
    }
    if (regionRects.length === 0) {
      return new Rect(0, 0, 0, 0);
    }
    let maxWidth = 0;
    let maxHeight = 0;
    regionRects.forEach((regionRect) => {
      maxWidth = Math.max(maxWidth, regionRect.x + regionRect.width);
      maxHeight = Math.max(maxHeight, regionRect.y + regionRect.height);
    });
    return new Rect(0, 0, maxWidth, maxHeight);
  }
  static mapRegionConfigsByType(regionConfigs) {
    const rectsByType = /* @__PURE__ */ new Map();
    for (const regionConfig of regionConfigs) {
      const { type } = regionConfig;
      const regionRect = new Rect(
        regionConfig.x,
        regionConfig.y,
        regionConfig.width,
        regionConfig.height
      );
      const regionRects = rectsByType.get(type) || [];
      regionRects.push(regionRect);
      rectsByType.set(type, regionRects);
    }
    return rectsByType;
  }
  static createTile(type) {
    switch (type) {
      case "brick" /* Brick */:
        return new BrickTerrainTile();
      case "steel" /* Steel */:
        return new SteelTerrainTile();
      case "jungle" /* Jungle */:
        return new JungleTerrainTile();
      case "water" /* Water */:
        return new WaterTerrainTile();
      case "ice" /* Ice */:
        return new IceTerrainTile();
      case "menu-brick" /* MenuBrick */:
        return new MenuBrickTerrainTile();
      case "inverse-brick" /* InverseBrick */:
        return new InverseBrickTerrainTile();
      case "blue-brick" /* BlueBrick */:
        return new BlueBrickTerrainTile();
      default:
        throw new Error(`Tile object for "${type}" not defined`);
    }
  }
  static getTileSize(type) {
    switch (type) {
      case "brick" /* Brick */:
        return new Size(BRICK_TILE_SIZE, BRICK_TILE_SIZE);
      case "brick-super" /* BrickSuper */:
        return new Size(
          BRICK_SUPER_TILE_SIZE,
          BRICK_SUPER_TILE_SIZE
        );
      case "steel" /* Steel */:
        return new Size(STEEL_TILE_SIZE, STEEL_TILE_SIZE);
      case "jungle" /* Jungle */:
        return new Size(JUNGLE_TILE_SIZE, JUNGLE_TILE_SIZE);
      case "water" /* Water */:
        return new Size(WATER_TILE_SIZE, WATER_TILE_SIZE);
      case "ice" /* Ice */:
        return new Size(ICE_TILE_SIZE, ICE_TILE_SIZE);
      case "menu-brick" /* MenuBrick */:
        return new Size(BRICK_TILE_SIZE, BRICK_TILE_SIZE);
      case "inverse-brick" /* InverseBrick */:
        return new Size(BRICK_TILE_SIZE, BRICK_TILE_SIZE);
      case "blue-brick" /* BlueBrick */:
        return new Size(BRICK_TILE_SIZE, BRICK_TILE_SIZE);
      default:
        throw new Error(`Tile size for "${type}" not defined`);
    }
  }
  static createTileSizeError(type, regionRect, propertyName, propertySize) {
    const message = `Map tile "${type}" with properties ${regionRect.toString()} has invalid property "${propertyName}": it must be divisible by tile size "${propertySize}"`;
    return new Error(message);
  }
};

// ../src/terrain/TerrainRegionConfig.ts
init_modules_watch_stub();

// ../src/gameObjects/editor/EditorEnemyPreview.ts
init_modules_watch_stub();

// ../src/gameObjects/editor/EditorEraserBrush.ts
init_modules_watch_stub();

// ../src/gameObjects/editor/EditorField.ts
init_modules_watch_stub();

// ../src/gameObjects/editor/EditorTankDummy.ts
init_modules_watch_stub();

// ../src/gameObjects/editor/EditorMap.ts
init_modules_watch_stub();

// ../src/gameObjects/editor/EditorSpawnBrush.ts
init_modules_watch_stub();

// ../src/gameObjects/editor/EditorTool.ts
init_modules_watch_stub();

// ../src/input/index.ts
init_modules_watch_stub();

// ../src/input/bindings/index.ts
init_modules_watch_stub();

// ../src/input/bindings/PrimaryGamepadInputBinding.ts
init_modules_watch_stub();

// ../src/input/InputControl.ts
init_modules_watch_stub();

// ../src/input/bindings/PrimaryKeyboardInputBinding.ts
init_modules_watch_stub();

// ../src/input/bindings/SecondaryGamepadInputBinding.ts
init_modules_watch_stub();

// ../src/input/bindings/SecondaryKeyboardInputBinding.ts
init_modules_watch_stub();

// ../src/input/bindings/TertiaryKeyboardInputBinding.ts
init_modules_watch_stub();

// ../src/input/contexts/index.ts
init_modules_watch_stub();

// ../src/input/contexts/EditorControlsInputContext.ts
init_modules_watch_stub();
var EditorControlsInputContext = {
  Skip: [5 /* PrimaryAction */]
};

// ../src/input/contexts/EditorMapInputContext.ts
init_modules_watch_stub();
var EditorMapInputContext = {
  MoveUp: [0 /* Up */],
  MoveDown: [1 /* Down */],
  MoveLeft: [2 /* Left */],
  MoveRight: [3 /* Right */],
  Draw: [5 /* PrimaryAction */],
  Erase: [6 /* SecondaryAction */],
  NextBrush: [8 /* FastForward */],
  PrevBrush: [7 /* Rewind */],
  Menu: [4 /* Select */]
};

// ../src/input/contexts/LevelControlsInputContext.ts
init_modules_watch_stub();
var LevelControlsInputContext = {
  Continue: [5 /* PrimaryAction */]
};

// ../src/input/contexts/LevelPlayInputContext.ts
init_modules_watch_stub();
var LevelPlayInputContext = {
  MoveUp: [0 /* Up */],
  MoveDown: [1 /* Down */],
  MoveLeft: [2 /* Left */],
  MoveRight: [3 /* Right */],
  Fire: [5 /* PrimaryAction */],
  RapidFire: [6 /* SecondaryAction */],
  PowerOne: [9 /* PowerOne */],
  PowerTwo: [10 /* PowerTwo */],
  PowerThree: [11 /* PowerThree */],
  PowerFour: [12 /* PowerFour */],
  Pause: [4 /* Select */]
};

// ../src/input/contexts/LevelSelectionInputContext.ts
init_modules_watch_stub();
var LevelSelectionInputContext = {
  Next: [3 /* Right */],
  Prev: [2 /* Left */],
  FastNext: [0 /* Up */],
  FastPrev: [1 /* Down */],
  Select: [5 /* PrimaryAction */]
};

// ../src/input/contexts/LevelScoreInputContext.ts
init_modules_watch_stub();
var LevelScoreInputContext = {
  Skip: [5 /* PrimaryAction */]
};

// ../src/input/contexts/MenuInputContext.ts
init_modules_watch_stub();
var MenuInputContext = {
  VerticalPrev: [0 /* Up */],
  VerticalNext: [1 /* Down */],
  HorizontalNext: [3 /* Right */],
  HorizontalPrev: [2 /* Left */],
  Skip: [5 /* PrimaryAction */, 4 /* Select */],
  Select: [5 /* PrimaryAction */, 4 /* Select */]
};

// ../src/input/mobile/index.ts
init_modules_watch_stub();

// ../src/input/mobile/MobileGamepadHost.ts
init_modules_watch_stub();

// ../src/input/mobile/MobileTouchController.ts
init_modules_watch_stub();

// ../src/shop/index.ts
init_modules_watch_stub();

// ../src/shop/ShopManager.ts
init_modules_watch_stub();

// ../src/shop/ShopTypes.ts
init_modules_watch_stub();

// ../src/powerup/index.ts
init_modules_watch_stub();

// ../src/powerup/PowerupFactory.ts
init_modules_watch_stub();

// ../src/powerup/PowerupType.ts
init_modules_watch_stub();

// ../src/powerup/PowerupFactory.ts
var AVAILABLE_TYPES = [
  "defence" /* BaseDefence */,
  "freeze" /* Freeze */,
  "life" /* Life */,
  "shield" /* Shield */,
  "speed" /* Speed */,
  "upgrade" /* Upgrade */,
  "zoomout" /* ZoomOut */,
  "wipeout" /* Wipeout */
];
var PowerupFactory = class {
  static {
    __name(this, "PowerupFactory");
  }
  static create(type) {
    const powerup = new Powerup(type);
    return powerup;
  }
  static createRandom(rng) {
    const type = rng.arrayElement(AVAILABLE_TYPES);
    const powerup = new Powerup(type);
    return powerup;
  }
};

// ../src/powerup/PowerupGrid.ts
init_modules_watch_stub();
var PowerupGrid = class {
  constructor(fieldWidth, fieldHeight) {
    this.grid = [];
    this.backupGrid = null;
    this.tileSize = TILE_SIZE_MEDIUM;
    this.rowCount = fieldHeight / this.tileSize;
    this.colCount = fieldWidth / this.tileSize;
    for (let rowIndex = 0; rowIndex < this.rowCount; rowIndex += 1) {
      this.grid[rowIndex] = [];
      for (let colIndex = 0; colIndex < this.colCount; colIndex += 1) {
        this.grid[rowIndex][colIndex] = true;
      }
    }
  }
  static {
    __name(this, "PowerupGrid");
  }
  blockCell(rowIndex, colIndex) {
    this.toggleCell(rowIndex, colIndex, false);
  }
  freeCell(rowIndex, colIndex) {
    this.toggleCell(rowIndex, colIndex, true);
  }
  blockRect(rect) {
    this.toggleRect(rect, false);
  }
  freeRect(rect) {
    this.toggleRect(rect, true);
  }
  toggleCell(rowIndex, colIndex, isFree) {
    this.grid[rowIndex][colIndex] = isFree;
  }
  toggleRect(rect, isFree) {
    const minIndex = new Vector(
      Math.max(0, Math.floor(rect.x / this.tileSize)),
      Math.max(0, Math.floor(rect.y / this.tileSize))
    );
    const maxIndex = new Vector(
      Math.min(this.rowCount, Math.ceil((rect.x + rect.width) / this.tileSize)),
      Math.min(
        this.colCount,
        Math.ceil((rect.y + rect.height) / this.tileSize)
      )
    );
    for (let rowIndex = minIndex.y; rowIndex < maxIndex.y; rowIndex += 1) {
      for (let colIndex = minIndex.x; colIndex < maxIndex.x; colIndex += 1) {
        this.toggleCell(rowIndex, colIndex, isFree);
      }
    }
  }
  getBlockedCellIndexes() {
    const blockedCells = [];
    for (let rowIndex = 0; rowIndex < this.rowCount; rowIndex += 1) {
      for (let colIndex = 0; colIndex < this.colCount; colIndex += 1) {
        const isFree = this.grid[rowIndex][colIndex];
        if (!isFree) {
          blockedCells.push(new Vector(colIndex, rowIndex));
        }
      }
    }
    return blockedCells;
  }
  // Find all possible spawn positions for powerups. Considering that
  // their size is "large" and cell size is "medium", we are iterating
  // with overlap.
  getFreeLargeCellIndexes() {
    const freeLargeCells = [];
    for (let rowIndex = 0; rowIndex < this.rowCount - 1; rowIndex += 1) {
      for (let colIndex = 0; colIndex < this.colCount - 1; colIndex += 1) {
        const isFree11 = this.grid[rowIndex][colIndex];
        const isFree12 = this.grid[rowIndex][colIndex + 1];
        const isFree21 = this.grid[rowIndex + 1][colIndex];
        const isFree22 = this.grid[rowIndex + 1][colIndex + 1];
        const isBlocked = !isFree11 && !isFree12 && !isFree21 && !isFree22;
        if (isBlocked) {
          continue;
        }
        freeLargeCells.push(new Vector(colIndex, rowIndex));
      }
    }
    return freeLargeCells;
  }
  getRandomPosition(rng) {
    const freeLargeCells = this.getFreeLargeCellIndexes();
    if (freeLargeCells.length === 0) {
      return null;
    }
    const index = rng.arrayElement(freeLargeCells);
    const position = index.multScalar(this.tileSize);
    return position;
  }
  backup() {
    this.backupGrid = [];
    for (let rowIndex = 0; rowIndex < this.rowCount; rowIndex += 1) {
      this.backupGrid[rowIndex] = this.grid[rowIndex].slice();
    }
  }
  restore() {
    if (this.backupGrid === null) {
      return;
    }
    for (let rowIndex = 0; rowIndex < this.rowCount; rowIndex += 1) {
      this.grid[rowIndex] = this.backupGrid[rowIndex].slice();
    }
    this.backupGrid = null;
  }
};

// ../src/shop/ShopTypes.ts
function getPowerupTypeForInventoryItem(itemId) {
  switch (itemId) {
    case "shield" /* Shield */:
      return "shield" /* Shield */;
    case "base-defence" /* BaseDefence */:
      return "defence" /* BaseDefence */;
    case "freeze" /* Freeze */:
      return "freeze" /* Freeze */;
    case "speed" /* Speed */:
      return "speed" /* Speed */;
    case "upgrade" /* Upgrade */:
      return "upgrade" /* Upgrade */;
    case "zoom-out" /* ZoomOut */:
      return "zoomout" /* ZoomOut */;
    case "wipeout" /* Wipeout */:
      return "wipeout" /* Wipeout */;
    case "extra-life" /* ExtraLife */:
      return "life" /* Life */;
    default:
      return null;
  }
}
__name(getPowerupTypeForInventoryItem, "getPowerupTypeForInventoryItem");

// ../src/shop/ShopManager.ts
var CATALOG = [
  {
    id: "fuel-one" /* FuelOne */,
    name: "FUEL X1",
    price: 10,
    solPrice: 0.01,
    reward: { fuel: 1 }
  },
  {
    id: "fuel-five" /* FuelFive */,
    name: "FUEL X5",
    price: 45,
    solPrice: 0.04,
    reward: { fuel: 5 }
  },
  {
    id: "fuel-twenty" /* FuelTwenty */,
    name: "FUEL X20",
    price: 160,
    solPrice: 0.12,
    reward: { fuel: 20 }
  },
  {
    id: "shield" /* Shield */,
    name: "SHIELD",
    price: 25,
    solPrice: 0.02,
    reward: { inventory: { ["shield" /* Shield */]: 1 } }
  },
  {
    id: "base-defence" /* BaseDefence */,
    name: "BASE DEFENCE",
    price: 30,
    solPrice: 0.025,
    reward: { inventory: { ["base-defence" /* BaseDefence */]: 1 } }
  },
  {
    id: "freeze" /* Freeze */,
    name: "FREEZE",
    price: 35,
    solPrice: 0.03,
    reward: { inventory: { ["freeze" /* Freeze */]: 1 } }
  },
  {
    id: "speed" /* Speed */,
    name: "SPEED",
    price: 35,
    solPrice: 0.03,
    reward: { inventory: { ["speed" /* Speed */]: 1 } }
  },
  {
    id: "upgrade" /* Upgrade */,
    name: "STAR",
    price: 50,
    solPrice: 0.045,
    reward: { inventory: { ["upgrade" /* Upgrade */]: 1 } }
  },
  {
    id: "zoom-out" /* ZoomOut */,
    name: "ZOOM OUT",
    price: 30,
    solPrice: 0.025,
    reward: { inventory: { ["zoom-out" /* ZoomOut */]: 1 } }
  },
  {
    id: "wipeout" /* Wipeout */,
    name: "WIPEOUT",
    price: 45,
    solPrice: 0.04,
    reward: { inventory: { ["wipeout" /* Wipeout */]: 1 } }
  },
  {
    id: "extra-life" /* ExtraLife */,
    name: "EXTRA LIFE",
    price: 40,
    solPrice: 0.035,
    reward: { inventory: { ["extra-life" /* ExtraLife */]: 1 } }
  },
  {
    id: "starter-pack" /* StarterPack */,
    name: "STARTER PACK",
    price: 90,
    solPrice: 0.08,
    reward: {
      fuel: 5,
      inventory: {
        ["shield" /* Shield */]: 1,
        ["base-defence" /* BaseDefence */]: 1
      }
    }
  }
];
var ACTIVE_ITEMS = [
  "shield" /* Shield */,
  "base-defence" /* BaseDefence */,
  "freeze" /* Freeze */,
  "speed" /* Speed */,
  "upgrade" /* Upgrade */,
  "zoom-out" /* ZoomOut */,
  "wipeout" /* Wipeout */,
  "extra-life" /* ExtraLife */
];
var PASSIVE_ITEMS = ["extra-life" /* ExtraLife */];
var MAX_POWERUP_STACK = 2;
var ACTIVE_LOADOUT_SLOTS = [
  "active-one" /* ActiveOne */,
  "active-two" /* ActiveTwo */,
  "active-three" /* ActiveThree */,
  "active-four" /* ActiveFour */
];
var ShopManager = class {
  static {
    __name(this, "ShopManager");
  }
  constructor(storage) {
    this.storage = storage;
  }
  getCatalog() {
    return CATALOG;
  }
  isWalletConnected() {
    return this.storage.getBoolean(
      STORAGE_KEY_SHOP_WALLET_CONNECTED,
      false
    );
  }
  connectWallet() {
    this.storage.setBoolean(STORAGE_KEY_SHOP_WALLET_CONNECTED, true);
    this.storage.set(STORAGE_KEY_SHOP_WALLET_ADDRESS, "0XBATTLECITIES");
    if (this.storage.get(STORAGE_KEY_SHOP_TOKEN_BALANCE) === void 0) {
      this.storage.setNumber(
        STORAGE_KEY_SHOP_TOKEN_BALANCE,
        SHOP_STARTING_TOKEN_BALANCE
      );
    }
    if (this.storage.get(STORAGE_KEY_SHOP_SOL_BALANCE) === void 0) {
      this.storage.setNumber(
        STORAGE_KEY_SHOP_SOL_BALANCE,
        SHOP_STARTING_SOL_BALANCE
      );
    }
    this.storage.save();
  }
  getWalletAddress() {
    return this.storage.get(STORAGE_KEY_SHOP_WALLET_ADDRESS) || "NONE";
  }
  getTokenBalance() {
    return this.storage.getNumber(
      STORAGE_KEY_SHOP_TOKEN_BALANCE,
      SHOP_STARTING_TOKEN_BALANCE
    );
  }
  getSolBalance() {
    return this.storage.getNumber(
      STORAGE_KEY_SHOP_SOL_BALANCE,
      SHOP_STARTING_SOL_BALANCE
    );
  }
  getFuelBalance() {
    if (!this.isWalletConnected()) {
      return SHOP_GUEST_FUEL_BALANCE;
    }
    return this.storage.getNumber(STORAGE_KEY_SHOP_FUEL_BALANCE, 0);
  }
  getInventory() {
    if (!this.isWalletConnected()) {
      const inventory = {};
      for (const itemId of ACTIVE_ITEMS) {
        inventory[itemId] = SHOP_GUEST_INVENTORY_COUNT;
      }
      return inventory;
    }
    return this.getJson(STORAGE_KEY_SHOP_INVENTORY, {});
  }
  getInventoryCount(itemId) {
    return this.getInventory()[itemId] || 0;
  }
  getEquipped(slot) {
    const loadout = this.getLoadout();
    const normalizedLoadout = this.normalizeLoadout(loadout);
    return normalizedLoadout[slot] || null;
  }
  getEquippedStackCount(slot) {
    const itemId = this.getEquipped(slot);
    if (itemId === null) {
      return 0;
    }
    return Math.min(MAX_POWERUP_STACK, this.getInventoryCount(itemId));
  }
  purchaseItem(itemId, currency = "token" /* Token */) {
    if (!this.isWalletConnected()) {
      return { ok: false, statusText: "CONNECT WALLET" };
    }
    const item = CATALOG.find((catalogItem) => catalogItem.id === itemId);
    if (item === void 0) {
      return { ok: false, statusText: "ITEM NOT FOUND" };
    }
    if (currency === "sol" /* Sol */) {
      const solBalance = this.getSolBalance();
      if (solBalance < item.solPrice) {
        return { ok: false, statusText: "NEED MORE SOL" };
      }
      this.storage.setNumber(
        STORAGE_KEY_SHOP_SOL_BALANCE,
        Number((solBalance - item.solPrice).toFixed(4))
      );
    } else {
      const tokenBalance = this.getTokenBalance();
      if (tokenBalance < item.price) {
        return { ok: false, statusText: "NEED MORE BACT" };
      }
      this.storage.setNumber(
        STORAGE_KEY_SHOP_TOKEN_BALANCE,
        tokenBalance - item.price
      );
    }
    this.addFuel(item.reward.fuel || 0);
    this.addInventory(item.reward.inventory || {});
    const txHash = this.createMockTransactionHash();
    this.storage.save();
    void this.syncPurchase(itemId, currency);
    return { ok: true, statusText: `BOUGHT ${item.name}`, txHash };
  }
  equipNext(slot) {
    const items = slot === "passive" /* Passive */ ? PASSIVE_ITEMS : ACTIVE_ITEMS;
    const loadout = this.getLoadout();
    this.normalizeLoadout(loadout);
    const currentItem = loadout[slot] || null;
    const equippedElsewhere = /* @__PURE__ */ new Set();
    Object.keys(loadout).forEach((slotKey) => {
      const loadoutSlot = slotKey;
      const itemId = loadout[loadoutSlot];
      if (loadoutSlot !== slot && itemId !== void 0) {
        equippedElsewhere.add(itemId);
      }
    });
    const ownedItems = items.filter((itemId) => {
      return this.getInventoryCount(itemId) > 0 && !equippedElsewhere.has(itemId);
    });
    const choices = [null, ...ownedItems];
    const currentIndex = choices.indexOf(currentItem);
    const nextIndex = currentIndex >= choices.length - 1 ? 0 : currentIndex + 1;
    const nextItem = choices[nextIndex];
    if (nextItem === null) {
      delete loadout[slot];
    } else {
      loadout[slot] = nextItem;
    }
    this.setJson(STORAGE_KEY_SHOP_LOADOUT, loadout);
    this.storage.save();
    void this.syncAccountSnapshot();
    return nextItem;
  }
  canStartRun(fuelCost = SHOP_RUN_FUEL_COST) {
    return this.getFuelBalance() >= this.normalizeFuelCost(fuelCost);
  }
  consumeFuelForRun(fuelCost = SHOP_RUN_FUEL_COST) {
    const normalizedFuelCost = this.normalizeFuelCost(fuelCost);
    if (!this.canStartRun(normalizedFuelCost)) {
      return false;
    }
    if (!this.isWalletConnected()) {
      return true;
    }
    this.storage.setNumber(
      STORAGE_KEY_SHOP_FUEL_BALANCE,
      this.getFuelBalance() - normalizedFuelCost
    );
    this.storage.save();
    void this.syncAccountSnapshot();
    return true;
  }
  normalizeFuelCost(fuelCost) {
    const parsed = Number(fuelCost);
    return Number.isFinite(parsed) ? Math.max(0, Math.floor(parsed)) : 0;
  }
  getEquippedRunConsumables() {
    const loadout = this.getLoadout();
    this.normalizeLoadout(loadout);
    const inventory = this.getInventory();
    const consumables = {
      powerups: [],
      powerupItems: [],
      powerupCounts: [],
      extraLives: 0
    };
    ACTIVE_LOADOUT_SLOTS.forEach((slot) => {
      const itemId = loadout[slot];
      if (itemId === void 0 || (inventory[itemId] || 0) <= 0) {
        return;
      }
      const powerupType = getPowerupTypeForInventoryItem(itemId);
      if (powerupType !== null) {
        if (consumables.powerupItems.indexOf(itemId) !== -1) {
          return;
        }
        consumables.powerupItems.push(itemId);
        consumables.powerups.push(powerupType);
        consumables.powerupCounts.push(
          Math.min(MAX_POWERUP_STACK, inventory[itemId] || 0)
        );
      }
    });
    return consumables;
  }
  async syncAccount() {
    await this.syncAccountSnapshot();
  }
  consumeInventoryItem(itemId) {
    if (!this.isWalletConnected()) {
      return true;
    }
    const inventory = this.getInventory();
    if ((inventory[itemId] || 0) <= 0) {
      return false;
    }
    inventory[itemId] -= 1;
    this.setJson(STORAGE_KEY_SHOP_INVENTORY, inventory);
    const loadout = this.getLoadout();
    this.normalizeLoadout(loadout);
    Object.keys(loadout).forEach((slotKey) => {
      const slot = slotKey;
      if (loadout[slot] === itemId && (inventory[itemId] || 0) <= 0) {
        delete loadout[slot];
      }
    });
    this.setJson(STORAGE_KEY_SHOP_LOADOUT, loadout);
    this.storage.save();
    void this.syncAccountSnapshot();
    return true;
  }
  consumeEquippedItems() {
    const loadout = this.getLoadout();
    this.normalizeLoadout(loadout);
    const inventory = this.getInventory();
    const consumables = {
      powerups: [],
      powerupItems: [],
      powerupCounts: [],
      extraLives: 0
    };
    const isGuest = !this.isWalletConnected();
    Object.keys(loadout).forEach((slotKey) => {
      const slot = slotKey;
      const itemId = loadout[slot];
      if (itemId === void 0 || (inventory[itemId] || 0) <= 0) {
        delete loadout[slot];
        return;
      }
      if (!isGuest) {
        inventory[itemId] -= 1;
        delete loadout[slot];
      }
      if (itemId === "extra-life" /* ExtraLife */) {
        consumables.extraLives += 1;
        return;
      }
      const powerupType = getPowerupTypeForInventoryItem(itemId);
      if (powerupType !== null) {
        consumables.powerupItems.push(itemId);
        consumables.powerups.push(powerupType);
        consumables.powerupCounts.push(1);
      }
    });
    if (!isGuest) {
      this.setJson(STORAGE_KEY_SHOP_INVENTORY, inventory);
      this.setJson(STORAGE_KEY_SHOP_LOADOUT, loadout);
      this.storage.save();
      void this.syncAccountSnapshot();
    }
    return consumables;
  }
  addFuel(fuel) {
    if (fuel <= 0) {
      return;
    }
    this.storage.setNumber(
      STORAGE_KEY_SHOP_FUEL_BALANCE,
      this.getFuelBalance() + fuel
    );
  }
  addInventory(reward) {
    const inventory = this.getInventory();
    Object.keys(reward).forEach((key) => {
      const itemId = key;
      inventory[itemId] = (inventory[itemId] || 0) + reward[itemId];
    });
    this.setJson(STORAGE_KEY_SHOP_INVENTORY, inventory);
  }
  getAccountSnapshot() {
    const loadout = this.getLoadout();
    this.normalizeLoadout(loadout);
    return {
      tokenBalance: this.getTokenBalance(),
      solBalance: this.getSolBalance(),
      fuelBalance: this.getFuelBalance(),
      inventory: this.getInventory(),
      loadout
    };
  }
  applyAccountSnapshot(account) {
    if (typeof account !== "object" || account === null) {
      return;
    }
    if (typeof account.tokenBalance === "number") {
      this.storage.setNumber(
        STORAGE_KEY_SHOP_TOKEN_BALANCE,
        account.tokenBalance
      );
    }
    if (typeof account.solBalance === "number") {
      this.storage.setNumber(
        STORAGE_KEY_SHOP_SOL_BALANCE,
        account.solBalance
      );
    }
    if (typeof account.fuelBalance === "number") {
      this.storage.setNumber(
        STORAGE_KEY_SHOP_FUEL_BALANCE,
        account.fuelBalance
      );
    }
    if (account.inventory !== void 0) {
      this.setJson(STORAGE_KEY_SHOP_INVENTORY, account.inventory);
    }
    if (account.loadout !== void 0) {
      this.setJson(STORAGE_KEY_SHOP_LOADOUT, account.loadout);
    }
    this.storage.save();
  }
  async syncAccountSnapshot() {
    try {
      const response = await apiFetch("/api/economy/account", {
        method: "PUT",
        headers: {
          "content-type": "application/json"
        },
        body: JSON.stringify({ account: this.getAccountSnapshot() })
      });
      if (!response.ok) {
        return;
      }
      const body = await response.json();
      if (body?.authenticated === true && body?.account !== void 0) {
        this.applyAccountSnapshot(body.account);
      }
    } catch {
    }
  }
  async syncPurchase(itemId, currency) {
    try {
      const response = await apiFetch("/api/economy/purchase", {
        method: "POST",
        headers: {
          "content-type": "application/json"
        },
        body: JSON.stringify({ itemId, currency })
      });
      if (!response.ok) {
        return;
      }
      const body = await response.json();
      if (body?.ok === true && body?.account !== void 0) {
        this.applyAccountSnapshot(body.account);
      }
    } catch {
    }
  }
  getLoadout() {
    return this.getJson(STORAGE_KEY_SHOP_LOADOUT, {});
  }
  normalizeLoadout(loadout) {
    const equippedItems = /* @__PURE__ */ new Set();
    if (loadout["passive" /* Passive */] !== void 0 && loadout["active-four" /* ActiveFour */] === void 0) {
      loadout["active-four" /* ActiveFour */] = loadout["passive" /* Passive */];
    }
    delete loadout["passive" /* Passive */];
    Object.keys(loadout).forEach((slotKey) => {
      const slot = slotKey;
      const itemId = loadout[slot];
      if (itemId === void 0) {
        return;
      }
      if (equippedItems.has(itemId)) {
        delete loadout[slot];
        return;
      }
      equippedItems.add(itemId);
    });
    return loadout;
  }
  createMockTransactionHash() {
    const nextIndex = this.storage.getNumber(STORAGE_KEY_SHOP_TX_INDEX, 0) + 1;
    this.storage.setNumber(STORAGE_KEY_SHOP_TX_INDEX, nextIndex);
    return `MOCKTX${nextIndex.toString().padStart(4, "0")}`;
  }
  getJson(key, defaultValue) {
    const json = this.storage.get(key);
    if (json === void 0) {
      return defaultValue;
    }
    try {
      return JSON.parse(json);
    } catch (err) {
      return defaultValue;
    }
  }
  setJson(key, value) {
    this.storage.set(key, JSON.stringify(value));
  }
};

// ../src/input/mobile/MobileTouchController.ts
var MOBILE_LAYOUT_QUERY = "(pointer: coarse) and (max-width: 900px)";
var POWER_ICONS = {
  ["shield" /* Shield */]: "data/graphics/powerup-helmet.png",
  ["base-defence" /* BaseDefence */]: "data/graphics/powerup-shovel.png",
  ["freeze" /* Freeze */]: "data/graphics/powerup-clock.png",
  ["speed" /* Speed */]: "data/graphics/powerup-speed.png",
  ["upgrade" /* Upgrade */]: "data/graphics/powerup-star.png",
  ["zoom-out" /* ZoomOut */]: "data/graphics/powerup-zoomout.png",
  ["wipeout" /* Wipeout */]: "data/graphics/powerup-grenade.png",
  ["extra-life" /* ExtraLife */]: "data/graphics/TANKS/powerup-life.png"
};
var MOVEMENT_CONTROLS = [
  { control: 0 /* Up */, label: "Move up", className: "up" },
  { control: 2 /* Left */, label: "Move left", className: "left" },
  { control: 3 /* Right */, label: "Move right", className: "right" },
  { control: 1 /* Down */, label: "Move down", className: "down" }
];
function isMobileTouchLayout() {
  return typeof window !== "undefined" && typeof window.matchMedia === "function" && window.matchMedia(MOBILE_LAYOUT_QUERY).matches;
}
__name(isMobileTouchLayout, "isMobileTouchLayout");

// ../src/input/presenters/index.ts
init_modules_watch_stub();

// ../src/input/presenters/GamepadButtonCodePresenter.ts
init_modules_watch_stub();

// ../src/input/presenters/KeyboardButtonCodePresenter.ts
init_modules_watch_stub();

// ../src/input/InputBindingType.ts
init_modules_watch_stub();

// ../src/input/InputDeviceType.ts
init_modules_watch_stub();

// ../src/input/InputBindingType.ts
var InputBindingType = class _InputBindingType {
  static {
    __name(this, "InputBindingType");
  }
  static {
    this.instances = [];
  }
  constructor(bindingIndex, deviceType) {
    this.bindingIndex = bindingIndex;
    this.deviceType = deviceType;
    for (const instance of _InputBindingType.instances) {
      if (instance.equals(this)) {
        return instance;
      }
    }
    _InputBindingType.instances.push(this);
  }
  equals(other) {
    return this.bindingIndex === other.bindingIndex && this.deviceType === other.deviceType;
  }
  // It should be backwards-compatible because it will be used as a key
  // for storage.
  serialize() {
    const bindingIndexPart = this.serializeBindingIndex();
    const deviceTypePart = this.serializeDeviceType();
    const serialized = `${bindingIndexPart}_${deviceTypePart}`;
    return serialized;
  }
  serializeBindingIndex() {
    switch (this.bindingIndex) {
      case 0:
        return "primary";
      case 1:
        return "secondary";
      case 2:
        return "tertiary";
    }
    return "unknown";
  }
  serializeDeviceType() {
    switch (this.deviceType) {
      case 0 /* Keyboard */:
        return "0";
      case 1 /* Gamepad */:
        return "1";
      case 2 /* MobileGamepad */:
        return "2";
    }
    return "?";
  }
  static {
    this.PrimaryKeyboard = new _InputBindingType(
      0,
      0 /* Keyboard */
    );
  }
  static {
    this.PrimaryGamepad = new _InputBindingType(
      0,
      1 /* Gamepad */
    );
  }
  static {
    this.SecondaryKeyboard = new _InputBindingType(
      1,
      0 /* Keyboard */
    );
  }
  static {
    this.SecondaryGamepad = new _InputBindingType(
      1,
      1 /* Gamepad */
    );
  }
  static {
    this.PrimaryMobileGamepad = new _InputBindingType(
      0,
      2 /* MobileGamepad */
    );
  }
  static {
    this.SecondaryMobileGamepad = new _InputBindingType(
      1,
      2 /* MobileGamepad */
    );
  }
  static {
    this.TertiaryKeyboard = new _InputBindingType(
      2,
      0 /* Keyboard */
    );
  }
};

// ../src/input/InputButtonCodePresenter.ts
init_modules_watch_stub();

// ../src/input/InputControlPresenter.ts
init_modules_watch_stub();

// ../src/input/InputHintSettings.ts
init_modules_watch_stub();

// ../src/input/InputHoldThrottle.ts
init_modules_watch_stub();

// ../src/input/InputManager.ts
init_modules_watch_stub();

// ../src/input/InputVariant.ts
init_modules_watch_stub();
var InputVariant = class _InputVariant {
  static {
    __name(this, "InputVariant");
  }
  static {
    this.instances = [];
  }
  constructor(bindingType, deviceIndex) {
    this.bindingType = bindingType;
    this.deviceIndex = deviceIndex;
    for (const instance of _InputVariant.instances) {
      if (instance.equals(this)) {
        return instance;
      }
    }
    _InputVariant.instances.push(this);
  }
  equals(other) {
    return this.bindingType === other.bindingType && this.deviceIndex === other.deviceIndex;
  }
  static {
    this.PrimaryKeyboard0 = new _InputVariant(
      InputBindingType.PrimaryKeyboard,
      0
    );
  }
  static {
    this.SecondaryKeyboard0 = new _InputVariant(
      InputBindingType.SecondaryKeyboard,
      0
    );
  }
  static {
    this.TertiaryKeyboard0 = new _InputVariant(
      InputBindingType.TertiaryKeyboard,
      0
    );
  }
  static {
    this.PrimaryGamepad0 = new _InputVariant(
      InputBindingType.PrimaryGamepad,
      0
    );
  }
  static {
    this.PrimaryGamepad1 = new _InputVariant(
      InputBindingType.PrimaryGamepad,
      1
    );
  }
  static {
    this.SecondaryGamepad0 = new _InputVariant(
      InputBindingType.SecondaryGamepad,
      0
    );
  }
  static {
    this.SecondaryGamepad1 = new _InputVariant(
      InputBindingType.SecondaryGamepad,
      1
    );
  }
  static {
    this.PrimaryMobileGamepad0 = new _InputVariant(
      InputBindingType.PrimaryMobileGamepad,
      0
    );
  }
  static {
    this.PrimaryMobileGamepad1 = new _InputVariant(
      InputBindingType.PrimaryMobileGamepad,
      1
    );
  }
  static {
    this.SecondaryMobileGamepad0 = new _InputVariant(
      InputBindingType.SecondaryMobileGamepad,
      0
    );
  }
  static {
    this.SecondaryMobileGamepad1 = new _InputVariant(
      InputBindingType.SecondaryMobileGamepad,
      1
    );
  }
};

// ../src/gameObjects/editor/EditorPalette.ts
init_modules_watch_stub();

// ../src/gameObjects/editor/EditorPaletteItem.ts
init_modules_watch_stub();

// ../src/gameObjects/info/index.ts
init_modules_watch_stub();

// ../src/gameObjects/info/LevelInfo.ts
init_modules_watch_stub();

// ../src/gameObjects/text/index.ts
init_modules_watch_stub();

// ../src/gameObjects/text/GameOverHeading.ts
init_modules_watch_stub();

// ../src/gameObjects/text/TerrainText.ts
init_modules_watch_stub();

// ../src/gameObjects/text/BrickTextShadow.ts
init_modules_watch_stub();

// ../src/gameObjects/DropShadowPainter.ts
init_modules_watch_stub();
var DropShadowPainter = class extends Painter {
  constructor() {
    super(...arguments);
    this.casters = [];
    this.offsetX = WALL_SHADOW_OFFSET_X;
    this.offsetY = WALL_SHADOW_OFFSET_Y;
    this.steps = WALL_SHADOW_STEPS;
    this.color = WALL_SHADOW_COLOR;
    this.alpha = WALL_SHADOW_ALPHA;
  }
  static {
    __name(this, "DropShadowPainter");
  }
  paint(context) {
    if (this.casters.length === 0) {
      return;
    }
    const prevAlpha = context.getGlobalAlpha();
    context.setGlobalAlpha(this.alpha);
    for (let step = 1; step <= this.steps; step += 1) {
      const fraction = step / this.steps;
      const dx = this.offsetX * fraction;
      const dy = this.offsetY * fraction;
      for (const caster of this.casters) {
        if (caster.isRemoved) {
          continue;
        }
        const box = caster.getWorldBoundingBox();
        const x = box.min.x + dx;
        const y = box.min.y + dy;
        const width = box.max.x - box.min.x;
        const height = box.max.y - box.min.y;
        if (!context.intersectsWorldCullBounds(x, y, width, height)) {
          continue;
        }
        context.fillRect(
          x,
          y,
          width,
          height,
          this.color
        );
      }
    }
    context.setGlobalAlpha(prevAlpha);
  }
};

// ../src/gameObjects/text/HighscoreHeading.ts
init_modules_watch_stub();

// ../src/gameObjects/text/LevelTitle.ts
init_modules_watch_stub();

// ../src/gameObjects/text/SpriteText.ts
init_modules_watch_stub();

// ../src/core/text/UiTypography.ts
init_modules_watch_stub();
var USE_MOBILE_READABLE_FONT = typeof window !== "undefined" && typeof window.matchMedia === "function" && window.matchMedia("(pointer: coarse) and (max-width: 900px)").matches;
var UI_FONT_FAMILY = USE_MOBILE_READABLE_FONT ? "Roboto, Arial, Helvetica, sans-serif" : '"Battle Cities UI", "Arial Narrow", "Segoe UI", Arial, sans-serif';
var UI_TEXT_STROKE_COLOR = "#030506";
var UI_TEXT_STROKE_WIDTH = USE_MOBILE_READABLE_FONT ? 0.3 : 0.65;
var UI_TEXT_LETTER_SPACING = 1;

// ../src/gameObjects/text/SpriteText.ts
var DEFAULT_OPTIONS3 = {
  alignment: 0 /* Left */,
  color: COLOR_BLACK,
  fontFamily: UI_FONT_FAMILY,
  fontSize: 24,
  fontWeight: "700",
  letterSpacing: UI_TEXT_LETTER_SPACING,
  lineSpacing: 8,
  opacity: 1,
  strokeColor: UI_TEXT_STROKE_COLOR,
  strokeWidth: UI_TEXT_STROKE_WIDTH
};
var NativeSpriteTextPainter = class extends Painter {
  constructor() {
    super(...arguments);
    this.text = "";
    this.color = COLOR_BLACK;
    this.opacity = 1;
    this.fontFamily = UI_FONT_FAMILY;
    this.fontSize = 24;
    this.fontWeight = "700";
    this.lineSpacing = 8;
    this.letterSpacing = UI_TEXT_LETTER_SPACING;
    this.maxWidth = 1;
    this.alignment = 0 /* Left */;
    this.strokeColor = UI_TEXT_STROKE_COLOR;
    this.strokeWidth = UI_TEXT_STROKE_WIDTH;
  }
  static {
    __name(this, "NativeSpriteTextPainter");
  }
  paint(context, renderObject) {
    const { min } = renderObject.getWorldBoundingBox();
    const previousAlpha = context.getGlobalAlpha();
    const lineHeight = Math.ceil(this.fontSize * 1.18);
    const align = this.getCanvasAlignment();
    if (this.opacity !== 1) {
      context.setGlobalAlpha(previousAlpha * this.opacity);
    }
    this.text.split("\n").forEach((line, index) => {
      context.drawText(
        line,
        min.x,
        min.y + index * (lineHeight + this.lineSpacing),
        this.maxWidth,
        this.fontSize,
        this.fontFamily,
        this.fontWeight,
        this.color,
        align,
        this.strokeColor,
        this.strokeWidth,
        this.letterSpacing
      );
    });
    if (this.opacity !== 1) {
      context.setGlobalAlpha(previousAlpha);
    }
  }
  getCanvasAlignment() {
    if (this.alignment === 1 /* Center */) {
      return "center";
    }
    if (this.alignment === 2 /* Right */) {
      return "right";
    }
    return "left";
  }
};
var SpriteText = class extends GameObject {
  constructor(text = "", options = {}) {
    super();
    this.painter = new NativeSpriteTextPainter();
    this.options = Object.assign({}, DEFAULT_OPTIONS3, options);
    this.text = text;
    this.painter.text = text;
    this.painter.color = this.options.color;
    this.painter.opacity = this.options.opacity;
    this.painter.fontFamily = this.options.fontFamily;
    this.painter.fontSize = this.options.fontSize;
    this.painter.fontWeight = this.options.fontWeight;
    this.painter.lineSpacing = this.options.lineSpacing;
    this.painter.letterSpacing = this.options.letterSpacing;
    this.painter.alignment = this.options.alignment;
    this.painter.strokeColor = this.options.strokeColor;
    this.painter.strokeWidth = this.options.strokeWidth;
    this.updateTextSize();
  }
  static {
    __name(this, "SpriteText");
  }
  setColor(color) {
    this.painter.color = color;
    this.setNeedsPaint();
  }
  setText(text) {
    this.dirtyPaintBox();
    this.text = text;
    this.painter.text = text;
    this.updateTextSize();
    this.updateMatrix();
    this.setNeedsPaint();
  }
  getTextSize() {
    return new Size(this.size.width, this.size.height);
  }
  updateTextSize() {
    const lines = this.text.split("\n");
    const fontSize = this.options.fontSize;
    const letterSpacing = this.options.letterSpacing;
    const lineHeight = Math.ceil(fontSize * 1.18);
    const lineSpacing = this.options.lineSpacing;
    const context = this.getMeasureContext();
    let width = 1;
    if (context !== null) {
      context.font = `${this.options.fontWeight} ${fontSize}px ${this.options.fontFamily}`;
    }
    lines.forEach((line) => {
      const measuredWidth = context === null ? line.length * fontSize * 0.56 + Math.max(0, Array.from(line).length - 1) * letterSpacing : measureTrackedTextWidth(context, line, letterSpacing);
      width = Math.max(width, measuredWidth);
    });
    const height = lines.length * lineHeight + Math.max(0, lines.length - 1) * lineSpacing;
    this.size.set(Math.ceil(width + this.options.strokeWidth * 2), height);
    this.painter.maxWidth = this.size.width;
  }
  getMeasureContext() {
    if (typeof document === "undefined") {
      return null;
    }
    const canvas = document.createElement("canvas");
    return canvas.getContext("2d");
  }
};

// ../src/gameObjects/text/LevelTitle.ts
var LevelTitle = class _LevelTitle extends SpriteText {
  static {
    __name(this, "LevelTitle");
  }
  constructor(levelNumber = 0, isPlaytest = false, options = {}) {
    const text = isPlaytest ? "PLAYTEST" : _LevelTitle.getLevelText(levelNumber);
    super(text, options);
    this.zIndex = LEVEL_TITLE_Z_INDEX;
  }
  setLevelNumber(levelNumber) {
    const text = _LevelTitle.getLevelText(levelNumber);
    this.setText(text);
  }
  static getLevelText(levelNumber) {
    const numberText = levelNumber.toString().padStart(2, " ");
    const text = `STAGE ${numberText}`;
    return text;
  }
};

// ../src/gameObjects/text/MainHeading.ts
init_modules_watch_stub();

// ../src/gameObjects/text/SceneMenuTitle.ts
init_modules_watch_stub();

// ../src/gameObjects/text/SceneInputHint.ts
init_modules_watch_stub();

// ../src/gameObjects/text/VictoryHeading.ts
init_modules_watch_stub();

// ../src/gameObjects/menu/index.ts
init_modules_watch_stub();

// ../src/gameObjects/menu/items/index.ts
init_modules_watch_stub();

// ../src/gameObjects/menu/items/DividerMenuItem.ts
init_modules_watch_stub();

// ../src/gameObjects/menu/items/TextMenuItem.ts
init_modules_watch_stub();

// ../src/gameObjects/menu/MenuItem.ts
init_modules_watch_stub();

// ../src/gameObjects/menu/items/SelectorMenuItem.ts
init_modules_watch_stub();

// ../src/gameObjects/menu/items/SpriteMenuItem.ts
init_modules_watch_stub();

// ../src/gameObjects/menu/Menu.ts
init_modules_watch_stub();

// ../src/gameObjects/menu/MenuCursor.ts
init_modules_watch_stub();

// ../src/gameObjects/menu/MenuDescription.ts
init_modules_watch_stub();

// ../src/gameObjects/menu/SceneMenu.ts
init_modules_watch_stub();

// ../src/gameObjects/modals/index.ts
init_modules_watch_stub();

// ../src/gameObjects/modals/AlertModal.ts
init_modules_watch_stub();

// ../src/gameObjects/modals/ConfirmModal.ts
init_modules_watch_stub();

// ../src/gameObjects/modals/InputButtonCaptureModal.ts
init_modules_watch_stub();

// ../src/gameObjects/score/index.ts
init_modules_watch_stub();

// ../src/gameObjects/score/ScoreBonus.ts
init_modules_watch_stub();

// ../src/gameObjects/score/ScoreTable.ts
init_modules_watch_stub();

// ../src/tank/TankTier.ts
init_modules_watch_stub();
var TankTier = /* @__PURE__ */ ((TankTier3) => {
  TankTier3["A"] = "a";
  TankTier3["B"] = "b";
  TankTier3["C"] = "c";
  TankTier3["D"] = "d";
  return TankTier3;
})(TankTier || {});

// ../src/gameObjects/score/ScoreTableCounter.ts
init_modules_watch_stub();

// ../src/gameObjects/score/ScoreTableTierIcon.ts
init_modules_watch_stub();

// ../src/gameObjects/score/ScoreTableUnderline.ts
init_modules_watch_stub();

// ../src/gameObjects/score/ScoreTable.ts
var TIERS = ["a" /* A */, "b" /* B */, "c" /* C */, "d" /* D */];

// ../src/gameObjects/terrain/index.ts
init_modules_watch_stub();

// ../src/gameObjects/Border.ts
init_modules_watch_stub();
var Border = class extends GameObject {
  constructor(fieldWidth, fieldHeight) {
    super();
    this.fieldWidth = fieldWidth;
    this.fieldHeight = fieldHeight;
  }
  static {
    __name(this, "Border");
  }
  setup() {
    const rects = [
      {
        x: 0,
        y: -BORDER_TOP_BOTTOM_HEIGHT,
        width: this.fieldWidth,
        height: BORDER_TOP_BOTTOM_HEIGHT
      },
      {
        x: 0,
        y: this.fieldHeight,
        width: this.fieldWidth,
        height: BORDER_TOP_BOTTOM_HEIGHT
      },
      {
        x: -BORDER_LEFT_WIDTH,
        y: -BORDER_TOP_BOTTOM_HEIGHT,
        width: BORDER_LEFT_WIDTH,
        height: this.fieldHeight + BORDER_TOP_BOTTOM_HEIGHT * 2
      },
      {
        x: this.fieldWidth,
        y: -BORDER_TOP_BOTTOM_HEIGHT,
        width: BORDER_RIGHT_WIDTH,
        height: this.fieldHeight + BORDER_TOP_BOTTOM_HEIGHT * 2
      }
    ];
    rects.forEach((rect) => {
      const wall = new BorderWall(rect.width, rect.height);
      wall.position.set(rect.x, rect.y);
      this.add(wall);
    });
  }
};

// ../src/gameObjects/Bullet.ts
init_modules_watch_stub();

// ../src/tank/TankBulletWallDamage.ts
init_modules_watch_stub();

// ../src/gameObjects/SmallExplosion.ts
init_modules_watch_stub();
var SmallExplosion = class extends GameObject {
  constructor() {
    super(64, 64);
    this.zIndex = SMALL_EXPLOSION_Z_INDEX;
    this.painter = new SpritePainter();
    this.done = new Subject();
    this.painter.alignment = 3 /* MiddleCenter */;
  }
  static {
    __name(this, "SmallExplosion");
  }
  setup({ spriteLoader, particles }) {
    this.animation = new Animation(
      spriteLoader.loadList([
        "explosion.small.1",
        "explosion.small.2",
        "explosion.small.3"
      ]),
      { delay: 0.05, loop: false }
    );
    this.updateMatrix();
    const center = this.getCenter();
    emitExplosion(particles, center.x, center.y, { scale: 0.45, smoke: false });
  }
  update(updateArgs) {
    if (this.animation.isComplete()) {
      this.dirtyPaintBox();
      this.removeSelf();
      this.done.notify(null);
      return;
    }
    this.animation.update(updateArgs.deltaTime);
    this.painter.sprite = SHOW_EXPLOSION_SPRITE ? this.animation.getCurrentFrame() : null;
    this.setNeedsPaint();
  }
};

// ../src/gameObjects/TerrainTileDestroyer.ts
init_modules_watch_stub();
var COVER_WINDOW = TILE_SIZE_LARGE;
var TerrainTileDestroyer = class extends GameObject {
  static {
    __name(this, "TerrainTileDestroyer");
  }
  constructor(argDamage) {
    const damage = Math.min(argDamage, 2 /* High */);
    const width = TILE_SIZE_LARGE;
    const depth = TILE_SIZE_SMALL;
    super(width, depth);
    this.damage = damage;
    this.collider = new BoxCollider(this, true);
    this.collisionSystem = null;
  }
  setup({ collisionSystem }) {
    this.collisionSystem = collisionSystem;
    collisionSystem.register(this.collider);
  }
  collide(collision) {
    const { contacts } = collision;
    const tileContacts = contacts.filter((contact) => {
      return contact.collider.object.tags.includes("Wall" /* Wall */);
    });
    if (tileContacts.length === 0) {
      this.destroy();
      return;
    }
    const frontContacts = this.getFrontRowContacts(tileContacts);
    frontContacts.forEach((contact) => {
      const tile = contact.collider.object;
      const isBrickWall = tile.tags.includes("Brick" /* Brick */);
      const isSteelWall = tile.tags.includes("Steel" /* Steel */);
      const canDestroySteelWall = this.damage === 2 /* High */;
      if (isBrickWall || isSteelWall && canDestroySteelWall) {
        tile.destroy();
        this.destroy();
      }
    });
  }
  getFrontRowContacts(contacts) {
    const rotation = this.getWorldRotation();
    if (rotation === 0 /* Up */) {
      const frontY = Math.max(...contacts.map((contact) => contact.box.max.y));
      return this.limitContactsToImpactWidth(
        contacts.filter((contact) => contact.box.max.y === frontY)
      );
    }
    if (rotation === 180 /* Down */) {
      const frontY = Math.min(...contacts.map((contact) => contact.box.min.y));
      return this.limitContactsToImpactWidth(
        contacts.filter((contact) => contact.box.min.y === frontY)
      );
    }
    if (rotation === 270 /* Left */) {
      const frontX = Math.max(...contacts.map((contact) => contact.box.max.x));
      return this.limitContactsToImpactWidth(
        contacts.filter((contact) => contact.box.max.x === frontX)
      );
    }
    if (rotation === 90 /* Right */) {
      const frontX = Math.min(...contacts.map((contact) => contact.box.min.x));
      return this.limitContactsToImpactWidth(
        contacts.filter((contact) => contact.box.min.x === frontX)
      );
    }
    return contacts;
  }
  limitContactsToImpactWidth(contacts) {
    const maxContacts = this.damage * 4;
    const exposed = contacts.filter(
      (contact) => !this.isCoveredFromFront(contact)
    );
    const contiguous = this.filterToContiguousGroup(exposed);
    if (contiguous.length <= maxContacts) {
      return contiguous;
    }
    const rotation = this.getWorldRotation();
    const center = this.getWorldBoundingBox().getCenter();
    const isVerticalHit = rotation === 0 /* Up */ || rotation === 180 /* Down */;
    return contiguous.slice().sort((a, b) => {
      const aCenter = a.box.getCenter();
      const bCenter = b.box.getCenter();
      const aDistance = isVerticalHit ? Math.abs(aCenter.x - center.x) : Math.abs(aCenter.y - center.y);
      const bDistance = isVerticalHit ? Math.abs(bCenter.x - center.x) : Math.abs(bCenter.y - center.y);
      return aDistance - bDistance;
    }).slice(0, maxContacts);
  }
  // True when another wall tile sits directly in front of this contact
  // (between it and the shooter), overlapping it on the axis perpendicular to
  // travel and within COVER_WINDOW along the travel axis. Border walls are
  // ignored: they surround the field, so a candidate can never legitimately
  // hide behind one from an in-field shooter, and their huge boxes would
  // otherwise "cover" everything. One linear scan of the static colliders per
  // candidate -- destroyers are one-shot objects, so this is not a hot path.
  isCoveredFromFront(contact) {
    if (this.collisionSystem === null) {
      return false;
    }
    const rotation = this.getWorldRotation();
    const box = contact.box;
    for (const collider of this.collisionSystem.getStaticColliders()) {
      const object2 = collider.object;
      if (object2 === contact.collider.object) {
        continue;
      }
      if (!object2.tags.includes("Wall" /* Wall */) || object2.tags.includes("Border" /* Border */)) {
        continue;
      }
      const other = collider.getBox();
      if (rotation === 0 /* Up */) {
        if (other.min.x < box.max.x && other.max.x > box.min.x && other.min.y >= box.max.y && other.min.y < box.max.y + COVER_WINDOW) {
          return true;
        }
      } else if (rotation === 180 /* Down */) {
        if (other.min.x < box.max.x && other.max.x > box.min.x && other.max.y <= box.min.y && other.max.y > box.min.y - COVER_WINDOW) {
          return true;
        }
      } else if (rotation === 270 /* Left */) {
        if (other.min.y < box.max.y && other.max.y > box.min.y && other.min.x >= box.max.x && other.min.x < box.max.x + COVER_WINDOW) {
          return true;
        }
      } else if (rotation === 90 /* Right */) {
        if (other.min.y < box.max.y && other.max.y > box.min.y && other.max.x <= box.min.x && other.max.x > box.min.x - COVER_WINDOW) {
          return true;
        }
      }
    }
    return false;
  }
  // Flood-fills outward (along the axis perpendicular to travel, since every
  // input contact already shares the same front-facing distance) from the
  // contact closest to the destroyer's own center, only crossing into a
  // neighbor whose box directly touches (shares an edge, no gap) an
  // already-included one. This is what actually prevents a hit from leaping
  // across empty space into a disconnected wall.
  filterToContiguousGroup(contacts) {
    if (contacts.length <= 1) {
      return contacts;
    }
    const rotation = this.getWorldRotation();
    const isVerticalHit = rotation === 0 /* Up */ || rotation === 180 /* Down */;
    const center = this.getWorldBoundingBox().getCenter();
    const axisCenter = isVerticalHit ? center.x : center.y;
    const axisRange = /* @__PURE__ */ __name((contact) => isVerticalHit ? [contact.box.min.x, contact.box.max.x] : [contact.box.min.y, contact.box.max.y], "axisRange");
    let seedIndex = 0;
    let seedDistance = Infinity;
    contacts.forEach((contact, index) => {
      const [min, max] = axisRange(contact);
      const distance = Math.abs((min + max) / 2 - axisCenter);
      if (distance < seedDistance) {
        seedDistance = distance;
        seedIndex = index;
      }
    });
    const touches = /* @__PURE__ */ __name((a, b) => a[1] === b[0] || b[1] === a[0], "touches");
    const included = [contacts[seedIndex]];
    const remaining = contacts.filter((_, index) => index !== seedIndex);
    let addedAny = true;
    while (addedAny) {
      addedAny = false;
      for (let i = remaining.length - 1; i >= 0; i -= 1) {
        const candidateRange = axisRange(remaining[i]);
        const touchesIncluded = included.some(
          (contact) => touches(axisRange(contact), candidateRange)
        );
        if (touchesIncluded) {
          included.push(remaining[i]);
          remaining.splice(i, 1);
          addedAny = true;
        }
      }
    }
    return included;
  }
  destroy() {
    this.removeSelf();
    this.collider.unregister();
  }
};

// ../src/gameObjects/Bullet.ts
var Bullet = class extends GameObject {
  constructor(ownerPartyIndex, speed, tankDamage, wallDamage) {
    super(BULLET_WIDTH, 16);
    this.collider = new SweptBoxCollider(this, true);
    this.painter = new SpritePainter();
    this.zIndex = BULLET_Z_INDEX;
    this.tags = ["Bullet" /* Bullet */];
    this.died = new Subject();
    // Becomes true once the bullet has been spent (exploded or nullified) so it
    // can only deal damage once — e.g. it can't kill two overlapping tanks.
    this.consumed = false;
    this.networkControlled = false;
    this.networkMovementControlled = false;
    this.localDamageDisabled = false;
    this.ownerPartyIndex = ownerPartyIndex;
    this.speed = speed;
    this.tankDamage = tankDamage;
    this.wallDamage = wallDamage;
    this.pivot.set(0.5, 0.5);
  }
  static {
    __name(this, "Bullet");
  }
  setNetworkControlled(networkControlled) {
    this.networkControlled = networkControlled;
    this.networkMovementControlled = networkControlled;
  }
  setNetworkCollisionControlled(networkControlled) {
    this.networkControlled = networkControlled;
    this.networkMovementControlled = false;
  }
  setLocalDamageDisabled(disabled) {
    this.localDamageDisabled = disabled;
  }
  isLocalDamageDisabled() {
    return this.localDamageDisabled;
  }
  setup({
    audioLoader,
    collisionSystem,
    particles,
    spriteLoader
  }) {
    collisionSystem.register(this.collider);
    this.hitBrickSound = audioLoader.load("hit.brick");
    this.hitSteelSound = audioLoader.load("hit.steel");
    const rotation = this.getWorldRotation();
    const spriteId = `bullet.${this.getRotationString(rotation)}`;
    const sprite = spriteLoader.load(spriteId);
    this.painter.sprite = sprite;
    let dirX = 0;
    let dirY = 0;
    if (rotation === 0 /* Up */) {
      dirY = -1;
    } else if (rotation === 180 /* Down */) {
      dirY = 1;
    } else if (rotation === 270 /* Left */) {
      dirX = -1;
    } else if (rotation === 90 /* Right */) {
      dirX = 1;
    }
    this.updateMatrix();
    const center = this.getCenter();
    emitMuzzleFlash(particles, center.x, center.y, dirX, dirY);
  }
  update(updateArgs) {
    this.dirtyPaintBox();
    if (!this.networkMovementControlled) {
      this.translateY(this.speed * updateArgs.deltaTime);
    }
    this.updateMatrix();
    this.collider.update();
    this.setNeedsPaint();
  }
  collide(collision) {
    if (this.networkControlled) {
      return;
    }
    this.collideBullets(collision);
    this.collideWalls(collision);
  }
  // True once the bullet has already hit something this collision pass. Tanks
  // check this so a single bullet can't damage more than one of them.
  isSpent() {
    return this.consumed;
  }
  nullify() {
    if (this.consumed) {
      return;
    }
    this.consumed = true;
    this.removeSelf();
    this.collider.unregister();
    this.died.notify(null);
  }
  explode() {
    if (this.consumed) {
      return;
    }
    this.consumed = true;
    const explosion = new SmallExplosion();
    explosion.updateMatrix();
    explosion.setCenter(this.getCenter());
    this.replaceSelf(explosion);
    this.collider.unregister();
    this.died.notify(null);
  }
  collideBullets(collision) {
    const bulletContacts = collision.contacts.filter((contact) => {
      return contact.collider.object.tags.includes("Bullet" /* Bullet */);
    });
    bulletContacts.forEach((contact) => {
      const bullet = contact.collider.object;
      if (bullet.tags.includes("Enemy" /* Enemy */) && this.tags.includes("Enemy" /* Enemy */)) {
        return;
      }
      if (bullet.tags.includes("Player" /* Player */) && this.tags.includes("Player" /* Player */) && bullet.ownerPartyIndex === this.ownerPartyIndex) {
        return;
      }
      this.nullify();
      bullet.nullify();
    });
  }
  collideWalls(collision) {
    const wallContacts = collision.contacts.filter((contact) => {
      return contact.collider.object.tags.includes("Wall" /* Wall */);
    });
    if (wallContacts.length === 0) {
      return;
    }
    let minDistance = null;
    wallContacts.forEach((contact) => {
      const prevBox = this.collider.getPrevBox();
      const distance = prevBox.distanceCenterToCenter(contact.box);
      if (minDistance === null || distance < minDistance) {
        minDistance = distance;
      }
    });
    const closestWallContacts = wallContacts.filter((contact) => {
      const prevBox = this.collider.getPrevBox();
      const distance = prevBox.distanceCenterToCenter(contact.box);
      return distance === minDistance;
    });
    if (closestWallContacts.length > 0) {
      const firstClosestWallContact = closestWallContacts[0];
      const wallWorldBox = firstClosestWallContact.box;
      const selfWorldBox = this.getWorldBoundingBox();
      const wall = firstClosestWallContact.collider.object;
      const isBrickWall = wall.tags.includes("Brick" /* Brick */);
      const isBorderWall = wall.tags.includes("Border" /* Border */);
      const isSteelWall = wall.tags.includes("Steel" /* Steel */);
      const canDestroySteelWall = this.wallDamage === 2 /* High */;
      if (isBrickWall || isSteelWall && canDestroySteelWall) {
        const destroyer = new TerrainTileDestroyer(this.wallDamage);
        this.updateWorldMatrix(true);
        this.add(destroyer);
        destroyer.updateMatrix();
        destroyer.setCenter(this.getSelfCenter());
        destroyer.updateMatrix();
        const destroyerWorldBox = destroyer.getWorldBoundingBox();
        const rotation2 = destroyer.getWorldRotation();
        if (rotation2 === 0 /* Up */) {
          destroyer.translateY(destroyerWorldBox.max.y - wallWorldBox.max.y);
        } else if (rotation2 === 180 /* Down */) {
          destroyer.translateY(wallWorldBox.min.y - destroyerWorldBox.min.y);
        } else if (rotation2 === 270 /* Left */) {
          destroyer.translateY(destroyerWorldBox.max.x - wallWorldBox.max.x);
        } else if (rotation2 === 90 /* Right */) {
          destroyer.translateY(wallWorldBox.min.x - destroyerWorldBox.min.x);
        }
        this.parent.attach(destroyer);
        this.snapDestroyerToBrickGrid(destroyer);
        if (this.tags.includes("Player" /* Player */)) {
          this.hitBrickSound.play();
        }
      } else if (isSteelWall || isBorderWall) {
        if (this.tags.includes("Player" /* Player */)) {
          this.hitSteelSound.play();
        }
      }
      const rotation = this.getWorldRotation();
      if (rotation === 0 /* Up */) {
        this.translateY(selfWorldBox.max.y - wallWorldBox.max.y);
      } else if (rotation === 180 /* Down */) {
        this.translateY(wallWorldBox.min.y - selfWorldBox.min.y);
      } else if (rotation === 270 /* Left */) {
        this.translateY(selfWorldBox.max.x - wallWorldBox.max.x);
      } else if (rotation === 90 /* Right */) {
        this.translateY(wallWorldBox.min.x - selfWorldBox.min.x);
      }
      this.updateMatrix();
      this.explode();
    }
  }
  getRotationString(rotation) {
    switch (rotation) {
      case 0 /* Up */:
        return "up";
      case 180 /* Down */:
        return "down";
      case 270 /* Left */:
        return "left";
      case 90 /* Right */:
        return "right";
      default:
        return "unknown";
    }
  }
  snapDestroyerToBrickGrid(destroyer) {
    destroyer.updateMatrix();
    const destroyerBox = destroyer.getBoundingBox();
    const rotation = destroyer.getWorldRotation();
    if (rotation === 0 /* Up */ || rotation === 180 /* Down */) {
      const snappedMinX = Math.round(destroyerBox.min.x / TILE_SIZE_SMALL) * TILE_SIZE_SMALL;
      destroyer.position.addX(snappedMinX - destroyerBox.min.x);
    } else if (rotation === 270 /* Left */ || rotation === 90 /* Right */) {
      const snappedMinY = Math.round(destroyerBox.min.y / TILE_SIZE_SMALL) * TILE_SIZE_SMALL;
      destroyer.position.addY(snappedMinY - destroyerBox.min.y);
    }
    destroyer.updateMatrix(true);
  }
};

// ../src/gameObjects/Curtain.ts
init_modules_watch_stub();
var SLIDE_SPEED = 1500;
var Curtain = class extends GameObject {
  constructor(width, height, isOpen = true) {
    super(width, height);
    this.zIndex = CURTAIN_Z_INDEX;
    this.state = isOpen ? 2 /* Open */ : 0 /* Closed */;
  }
  static {
    __name(this, "Curtain");
  }
  setup() {
    this.targetHeight = this.size.height / 2;
    const initialHeight = this.state === 2 /* Open */ ? 0 : this.targetHeight;
    this.topPart = new GameObject(this.size.width, initialHeight + 1);
    this.topPart.painter = new RectPainter(COLOR_GRAY);
    this.bottomPart = new GameObject(this.size.width, initialHeight + 1);
    this.bottomPart.painter = new RectPainter(COLOR_GRAY);
    this.bottomPart.origin.set(0, 1);
    this.bottomPart.position.setY(this.size.height);
    this.add(this.topPart);
    this.add(this.bottomPart);
  }
  update(updateArgs) {
    const { deltaTime } = updateArgs;
    if (this.state === 2 /* Open */ || this.state === 0 /* Closed */) {
      return;
    }
    let nextHeight = this.topPart.size.height;
    if (this.state === 1 /* Closing */) {
      nextHeight += SLIDE_SPEED * deltaTime;
      if (nextHeight >= this.targetHeight) {
        nextHeight = this.targetHeight;
        this.state = 0 /* Closed */;
      }
    } else if (this.state === 3 /* Opening */) {
      nextHeight -= SLIDE_SPEED * deltaTime;
      if (nextHeight <= 0) {
        nextHeight = 0;
        this.state = 2 /* Open */;
      }
    }
    this.topPart.size.setHeight(nextHeight + 1);
    this.topPart.updateMatrix();
    this.bottomPart.size.setHeight(nextHeight + 1);
    this.bottomPart.updateMatrix();
    this.dirtyPaintBox();
  }
  close() {
    if (this.state !== 2 /* Open */) {
      return;
    }
    this.state = 1 /* Closing */;
  }
  open() {
    if (this.state !== 0 /* Closed */) {
      return;
    }
    this.state = 3 /* Opening */;
  }
};

// ../src/gameObjects/EditorInputHint.ts
init_modules_watch_stub();

// ../src/gameObjects/EnemyTank.ts
init_modules_watch_stub();

// ../src/tank/TankColor.ts
init_modules_watch_stub();

// ../src/tank/TankSkinAnimation.ts
init_modules_watch_stub();

// ../src/gameObjects/Tank.ts
init_modules_watch_stub();

// ../src/tank/TankAttributesFactory.ts
init_modules_watch_stub();

// ../src/tank/TankParty.ts
init_modules_watch_stub();

// ../src/tank/TankAttributesFactory.ts
var config = {
  list: [
    {
      selector: {
        party: "player" /* Player */,
        tier: "a" /* A */
      },
      attributes: {
        bulletMaxCount: 1,
        bulletRapidFireDelay: 0.16 /* Slow */,
        bulletSpeed: 600 /* Slow */,
        bulletTankDamage: 1,
        bulletWallDamage: 1 /* Low */,
        health: 1,
        moveSpeed: 180 /* Medium */
      }
    },
    {
      selector: {
        party: "player" /* Player */,
        tier: "b" /* B */
      },
      attributes: {
        bulletMaxCount: 1,
        bulletRapidFireDelay: 0.16 /* Slow */,
        bulletSpeed: 900 /* Fast */,
        bulletTankDamage: 1,
        bulletWallDamage: 1 /* Low */,
        health: 1,
        moveSpeed: 180 /* Medium */
      }
    },
    {
      selector: {
        party: "player" /* Player */,
        tier: "c" /* C */
      },
      attributes: {
        bulletMaxCount: 2,
        bulletRapidFireDelay: 0.04 /* Fast */,
        bulletSpeed: 900 /* Fast */,
        bulletTankDamage: 1,
        bulletWallDamage: 1 /* Low */,
        health: 1,
        moveSpeed: 180 /* Medium */
      }
    },
    {
      selector: {
        party: "player" /* Player */,
        tier: "d" /* D */
      },
      attributes: {
        bulletMaxCount: 2,
        bulletRapidFireDelay: 0.04 /* Fast */,
        bulletSpeed: 900 /* Fast */,
        bulletTankDamage: 1,
        bulletWallDamage: 2 /* High */,
        health: 1,
        moveSpeed: 180 /* Medium */
      }
    },
    {
      selector: {
        party: "enemy" /* Enemy */,
        tier: "a" /* A */
      },
      attributes: {
        bulletMaxCount: 1,
        bulletRapidFireDelay: 0.16 /* Slow */,
        bulletSpeed: 600 /* Slow */,
        bulletTankDamage: 1,
        bulletWallDamage: 1 /* Low */,
        health: 1,
        moveSpeed: 120 /* Slow */
      }
    },
    {
      selector: {
        party: "enemy" /* Enemy */,
        tier: "b" /* B */
      },
      attributes: {
        bulletMaxCount: 1,
        bulletRapidFireDelay: 0.16 /* Slow */,
        bulletSpeed: 600 /* Slow */,
        bulletTankDamage: 1,
        bulletWallDamage: 1 /* Low */,
        health: 1,
        moveSpeed: 240 /* Fast */
      }
    },
    {
      selector: {
        party: "enemy" /* Enemy */,
        tier: "c" /* C */
      },
      attributes: {
        bulletMaxCount: 1,
        bulletRapidFireDelay: 0.16 /* Slow */,
        bulletSpeed: 900 /* Fast */,
        bulletTankDamage: 1,
        bulletWallDamage: 1 /* Low */,
        health: 1,
        moveSpeed: 120 /* Slow */
      }
    },
    {
      selector: {
        party: "enemy" /* Enemy */,
        tier: "d" /* D */
      },
      attributes: {
        bulletMaxCount: 1,
        bulletRapidFireDelay: 0.16 /* Slow */,
        bulletSpeed: 600 /* Slow */,
        bulletTankDamage: 1,
        bulletWallDamage: 1 /* Low */,
        health: 4,
        moveSpeed: 120 /* Slow */
      }
    }
  ]
};
var TankAttributesFactory = class {
  static {
    __name(this, "TankAttributesFactory");
  }
  static create(type) {
    const foundItem = config.list.find((item) => {
      const { selector } = item;
      return selector.party === type.party && selector.tier === type.tier;
    });
    if (foundItem === void 0) {
      throw new Error(
        `Tank attributes not found for type = "${type.serialize()}"`
      );
    }
    const attributes = Object.assign({}, foundItem.attributes);
    return attributes;
  }
};

// ../src/tank/TankDeathReason.ts
init_modules_watch_stub();

// ../src/gameObjects/Shield.ts
init_modules_watch_stub();
var Shield = class extends GameObject {
  constructor() {
    super(64, 64);
    this.ignorePause = true;
    this.zIndex = SHIELD_Z_INDEX;
    this.painter = new SpritePainter();
  }
  static {
    __name(this, "Shield");
  }
  setup({ spriteLoader }) {
    this.animation = new Animation(
      spriteLoader.loadList(["shield.1", "shield.2"]),
      { delay: 0.05, loop: true }
    );
  }
  update({ deltaTime, gameState }) {
    if (gameState.hasChangedTo(1 /* Paused */)) {
      this.setVisible(false);
    }
    if (gameState.hasChangedTo(0 /* Playing */)) {
      this.setVisible(true);
    }
    if (gameState.is(1 /* Paused */)) {
      return;
    }
    this.animation.update(deltaTime);
    this.painter.sprite = this.animation.getCurrentFrame();
    this.setNeedsPaint();
  }
};

// ../src/gameObjects/Tank.ts
var SKIN_LAYER_DESCRIPTIONS = [{ opacity: 1 }, { opacity: 0.5 }];
var SNAP_SIZE = TILE_SIZE_MEDIUM;
var STUN_BLINK_DELAY = 0.1;
var MOVE_ANIMATION_MIN_DISTANCE = 0.25;
var Tank = class extends GameObject {
  constructor(type, behavior, partyIndex) {
    super(64, 64);
    this.collider = new SweptBoxCollider(this, true);
    this.tags = ["Tank" /* Tank */];
    // Tank index within it's party: players (0-1), enemies (0-19).
    this.partyIndex = -1;
    this.bullets = [];
    this.shield = null;
    this.fired = new Subject();
    this.died = new Subject();
    this.hit = new Subject();
    this.slided = new Subject();
    this.state = 0 /* Uninitialized */;
    this.freezeState = new State(false);
    this.isOnIce = false;
    // Per-sprite hit flash [0..1], set to full on receiveHit and decayed in
    // updateAnimation. Render-only cosmetic (see SpritePainter.flash) — never read
    // by the sim, so it can't affect replay determinism.
    this.hitFlash = 0;
    this.shieldTimer = new Timer();
    this.skinLayers = [];
    this.lastFireTimer = new Timer();
    this.slideTimer = new Timer();
    this.stunTimer = new Timer();
    this.stunBlinkTimer = new Timer();
    this.spawnCollisionState = new State(
      0 /* WaitUpdate */
    );
    this.playerCollisionState = new State(
      0 /* NotColliding */
    );
    this.tankCollisionResolution = 0 /* Unknown */;
    this.isCollisionAbusedByPlayer = false;
    this.lastSettledAnimationPosition = null;
    this.handleStunTimer = /* @__PURE__ */ __name(() => {
      this.stunBlinkTimer.stop();
      this.setVisible(true);
    }, "handleStunTimer");
    this.handleShieldTimer = /* @__PURE__ */ __name(() => {
      this.shield.removeSelf();
      this.shield = null;
    }, "handleShieldTimer");
    this.pivot.set(0.5, 0.5);
    this.type = type;
    this.behavior = behavior;
    this.partyIndex = partyIndex;
    this.attributes = TankAttributesFactory.create(this.type);
    this.shieldTimer.done.addListener(this.handleShieldTimer);
    this.stunTimer.done.addListener(this.handleStunTimer);
  }
  static {
    __name(this, "Tank");
  }
  setup(updateArgs) {
    const { collisionSystem } = updateArgs;
    this.collisionSystem = collisionSystem;
    collisionSystem.register(this.collider);
    this.behavior.setup(this, updateArgs);
    this.lastSettledAnimationPosition = this.position.clone();
    SKIN_LAYER_DESCRIPTIONS.forEach(() => {
      const layer = new GameObject();
      layer.size.copyFrom(this.size);
      const painter = new SpritePainter();
      painter.alignment = 3 /* MiddleCenter */;
      layer.painter = painter;
      this.skinLayers.push(layer);
      this.add(layer);
    });
  }
  update(updateArgs) {
    const { deltaTime, gameState } = updateArgs;
    const didMoveLastTick = this.consumeSettledMovementForAnimation();
    this.updateCollisionStates();
    this.shieldTimer.update(deltaTime);
    const shouldIdle = this.freezeState.hasChangedTo(true) || gameState.hasChangedTo(1 /* Paused */);
    if (shouldIdle) {
      this.idle();
    }
    const isIdle = this.freezeState.is(true) || gameState.is(1 /* Paused */);
    if (isIdle) {
      this.updateAnimation(deltaTime);
      return;
    }
    this.dirtyPaintBox();
    if (this.isSliding()) {
      if (this.isOnIce) {
        this.slideTimer.update(deltaTime);
        if (this.slideTimer.isDone()) {
          this.idle(false);
        } else {
          this.move(deltaTime);
        }
      } else {
        this.slideTimer.stop();
        this.idle(false);
      }
    }
    this.updateStun(deltaTime);
    this.behavior.update(this, updateArgs);
    this.lastFireTimer.update(deltaTime);
    this.updateAnimation(deltaTime, didMoveLastTick);
    this.collider.update();
    this.isOnIce = false;
    this.tankCollisionResolution = 0 /* Unknown */;
  }
  updateCollisionStates() {
    if (this.spawnCollisionState.is(1 /* WaitCollide */)) {
      this.enablePostSpawnCollision();
    } else if (this.spawnCollisionState.is(0 /* WaitUpdate */)) {
      this.spawnCollisionState.set(1 /* WaitCollide */);
    }
    if (this.playerCollisionState.is(2 /* WaitCollide */)) {
      this.playerCollisionState.set(0 /* NotColliding */);
    } else if (this.playerCollisionState.is(1 /* Colliding */)) {
      this.playerCollisionState.set(2 /* WaitCollide */);
    }
  }
  updateAnimation(deltaTime, advanceFrames = true) {
    this.skinAnimation.update(this, deltaTime, advanceFrames);
    const frame = this.skinAnimation.getCurrentFrame();
    if (this.hitFlash > 0) {
      this.hitFlash = Math.max(
        0,
        this.hitFlash - deltaTime / SPRITE_FLASH_DECAY_SECONDS
      );
    }
    this.skinLayers.forEach((layer, index) => {
      const description = SKIN_LAYER_DESCRIPTIONS[index];
      const painter = layer.painter;
      const sprite = frame.getSprite(index);
      painter.opacity = description.opacity;
      painter.sprite = sprite;
      painter.flash = this.hitFlash;
    });
  }
  consumeSettledMovementForAnimation() {
    if (this.lastSettledAnimationPosition === null) {
      this.lastSettledAnimationPosition = this.position.clone();
      return false;
    }
    const distance = this.position.distanceTo(this.lastSettledAnimationPosition);
    this.lastSettledAnimationPosition.copyFrom(this.position);
    return distance >= MOVE_ANIMATION_MIN_DISTANCE;
  }
  collide(collision) {
    this.collideIce(collision);
    this.collideSpawnedTanks(collision);
    this.collideWalls(collision);
    this.collideTanks(collision);
    this.collideBullets(collision);
  }
  fire(ignoreLocalLimits = false) {
    if (!ignoreLocalLimits && this.bullets.length >= this.attributes.bulletMaxCount) {
      return;
    }
    if (!ignoreLocalLimits && this.lastFireTimer.isActive()) {
      return;
    }
    const bullet = new Bullet(
      this.partyIndex,
      this.attributes.bulletSpeed,
      this.attributes.bulletTankDamage,
      this.attributes.bulletWallDamage
    );
    this.updateWorldMatrix(true);
    this.add(bullet);
    bullet.updateMatrix();
    bullet.setCenter(this.getSelfCenter());
    bullet.translateY(this.size.height / 2 - bullet.size.height / 2);
    bullet.updateMatrix();
    this.parent.attach(bullet);
    if (this.tags.includes("Player" /* Player */)) {
      bullet.tags.push("Player" /* Player */);
    } else if (this.tags.includes("Enemy" /* Enemy */)) {
      bullet.tags.push("Enemy" /* Enemy */);
    }
    this.bullets.push(bullet);
    bullet.died.addListener(() => {
      this.bullets = this.bullets.filter((tankBullet) => {
        return tankBullet !== bullet;
      });
    });
    this.fired.notify(null);
    if (!ignoreLocalLimits) {
      this.lastFireTimer.reset(this.attributes.bulletRapidFireDelay);
    }
    return true;
  }
  fireFromNetwork(x, y, rotation) {
    const localPosition = this.position.clone();
    const localRotation = this.rotation;
    try {
      this.position.set(x, y);
      this.rotation = rotation;
      this.updateMatrix(true);
      const bulletCount = this.bullets.length;
      if (!this.fire(true) || this.bullets.length === bulletCount) {
        return null;
      }
      return this.bullets[this.bullets.length - 1];
    } finally {
      this.position.copyFrom(localPosition);
      this.rotation = localRotation;
      this.updateMatrix(true);
    }
  }
  move(deltaTime) {
    if (this.state !== 2 /* Moving */) {
      this.state = 2 /* Moving */;
    }
    this.translateY(this.attributes.moveSpeed * deltaTime);
    this.updateMatrix(true);
  }
  updateStun(deltaTime) {
    if (!this.isStunned()) {
      return;
    }
    if (this.stunBlinkTimer.isDone()) {
      this.stunBlinkTimer.reset(STUN_BLINK_DELAY);
      this.setVisible(!this.getVisible());
    }
    this.stunBlinkTimer.update(deltaTime);
    this.stunTimer.update(deltaTime);
  }
  applyNetworkMovement(rotation, moving, deltaX, deltaY) {
    this.rotation = rotation;
    this.state = moving ? 2 /* Moving */ : 1 /* Idle */;
    this.position.set(
      this.position.x + deltaX,
      this.position.y + deltaY
    );
    this.updateMatrix(true);
  }
  idle(checkIce = true) {
    if (this.state !== 1 /* Idle */) {
      this.state = 1 /* Idle */;
    }
    if (checkIce && this.tags.includes("Player" /* Player */) && this.isOnIce && !this.isSliding()) {
      this.slided.notify(null);
      this.slideTimer.reset(ICE_SLIDE_DURATION);
    }
  }
  rotate(rotation) {
    if (rotation !== this.rotation) {
      if (rotation === 0 /* Up */ || rotation === 180 /* Down */) {
        this.position.snapX(SNAP_SIZE);
      } else if (rotation === 270 /* Left */ || rotation === 90 /* Right */) {
        this.position.snapY(SNAP_SIZE);
      }
    }
    super.rotate(rotation);
    return this;
  }
  die(reason = "bullet" /* Bullet */, hitterPartyIndex = null) {
    const event = {
      hitterPartyIndex,
      reason
    };
    this.died.notify(event);
    this.collider.unregister();
  }
  activateShield(duration) {
    if (this.shield !== null) {
      this.shield.removeSelf();
      this.shieldTimer.stop();
      this.shield = null;
    }
    this.shield = new Shield();
    this.shield.updateMatrix();
    this.shield.setCenter(this.getSelfCenter());
    this.add(this.shield);
    this.shieldTimer.reset(duration);
  }
  isAlive() {
    return this.attributes.health > 0;
  }
  receiveHit(damage, hitterPartyIndex) {
    this.attributes.health = Math.max(0, this.attributes.health - damage);
    this.hitFlash = SPRITE_FLASH_HIT * CAMERA_SHAKE_INTENSITY;
    this.hit.notify(null);
    if (!this.isAlive()) {
      this.die("bullet" /* Bullet */, hitterPartyIndex);
    }
  }
  isSliding() {
    return this.slideTimer.isActive();
  }
  isStunned() {
    return this.stunTimer.isActive();
  }
  collideIce(collision) {
    if (this.tags.includes("Enemy" /* Enemy */)) {
      return;
    }
    const iceTileContacts = collision.contacts.filter((contact) => {
      return contact.collider.object.tags.includes("Ice" /* Ice */);
    });
    if (iceTileContacts.length === 0) {
      return;
    }
    const selfBox = this.getWorldBoundingBox();
    const selfCenter = selfBox.getCenter();
    const sumBox = new BoundingBox();
    for (const contact of iceTileContacts) {
      sumBox.unionWith(contact.box);
    }
    const isOnIce = sumBox.containsPoint(selfCenter);
    this.isOnIce = isOnIce;
  }
  // Try to solve the issue when some alive tank is
  // moving on top of a spawn and at the same time new tank has been spawned.
  // Not to toss the tanks around we simply don't enable collisions for
  // a newly spawned tank until these both tanks move away from each other.
  collideSpawnedTanks(collision) {
    if (this.spawnCollisionState.is(1 /* WaitCollide */)) {
      const tankContacts = collision.contacts.filter((contact) => {
        return contact.collider.object.tags.includes("Tank" /* Tank */);
      });
      if (tankContacts.length > 0) {
        this.spawnCollisionState.set(0 /* WaitUpdate */);
      } else {
        this.enablePostSpawnCollision();
      }
    }
  }
  collideWalls(collision) {
    const wallContacts = [];
    for (const contact of collision.contacts) {
      const { tags } = contact.collider.object;
      if (tags.includes("BlockMove" /* BlockMove */) && !tags.includes("Tank" /* Tank */)) {
        wallContacts.push(contact);
      }
    }
    if (wallContacts.length === 0) {
      return;
    }
    const closestWallContacts = this.getClosestContacts(
      wallContacts,
      this.collider.getPrevBox()
    );
    if (closestWallContacts.length === 0) {
      return;
    }
    const firstWallContact = closestWallContacts[0];
    const otherCurrentBox = firstWallContact.collider.getCurrentBox();
    this.resolveMinkowski(otherCurrentBox, true);
  }
  resolveMinkowski(otherBox, shouldSnap = false) {
    const selfCurrentBox = this.collider.getCurrentBox();
    const selfPrevBox = this.collider.getPrevBox();
    const selfPrevCenter = selfPrevBox.getCenter();
    const minkowskiBox = otherBox.clone().minkowskiSum(selfCurrentBox);
    const minkowskiRect = minkowskiBox.toRect();
    const minkowskiCenter = minkowskiBox.getCenter();
    const localPrev = new Vector(
      selfPrevCenter.x - minkowskiCenter.x,
      selfPrevCenter.y - minkowskiCenter.y
    );
    const blTrLocalDiag = new Vector(
      minkowskiRect.width / 2,
      minkowskiRect.height / 2
    );
    const tlBrLocalDiag = new Vector(
      minkowskiRect.width / 2,
      -minkowskiRect.height / 2
    );
    const blTrCrossProduct = localPrev.cross(blTrLocalDiag);
    const tlBrCrossProduct = localPrev.cross(tlBrLocalDiag);
    const isTop = blTrCrossProduct < 0 && tlBrCrossProduct < 0;
    const isBottom = blTrCrossProduct > 0 && tlBrCrossProduct > 0;
    const isLeft = blTrCrossProduct > 0 && tlBrCrossProduct < 0;
    const isRight = blTrCrossProduct < 0 && tlBrCrossProduct > 0;
    if (isTop) {
      this.position.subY(selfCurrentBox.min.y - otherBox.max.y);
      if (shouldSnap) {
        this.position.snapY(SNAP_SIZE);
      }
    } else if (isBottom) {
      this.position.subY(selfCurrentBox.max.y - otherBox.min.y);
      if (shouldSnap) {
        this.position.snapY(SNAP_SIZE);
      }
    } else if (isLeft) {
      this.position.subX(selfCurrentBox.min.x - otherBox.max.x);
      if (shouldSnap) {
        this.position.snapX(SNAP_SIZE);
      }
    } else if (isRight) {
      this.position.subX(selfCurrentBox.max.x - otherBox.min.x);
      if (shouldSnap) {
        this.position.snapX(SNAP_SIZE);
      }
    }
    this.updateMatrix(true);
    this.collider.update();
  }
  collideTanks(collision) {
    if (!this.tags.includes("BlockMove" /* BlockMove */)) {
      return;
    }
    const tankContacts = [];
    const playerTankContacts = [];
    const wallContacts = [];
    for (const contact of collision.contacts) {
      const { tags } = contact.collider.object;
      if (tags.includes("BlockMove" /* BlockMove */) && tags.includes("Tank" /* Tank */)) {
        tankContacts.push(contact);
      }
      if (tags.includes("BlockMove" /* BlockMove */) && !tags.includes("Tank" /* Tank */)) {
        wallContacts.push(contact);
      }
      if (tags.includes("Tank" /* Tank */) && tags.includes("Player" /* Player */)) {
        playerTankContacts.push(contact);
      }
    }
    if (this.playerCollisionState.is(2 /* WaitCollide */)) {
      if (playerTankContacts.length > 0) {
        this.playerCollisionState.set(1 /* Colliding */);
      } else {
        this.playerCollisionState.set(0 /* NotColliding */);
      }
    }
    if (tankContacts.length === 0) {
      return;
    }
    const closestTankContacts = this.getClosestContacts(
      tankContacts,
      this.collider.getPrevBox()
    );
    const firstTankContact = closestTankContacts[0];
    const otherCollider = firstTankContact.collider;
    const other = otherCollider.object;
    if (other.tankCollisionResolution === 1 /* Self */) {
      return;
    }
    const selfCurrentBox = this.collider.getCurrentBox();
    const selfPrevBox = this.collider.getPrevBox();
    const isSelfMoving = !selfCurrentBox.equals(selfPrevBox);
    const otherPrevBox = otherCollider.getPrevBox();
    const otherCurrentBox = otherCollider.getCurrentBox();
    const isOtherMoving = !otherCurrentBox.equals(otherPrevBox);
    if (!isOtherMoving && !isSelfMoving) {
      return;
    }
    if (isOtherMoving && !isSelfMoving) {
      return;
    }
    const selfDirection = this.collider.getDirection().normalize();
    const otherDirection = otherCollider.getDirection().normalize();
    if (!isOtherMoving && isSelfMoving) {
      if (this.tags.includes("Enemy" /* Enemy */) && other.tags.includes("Player" /* Player */) && !this.isFrozenCollision(other)) {
        if (this.playerCollisionState.is(1 /* Colliding */)) {
          return;
        }
        const roundedPosition = this.position.clone().round();
        const isMovingOnGridHorizontally = (selfDirection.x === 1 || selfDirection.x === -1) && roundedPosition.y % TILE_SIZE_MEDIUM === 0;
        const isMovingOnGridVertically = (selfDirection.y === 1 || selfDirection.y === -1) && roundedPosition.x % TILE_SIZE_MEDIUM === 0;
        const intersectionBox = selfCurrentBox.clone().intersectWith(otherCurrentBox);
        const intersectionRect = intersectionBox.toRect();
        const thresholdWidth = (this.size.width - BULLET_WIDTH) / 2;
        const thresholdHeight = (this.size.height - BULLET_WIDTH) / 2;
        if (isMovingOnGridVertically && intersectionRect.width <= thresholdWidth) {
          this.playerCollisionState.set(1 /* Colliding */);
          return;
        }
        if (isMovingOnGridHorizontally && intersectionRect.height <= thresholdHeight) {
          this.playerCollisionState.set(1 /* Colliding */);
          return;
        }
      }
      this.resolveMinkowski(otherPrevBox);
      this.tankCollisionResolution = 1 /* Self */;
      return;
    }
    if (this.tags.includes("Player" /* Player */) && other.tags.includes("Enemy" /* Enemy */) && !this.isFrozenCollision(other)) {
      if (other.playerCollisionState.is(1 /* Colliding */) || other.playerCollisionState.is(2 /* WaitCollide */)) {
        return;
      }
    }
    if (this.tags.includes("Enemy" /* Enemy */) && other.tags.includes("Player" /* Player */) && !this.isFrozenCollision(other)) {
      if (this.playerCollisionState.is(1 /* Colliding */) || other.playerCollisionState.is(2 /* WaitCollide */)) {
        return;
      }
    }
    if (other.tankCollisionResolution === 2 /* Both */) {
      this.resolveMinkowski(otherCurrentBox);
      return;
    }
    const hasWallCollision = wallContacts.length > 0;
    const selfCurrentCenter = selfCurrentBox.getCenter();
    const otherCurrentCenter = otherCurrentBox.getCenter();
    const selfCollisionDirection = otherCurrentCenter.clone().sub(selfCurrentCenter).normalize();
    const otherCollisionDirection = selfCurrentCenter.clone().sub(otherCurrentCenter).normalize();
    const selfDot = selfDirection.dot(selfCollisionDirection);
    const otherDot = otherDirection.dot(otherCollisionDirection);
    let isSelfInitiator = selfDot > 0;
    let isOtherInitiator = otherDot > 0;
    if (selfDot > 0 && otherDot > 0) {
      if (selfDot === otherDot) {
        isSelfInitiator = true;
        isOtherInitiator = true;
      } else {
        isSelfInitiator = selfDot > otherDot;
        isOtherInitiator = otherDot > selfDot;
      }
    }
    if (this.tags.includes("Player" /* Player */) && other.tags.includes("Enemy" /* Enemy */) && selfDot > 0) {
      this.resolveMinkowski(otherPrevBox);
      this.tankCollisionResolution = 1 /* Self */;
      return;
    }
    if (this.tags.includes("Enemy" /* Enemy */) && other.tags.includes("Player" /* Player */) && otherDot > 0) {
      return;
    }
    if (hasWallCollision) {
      isSelfInitiator = false;
      isOtherInitiator = true;
    }
    if (isSelfInitiator && !isOtherInitiator) {
      this.resolveMinkowski(otherPrevBox);
      this.tankCollisionResolution = 1 /* Self */;
      return;
    }
    if (!isSelfInitiator && isOtherInitiator) {
      return;
    }
    if (isSelfInitiator && isOtherInitiator) {
      this.resolveByRollback(this.collider.getDirection());
      this.tankCollisionResolution = 2 /* Both */;
      return;
    }
    const otherCollision = this.collisionSystem.getCollisionByCollider(
      other.collider
    );
    const selfContactsExceptOther = collision.contacts.filter((contact) => {
      return contact.collider !== other.collider;
    });
    const otherContactsExceptSelf = otherCollision.contacts.filter(
      (contact) => {
        return contact.collider !== this.collider;
      }
    );
    if (otherContactsExceptSelf.length > 0 && selfContactsExceptOther.length === 0) {
      this.resolveMinkowski(otherPrevBox);
      this.tankCollisionResolution = 1 /* Self */;
      return;
    }
    if (this.tags.includes("Enemy" /* Enemy */) && other.tags.includes("Enemy" /* Enemy */) && isSelfMoving) {
      this.resolveMinkowski(otherPrevBox);
      this.tankCollisionResolution = 1 /* Self */;
      return;
    }
  }
  collideBullets(collision) {
    const bulletContacts = collision.contacts.filter((contact) => {
      return contact.collider.object.tags.includes("Bullet" /* Bullet */);
    });
    if (bulletContacts.length === 0) {
      return;
    }
    bulletContacts.forEach((contact) => {
      const bullet = contact.collider.object;
      if (this.bullets.includes(bullet)) {
        return;
      }
      if (bullet.isSpent()) {
        return;
      }
      if (bullet.isLocalDamageDisabled()) {
        return;
      }
      if (this.shield !== null) {
        bullet.nullify();
        return;
      }
      if (bullet.tags.includes("Enemy" /* Enemy */) && this.tags.includes("Enemy" /* Enemy */)) {
        return;
      }
      bullet.explode();
      if (bullet.tags.includes("Player" /* Player */) && this.tags.includes("Player" /* Player */)) {
        if (this.isStunned()) {
          return;
        }
        this.stunTimer.reset(FRIENDLY_FIRE_STUN_DURATION);
        this.stunBlinkTimer.reset(STUN_BLINK_DELAY);
        this.setVisible(false);
        this.idle();
        return;
      }
      this.receiveHit(bullet.tankDamage, bullet.ownerPartyIndex);
    });
  }
  resolveByRollback(direction) {
    this.position.sub(direction);
    this.updateMatrix(true);
    this.collider.update();
  }
  isFrozenCollision(other) {
    return this.freezeState.is(true) || other.freezeState.is(true);
  }
  getClosestContacts(contacts, selfBox) {
    let minDistance = null;
    for (const contact of contacts) {
      const prevBox = selfBox;
      const distance = prevBox.distanceCenterToCenter(contact.box);
      if (minDistance === null || distance < minDistance) {
        minDistance = distance;
      }
    }
    const closestContacts = [];
    for (const contact of contacts) {
      const prevBox = selfBox;
      const distance = prevBox.distanceCenterToCenter(contact.box);
      if (distance === minDistance) {
        closestContacts.push(contact);
      }
    }
    return closestContacts;
  }
  enablePostSpawnCollision() {
    this.spawnCollisionState.set(3 /* Resolved */);
    this.tags.push("BlockMove" /* BlockMove */);
  }
  getDirection() {
    if (this.rotation === 0 /* Up */) {
      return new Vector(0, -1);
    }
    if (this.rotation === 180 /* Down */) {
      return new Vector(0, 1);
    }
    if (this.rotation === 270 /* Left */) {
      return new Vector(-1, 0);
    }
    if (this.rotation === 90 /* Right */) {
      return new Vector(1, 0);
    }
  }
  /**
   * @deprecated Use resolveMinkowski, left for history reference
   */
  resolveBasedOnDirection(selfCurrentBox, otherBox, shouldSnap = false) {
    const direction = this.collider.getDirection().normalize();
    if (direction.y < 0) {
      this.position.subY(Math.max(0, selfCurrentBox.min.y - otherBox.max.y));
      if (shouldSnap) {
        this.position.snapY(SNAP_SIZE);
      }
    } else if (direction.y > 0) {
      this.position.subY(Math.max(0, selfCurrentBox.max.y - otherBox.min.y));
      if (shouldSnap) {
        this.position.snapY(SNAP_SIZE);
      }
    } else if (direction.x < 0) {
      this.position.subX(Math.max(0, selfCurrentBox.min.x - otherBox.max.x));
      if (shouldSnap) {
        this.position.snapX(SNAP_SIZE);
      }
    } else if (direction.x > 0) {
      this.position.subX(Math.max(0, selfCurrentBox.max.x - otherBox.min.x));
      if (shouldSnap) {
        this.position.snapX(SNAP_SIZE);
      }
    }
    this.updateMatrix(true);
    this.collider.update();
  }
  /**
   * @deprecated Use resolveMinkowski, left for history reference
   */
  resolveBasedOnRotation(selfBox, otherBox, shouldSnap = false) {
    const rotation = this.getWorldRotation();
    if (rotation == 0 /* Up */) {
      this.position.addY(otherBox.max.y - selfBox.min.y);
      if (shouldSnap) {
        this.position.snapY(SNAP_SIZE);
      }
    } else if (rotation === 180 /* Down */) {
      this.position.addY(otherBox.min.y - selfBox.max.y);
      if (shouldSnap) {
        this.position.snapY(SNAP_SIZE);
      }
    } else if (rotation === 270 /* Left */) {
      this.position.addX(otherBox.max.x - selfBox.min.x);
      if (shouldSnap) {
        this.position.snapX(SNAP_SIZE);
      }
    } else if (rotation === 90 /* Right */) {
      this.position.addX(otherBox.min.x - selfBox.max.x);
      if (shouldSnap) {
        this.position.snapX(SNAP_SIZE);
      }
    }
    this.updateMatrix(true);
    this.collider.update();
  }
};

// ../src/tank/TankSkinAnimation.ts
var TankSkinAnimation = class {
  constructor(spriteLoader, type, colors) {
    this.rotation = 0 /* Up */;
    this.tankState = 0 /* Uninitialized */;
    this.moveAnimationMap = new RotationMap();
    this.idleAnimationMap = new RotationMap();
    this.idleAnimationMap.set(
      0 /* Up */,
      new TankIdleAnimation(spriteLoader, type, colors, 0 /* Up */)
    );
    this.idleAnimationMap.set(
      180 /* Down */,
      new TankIdleAnimation(spriteLoader, type, colors, 180 /* Down */)
    );
    this.idleAnimationMap.set(
      270 /* Left */,
      new TankIdleAnimation(spriteLoader, type, colors, 270 /* Left */)
    );
    this.idleAnimationMap.set(
      90 /* Right */,
      new TankIdleAnimation(spriteLoader, type, colors, 90 /* Right */)
    );
    this.moveAnimationMap.set(
      0 /* Up */,
      new TankMoveAnimation(spriteLoader, type, colors, 0 /* Up */)
    );
    this.moveAnimationMap.set(
      180 /* Down */,
      new TankMoveAnimation(spriteLoader, type, colors, 180 /* Down */)
    );
    this.moveAnimationMap.set(
      270 /* Left */,
      new TankMoveAnimation(spriteLoader, type, colors, 270 /* Left */)
    );
    this.moveAnimationMap.set(
      90 /* Right */,
      new TankMoveAnimation(spriteLoader, type, colors, 90 /* Right */)
    );
    this.currentAnimationMap = this.idleAnimationMap;
  }
  static {
    __name(this, "TankSkinAnimation");
  }
  update(tank, deltaTime, advanceFrames = true) {
    this.rotation = tank.rotation;
    if (tank.state === this.tankState) {
      const animation2 = this.currentAnimationMap.get(this.rotation);
      if (advanceFrames) {
        animation2.update(deltaTime);
      }
      return;
    }
    this.tankState = tank.state;
    this.currentAnimationMap = tank.state === 1 /* Idle */ ? this.idleAnimationMap : this.moveAnimationMap;
    const animation = this.currentAnimationMap.get(tank.rotation);
    animation.reset();
  }
  // Tank might lose his drop, use it remove drop animation frames
  updateFrames() {
    this.idleAnimationMap.forEach((animation) => {
      animation.updateFrames();
    });
    this.moveAnimationMap.forEach((animation) => {
      animation.updateFrames();
    });
  }
  getCurrentFrame() {
    const animation = this.currentAnimationMap.get(this.rotation);
    return animation.getCurrentFrame();
  }
};

// ../src/gameObjects/EnemyTank.ts
var EnemyTank = class extends Tank {
  constructor() {
    super(...arguments);
    this.tags = ["Tank" /* Tank */, "Enemy" /* Enemy */];
    this.zIndex = ENEMY_TANK_Z_INDEX;
    this.healthSkinAnimations = /* @__PURE__ */ new Map();
    this.dropBlinkElapsed = 0;
    this.networkControlled = false;
  }
  static {
    __name(this, "EnemyTank");
  }
  setNetworkControlled(controlled) {
    this.networkControlled = controlled;
    return this;
  }
  beginNetworkDeathGrace() {
    this.tags = ["Enemy" /* Enemy */];
    this.setVisible(false);
  }
  finishNetworkRemoval() {
    this.collider.unregister();
    this.removeSelf();
  }
  applyNetworkHealth(health) {
    const wasHit = health < this.attributes.health;
    this.attributes.health = Math.max(0, health);
    const animation = this.healthSkinAnimations.get(this.attributes.health);
    if (animation !== void 0) {
      this.skinAnimation = animation;
    }
    if (wasHit) {
      this.hit.notify(null);
      if (this.type.hasDrop) {
        this.discardDrop();
      }
    }
  }
  setup(updateArgs) {
    const { audioLoader, spriteLoader } = updateArgs;
    this.hitSound = audioLoader.load("hit.enemy");
    if (this.type.hasDrop) {
      this.ignorePause = true;
    }
    if (this.type.tier === "d" /* D */) {
      this.healthSkinAnimations.set(
        4,
        new TankSkinAnimation(spriteLoader, this.type, [
          "default" /* Default */,
          "secondary" /* Secondary */
        ])
      );
      this.healthSkinAnimations.set(
        3,
        new TankSkinAnimation(spriteLoader, this.type, [
          "default" /* Default */,
          "primary" /* Primary */
        ])
      );
      this.healthSkinAnimations.set(
        2,
        new TankSkinAnimation(spriteLoader, this.type, [
          "secondary" /* Secondary */,
          "primary" /* Primary */
        ])
      );
    }
    this.healthSkinAnimations.set(
      1,
      new TankSkinAnimation(spriteLoader, this.type, ["default" /* Default */])
    );
    this.skinAnimation = this.healthSkinAnimations.get(this.attributes.health);
    super.setup(updateArgs);
  }
  update(updateArgs) {
    const { gameState } = updateArgs;
    if (this.networkControlled) {
      this.updateCollisionStates();
      this.collider.update();
      this.updateAnimation(updateArgs.deltaTime);
      this.setNeedsPaint();
      return;
    }
    const shouldIdle = this.freezeState.hasChangedTo(true) || gameState.hasChangedTo(1 /* Paused */);
    if (shouldIdle) {
      this.idle();
    }
    const isIdle = this.freezeState.is(true) || gameState.is(1 /* Paused */);
    if (isIdle) {
      this.updateCollisionStates();
      this.collider.update();
      this.tankCollisionResolution = 0 /* Unknown */;
      this.updateAnimation(updateArgs.deltaTime);
      this.setNeedsPaint();
      return;
    }
    super.update(updateArgs);
  }
  receiveHit(damage, hitterPartyIndex) {
    if (this.networkControlled) {
      return;
    }
    super.receiveHit(damage, hitterPartyIndex);
    if (!this.isAlive()) {
      return;
    }
    this.hitSound.play();
    this.discardDrop();
    this.skinAnimation = this.healthSkinAnimations.get(this.attributes.health);
  }
  collide(collision) {
    if (this.networkControlled) {
      this.collideBullets(collision);
      return;
    }
    super.collide(collision);
  }
  discardDrop() {
    this.type.hasDrop = false;
    this.ignorePause = false;
    this.dropBlinkElapsed = 0;
    this.healthSkinAnimations.forEach((animation) => {
      animation.updateFrames();
    });
    return this;
  }
  updateAnimation(deltaTime, advanceFrames = true) {
    super.updateAnimation(deltaTime, advanceFrames);
    const shouldTint = this.type.hasDrop && this.isDropBlinkVisible(deltaTime);
    this.skinLayers.forEach((layer) => {
      const painter = layer.painter;
      painter.tintColor = shouldTint ? ENEMY_DROP_BLINK_COLOR : null;
      painter.tintAlpha = shouldTint ? ENEMY_DROP_BLINK_ALPHA : 0;
    });
  }
  isDropBlinkVisible(deltaTime) {
    if (!this.type.hasDrop) {
      this.dropBlinkElapsed = 0;
      return false;
    }
    this.dropBlinkElapsed += deltaTime;
    return Math.floor(this.dropBlinkElapsed / ENEMY_DROP_BLINK_INTERVAL) % 2 === 1;
  }
};

// ../src/gameObjects/Field.ts
init_modules_watch_stub();
var Field = class extends GameObject {
  static {
    __name(this, "Field");
  }
  constructor(width, height) {
    super(width, height);
  }
};

// ../src/gameObjects/GameOverNotice.ts
init_modules_watch_stub();
var GameOverNotice = class extends GameObject {
  constructor() {
    super(124, 60);
    this.zIndex = GAME_OVER_NOTICE_Z_INDEX;
    this.painter = new SpritePainter();
  }
  static {
    __name(this, "GameOverNotice");
  }
  setup({ spriteLoader }) {
    this.painter.sprite = spriteLoader.load("ui.gameOver");
  }
};

// ../src/gameObjects/GhostBullet.ts
init_modules_watch_stub();

// ../src/gameObjects/GhostTank.ts
init_modules_watch_stub();

// ../src/gameObjects/GroundField.ts
init_modules_watch_stub();
var GroundFieldPainter = class extends Painter {
  constructor() {
    super(...arguments);
    this.sprites = [];
    this.tileSize = TILE_SIZE_MEDIUM;
    this.destinationRect = new Rect();
  }
  static {
    __name(this, "GroundFieldPainter");
  }
  paint(context, renderObject) {
    if (this.sprites.length === 0 || !this.sprites[0].isImageLoaded()) {
      return;
    }
    const box = renderObject.getWorldBoundingBox();
    const fieldX = box.min.x;
    const fieldY = box.min.y;
    const fieldWidth = box.max.x - box.min.x;
    const fieldHeight = box.max.y - box.min.y;
    const cols = Math.ceil(fieldWidth / this.tileSize);
    const rows = Math.ceil(fieldHeight / this.tileSize);
    const cull = context.getWorldCullBounds();
    let startCol = 0;
    let endCol = cols;
    let startRow = 0;
    let endRow = rows;
    if (cull !== null) {
      startCol = Math.max(0, Math.floor((cull.minX - fieldX) / this.tileSize));
      endCol = Math.min(cols, Math.ceil((cull.maxX - fieldX) / this.tileSize));
      startRow = Math.max(0, Math.floor((cull.minY - fieldY) / this.tileSize));
      endRow = Math.min(rows, Math.ceil((cull.maxY - fieldY) / this.tileSize));
      if (startCol >= endCol || startRow >= endRow) {
        return;
      }
    }
    const dest = this.destinationRect;
    dest.width = this.tileSize;
    dest.height = this.tileSize;
    for (let row = startRow; row < endRow; row += 1) {
      dest.y = fieldY + row * this.tileSize;
      for (let col = startCol; col < endCol; col += 1) {
        const hash = Math.abs(col * 73856093 ^ row * 19349663);
        const sprite = this.sprites[hash % this.sprites.length];
        dest.x = fieldX + col * this.tileSize;
        context.drawImage(sprite.image, sprite.sourceRect, dest);
      }
    }
  }
};
var GroundField = class extends GameObject {
  constructor() {
    super(...arguments);
    this.zIndex = GROUND_FIELD_Z_INDEX;
    this.painter = new GroundFieldPainter();
  }
  static {
    __name(this, "GroundField");
  }
  setup({ spriteLoader }) {
    this.painter.sprites = spriteLoader.loadSequence("terrain.grass");
  }
};

// ../src/gameObjects/LevelInputHint.ts
init_modules_watch_stub();

// ../src/gameObjects/LevelSelector.ts
init_modules_watch_stub();

// ../src/gameObjects/Minimap.ts
init_modules_watch_stub();

// ../src/gameObjects/PlayerTank.ts
init_modules_watch_stub();

// ../src/tank/TankColorFactory.ts
init_modules_watch_stub();
var COLORS = ["primary" /* Primary */, "secondary" /* Secondary */];
var TankColorFactory = class {
  static {
    __name(this, "TankColorFactory");
  }
  static createPlayerColor(playerIndex) {
    return COLORS[playerIndex];
  }
};

// ../src/tank/TankType.ts
init_modules_watch_stub();
var TankType3 = class _TankType {
  static {
    __name(this, "TankType");
  }
  constructor(party, tier, hasDrop = false) {
    this.party = party;
    this.tier = tier;
    this.hasDrop = hasDrop;
  }
  setHasDrop(hasDrop) {
    this.hasDrop = hasDrop;
    return this;
  }
  clone() {
    return new _TankType(this.party, this.tier);
  }
  increaseTier(targetTier = null) {
    if (targetTier !== null) {
      this.tier = targetTier;
      return this;
    }
    switch (this.tier) {
      case "a" /* A */:
        this.tier = "b" /* B */;
        break;
      case "b" /* B */:
        this.tier = "c" /* C */;
        break;
      case "c" /* C */:
        this.tier = "d" /* D */;
      default:
        break;
    }
    return this;
  }
  isMaxTier() {
    return this.tier === "d" /* D */;
  }
  equals(other) {
    return this.party === other.party && this.tier === other.tier && this.hasDrop === other.hasDrop;
  }
  serialize() {
    return `${this.party}-${this.tier}-${this.hasDrop}`;
  }
  toString() {
    return this.serialize();
  }
  static PlayerA() {
    return new _TankType("player" /* Player */, "a" /* A */);
  }
  static PlayerB() {
    return new _TankType("player" /* Player */, "b" /* B */);
  }
  static PlayerC() {
    return new _TankType("player" /* Player */, "c" /* C */);
  }
  static PlayerD() {
    return new _TankType("player" /* Player */, "d" /* D */);
  }
  static EnemyA() {
    return new _TankType("enemy" /* Enemy */, "a" /* A */);
  }
  static EnemyB() {
    return new _TankType("enemy" /* Enemy */, "b" /* B */);
  }
  static EnemyC() {
    return new _TankType("enemy" /* Enemy */, "c" /* C */);
  }
  static EnemyD() {
    return new _TankType("enemy" /* Enemy */, "d" /* D */);
  }
};

// ../src/gameObjects/PlayerTank.ts
var PlayerTank = class extends Tank {
  constructor() {
    super(...arguments);
    this.upgraded = new Subject();
    this.tags = ["Tank" /* Tank */, "Player" /* Player */];
    this.zIndex = PLAYER_TANK_Z_INDEX;
    this.tierSkinAnimations = /* @__PURE__ */ new Map();
    this.colors = [];
    this.speedBoostTimer = new Timer();
    this.speedBoostMultiplier = 1;
    this.networkControlled = false;
    // Run-long trait boosts (trading/staking), applied deterministically:
    // hull/armor add flat bonus health, engine multiplies move speed for the
    // whole run (unlike the temporary powerup speedBoostMultiplier). Values
    // come from the session at spawn (or from the replay being re-enacted).
    this.runBoostBonusHealth = 0;
    this.runBoostSpeedMultiplier = 1;
    this.handleSpeedBoostTimer = /* @__PURE__ */ __name(() => {
      this.speedBoostMultiplier = 1;
      this.applySpeedBoost();
    }, "handleSpeedBoostTimer");
  }
  static {
    __name(this, "PlayerTank");
  }
  setup(updateArgs) {
    const { spriteLoader } = updateArgs;
    this.colors.push(TankColorFactory.createPlayerColor(this.partyIndex));
    this.tierSkinAnimations.set(
      "a" /* A */,
      new TankSkinAnimation(spriteLoader, TankType3.PlayerA(), this.colors)
    );
    this.tierSkinAnimations.set(
      "b" /* B */,
      new TankSkinAnimation(spriteLoader, TankType3.PlayerB(), this.colors)
    );
    this.tierSkinAnimations.set(
      "c" /* C */,
      new TankSkinAnimation(spriteLoader, TankType3.PlayerC(), this.colors)
    );
    this.tierSkinAnimations.set(
      "d" /* D */,
      new TankSkinAnimation(spriteLoader, TankType3.PlayerD(), this.colors)
    );
    this.skinAnimation = this.tierSkinAnimations.get(this.type.tier);
    super.setup(updateArgs);
    this.speedBoostTimer.done.addListener(this.handleSpeedBoostTimer);
  }
  update(updateArgs) {
    if (this.networkControlled) {
      this.updateCollisionStates();
      this.shieldTimer.update(updateArgs.deltaTime);
      this.updateStun(updateArgs.deltaTime);
      this.behavior.update(this, updateArgs);
      this.lastFireTimer.update(updateArgs.deltaTime);
      this.updateAnimation(updateArgs.deltaTime);
      this.collider.update();
      this.speedBoostTimer.update(updateArgs.deltaTime);
      this.setNeedsPaint();
      return;
    }
    super.update(updateArgs);
    this.speedBoostTimer.update(updateArgs.deltaTime);
  }
  setNetworkControlled(controlled) {
    this.networkControlled = controlled;
    return this;
  }
  setNetworkTier(tier) {
    if (this.type.tier === tier) {
      return;
    }
    this.type.increaseTier(tier);
    this.applyTier(true);
  }
  // If tier is provided - it means that specific tier needs to be activated
  // when transitioning to the next level.next
  // If not - then most likely powerup has been picked up and we simply need
  // to upgrade the tank one tier up.
  upgrade(targetTier = null, notify = true) {
    if (this.type.isMaxTier()) {
      return;
    }
    this.type.increaseTier(targetTier);
    this.applyTier(notify);
  }
  activateSpeedBoost(duration, multiplier) {
    this.speedBoostMultiplier = multiplier;
    this.applySpeedBoost();
    this.speedBoostTimer.reset(duration);
  }
  // Call right after creation (before the first update). Every +10% hull and
  // every +15% armor grant one bonus hit; engine % maps directly to speed.
  setRunBoosts(boosts) {
    this.runBoostBonusHealth = Math.floor((boosts?.hull || 0) / 10) + Math.floor((boosts?.armor || 0) / 15);
    this.runBoostSpeedMultiplier = 1 + (boosts?.engine || 0) / 100;
    this.attributes.health += this.runBoostBonusHealth;
    this.applySpeedBoost();
  }
  receiveHit(damage, hitterPartyIndex) {
    const wasMaxTier = this.type.isMaxTier();
    this.attributes.health = Math.max(0, this.attributes.health - damage);
    this.hit.notify(null);
    if (this.isAlive()) {
      return;
    }
    if (wasMaxTier) {
      this.type.increaseTier("c" /* C */);
      this.applyTier(true);
      return;
    }
    this.die("bullet" /* Bullet */, hitterPartyIndex);
  }
  collide(collision) {
    if (this.networkControlled) {
      this.collideBullets(collision);
      return;
    }
    super.collide(collision);
  }
  applyTier(notify) {
    this.attributes = TankAttributesFactory.create(this.type);
    this.attributes.health += this.runBoostBonusHealth;
    this.applySpeedBoost();
    this.skinAnimation = this.tierSkinAnimations.get(this.type.tier);
    if (notify === true) {
      this.upgraded.notify({ tier: this.type.tier });
    }
  }
  applySpeedBoost() {
    const baseAttributes = TankAttributesFactory.create(this.type);
    this.attributes.moveSpeed = baseAttributes.moveSpeed * this.speedBoostMultiplier * this.runBoostSpeedMultiplier;
  }
};

// ../src/gameObjects/PauseNotice.ts
init_modules_watch_stub();

// ../src/gameObjects/Points.ts
init_modules_watch_stub();
var SPRITE_POINTS_PREFIX = "points";
var SPRITE_ID_SEPARATOR2 = ".";
var Points = class extends GameObject {
  constructor(value, duration) {
    super(56, 28);
    this.zIndex = POINTS_Z_INDEX;
    this.painter = new SpritePainter();
    this.timer = new Timer();
    this.handleTimer = /* @__PURE__ */ __name(() => {
      this.dirtyPaintBox();
      this.removeSelf();
    }, "handleTimer");
    this.value = value;
    this.timer.reset(duration);
    this.timer.done.addListener(this.handleTimer);
  }
  static {
    __name(this, "Points");
  }
  setup({ spriteLoader }) {
    const spriteId = this.getSpriteId(this.value);
    this.painter.sprite = spriteLoader.load(spriteId);
  }
  update(updateArgs) {
    this.timer.update(updateArgs.deltaTime);
  }
  getSpriteId(value) {
    const parts = [SPRITE_POINTS_PREFIX, value.toString()];
    const spriteId = parts.join(SPRITE_ID_SEPARATOR2);
    return spriteId;
  }
};

// ../src/gameObjects/Powerup.ts
init_modules_watch_stub();
var PICKUP_MIN_INTERSECTION_SIZE = 16;
var Powerup = class extends GameObject {
  constructor(type) {
    super(64, 64);
    this.zIndex = POWERUP_Z_INDEX;
    this.collider = new BoxCollider(this, true);
    this.painter = new SpritePainter();
    this.ignorePause = true;
    this.picked = new Subject();
    this.networkControlled = false;
    this.type = type;
  }
  static {
    __name(this, "Powerup");
  }
  destroy() {
    this.dirtyPaintBox();
    this.removeSelf();
    this.collider.unregister();
  }
  setNetworkControlled(controlled) {
    this.networkControlled = controlled;
  }
  setup({ collisionSystem, spriteLoader }) {
    collisionSystem.register(this.collider);
    const spriteId = this.getSpriteId();
    const frames = [spriteLoader.load(spriteId), null];
    this.animation = new Animation(frames, {
      delay: 0.12,
      loop: true
    });
  }
  update(updateArgs) {
    this.collider.update();
    this.animation.update(updateArgs.deltaTime);
    this.painter.sprite = this.animation.getCurrentFrame();
    this.setNeedsPaint();
  }
  collide(collision) {
    if (this.networkControlled) {
      return;
    }
    const playerTankContacts = collision.contacts.filter((contact) => {
      return contact.collider.object.tags.includes("Tank" /* Tank */) && contact.collider.object.tags.includes("Player" /* Player */);
    });
    if (playerTankContacts.length > 0) {
      const firstPlayerTankContact = playerTankContacts[0];
      const tankBox = firstPlayerTankContact.collider.getBox();
      const selfBox = this.collider.getBox();
      const intersectionBox = selfBox.clone().intersectWith(tankBox);
      const intersectionRect = intersectionBox.toRect();
      if (intersectionRect.width > PICKUP_MIN_INTERSECTION_SIZE && intersectionRect.height > PICKUP_MIN_INTERSECTION_SIZE) {
        const tank = firstPlayerTankContact.collider.object;
        const { partyIndex } = tank;
        this.destroy();
        this.picked.notify({ partyIndex });
      }
    }
  }
  getSpriteId() {
    switch (this.type) {
      case "defence" /* BaseDefence */:
        return "powerup.shovel";
      case "freeze" /* Freeze */:
        return "powerup.clock";
      case "life" /* Life */:
        return "powerup.life";
      case "shield" /* Shield */:
        return "powerup.helmet";
      case "speed" /* Speed */:
        return "powerup.speed";
      case "upgrade" /* Upgrade */:
        return "powerup.star";
      case "zoomout" /* ZoomOut */:
        return "powerup.zoomout";
      case "wipeout" /* Wipeout */:
        return "powerup.grenade";
    }
    return "unknown";
  }
};

// ../src/gameObjects/Spawn.ts
init_modules_watch_stub();
var Spawn = class extends GameObject {
  constructor() {
    super(64, 64);
    this.zIndex = SPAWN_Z_INDEX;
    this.painter = new SpritePainter();
    this.completed = new Subject();
    this.painter.alignment = 3 /* MiddleCenter */;
  }
  static {
    __name(this, "Spawn");
  }
  setup({ spriteLoader }) {
    this.animation = new Animation(
      spriteLoader.loadList(["spawn.1", "spawn.2", "spawn.3", "spawn.4"]),
      { delay: 0.05, loop: 3 }
    );
  }
  update({ deltaTime }) {
    if (this.animation.isComplete()) {
      this.completed.notify(null);
      return;
    }
    this.animation.update(deltaTime);
    this.painter.sprite = this.animation.getCurrentFrame();
    this.setNeedsPaint();
  }
};

// ../src/gameObjects/VictoryMap.ts
init_modules_watch_stub();

// ../src/gameObjects/WallShadowField.ts
init_modules_watch_stub();
var WallShadowField = class extends GameObject {
  constructor() {
    super(...arguments);
    this.zIndex = WALL_SHADOW_Z_INDEX;
    this.painter = new DropShadowPainter();
  }
  static {
    __name(this, "WallShadowField");
  }
  update() {
    const casters = [];
    const field = this.parent;
    if (field !== null) {
      field.traverse((node) => {
        const { tags } = node;
        if (tags.includes("Wall" /* Wall */) && !tags.includes("Border" /* Border */)) {
          casters.push(node);
        }
      });
    }
    this.painter.casters = casters;
    this.setNeedsPaint();
  }
};

// ../src/tank/behaviors/PlayerTankBehavior.ts
var MOVE_CONTROLS = [
  ...LevelPlayInputContext.MoveUp,
  ...LevelPlayInputContext.MoveDown,
  ...LevelPlayInputContext.MoveLeft,
  ...LevelPlayInputContext.MoveRight
];
var PlayerTankBehavior = class extends TankBehavior {
  static {
    __name(this, "PlayerTankBehavior");
  }
  update(tank, updateArgs) {
    const { deltaTime, inputManager, session } = updateArgs;
    if (updateArgs.webRtcMatch.handlePlayerTank(tank, updateArgs)) {
      return;
    }
    if (tank.partyIndex === 0 && updateArgs.magicBlockMovement.isWatching()) {
      tank.idle(false);
      return;
    }
    if (updateArgs.magicBlockMovement.isRemoteTank(tank.partyIndex)) {
      tank.idle(false);
      return;
    }
    if (updateArgs.magicBlockMovement.isLocalServerMatchWaitingForStart()) {
      tank.idle(false);
      return;
    }
    let inputMethod = inputManager.getActiveMethod();
    if (session.isMultiplayer() && !updateArgs.magicBlockMovement.isOnlineMatch() && !updateArgs.webRtcMatch.isEnabled()) {
      const playerSession = session.getPlayer(tank.partyIndex);
      const playerInputVariant = playerSession.getInputVariant();
      inputMethod = inputManager.getMethodByVariant(playerInputVariant);
    }
    if (inputMethod.isDownAny(LevelPlayInputContext.Fire)) {
      if (updateArgs.magicBlockMovement.isLocalServerMatch()) {
        updateArgs.magicBlockMovement.recordLocalFire(tank);
      } else if (tank.fire()) {
        this.disableLocalBulletDamageInAuthoritativeMatch(tank, updateArgs);
        updateArgs.magicBlockMovement.recordLocalFire(tank);
      }
    }
    if (inputMethod.isHoldAny(LevelPlayInputContext.RapidFire)) {
      if (updateArgs.magicBlockMovement.isLocalServerMatch()) {
        updateArgs.magicBlockMovement.recordLocalFire(tank);
      } else if (tank.fire()) {
        this.disableLocalBulletDamageInAuthoritativeMatch(tank, updateArgs);
        updateArgs.magicBlockMovement.recordLocalFire(tank);
      }
    }
    if (!tank.isSliding() && !tank.isStunned()) {
      const directions = [
        [LevelPlayInputContext.MoveUp, 0 /* Up */],
        [LevelPlayInputContext.MoveDown, 180 /* Down */],
        [LevelPlayInputContext.MoveLeft, 270 /* Left */],
        [LevelPlayInputContext.MoveRight, 90 /* Right */]
      ];
      let bestIndex = -1;
      let bestRotation = null;
      for (const [controls, rotation] of directions) {
        const index = inputMethod.getHoldLastIndex(controls);
        if (index > bestIndex) {
          bestIndex = index;
          bestRotation = rotation;
        }
      }
      if (bestRotation !== null) {
        tank.rotate(bestRotation);
        tank.move(deltaTime);
      }
    }
    if (inputMethod.isNotHoldAll(MOVE_CONTROLS) && tank.state !== 1 /* Idle */) {
      tank.idle();
    }
  }
  disableLocalBulletDamageInAuthoritativeMatch(tank, updateArgs) {
    if (!updateArgs.magicBlockMovement.isOnlineMatch()) {
      return;
    }
    tank.bullets[tank.bullets.length - 1]?.setLocalDamageDisabled(true);
  }
};

// ../src/tank/behaviors/RecordedTankBehavior.ts
init_modules_watch_stub();
var RecordedTankBehavior = class extends TankBehavior {
  constructor(trace) {
    super();
    this.tickIndex = 0;
    this.trace = trace;
  }
  static {
    __name(this, "RecordedTankBehavior");
  }
  update(tank) {
    const frame = this.trace[this.tickIndex];
    this.tickIndex += 1;
    if (frame === void 0) {
      return;
    }
    tank.rotation = frame.rotation;
    tank.position.set(frame.x, frame.y);
    tank.updateMatrix(true);
    if (frame.fired) {
      tank.fire();
    }
  }
};

// ../src/tank/behaviors/StandFireTankBehavior.ts
init_modules_watch_stub();

// ../src/tank/behaviors/StandStillTankBehavior.ts
init_modules_watch_stub();
var StandStillTankBehavior = class extends TankBehavior {
  static {
    __name(this, "StandStillTankBehavior");
  }
  update() {
  }
};

// ../src/tank/behaviors/VictoryTankBehavior.ts
init_modules_watch_stub();

// ../src/tank/TankFactory.ts
init_modules_watch_stub();
var TankFactory = class {
  static {
    __name(this, "TankFactory");
  }
  static createPlayer(partyIndex, type = TankType3.PlayerA(), behavior = new PlayerTankBehavior()) {
    return new PlayerTank(type, behavior, partyIndex);
  }
  static createPlayerType() {
    return TankType3.PlayerA();
  }
  static createEnemy(partyIndex, type = TankType3.EnemyA(), behavior = new AiTankBehavior()) {
    return new EnemyTank(type, behavior, partyIndex);
  }
};

// ../src/points/PointsRecord.ts
var TANK_POINTS_MAP = {
  ["a" /* A */]: 100,
  ["b" /* B */]: 200,
  ["c" /* C */]: 300,
  ["d" /* D */]: 400
};
var POWERUP_POINTS = 500;
var PointsRecord = class {
  constructor() {
    this.kills = [];
    this.powerups = [];
    this.bonus = false;
  }
  static {
    __name(this, "PointsRecord");
  }
  addKill(tier) {
    this.kills.push(tier);
    return this;
  }
  addPowerup(type) {
    this.powerups.push(type);
    return this;
  }
  addBonus() {
    this.bonus = true;
    return this;
  }
  hasBonus() {
    return this.bonus === true;
  }
  getTierKillCost(tier) {
    return TANK_POINTS_MAP[tier];
  }
  getPowerupCost() {
    return POWERUP_POINTS;
  }
  getKillTotalCount() {
    return this.kills.length;
  }
  getTierKillCount(tierToFind) {
    const kills = this.kills.filter((tier) => tier === tierToFind);
    const count = kills.length;
    return count;
  }
  setTierKillCounts(counts) {
    this.kills = [];
    ["a" /* A */, "b" /* B */, "c" /* C */, "d" /* D */].forEach(
      (tier, index) => {
        const count = Math.min(
          1e3,
          Math.max(0, Math.floor(Number(counts[index]) || 0))
        );
        for (let kill = 0; kill < count; kill += 1) {
          this.kills.push(tier);
        }
      }
    );
  }
  getTierPoints(tierToFind) {
    let total = 0;
    this.kills.forEach((tier) => {
      if (tier !== tierToFind) {
        return;
      }
      total += this.getTierKillCost(tier);
    });
    return total;
  }
  getKillTotalPoints() {
    let total = 0;
    this.kills.forEach((tier) => {
      total += this.getTierKillCost(tier);
    });
    return total;
  }
  getPowerupTotalPoints() {
    const total = this.powerups.length * this.getPowerupCost();
    return total;
  }
  getBonusTotalPoints() {
    if (this.bonus) {
      return BONUS_POINTS;
    }
    return 0;
  }
  getTotalPoints() {
    const killTotal = this.getKillTotalPoints();
    const powerupTotal = this.getPowerupTotalPoints();
    const bonusTotal = this.getBonusTotalPoints();
    const total = killTotal + powerupTotal + bonusTotal;
    return total;
  }
  reset() {
    this.kills = [];
    this.powerups = [];
    this.bonus = false;
  }
};

// ../src/points/PointsValue.ts
init_modules_watch_stub();

// ../src/game/SessionPlayer.ts
var SessionPlayer = class {
  constructor() {
    this.lifeup = new Subject();
    this.reset();
  }
  static {
    __name(this, "SessionPlayer");
  }
  reset() {
    this.levelPointsRecord = new PointsRecord();
    this.gamePoints = 0;
    this.lastGamePoints = null;
    this.lives = PLAYER_INITIAL_LIVES;
    this.nextLifePointThreshold = PLAYER_EXTRA_LIVE_POINTS;
    this.tankTier = "a" /* A */;
    this.levelFirstSpawned = true;
    this.inputVariant = null;
  }
  addKillPoints(tier) {
    this.levelPointsRecord.addKill(tier);
    this.checkLifeup();
  }
  addPowerupPoints(type) {
    this.levelPointsRecord.addPowerup(type);
    this.checkLifeup();
  }
  addBonusPoints() {
    this.levelPointsRecord.addBonus();
    this.checkLifeup();
  }
  completeLevel() {
    this.gamePoints += this.getLevelPoints();
    this.levelPointsRecord.reset();
    this.resetLevelFirstSpawn();
  }
  // Sum of all previous levels and current level
  getGamePoints() {
    return this.gamePoints + this.getLevelPoints();
  }
  setAuthoritativeGamePoints(points) {
    const total = Math.max(0, Math.floor(points));
    this.gamePoints = total - this.getLevelPoints();
  }
  getLevelPoints() {
    return this.levelPointsRecord.getTotalPoints();
  }
  setLastGamePoints(lastGamePoints) {
    this.lastGamePoints = lastGamePoints;
  }
  getLastGamePoints() {
    return this.lastGamePoints;
  }
  wasInLastGame() {
    return this.lastGamePoints !== null;
  }
  hasBonusPoints() {
    return this.levelPointsRecord.hasBonus();
  }
  getLevelPointsRecord() {
    return this.levelPointsRecord;
  }
  getLivesCount() {
    return this.lives;
  }
  setLivesCount(lives) {
    this.lives = Math.max(0, Math.floor(lives));
  }
  isAlive() {
    return this.lives > 0;
  }
  getTankTier() {
    return this.tankTier;
  }
  setTankTier(tankTier) {
    this.tankTier = tankTier;
  }
  resetTankTier() {
    this.tankTier = "a" /* A */;
  }
  isLevelFirstSpawn() {
    return this.levelFirstSpawned;
  }
  setLevelSpawned() {
    this.levelFirstSpawned = false;
  }
  resetLevelFirstSpawn() {
    this.levelFirstSpawned = true;
  }
  addLife() {
    if (!this.isAlive()) {
      return;
    }
    this.lives += 1;
    this.lifeup.notify(null);
  }
  removeLife() {
    this.lives = Math.max(0, this.lives - 1);
  }
  setInputVariant(inputVariant) {
    this.inputVariant = inputVariant;
  }
  getInputVariant() {
    return this.inputVariant;
  }
  checkLifeup() {
    if (this.getGamePoints() >= this.nextLifePointThreshold) {
      this.nextLifePointThreshold += PLAYER_EXTRA_LIVE_POINTS;
      this.addLife();
    }
  }
};

// ../src/game/Session.ts
function createEmptyRunBoosts() {
  return { hull: 0, armor: 0, engine: 0, salvage: 0 };
}
__name(createEmptyRunBoosts, "createEmptyRunBoosts");
var Session = class {
  constructor() {
    this.primaryPlayer = new SessionPlayer();
    this.secondaryPlayer = new SessionPlayer();
    this.players = [];
    this.reset();
    this.players.push(this.primaryPlayer, this.secondaryPlayer);
  }
  static {
    __name(this, "Session");
  }
  start(startLevelNumber, endLevelNumber) {
    if (this.state !== 0 /* Idle */) {
      return;
    }
    this.startLevelNumber = startLevelNumber;
    this.endLevelNumber = endLevelNumber;
    this.currentLevelNumber = startLevelNumber;
    this.state = 1 /* Playing */;
  }
  reset() {
    this.seenIntro = false;
    this.startLevelNumber = 1;
    this.currentLevelNumber = 1;
    this.endLevelNumber = 1;
    this.state = 0 /* Idle */;
    this.playtest = false;
    this.multiplayer = false;
    this.runConsumables = {
      powerups: [],
      powerupItems: [],
      powerupCounts: [],
      extraLives: 0
    };
    this.runExtraLivesApplied = false;
    this.runBoosts = createEmptyRunBoosts();
    this.playerTankTiers = ["a" /* A */, "a" /* A */];
    this.levelEnemyTotal = 0;
    this.levelEnemiesDefeated = 0;
    this.levelDurationTicks = 0;
    this.primaryPlayer.reset();
    this.secondaryPlayer.reset();
  }
  getPlayer(playerIndex) {
    return this.players[playerIndex];
  }
  getPlayers() {
    return this.players;
  }
  isAnyPlayerAlive() {
    if (!this.multiplayer) {
      return this.primaryPlayer.isAlive();
    }
    return this.players.some((player) => {
      return player.isAlive();
    });
  }
  resetExceptIntro() {
    this.startLevelNumber = 1;
    this.currentLevelNumber = 1;
    this.endLevelNumber = 1;
    this.state = 0 /* Idle */;
    this.playtest = false;
    this.playerTankTiers = ["a" /* A */, "a" /* A */];
    this.runExtraLivesApplied = false;
    this.primaryPlayer.reset();
    this.secondaryPlayer.reset();
  }
  activateNextLevel() {
    this.currentLevelNumber += 1;
    this.primaryPlayer.completeLevel();
    this.secondaryPlayer.completeLevel();
  }
  getMaxLevelPoints() {
    let maxPoints = 0;
    for (const player of this.players) {
      const points = player.getLevelPoints();
      if (points > maxPoints) {
        maxPoints = points;
      }
    }
    return maxPoints;
  }
  getMaxGamePoints() {
    let maxPoints = 0;
    for (const player of this.players) {
      const points = player.getGamePoints();
      if (points > maxPoints) {
        maxPoints = points;
      }
    }
    return maxPoints;
  }
  anybodyHasBonusPoints() {
    return this.players.some((player) => {
      return player.hasBonusPoints();
    });
  }
  getLevelNumber() {
    return this.currentLevelNumber;
  }
  isLastLevel() {
    return this.currentLevelNumber === this.endLevelNumber;
  }
  setGameOver() {
    this.state = 2 /* GameOver */;
  }
  isGameOver() {
    return this.state === 2 /* GameOver */;
  }
  setSeenIntro(seenIntro) {
    this.seenIntro = seenIntro;
  }
  haveSeenIntro() {
    return this.seenIntro;
  }
  setPlaytest() {
    this.playtest = true;
  }
  resetPlaytest() {
    this.playtest = false;
  }
  isPlaytest() {
    return this.playtest;
  }
  setMultiplayer() {
    this.multiplayer = true;
  }
  isMultiplayer() {
    return this.multiplayer;
  }
  setRunConsumables(runConsumables) {
    this.runConsumables = runConsumables;
    this.runExtraLivesApplied = false;
  }
  getRunConsumables() {
    return this.runConsumables;
  }
  consumeInitialExtraLives() {
    if (this.runExtraLivesApplied) {
      return 0;
    }
    this.runExtraLivesApplied = true;
    return Math.max(0, Math.floor(this.runConsumables.extraLives || 0));
  }
  clearRunConsumables() {
    this.runConsumables = {
      powerups: [],
      powerupItems: [],
      powerupCounts: [],
      extraLives: 0
    };
    this.runExtraLivesApplied = false;
  }
  setRunBoosts(runBoosts) {
    this.runBoosts = {
      hull: clampBoostPercent(runBoosts?.hull),
      armor: clampBoostPercent(runBoosts?.armor),
      engine: clampBoostPercent(runBoosts?.engine),
      salvage: clampBoostPercent(runBoosts?.salvage)
    };
  }
  getRunBoosts() {
    return this.runBoosts;
  }
  setPlayerTankTier(playerIndex, tier) {
    if (playerIndex < 0 || playerIndex >= this.playerTankTiers.length) {
      return;
    }
    this.playerTankTiers[playerIndex] = isTankTier(tier) ? tier : "a" /* A */;
  }
  getPlayerTankTier(playerIndex) {
    return this.playerTankTiers[playerIndex] || "a" /* A */;
  }
  getPlayerTankTiers() {
    return this.playerTankTiers.slice();
  }
  startLevelStats(enemyTotal) {
    this.levelEnemyTotal = Math.max(0, Math.floor(enemyTotal));
    this.levelEnemiesDefeated = 0;
    this.levelDurationTicks = 0;
  }
  recordLevelTick() {
    this.levelDurationTicks += 1;
  }
  recordEnemyDefeated() {
    this.levelEnemiesDefeated += 1;
  }
  getLevelEnemyTotal() {
    return this.levelEnemyTotal;
  }
  getLevelEnemiesDefeated() {
    return this.levelEnemiesDefeated;
  }
  getLevelDurationTicks() {
    return this.levelDurationTicks;
  }
};
function clampBoostPercent(value) {
  const parsed = Number(value);
  if (!Number.isFinite(parsed)) {
    return 0;
  }
  return Math.max(0, Math.min(60, Math.floor(parsed)));
}
__name(clampBoostPercent, "clampBoostPercent");
function isTankTier(value) {
  return value === "a" /* A */ || value === "b" /* B */ || value === "c" /* C */ || value === "d" /* D */;
}
__name(isTankTier, "isTankTier");

// ../src/level/LevelEventBus.ts
init_modules_watch_stub();
var LevelEventBus = class {
  constructor() {
    this.baseDied = new Subject();
    this.enemyAllDied = new Subject();
    this.enemyDied = new Subject();
    this.enemyExploded = new Subject();
    this.enemyHit = new Subject();
    this.enemySpawnCompleted = new Subject();
    this.enemySpawnRequested = new Subject();
    this.mapTileDestroyed = new Subject();
    this.levelPaused = new Subject();
    this.levelUnpaused = new Subject();
    this.levelGameOverMoveBlocked = new Subject();
    this.levelGameOverCompleted = new Subject();
    this.levelWinCompleted = new Subject();
    this.playerDied = new Subject();
    this.playerFired = new Subject();
    this.playerSlided = new Subject();
    this.playerSpawnCompleted = new Subject();
    this.playerSpawnRequested = new Subject();
    this.powerupSpawned = new Subject();
    this.powerupPicked = new Subject();
    this.powerupRevoked = new Subject();
  }
  static {
    __name(this, "LevelEventBus");
  }
};

// ../src/level/LevelMatchLifecycle.ts
init_modules_watch_stub();
function prepareLevelSession(session, enemyTotal) {
  session.startLevelStats(enemyTotal);
  const extraLives = session.consumeInitialExtraLives();
  for (let index = 0; index < extraLives; index += 1) {
    session.primaryPlayer.addLife();
    if (session.isMultiplayer()) {
      session.secondaryPlayer.addLife();
    }
  }
}
__name(prepareLevelSession, "prepareLevelSession");
var LevelMatchLifecycle = class {
  constructor(eventBus, session, scripts) {
    this.eventBus = eventBus;
    this.session = session;
    this.scripts = scripts;
    this.result = null;
    this.completed = false;
    this.handleBaseDied = /* @__PURE__ */ __name(() => {
      this.activateGameOver();
    }, "handleBaseDied");
    this.handleEnemyAllDied = /* @__PURE__ */ __name(() => {
      this.scripts.pause?.disable();
      this.scripts.win.enable();
    }, "handleEnemyAllDied");
    this.handleEnemyDied = /* @__PURE__ */ __name((event) => {
      if (event.networkMirror === true) {
        return;
      }
      this.session.recordEnemyDefeated();
      if (event.reason === "wipeout" /* WipeoutPowerup */ || event.hitterPartyIndex === null || event.hitterPartyIndex === void 0) {
        return;
      }
      this.session.getPlayer(event.hitterPartyIndex).addKillPoints(event.type.tier);
    }, "handleEnemyDied");
    this.handlePlayerDied = /* @__PURE__ */ __name((event) => {
      const playerSession = this.session.getPlayer(event.partyIndex);
      playerSession.removeLife();
      if (this.session.isAnyPlayerAlive()) {
        if (!playerSession.isAlive()) {
          this.scripts.playerOver.setPlayerIndex(event.partyIndex);
          this.scripts.playerOver.enable();
        }
        return;
      }
      this.activateGameOver();
    }, "handlePlayerDied");
    this.handlePowerupPicked = /* @__PURE__ */ __name((event) => {
      const playerSession = this.session.getPlayer(event.partyIndex);
      if (!playerSession.isAlive()) {
        return;
      }
      playerSession.addPowerupPoints(event.type);
      if (event.type === "life" /* Life */) {
        playerSession.addLife();
      }
    }, "handlePowerupPicked");
    eventBus.baseDied.addListener(this.handleBaseDied);
    eventBus.enemyAllDied.addListener(this.handleEnemyAllDied);
    eventBus.enemyDied.addListener(this.handleEnemyDied);
    eventBus.playerDied.addListener(this.handlePlayerDied);
    eventBus.powerupPicked.addListener(this.handlePowerupPicked);
    eventBus.levelGameOverCompleted.addListener(() => {
      this.result = "loss";
      this.completed = true;
    });
    eventBus.levelWinCompleted.addListener(() => {
      if (!this.session.isGameOver()) {
        this.result = "win";
        this.completed = true;
      }
    });
  }
  static {
    __name(this, "LevelMatchLifecycle");
  }
  isComplete() {
    return this.completed;
  }
  getResult() {
    return this.result;
  }
  loseMatch() {
    this.activateGameOver();
  }
  activateGameOver() {
    this.result = "loss";
    this.completed = false;
    this.session.setGameOver();
    this.scripts.pause?.disable();
    this.scripts.player.disable();
    this.scripts.gameOver.enable();
    this.scripts.win.disable();
  }
};

// ../src/level/LevelWorld.ts
init_modules_watch_stub();
var LevelWorld = class {
  constructor(sceneRoot, fieldWidth, fieldHeight) {
    this.playerTanks = [];
    this.sceneRoot = sceneRoot;
    this.field = new Field(fieldWidth, fieldHeight);
  }
  static {
    __name(this, "LevelWorld");
  }
  addPlayerTank(playerIndex, playerTank) {
    this.playerTanks[playerIndex] = playerTank;
    this.field.add(playerTank);
  }
  removePlayerTank(playerIndex) {
    const playerTank = this.playerTanks[playerIndex];
    if (playerTank === null || playerTank === void 0) {
      return;
    }
    playerTank.removeSelf();
    this.playerTanks[playerIndex] = null;
  }
  getPlayerTanks() {
    return this.playerTanks;
  }
};

// ../src/level/scripts/LevelBaseScript.ts
init_modules_watch_stub();

// ../src/level/LevelScript.ts
init_modules_watch_stub();
var LevelScript = class {
  constructor() {
    this.enabled = true;
    this.needsSetup = true;
  }
  static {
    __name(this, "LevelScript");
  }
  isEnabled() {
    return this.enabled;
  }
  enable() {
    this.enabled = true;
  }
  disable() {
    this.enabled = false;
  }
  invokeInit(world, eventBus, session, mapConfig) {
    this.world = world;
    this.eventBus = eventBus;
    this.session = session;
    this.mapConfig = mapConfig;
    this.init();
  }
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  invokeUpdate(updateArgs) {
    if (this.enabled === false) {
      return;
    }
    if (this.needsSetup === true) {
      this.needsSetup = false;
      this.setup(updateArgs);
    }
    this.update(updateArgs);
  }
  init() {
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  setup(updateArgs) {
  }
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  update(updateArgs) {
  }
};

// ../src/level/scripts/LevelBaseScript.ts
var WALL_REGIONS = [
  { x: 0, y: 0, width: 128, height: 32 },
  { x: 0, y: 32, width: 32, height: 64 },
  { x: 96, y: 32, width: 32, height: 64 }
];
var LevelBaseScript = class extends LevelScript {
  constructor(isWebRtcMatch = detectWebRtcMatch()) {
    super();
    this.defenceTimer = new Timer();
    this.handlePowerupPicked = /* @__PURE__ */ __name((event) => {
      const { type: powerupType } = event;
      if (powerupType === "defence" /* BaseDefence */) {
        if (this.isWebRtcMatch) {
          this.replaceBaseWalls("steel" /* Steel */);
          this.defenceTimer.reset(BASE_DEFENCE_POWERUP_DURATION);
          return;
        }
        this.base.activateDefence(BASE_DEFENCE_POWERUP_DURATION);
      }
    }, "handlePowerupPicked");
    this.restoreBrickWalls = /* @__PURE__ */ __name(() => {
      this.replaceBaseWalls("brick" /* Brick */);
    }, "restoreBrickWalls");
    this.isWebRtcMatch = isWebRtcMatch;
  }
  static {
    __name(this, "LevelBaseScript");
  }
  setup() {
    this.eventBus.powerupPicked.addListener(this.handlePowerupPicked);
    this.defenceTimer.done.addListener(this.restoreBrickWalls);
    this.base = new Base();
    this.base.position.copyFrom(this.mapConfig.getBasePosition());
    this.base.died.addListener(() => {
      this.eventBus.baseDied.notify(null);
    });
    this.world.field.add(this.base);
  }
  update(updateArgs) {
    if (this.isWebRtcMatch) {
      this.defenceTimer.update(updateArgs.deltaTime);
    }
  }
  replaceBaseWalls(type) {
    const basePosition = this.mapConfig.getBasePosition();
    const regions = WALL_REGIONS.map((region) => {
      return new Rect(
        basePosition.x + region.x,
        basePosition.y + region.y,
        region.width,
        region.height
      );
    });
    [...this.world.field.children].forEach((node) => {
      if (!(node instanceof TerrainTile)) {
        return;
      }
      const isBaseWall = regions.some((region) => {
        return node.position.x >= region.x && node.position.x < region.x + region.width && node.position.y >= region.y && node.position.y < region.y + region.height;
      });
      if (isBaseWall) {
        node.destroy(false);
      }
    });
    const baseRect = new Rect(
      basePosition.x,
      basePosition.y,
      BASE_DEFAULT_SIZE.width,
      BASE_DEFAULT_SIZE.height
    );
    const tiles = TerrainFactory.createMapFromRegionConfigs(
      regions.map((region) => ({
        type,
        x: region.x,
        y: region.y,
        width: region.width,
        height: region.height
      })),
      this.world.field.size.width,
      this.world.field.size.height,
      [baseRect]
    );
    tiles.forEach((tile) => {
      tile.destroyed.addListener(() => {
        this.eventBus.mapTileDestroyed.notify({
          type: tile.type,
          position: tile.position.clone(),
          size: tile.size.clone()
        });
      });
    });
    this.world.field.add(...tiles);
    this.world.field.setNeedsPaint();
  }
};
function detectWebRtcMatch() {
  return typeof window !== "undefined" && new URLSearchParams(window.location.search).get("mode") === "webrtc";
}
__name(detectWebRtcMatch, "detectWebRtcMatch");

// ../src/level/scripts/LevelEnemyScript.ts
init_modules_watch_stub();

// ../src/debug/index.ts
init_modules_watch_stub();

// ../src/debug/menu/index.ts
init_modules_watch_stub();

// ../src/debug/menu/DebugCameraMenu.ts
init_modules_watch_stub();

// ../src/debug/DebugMenu.ts
init_modules_watch_stub();
var DEFAULT_OPTIONS4 = {
  top: 0,
  left: null,
  right: 0
};
var DebugMenu = class {
  static {
    __name(this, "DebugMenu");
  }
  constructor(titleText = "Untitled", options = {}) {
    this.options = Object.assign({}, DEFAULT_OPTIONS4, options);
    this.container = document.createElement("div");
    this.container.setAttribute(
      "style",
      `background: white;
      color: black;
      position: absolute;
      left: ${this.getOffsetStyle(this.options.left)};
      right: ${this.getOffsetStyle(this.options.right)};
      top: ${this.getOffsetStyle(this.options.top)};
      padding: 10px;
      display: flex;
      flex-direction: column`
    );
    const title = document.createElement("div");
    title.textContent = titleText;
    this.container.appendChild(title);
  }
  attach() {
    if (IS_PROD) {
      return;
    }
    document.body.appendChild(this.container);
  }
  detach() {
    document.body.removeChild(this.container);
  }
  appendButton(text, handler) {
    const button = this.createButton(text, handler);
    this.container.appendChild(button);
    return button;
  }
  createButton(text, handler) {
    const button = document.createElement("button");
    button.textContent = text;
    button.addEventListener("click", handler);
    return button;
  }
  getOffsetStyle(value) {
    if (value === null) {
      return "initial";
    }
    return `${value}px`;
  }
};

// ../src/debug/menu/DebugCollisionMenu.ts
init_modules_watch_stub();

// ../src/debug/menu/DebugGameLoopMenu.ts
init_modules_watch_stub();

// ../src/debug/menu/DebugLevelEnemyMenu.ts
init_modules_watch_stub();
var DebugLevelEnemyMenu = class extends DebugMenu {
  constructor(options = {}) {
    super("Level Enemy", options);
    this.movementToggleRequest = new Subject();
    this.playerMirrorBulletsToggleRequest = new Subject();
    this.movementStopped = false;
    this.playerMirrorBulletsHidden = false;
    this.handleMovementToggle = /* @__PURE__ */ __name(() => {
      this.movementStopped = !this.movementStopped;
      this.movementButton.textContent = this.movementStopped ? "Resume enemy movement" : "Stop enemy movement";
      this.movementButton.setAttribute(
        "aria-pressed",
        String(this.movementStopped)
      );
      this.movementToggleRequest.notify(this.movementStopped);
    }, "handleMovementToggle");
    this.handlePlayerMirrorBulletsToggle = /* @__PURE__ */ __name(() => {
      this.playerMirrorBulletsHidden = !this.playerMirrorBulletsHidden;
      this.playerMirrorBulletsButton.textContent = this.playerMirrorBulletsHidden ? "Show player mirror bullets" : "Hide player mirror bullets";
      this.playerMirrorBulletsButton.setAttribute(
        "aria-pressed",
        String(this.playerMirrorBulletsHidden)
      );
      this.playerMirrorBulletsToggleRequest.notify(this.playerMirrorBulletsHidden);
    }, "handlePlayerMirrorBulletsToggle");
    this.movementButton = this.appendButton(
      "Stop enemy movement",
      this.handleMovementToggle
    );
    this.movementButton.setAttribute("aria-pressed", "false");
    this.movementButton.style.minHeight = "40px";
    this.playerMirrorBulletsButton = this.appendButton(
      "Hide player mirror bullets",
      this.handlePlayerMirrorBulletsToggle
    );
    this.playerMirrorBulletsButton.setAttribute("aria-pressed", "false");
    this.playerMirrorBulletsButton.style.minHeight = "40px";
  }
  static {
    __name(this, "DebugLevelEnemyMenu");
  }
};

// ../src/debug/menu/DebugLevelPlayerMenu.ts
init_modules_watch_stub();
var DebugLevelPlayerMenu = class extends DebugMenu {
  constructor(options = {}) {
    super("Level Player", options);
    this.deathRequest = new Subject();
    this.upgradeRequest = new Subject();
    this.moveSpeedUpRequest = new Subject();
    this.requestDeath = /* @__PURE__ */ __name((partyIndex) => {
      this.deathRequest.notify(partyIndex);
    }, "requestDeath");
    this.requestMoveSpeedUp = /* @__PURE__ */ __name((partyIndex) => {
      this.moveSpeedUpRequest.notify({ partyIndex, speed: 500 });
    }, "requestMoveSpeedUp");
    this.requestUpgrade = /* @__PURE__ */ __name((partyIndex) => {
      this.upgradeRequest.notify(partyIndex);
    }, "requestUpgrade");
    this.appendButton("#1 Upgrade", () => this.requestUpgrade(0));
    this.appendButton("#1 Die", () => this.requestDeath(0));
    this.appendButton("#1 Speed +500", () => this.requestMoveSpeedUp(0));
    this.appendButton("#2 Upgrade", () => this.requestUpgrade(1));
    this.appendButton("#2 Die", () => this.requestDeath(1));
    this.appendButton("#2 Speed +500", () => this.requestMoveSpeedUp(1));
  }
  static {
    __name(this, "DebugLevelPlayerMenu");
  }
};

// ../src/debug/menu/DebugLevelPowerupMenu.ts
init_modules_watch_stub();

// ../src/debug/DebugGrid.ts
init_modules_watch_stub();
var DebugGrid = class extends GameObject {
  constructor(width, height, step, color = "#fff") {
    super(width, height);
    this.zIndex = DEBUG_GRID_Z_INDEX;
    this.highlightedCells = [];
    this.step = step;
    for (let x = 0; x <= width; x += step) {
      const line = new GameObject();
      const painter = new LinePainter();
      painter.strokeColor = color;
      painter.positions.push(new Vector(x, 0), new Vector(x, height));
      line.painter = painter;
      this.add(line);
    }
    for (let y = 0; y <= height; y += step) {
      const line = new GameObject();
      const painter = new LinePainter();
      painter.strokeColor = color;
      painter.positions.push(new Vector(0, y), new Vector(width, y));
      line.painter = painter;
      this.add(line);
    }
  }
  static {
    __name(this, "DebugGrid");
  }
  highlightCell(index, color = "rgba(255, 0, 0, 0.5)") {
    const cell = new GameObject(this.step, this.step);
    cell.painter = new RectPainter(color);
    cell.position.set(index.x * this.step, index.y * this.step);
    this.highlightedCells.push(cell);
    this.add(cell);
  }
  removeAllCellHighlights() {
    this.highlightedCells.forEach((cell) => {
      cell.removeSelf();
    });
    this.highlightedCells = [];
  }
};

// ../src/debug/menu/DebugLevelPowerupMenu.ts
var DebugLevelPowerupMenu = class extends DebugMenu {
  constructor(levelWorld, powerupGrid, options = {}) {
    super("Level Powerup", options);
    this.spawnRequest = new Subject();
    this.handleGridShow = /* @__PURE__ */ __name(() => {
      this.showGrid();
    }, "handleGridShow");
    this.handleGridUpdate = /* @__PURE__ */ __name(() => {
      this.updateDebugGrid();
    }, "handleGridUpdate");
    this.handleGridHide = /* @__PURE__ */ __name(() => {
      this.debugGrid.removeSelf();
    }, "handleGridHide");
    this.handleSpawn = /* @__PURE__ */ __name(() => {
      this.spawnRequest.notify(null);
    }, "handleSpawn");
    this.handleSpawnBaseDefence = /* @__PURE__ */ __name(() => {
      this.spawnRequest.notify("defence" /* BaseDefence */);
    }, "handleSpawnBaseDefence");
    this.handleSpawnFreeze = /* @__PURE__ */ __name(() => {
      this.spawnRequest.notify("freeze" /* Freeze */);
    }, "handleSpawnFreeze");
    this.handleSpawnSpeed = /* @__PURE__ */ __name(() => {
      this.spawnRequest.notify("speed" /* Speed */);
    }, "handleSpawnSpeed");
    this.handleSpawnZoomOut = /* @__PURE__ */ __name(() => {
      this.spawnRequest.notify("zoomout" /* ZoomOut */);
    }, "handleSpawnZoomOut");
    this.handleSpawnWipeout = /* @__PURE__ */ __name(() => {
      this.spawnRequest.notify("wipeout" /* Wipeout */);
    }, "handleSpawnWipeout");
    this.levelWorld = levelWorld;
    this.powerupGrid = powerupGrid;
    this.debugGrid = new DebugGrid(
      this.levelWorld.field.size.width,
      this.levelWorld.field.size.height,
      TILE_SIZE_MEDIUM
    );
    this.appendButton("Show grid", this.handleGridShow);
    this.appendButton("Hide grid", this.handleGridHide);
    this.appendButton("Update grid", this.handleGridUpdate);
    this.appendButton("Spawn", this.handleSpawn);
    this.appendButton("Spawn: base defence", this.handleSpawnBaseDefence);
    this.appendButton("Spawn: freeze", this.handleSpawnFreeze);
    this.appendButton("Spawn: speed", this.handleSpawnSpeed);
    this.appendButton("Spawn: zoom out", this.handleSpawnZoomOut);
    this.appendButton("Spawn: wipeout", this.handleSpawnWipeout);
  }
  static {
    __name(this, "DebugLevelPowerupMenu");
  }
  showGrid() {
    this.levelWorld.field.add(this.debugGrid);
    this.updateDebugGrid();
  }
  updateDebugGrid() {
    this.debugGrid.removeAllCellHighlights();
    const blockedCellIndexes = this.powerupGrid.getBlockedCellIndexes();
    blockedCellIndexes.forEach((cellIndex) => {
      this.debugGrid.highlightCell(cellIndex);
    });
  }
};

// ../src/debug/DebugInspector.ts
init_modules_watch_stub();

// ../src/level/scripts/LevelEnemyScript.ts
var NETWORK_DEATH_COLLISION_GRACE = 0.2;
var TANK_SPAWN_SIZE = 64;
var LevelEnemyScript = class extends LevelScript {
  constructor(isNetworkEnemyMirror = detectNetworkEnemyMirror(), headless = false) {
    super();
    this.list = [];
    this.listIndex = 0;
    this.aliveTanks = [];
    this.positions = [];
    this.positionIndex = 0;
    this.spawnTimer = new Timer();
    this.freezeTimer = new Timer();
    this.debugMovementStopped = false;
    this.debugPlayerMirrorBulletsHidden = false;
    this.spawningCount = 0;
    this.activeEnemyIds = /* @__PURE__ */ new Set();
    this.pendingNetworkRemovals = [];
    // Dev-only match replay (see src/replay): when set, newly spawned enemies
    // re-enact this recorded movement instead of deciding for themselves (see
    // RecordedTankBehavior) -- keyed by partyIndex, same as the saved replay's
    // own enemyTraces.
    this.replayEnemyTraces = null;
    // Fires synchronously the moment a tank is constructed and pushed to
    // aliveTanks -- deliberately NOT the eventBus.enemySpawnCompleted Subject,
    // whose listener order between scripts depends on when each script's own
    // (lazy, first-update-triggered) setup() runs, which isn't guaranteed to
    // be before a listener registered eagerly elsewhere (see LevelPlayScene's
    // enemy-fire recording, which needs the tank to already exist).
    this.tankCreated = new Subject();
    this.handleSpawnTimer = /* @__PURE__ */ __name(() => {
      if (this.aliveTanks.length >= this.getMaxAliveCount()) {
        this.spawnTimer.stop();
        return;
      }
      if (this.listIndex >= this.list.length) {
        this.spawnTimer.stop();
        return;
      }
      if (!this.requestSpawn()) {
        this.spawnTimer.reset(0);
        return;
      }
      this.spawnTimer.reset(ENEMY_SPAWN_DELAY);
    }, "handleSpawnTimer");
    this.handleSpawnCompleted = /* @__PURE__ */ __name((event) => {
      this.spawningCount -= 1;
      const { type } = event;
      if (type.party !== "enemy" /* Enemy */) {
        return;
      }
      const behavior = this.replayEnemyTraces !== null ? new RecordedTankBehavior(this.replayEnemyTraces[event.partyIndex] ?? []) : this.isNetworkEnemyMirror ? new StandStillTankBehavior() : new AiTankBehavior();
      const tank = TankFactory.createEnemy(event.partyIndex, type, behavior);
      tank.setNetworkControlled(this.isNetworkEnemyMirror);
      if (tank.behavior instanceof AiTankBehavior) {
        tank.behavior.setBasePosition(this.mapConfig.getBasePosition());
      }
      tank.updateMatrix();
      tank.rotate(180 /* Down */);
      tank.setCenter(event.centerPosition);
      tank.updateMatrix();
      if (this.freezeTimer.isActive() || this.debugMovementStopped) {
        tank.freezeState.set(true);
      }
      tank.hit.addListener(() => {
        this.eventBus.enemyHit.notify({
          type: tank.type
        });
      });
      tank.died.addListener((deathEvent) => {
        this.eventBus.enemyDied.notify({
          type: tank.type,
          centerPosition: tank.getCenter(),
          reason: deathEvent.reason,
          hitterPartyIndex: deathEvent.hitterPartyIndex
        });
        tank.removeSelf();
        this.activeEnemyIds.delete(tank.partyIndex);
        this.aliveTanks = this.aliveTanks.filter((aliveTank) => {
          return aliveTank !== tank;
        });
        if (!this.isNetworkEnemyMirror && !this.spawnTimer.isActive()) {
          this.spawnTimer.reset(ENEMY_SPAWN_DELAY);
        }
        if (this.areAllDead()) {
          this.eventBus.enemyAllDied.notify(null);
        }
      });
      this.aliveTanks.push(tank);
      this.tankCreated.notify(tank);
      this.world.field.add(tank);
    }, "handleSpawnCompleted");
    this.handleFreezeTimer = /* @__PURE__ */ __name(() => {
      this.aliveTanks.forEach((tank) => {
        tank.freezeState.set(this.debugMovementStopped);
      });
    }, "handleFreezeTimer");
    this.handleDebugMovementToggle = /* @__PURE__ */ __name((stopped) => {
      this.debugMovementStopped = stopped;
      this.aliveTanks.forEach((tank) => {
        tank.freezeState.set(stopped || this.freezeTimer.isActive());
      });
    }, "handleDebugMovementToggle");
    this.handleDebugPlayerMirrorBulletsToggle = /* @__PURE__ */ __name((hidden) => {
      this.debugPlayerMirrorBulletsHidden = hidden;
    }, "handleDebugPlayerMirrorBulletsToggle");
    this.handlePowerupPicked = /* @__PURE__ */ __name((event) => {
      const { type: powerupType } = event;
      if (powerupType === "freeze" /* Freeze */) {
        this.freezeTimer.reset(FREEZE_POWERUP_DURATION);
        this.aliveTanks.forEach((tank) => {
          tank.freezeState.set(true);
        });
      }
      if (powerupType === "wipeout" /* Wipeout */) {
        this.aliveTanks.forEach((tank) => {
          tank.discardDrop();
          tank.die("wipeout" /* WipeoutPowerup */);
        });
      }
    }, "handlePowerupPicked");
    this.isNetworkEnemyMirror = isNetworkEnemyMirror;
    this.headless = headless;
  }
  static {
    __name(this, "LevelEnemyScript");
  }
  setReplayEnemyTraces(traces) {
    this.replayEnemyTraces = traces;
  }
  // Exposes the currently-alive enemies so the scene can record their
  // per-tick movement during a real (non-replay) playthrough.
  getAliveTanks() {
    return this.aliveTanks;
  }
  getActiveEnemyIds() {
    return Array.from(this.activeEnemyIds);
  }
  syncNetworkEnemyCount(activeIds) {
    if (!this.isNetworkEnemyMirror) {
      return;
    }
    const activeIdSet = new Set(activeIds.filter((id) => {
      return Number.isInteger(id) && id >= 0 && id < this.list.length;
    }));
    this.aliveTanks.filter((tank) => !activeIdSet.has(tank.partyIndex)).forEach((tank) => this.removeNetworkEnemy(tank));
    if (activeIdSet.size === 0) {
      return;
    }
    const desiredCount = Math.max(...Array.from(activeIdSet)) + 1;
    while (this.listIndex < desiredCount && this.listIndex < this.list.length) {
      this.requestSpawn();
    }
  }
  syncNetworkReplayEnemies(frames) {
    if (!this.isNetworkEnemyMirror) {
      return;
    }
    frames.forEach((frame) => {
      if (this.aliveTanks.some((tank) => tank.partyIndex === frame.partyIndex) || !Number.isInteger(frame.partyIndex) || frame.partyIndex < 0 || frame.partyIndex >= this.list.length) {
        return;
      }
      this.activeEnemyIds.add(frame.partyIndex);
      this.listIndex = Math.max(this.listIndex, frame.partyIndex + 1);
      this.spawningCount += 1;
      this.handleSpawnCompleted({
        type: this.list[frame.partyIndex],
        centerPosition: new Vector(
          frame.x + TANK_SPAWN_SIZE / 2,
          frame.y + TANK_SPAWN_SIZE / 2
        ),
        partyIndex: frame.partyIndex
      });
    });
  }
  syncNetworkEnemyDeaths(deaths, showEffects = true) {
    if (!this.isNetworkEnemyMirror) {
      return;
    }
    deaths.forEach((death) => {
      const tank = this.aliveTanks.find((candidate) => {
        return candidate.partyIndex === death.partyIndex;
      });
      if (tank !== void 0) {
        this.removeNetworkEnemy(tank, death, showEffects);
      }
    });
  }
  removeNetworkEnemy(tank, death = null, showEffects = true) {
    this.activeEnemyIds.delete(tank.partyIndex);
    const centerPosition = death !== null && Number.isFinite(death.x) && Number.isFinite(death.y) ? new Vector(death.x, death.y) : tank.getCenter();
    if (showEffects) {
      this.eventBus.enemyDied.notify({
        type: tank.type,
        centerPosition,
        reason: death?.reason ?? "bullet" /* Bullet */,
        hitterPartyIndex: death?.hitterPartyIndex ?? null,
        networkMirror: true
      });
    }
    if (showEffects) {
      tank.beginNetworkDeathGrace();
      this.pendingNetworkRemovals.push({
        tank,
        remaining: NETWORK_DEATH_COLLISION_GRACE
      });
    } else {
      tank.finishNetworkRemoval();
    }
    this.aliveTanks = this.aliveTanks.filter((aliveTank) => {
      return aliveTank !== tank;
    });
  }
  init() {
    this.eventBus.enemySpawnCompleted.addListener(this.handleSpawnCompleted);
    this.eventBus.powerupPicked.addListener(this.handlePowerupPicked);
    this.list = this.mapConfig.getEnemySpawnList();
    this.positions = this.mapConfig.getEnemySpawnPositions();
  }
  setup() {
    if (!this.isNetworkEnemyMirror) {
      this.spawnTimer.reset(ENEMY_FIRST_SPAWN_DELAY);
    }
    this.spawnTimer.done.addListener(this.handleSpawnTimer);
    this.freezeTimer.done.addListener(this.handleFreezeTimer);
    if (IS_DEV && !this.headless) {
      const debugMenu = new DebugLevelEnemyMenu({
        top: 365,
        left: 0,
        right: null
      });
      debugMenu.attach();
      debugMenu.movementToggleRequest.addListener(
        this.handleDebugMovementToggle
      );
      debugMenu.playerMirrorBulletsToggleRequest.addListener(
        this.handleDebugPlayerMirrorBulletsToggle
      );
    }
  }
  update(updateArgs) {
    const { deltaTime } = updateArgs;
    updateArgs.magicBlockMovement.setPlayerMirrorBulletsSuppressed(
      this.debugPlayerMirrorBulletsHidden
    );
    if (!this.isNetworkEnemyMirror) {
      this.spawnTimer.update(deltaTime);
    }
    this.freezeTimer.update(deltaTime);
    this.updatePendingNetworkRemovals(deltaTime);
  }
  updatePendingNetworkRemovals(deltaTime) {
    this.pendingNetworkRemovals.forEach((pending) => {
      pending.remaining -= deltaTime;
    });
    this.pendingNetworkRemovals.filter((pending) => pending.remaining <= 0).forEach((pending) => pending.tank.finishNetworkRemoval());
    this.pendingNetworkRemovals = this.pendingNetworkRemovals.filter(
      (pending) => pending.remaining > 0
    );
  }
  requestSpawn() {
    const type = this.list[this.listIndex];
    const position = this.positions[this.positionIndex];
    if (type === void 0 || position === void 0) {
      this.spawnTimer.stop();
      return false;
    }
    if (!this.isNetworkEnemyMirror && this.isSpawnPositionOccupied(position)) {
      return false;
    }
    this.spawningCount += 1;
    const partyIndex = this.listIndex;
    this.activeEnemyIds.add(partyIndex);
    this.listIndex += 1;
    this.positionIndex += 1;
    if (this.positionIndex >= this.positions.length) {
      this.positionIndex = 0;
    }
    const unspawnedCount = this.getUnspawnedCount();
    this.eventBus.enemySpawnRequested.notify({
      type,
      position,
      partyIndex,
      unspawnedCount
    });
    return true;
  }
  isSpawnPositionOccupied(position) {
    const spawnBox = new BoundingBox(
      position.clone(),
      new Vector(position.x + TANK_SPAWN_SIZE, position.y + TANK_SPAWN_SIZE)
    );
    const tanks = [...this.aliveTanks, ...this.world.getPlayerTanks()].filter(
      (tank) => tank !== null && tank !== void 0
    );
    return tanks.some((tank) => {
      tank.updateMatrix();
      return spawnBox.intersectsBox(tank.getBoundingBox());
    });
  }
  getUnspawnedCount() {
    return this.list.length - this.listIndex;
  }
  areAllDead() {
    const spawningCount = this.spawningCount;
    const unspawnedCount = this.getUnspawnedCount();
    const aliveCount = this.aliveTanks.length;
    const areAllDead = spawningCount === 0 && unspawnedCount === 0 && aliveCount === 0;
    return areAllDead;
  }
  getMaxAliveCount() {
    if (this.session.isMultiplayer()) {
      return ENEMY_MAX_ALIVE_COUNT_MULTIPLAYER;
    }
    return ENEMY_MAX_ALIVE_COUNT;
  }
};
function detectNetworkEnemyMirror() {
  if (typeof window === "undefined") {
    return false;
  }
  const params = new URLSearchParams(window.location.search);
  const mode = params.get("mode");
  return mode === "match" || mode === "local" || mode === "webrtc" && params.get("broadcaster") !== "1";
}
__name(detectNetworkEnemyMirror, "detectNetworkEnemyMirror");

// ../src/level/scripts/LevelExplosionScript.ts
init_modules_watch_stub();
var LevelExplosionScript = class extends LevelScript {
  constructor() {
    super(...arguments);
    this.handleEnemyDied = /* @__PURE__ */ __name((event) => {
      const explosion = new Explosion();
      explosion.updateMatrix();
      explosion.setCenter(event.centerPosition);
      explosion.completed.addListener(() => {
        this.eventBus.enemyExploded.notify(event);
      });
      this.world.field.add(explosion);
    }, "handleEnemyDied");
    this.handlePlayerDied = /* @__PURE__ */ __name((event) => {
      const explosion = new Explosion();
      explosion.updateMatrix();
      explosion.setCenter(event.centerPosition);
      this.world.field.add(explosion);
    }, "handlePlayerDied");
  }
  static {
    __name(this, "LevelExplosionScript");
  }
  setup() {
    this.eventBus.enemyDied.addListener(this.handleEnemyDied);
    this.eventBus.playerDied.addListener(this.handlePlayerDied);
  }
};

// ../src/level/scripts/LevelGameOverScript.ts
init_modules_watch_stub();
var SLIDE_SPEED2 = 200;
var TARGET_POSITION_Y = 360;
var TOTAL_DURATION = 5;
var MOVE_BLOCK_DELAY = 1;
var LevelGameOverScript = class extends LevelScript {
  constructor() {
    super(...arguments);
    // Disable by default
    this.enabled = false;
    this.moveBlockTimer = new Timer();
    this.totalTimer = new Timer();
    this.handleMoveBlockTimer = /* @__PURE__ */ __name(() => {
      this.eventBus.levelGameOverMoveBlocked.notify(null);
    }, "handleMoveBlockTimer");
    this.handleTotalTimer = /* @__PURE__ */ __name(() => {
      this.eventBus.levelGameOverCompleted.notify(null);
    }, "handleTotalTimer");
  }
  static {
    __name(this, "LevelGameOverScript");
  }
  setup() {
    this.notice = new GameOverNotice();
    this.notice.updateMatrix();
    this.notice.setCenterX(this.world.field.getSelfCenter().x);
    this.notice.position.y = this.world.field.size.height + 100;
    this.world.field.add(this.notice);
    this.moveBlockTimer.reset(MOVE_BLOCK_DELAY);
    this.moveBlockTimer.done.addListener(this.handleMoveBlockTimer);
    this.totalTimer.reset(TOTAL_DURATION);
    this.totalTimer.done.addListener(this.handleTotalTimer);
  }
  update(updateArgs) {
    const { deltaTime } = updateArgs;
    this.notice.dirtyPaintBox();
    this.notice.position.y -= SLIDE_SPEED2 * deltaTime;
    if (this.notice.position.y <= TARGET_POSITION_Y) {
      this.notice.position.y = TARGET_POSITION_Y;
    }
    this.notice.updateMatrix();
    this.moveBlockTimer.update(deltaTime);
    this.totalTimer.update(deltaTime);
  }
};

// ../src/level/scripts/LevelIntroScript.ts
init_modules_watch_stub();
var LevelIntroScript = class extends LevelScript {
  constructor() {
    super(...arguments);
    this.completed = new Subject();
    this.handleTimer = /* @__PURE__ */ __name(() => {
      this.curtain.open();
      this.title.setVisible(false);
      this.completed.notify(null);
    }, "handleTimer");
  }
  static {
    __name(this, "LevelIntroScript");
  }
  setup() {
    this.timer = new Timer(LEVEL_START_DELAY);
    this.timer.done.addListener(this.handleTimer);
    this.curtain = new Curtain(
      this.world.sceneRoot.size.width,
      this.world.sceneRoot.size.height,
      false
    );
    this.world.sceneRoot.add(this.curtain);
    this.title = new LevelTitle(
      this.session.getLevelNumber(),
      this.session.isPlaytest()
    );
    this.title.setCenter(this.world.sceneRoot.getSelfCenter());
    this.title.origin.set(0.5, 0.5);
    this.world.sceneRoot.add(this.title);
  }
  update(updateArgs) {
    if (updateArgs.webRtcMatch.isWaitingForPeer()) {
      return;
    }
    this.timer.update(updateArgs.deltaTime);
  }
};

// ../src/level/scripts/LevelPlayerOverScript.ts
init_modules_watch_stub();
var MOVE_DURATION = 1;
var STAY_DURATION = 1;
var SLIDE_POSITIONS = [
  { startX: 64, endX: 224 },
  { startX: 640, endX: 480 }
];
var LevelPlayerOverScript = class extends LevelScript {
  constructor() {
    super(...arguments);
    // Disable by default
    this.enabled = false;
    this.timer = new Timer();
    this.playerIndex = -1;
    this.state = 0 /* Idle */;
    this.handleTimerDone = /* @__PURE__ */ __name(() => {
      if (this.state === 1 /* Moving */) {
        this.state = 2 /* Staying */;
        this.timer.reset(STAY_DURATION);
        return;
      }
      if (this.state === 2 /* Staying */) {
        this.state = 3 /* Done */;
        this.notice.dirtyPaintBox();
        this.notice.removeSelf();
        return;
      }
    }, "handleTimerDone");
  }
  static {
    __name(this, "LevelPlayerOverScript");
  }
  setPlayerIndex(playerIndex) {
    this.playerIndex = playerIndex;
  }
  setup() {
    this.notice = new GameOverNotice();
    this.notice.updateMatrix();
    this.notice.position.setX(SLIDE_POSITIONS[this.playerIndex].startX);
    this.notice.position.setY(
      this.world.field.size.height - this.notice.size.height
    );
    this.world.field.add(this.notice);
    this.state = 1 /* Moving */;
    this.timer.reset(MOVE_DURATION);
    this.timer.done.addListener(this.handleTimerDone);
  }
  update(updateArgs) {
    const { deltaTime } = updateArgs;
    if (this.state === 1 /* Moving */) {
      this.notice.dirtyPaintBox();
      const { startX, endX } = SLIDE_POSITIONS[this.playerIndex];
      const totalDistance = endX - startX;
      const speed = totalDistance / MOVE_DURATION;
      this.notice.position.x += speed * deltaTime;
      if (this.playerIndex === 0 && this.notice.position.x > endX) {
        this.notice.position.x = endX;
      } else if (this.playerIndex === 1 && this.notice.position.x < endX) {
        this.notice.position.x = endX;
      }
      this.notice.updateMatrix();
      this.timer.update(deltaTime);
    }
    if (this.state === 2 /* Staying */) {
      this.timer.update(deltaTime);
    }
  }
};

// ../src/level/scripts/LevelPlayerScript.ts
init_modules_watch_stub();
var HOTBAR_SLOT_SIZE = 70;
var HOTBAR_SLOT_GAP = 12;
var HOTBAR_SLOT_COUNT = 4;
var PowerHotbarSlot = class extends GameObject {
  static {
    __name(this, "PowerHotbarSlot");
  }
  constructor(index, itemId = null, count = 0) {
    super(HOTBAR_SLOT_SIZE, HOTBAR_SLOT_SIZE);
    this.itemId = itemId;
    this.painter = new RectPainter(
      itemId === null ? "rgba(26, 24, 16, 0.62)" : "rgba(68, 56, 10, 0.82)",
      itemId === null ? "rgba(145, 119, 20, 0.35)" : COLOR_YELLOW
    );
    this.painter.lineWidth = 3;
    this.keyText = new SpriteText((index + 1).toString(), {
      color: itemId === null ? COLOR_GRAY : COLOR_WHITE,
      letterSpacing: 0
    });
    this.keyText.position.set(6, 4);
    this.add(this.keyText);
    this.icon = new GameObject(44, 44);
    this.icon.position.set(17, 19);
    this.icon.painter = new SpritePainter(null, 0 /* Stretch */);
    this.add(this.icon);
    this.countText = new SpriteText(count > 1 ? `X${count}` : "", {
      color: COLOR_YELLOW,
      letterSpacing: 0
    });
    this.countText.position.set(34, 48);
    this.add(this.countText);
  }
  setup({ spriteLoader }) {
    if (this.itemId === null) {
      return;
    }
    this.icon.painter.sprite = spriteLoader.load(
      this.getIconId(this.itemId)
    );
  }
  getIconId(itemId) {
    switch (itemId) {
      case "shield" /* Shield */:
        return "powerup.helmet";
      case "base-defence" /* BaseDefence */:
        return "powerup.shovel";
      case "freeze" /* Freeze */:
        return "powerup.clock";
      case "speed" /* Speed */:
        return "powerup.speed";
      case "upgrade" /* Upgrade */:
        return "powerup.star";
      case "zoom-out" /* ZoomOut */:
        return "powerup.zoomout";
      case "wipeout" /* Wipeout */:
        return "powerup.grenade";
      case "extra-life" /* ExtraLife */:
        return "powerup.life";
      default:
        return "shop.bundle";
    }
  }
};
var LevelPlayerScript = class extends LevelScript {
  constructor(options = {}) {
    super();
    this.tankCreated = new Subject();
    this.positions = [];
    this.timers = [];
    this.tanks = [];
    this.hotbar = new GameObject();
    this.shopManager = null;
    this.isReplaying = false;
    this.isWebRtcClient = false;
    this.localPlayerIndex = 0;
    this.requestSpawn = /* @__PURE__ */ __name((partyIndex) => {
      const playerSession = this.session.getPlayer(partyIndex);
      if (!playerSession.isAlive()) {
        return;
      }
      const position = this.positions[partyIndex];
      const type = new TankType3(
        "player" /* Player */,
        this.session.getPlayerTankTier(partyIndex)
      );
      this.eventBus.playerSpawnRequested.notify({
        type,
        partyIndex,
        position
      });
    }, "requestSpawn");
    this.handleSpawnCompleted = /* @__PURE__ */ __name((event) => {
      if (event.type.party !== "player" /* Player */) {
        return;
      }
      const { partyIndex } = event;
      const tank = TankFactory.createPlayer(partyIndex, event.type.clone());
      tank.updateMatrix();
      tank.setCenter(event.centerPosition);
      tank.updateMatrix();
      tank.activateShield(SHIELD_SPAWN_DURATION);
      tank.setRunBoosts(this.session.getRunBoosts());
      const playerSession = this.session.getPlayer(partyIndex);
      const isLevelFirstSpawn = playerSession.isLevelFirstSpawn();
      if (isLevelFirstSpawn) {
        const carryoverTier = playerSession.getTankTier();
        if (getTankTierRank(carryoverTier) > getTankTierRank(tank.type.tier)) {
          tank.upgrade(carryoverTier, false);
        }
      }
      playerSession.setTankTier(tank.type.tier);
      this.session.setPlayerTankTier(partyIndex, tank.type.tier);
      tank.died.addListener(() => {
        this.eventBus.playerDied.notify({
          type: event.type,
          centerPosition: tank.getCenter(),
          partyIndex
        });
        tank.removeSelf();
        this.tanks[partyIndex] = null;
        this.world.removePlayerTank(partyIndex);
        this.timers[partyIndex].reset(PLAYER_SPAWN_DELAY);
        playerSession.resetTankTier();
        this.session.setPlayerTankTier(partyIndex, "a" /* A */);
      });
      tank.fired.addListener(() => {
        this.eventBus.playerFired.notify(null);
      });
      tank.upgraded.addListener((event2) => {
        playerSession.setTankTier(event2.tier);
        this.session.setPlayerTankTier(partyIndex, event2.tier);
      });
      tank.slided.addListener(() => {
        this.eventBus.playerSlided.notify(null);
      });
      playerSession.setLevelSpawned();
      this.tanks[partyIndex] = tank;
      this.tankCreated.notify(tank);
      this.world.addPlayerTank(partyIndex, tank);
    }, "handleSpawnCompleted");
    this.handlePowerupPicked = /* @__PURE__ */ __name((event) => {
      const { type: powerupType, partyIndex, hotbarSlot } = event;
      if (this.isWebRtcClient && partyIndex === this.localPlayerIndex && Number.isInteger(hotbarSlot)) {
        this.consumeClientHotbarSlot(hotbarSlot);
      }
      const tank = this.tanks[partyIndex];
      if (powerupType === "shield" /* Shield */) {
        tank.activateShield(SHIELD_POWERUP_DURATION);
      }
      if (powerupType === "speed" /* Speed */) {
        tank.activateSpeedBoost(
          SPEED_POWERUP_DURATION,
          SPEED_POWERUP_MULTIPLIER
        );
      }
      if (powerupType === "upgrade" /* Upgrade */) {
        tank.upgrade();
      }
    }, "handlePowerupPicked");
    this.handleLevelGameOverMoveBlocked = /* @__PURE__ */ __name(() => {
      this.tanks.forEach((tank) => {
        if (tank === null) {
          return;
        }
        tank.freezeState.set(true);
      });
    }, "handleLevelGameOverMoveBlocked");
    this.headless = options.headless === true;
    this.playerRunConsumables = options.playerRunConsumables === void 0 ? null : options.playerRunConsumables.map((consumables) => ({
      powerups: [...consumables.powerups],
      powerupCounts: [...consumables.powerupCounts]
    }));
  }
  static {
    __name(this, "LevelPlayerScript");
  }
  setup(updateArgs) {
    const {
      gameStorage,
      inputManager,
      session,
      webRtcMatch
    } = updateArgs;
    this.isWebRtcClient = webRtcMatch.isEnabled() && !webRtcMatch.isBroadcaster();
    const isWebRtcObserver = !this.headless && webRtcMatch.isObserver();
    if (this.isWebRtcClient) {
      this.localPlayerIndex = webRtcMatch.getLocalPlayerIndex();
    }
    if (!this.headless) {
      this.shopManager = new ShopManager(gameStorage);
      this.isReplaying = inputManager.isReplaying();
    }
    const runConsumables = session.getRunConsumables();
    if (!this.headless && !isWebRtcObserver && !this.isReplaying && session.getLevelNumber() === 1 && runConsumables.powerupItems.length === 0 && runConsumables.powerups.length === 0) {
      const equippedConsumables = this.shopManager.getEquippedRunConsumables();
      if (equippedConsumables.powerupItems.length > 0) {
        session.setRunConsumables(equippedConsumables);
      }
    }
    this.eventBus.playerSpawnCompleted.addListener(this.handleSpawnCompleted);
    this.eventBus.powerupPicked.addListener(this.handlePowerupPicked);
    this.eventBus.levelGameOverMoveBlocked.addListener(
      this.handleLevelGameOverMoveBlocked
    );
    this.positions = this.mapConfig.getPlayerSpawnPositions();
    if (!session.isMultiplayer()) {
      this.positions = this.positions.slice(0, 1);
    }
    this.positions.forEach((position, index) => {
      const timer = new Timer(PLAYER_FIRST_SPAWN_DELAY);
      timer.done.addListener(() => {
        this.requestSpawn(index);
      });
      this.timers.push(timer);
      this.tanks.push(null);
    });
    if (IS_DEV && !this.headless) {
      const debugMenu = new DebugLevelPlayerMenu({
        top: 365
      });
      debugMenu.attach();
      debugMenu.upgradeRequest.addListener((partyIndex) => {
        const tank = this.tanks[partyIndex];
        if (tank === null) {
          return;
        }
        tank.upgrade();
      });
      debugMenu.deathRequest.addListener((partyIndex) => {
        const tank = this.tanks[partyIndex];
        if (tank === null) {
          return;
        }
        tank.die();
      });
      debugMenu.moveSpeedUpRequest.addListener(({ partyIndex, speed }) => {
        const tank = this.tanks[partyIndex];
        if (tank === null) {
          return;
        }
        tank.attributes.moveSpeed += speed;
      });
    }
    this.hotbar.position.set(
      CANVAS_WIDTH - BORDER_RIGHT_WIDTH - HOTBAR_SLOT_COUNT * HOTBAR_SLOT_SIZE - (HOTBAR_SLOT_COUNT - 1) * HOTBAR_SLOT_GAP - 18,
      CANVAS_HEIGHT - BORDER_TOP_BOTTOM_HEIGHT - HOTBAR_SLOT_SIZE - 14
    );
    this.hotbar.setZIndex(500);
    if (!this.headless && !isWebRtcObserver && !isMobileTouchLayout()) {
      this.world.sceneRoot.add(this.hotbar);
      this.renderHotbar();
    }
  }
  update(updateArgs) {
    const { deltaTime, inputManager, webRtcMatch } = updateArgs;
    this.timers.forEach((timer) => {
      timer.update(deltaTime);
    });
    if (webRtcMatch.isEnabled()) {
      if (webRtcMatch.isBroadcaster()) {
        this.tanks.forEach((_tank, partyIndex) => {
          for (let action = 0; action < HOTBAR_SLOT_COUNT; action += 1) {
            const slot = webRtcMatch.consumeRemotePowerSlot(partyIndex);
            if (slot === null) {
              break;
            }
            this.useHotbarPower(slot, partyIndex, false);
          }
        });
      }
      return;
    }
    if (this.headless) {
      return;
    }
    const inputMethod = inputManager.getActiveMethod();
    if (inputMethod.isDownAny(LevelPlayInputContext.PowerOne)) {
      this.useHotbarPower(0);
    } else if (inputMethod.isDownAny(LevelPlayInputContext.PowerTwo)) {
      this.useHotbarPower(1);
    } else if (inputMethod.isDownAny(LevelPlayInputContext.PowerThree)) {
      this.useHotbarPower(2);
    } else if (inputMethod.isDownAny(LevelPlayInputContext.PowerFour)) {
      this.useHotbarPower(3);
    }
  }
  getAuthoritativeRunConsumables() {
    if (this.playerRunConsumables === null) {
      return null;
    }
    return this.playerRunConsumables.map((consumables) => ({
      powerups: [...consumables.powerups],
      powerupCounts: [...consumables.powerupCounts]
    }));
  }
  useHotbarPower(index, partyIndex = 0, consumeStoredInventory = true) {
    const tank = this.tanks[partyIndex];
    if (tank === null || tank === void 0) {
      return;
    }
    const authoritativeConsumables = this.playerRunConsumables?.[partyIndex] ?? null;
    const runConsumables = authoritativeConsumables ?? this.session.getRunConsumables();
    const powerupCounts = runConsumables.powerupCounts || [];
    runConsumables.powerupCounts = powerupCounts;
    const itemId = authoritativeConsumables === null ? this.session.getRunConsumables().powerupItems[index] : null;
    const type = runConsumables.powerups[index];
    if (type === void 0 || consumeStoredInventory && itemId === void 0) {
      return;
    }
    if (consumeStoredInventory && !this.isReplaying && !this.shopManager.consumeInventoryItem(itemId)) {
      this.session.getRunConsumables().powerupItems.splice(index, 1);
      runConsumables.powerups.splice(index, 1);
      powerupCounts.splice(index, 1);
      this.renderHotbar();
      return;
    }
    const stackCount = powerupCounts[index] || 1;
    if (stackCount > 1) {
      powerupCounts[index] = stackCount - 1;
    } else {
      if (authoritativeConsumables === null) {
        this.session.getRunConsumables().powerupItems.splice(index, 1);
      }
      runConsumables.powerups.splice(index, 1);
      powerupCounts.splice(index, 1);
    }
    this.eventBus.powerupPicked.notify({
      type,
      centerPosition: tank.getCenter(),
      partyIndex,
      hotbarSlot: index
    });
    if (!this.headless) {
      this.renderHotbar();
    }
  }
  renderHotbar() {
    this.hotbar.removeAllChildren();
    const runConsumables = this.session.getRunConsumables();
    const powerupCounts = runConsumables.powerupCounts || [];
    runConsumables.powerupCounts = powerupCounts;
    for (let index = 0; index < HOTBAR_SLOT_COUNT; index += 1) {
      const slot = new PowerHotbarSlot(
        index,
        runConsumables.powerupItems[index],
        powerupCounts[index] || 0
      );
      slot.position.set(index * (HOTBAR_SLOT_SIZE + HOTBAR_SLOT_GAP), 0);
      this.hotbar.add(slot);
    }
  }
  consumeClientHotbarSlot(index) {
    const runConsumables = this.session.getRunConsumables();
    const powerupCounts = runConsumables.powerupCounts || [];
    const stackCount = powerupCounts[index] || 1;
    if (stackCount > 1) {
      powerupCounts[index] = stackCount - 1;
    } else {
      runConsumables.powerupItems.splice(index, 1);
      runConsumables.powerups.splice(index, 1);
      powerupCounts.splice(index, 1);
    }
    runConsumables.powerupCounts = powerupCounts;
    this.renderHotbar();
  }
};
function getTankTierRank(tier) {
  switch (tier) {
    case "b" /* B */:
      return 1;
    case "c" /* C */:
      return 2;
    case "d" /* D */:
      return 3;
    default:
      return 0;
  }
}
__name(getTankTierRank, "getTankTierRank");

// ../src/level/scripts/LevelPointsScript.ts
init_modules_watch_stub();
var LevelPointsScript = class extends LevelScript {
  constructor() {
    super(...arguments);
    this.handleEnemyExploded = /* @__PURE__ */ __name((event) => {
      if (event.reason === "wipeout" /* WipeoutPowerup */) {
        return;
      }
      const value = this.getEnemyTankPointsValue(event.type);
      const points = new Points(value, POINTS_ENEMY_TANK_DURATION);
      points.updateMatrix();
      points.setCenter(event.centerPosition);
      this.world.field.add(points);
    }, "handleEnemyExploded");
    this.handlePowerupPicked = /* @__PURE__ */ __name((event) => {
      const points = new Points(500 /* V500 */, POINTS_POWERUP_DURATION);
      points.updateMatrix();
      points.setCenter(event.centerPosition);
      this.world.field.add(points);
    }, "handlePowerupPicked");
  }
  static {
    __name(this, "LevelPointsScript");
  }
  setup() {
    this.eventBus.enemyExploded.addListener(this.handleEnemyExploded);
    this.eventBus.powerupPicked.addListener(this.handlePowerupPicked);
  }
  getEnemyTankPointsValue(type) {
    switch (type.tier) {
      case "a" /* A */:
        return 100 /* V100 */;
      case "b" /* B */:
        return 200 /* V200 */;
      case "c" /* C */:
        return 300 /* V300 */;
      case "d" /* D */:
        return 400 /* V400 */;
      default:
        return 0;
    }
  }
};

// ../src/level/scripts/LevelPowerupScript.ts
init_modules_watch_stub();
var LevelPowerupScript = class extends LevelScript {
  constructor(options = {}) {
    super();
    this.networkPowerupId = null;
    this.activePowerupId = null;
    this.powerupSequence = 0;
    this.pickupSequence = 0;
    this.lastNetworkPickupSequence = 0;
    this.latestPickup = null;
    this.activePowerup = null;
    // Dev-only match replay (see src/replay/PowerupSpawnFrame): when set, each
    // spawn() call re-enacts the next recorded (type, position) pair instead of
    // drawing from rng.
    this.replayPowerupSpawns = null;
    this.replaySpawnIndex = 0;
    this.isRecordingPowerups = false;
    this.recordedPowerupSpawns = [];
    this.handleEnemyHit = /* @__PURE__ */ __name((event) => {
      if (this.isLocalServerMatch || this.isWebRtcClient) {
        return;
      }
      const { type: tankType } = event;
      if (!tankType.hasDrop) {
        return;
      }
      this.spawn();
    }, "handleEnemyHit");
    // Remove active powerup whenever new enemy spawns with drop
    this.handleEnemySpawnCompleted = /* @__PURE__ */ __name((event) => {
      if (this.isLocalServerMatch || this.isWebRtcClient) {
        return;
      }
      const { type: tankType } = event;
      if (!tankType.hasDrop) {
        return;
      }
      this.revoke();
    }, "handleEnemySpawnCompleted");
    // Remove powerup after timer expires
    this.handleTimer = /* @__PURE__ */ __name(() => {
      this.revoke();
    }, "handleTimer");
    this.handleMapTileDestroyed = /* @__PURE__ */ __name((event) => {
      const { type: terrainType, position, size } = event;
      if (terrainType !== "steel" /* Steel */) {
        return;
      }
      const rect = new Rect(position.x, position.y, size.width, size.height);
      this.grid.freeRect(rect);
    }, "handleMapTileDestroyed");
    this.capturePowerupPickup = /* @__PURE__ */ __name((event) => {
      this.pickupSequence += 1;
      this.latestPickup = {
        seq: this.pickupSequence,
        type: event.type,
        partyIndex: event.partyIndex,
        x: event.centerPosition.x,
        y: event.centerPosition.y,
        hotbarSlot: event.hotbarSlot
      };
    }, "capturePowerupPickup");
    const detected = detectNetworkMode();
    this.isLocalServerMatch = options.isLocalServerMatch ?? detected.isLocalServerMatch;
    this.isWebRtcClient = options.isWebRtcClient ?? detected.isWebRtcClient;
    this.headless = options.headless === true;
  }
  static {
    __name(this, "LevelPowerupScript");
  }
  setReplayPowerupSpawns(frames) {
    this.replayPowerupSpawns = frames;
  }
  startRecordingPowerups() {
    this.isRecordingPowerups = true;
  }
  getRecordedPowerupSpawns() {
    return this.recordedPowerupSpawns;
  }
  syncNetworkPowerup(snapshot) {
    if (!this.isLocalServerMatch) {
      return;
    }
    this.applyNetworkPowerup(snapshot);
  }
  syncWebRtcPowerup(snapshot) {
    if (!this.isWebRtcClient) {
      return;
    }
    this.applyNetworkPowerup(snapshot);
  }
  syncWebRtcPickup(snapshot) {
    if (!this.isWebRtcClient || snapshot === null || snapshot.seq <= this.lastNetworkPickupSequence) {
      return;
    }
    this.lastNetworkPickupSequence = snapshot.seq;
    this.eventBus.powerupPicked.notify({
      type: snapshot.type,
      partyIndex: snapshot.partyIndex,
      centerPosition: new Vector(snapshot.x, snapshot.y),
      hotbarSlot: snapshot.hotbarSlot
    });
  }
  getWebRtcPowerup() {
    if (this.activePowerup === null || this.activePowerupId === null) {
      return null;
    }
    return {
      id: this.activePowerupId,
      kind: this.activePowerup.type,
      x: this.activePowerup.position.x,
      y: this.activePowerup.position.y
    };
  }
  getWebRtcPickup() {
    return this.latestPickup;
  }
  applyNetworkPowerup(snapshot) {
    if (snapshot === null) {
      if (this.networkPowerupId !== null) {
        this.revoke();
        this.networkPowerupId = null;
      }
      return;
    }
    if (snapshot.id === this.networkPowerupId) {
      return;
    }
    this.revoke();
    const powerup = PowerupFactory.create(snapshot.kind);
    powerup.position.set(snapshot.x, snapshot.y);
    powerup.setNetworkControlled(true);
    this.activePowerup = powerup;
    this.networkPowerupId = snapshot.id;
    this.world.field.add(powerup);
    this.eventBus.powerupSpawned.notify({
      type: powerup.type,
      position: powerup.position.clone()
    });
  }
  setup(updateArgs) {
    this.rng = updateArgs.rng;
    this.eventBus.enemyHit.addListener(this.handleEnemyHit);
    this.eventBus.enemySpawnCompleted.addListener(
      this.handleEnemySpawnCompleted
    );
    this.eventBus.mapTileDestroyed.addListener(this.handleMapTileDestroyed);
    this.eventBus.powerupPicked.addListener(this.capturePowerupPickup);
    this.timer = new Timer();
    this.timer.done.addListener(this.handleTimer);
    this.grid = new PowerupGrid(
      this.mapConfig.getFieldWidth(),
      this.mapConfig.getFieldHeight()
    );
    this.blockGridDefaults();
    this.blockGridInitialMap();
    if (IS_DEV && !this.headless) {
      const debugMenu = new DebugLevelPowerupMenu(this.world, this.grid, {
        top: 125
      });
      debugMenu.attach();
      debugMenu.spawnRequest.addListener((type) => {
        this.spawn(type);
      });
    }
  }
  update({ deltaTime }) {
    if (this.isWebRtcClient) {
      return;
    }
    this.timer.update(deltaTime);
  }
  spawn(type = null) {
    this.revoke();
    const playerTankRects = this.createPlayerTankRects();
    let powerup;
    let position;
    if (this.replayPowerupSpawns !== null) {
      const frame = this.replayPowerupSpawns[this.replaySpawnIndex] ?? null;
      this.replaySpawnIndex += 1;
      powerup = frame !== null ? PowerupFactory.create(frame.type) : PowerupFactory.createRandom(this.rng);
      position = frame !== null && frame.position !== null ? new Vector(frame.position.x, frame.position.y) : null;
    } else {
      powerup = type !== null ? PowerupFactory.create(type) : PowerupFactory.createRandom(this.rng);
      if (playerTankRects.length > 0) {
        this.grid.backup();
        playerTankRects.forEach((playerTankRect) => {
          if (playerTankRect === null) {
            return;
          }
          this.grid.blockRect(playerTankRect);
        });
      }
      position = this.grid.getRandomPosition(this.rng);
      if (playerTankRects.length > 0) {
        this.grid.restore();
      }
    }
    if (this.isRecordingPowerups) {
      this.recordedPowerupSpawns.push({
        type: powerup.type,
        position: position !== null ? { x: position.x, y: position.y } : null
      });
    }
    if (position === null) {
      let partyIndex = 0;
      if (playerTankRects[0] === null) {
        partyIndex = 1;
      }
      const directRect = playerTankRects[partyIndex] ?? this.createBaseRect();
      this.eventBus.powerupPicked.notify({
        type: powerup.type,
        centerPosition: directRect.getCenter(),
        partyIndex
      });
      return;
    }
    powerup.position.copyFrom(position);
    powerup.picked.addListener(({ partyIndex }) => {
      this.activePowerup = null;
      this.activePowerupId = null;
      this.timer.stop();
      this.eventBus.powerupPicked.notify({
        type: powerup.type,
        centerPosition: powerup.getCenter(),
        partyIndex
      });
    });
    const salvage = this.session.getRunBoosts().salvage;
    this.timer.reset(POWERUP_DURATION * (1 + salvage / 100));
    this.activePowerup = powerup;
    this.activePowerupId = ++this.powerupSequence;
    this.world.field.add(powerup);
    this.eventBus.powerupSpawned.notify({
      type: powerup.type,
      position
    });
  }
  revoke() {
    if (this.activePowerup === null) {
      return;
    }
    this.activePowerup.destroy();
    this.activePowerup = null;
    this.activePowerupId = null;
    this.eventBus.powerupRevoked.notify(null);
  }
  createBaseRect() {
    const basePosition = this.mapConfig.getBasePosition();
    return new Rect(
      basePosition.x,
      basePosition.y,
      BASE_DEFAULT_SIZE.width,
      BASE_DEFAULT_SIZE.height
    );
  }
  createPlayerTankRects() {
    const rects = [];
    const playerTanks = this.world.getPlayerTanks();
    playerTanks.forEach((playerTank) => {
      if (playerTank === null) {
        rects.push(null);
        return;
      }
      const margin = TILE_SIZE_LARGE;
      const rect = new Rect(
        playerTank.position.x - margin,
        playerTank.position.y - margin,
        playerTank.size.width + margin * 2,
        playerTank.size.height + margin * 2
      );
      rects.push(rect);
    });
    return rects;
  }
  blockGridDefaults() {
    this.grid.blockRect(this.createBaseRect());
    const playerSpawnPositions = this.mapConfig.getPlayerSpawnPositions();
    playerSpawnPositions.forEach((position) => {
      this.grid.blockRect(new Rect(position.x, position.y, 64, 64));
    });
    const enemySpawnPositions = this.mapConfig.getEnemySpawnPositions();
    enemySpawnPositions.forEach((position) => {
      this.grid.blockRect(new Rect(position.x, position.y, 64, 64));
    });
  }
  blockGridInitialMap() {
    const denyTypes = ["steel" /* Steel */, "water" /* Water */];
    const regions = this.mapConfig.getTerrainRegions();
    regions.forEach((region) => {
      if (!denyTypes.includes(region.type)) {
        return;
      }
      this.grid.blockRect(
        new Rect(region.x, region.y, region.width, region.height)
      );
    });
  }
};
function detectNetworkMode() {
  if (typeof window === "undefined") {
    return { isLocalServerMatch: false, isWebRtcClient: false };
  }
  const params = new URLSearchParams(window.location.search);
  return {
    isLocalServerMatch: params.get("mode") === "local",
    isWebRtcClient: params.get("mode") === "webrtc" && params.get("broadcaster") !== "1"
  };
}
__name(detectNetworkMode, "detectNetworkMode");

// ../src/level/scripts/LevelSpawnScript.ts
init_modules_watch_stub();
var LevelSpawnScript = class extends LevelScript {
  constructor() {
    super(...arguments);
    this.handleEnemySpawnRequested = /* @__PURE__ */ __name((event) => {
      const spawn = new Spawn();
      spawn.position.copyFrom(event.position);
      spawn.updateMatrix();
      spawn.completed.addListenerOnce(() => {
        this.eventBus.enemySpawnCompleted.notify({
          type: event.type,
          centerPosition: spawn.getCenter(),
          partyIndex: event.partyIndex
        });
        spawn.removeSelf();
      });
      this.world.field.add(spawn);
    }, "handleEnemySpawnRequested");
    this.handlePlayerSpawnRequested = /* @__PURE__ */ __name((event) => {
      const spawn = new Spawn();
      spawn.position.copyFrom(event.position);
      spawn.updateMatrix();
      spawn.completed.addListenerOnce(() => {
        this.eventBus.playerSpawnCompleted.notify({
          type: event.type,
          centerPosition: spawn.getCenter(),
          partyIndex: event.partyIndex
        });
        spawn.removeSelf();
      });
      this.world.field.add(spawn);
    }, "handlePlayerSpawnRequested");
  }
  static {
    __name(this, "LevelSpawnScript");
  }
  init() {
    this.eventBus.enemySpawnRequested.addListener(
      this.handleEnemySpawnRequested
    );
    this.eventBus.playerSpawnRequested.addListener(
      this.handlePlayerSpawnRequested
    );
  }
};

// ../src/level/scripts/LevelWinScript.ts
init_modules_watch_stub();
var POST_WIN_DELAY = 3;
var LevelWinScript = class extends LevelScript {
  constructor() {
    super(...arguments);
    // Disable by default
    this.enabled = false;
    this.timer = new Timer();
    this.handleTimer = /* @__PURE__ */ __name(() => {
      this.eventBus.levelWinCompleted.notify(null);
    }, "handleTimer");
  }
  static {
    __name(this, "LevelWinScript");
  }
  setup() {
    this.timer.reset(POST_WIN_DELAY);
    this.timer.done.addListener(this.handleTimer);
  }
  update(updateArgs) {
    this.timer.update(updateArgs.deltaTime);
  }
};

// ../src/map/MapConfig.ts
init_modules_watch_stub();

// ../src/map/MapDtoSchema.ts
init_modules_watch_stub();
var Joi = __toESM(require_lib4());
var DEFAULT_VERSION = 2;
var MapDtoSchema = Joi.object({
  base: Joi.object({
    x: Joi.number().required(),
    y: Joi.number().required()
  }).optional(),
  field: Joi.object({
    widthTiles: Joi.number().integer().min(8).max(40).default(20),
    heightTiles: Joi.number().integer().min(8).max(40).default(20)
  }).default(),
  version: Joi.number().default(DEFAULT_VERSION),
  spawn: Joi.object({
    enemy: Joi.object({
      locations: Joi.array().items(
        Joi.object({
          x: Joi.number().required(),
          y: Joi.number().required()
        })
      ).default([]),
      list: Joi.array().items(
        Joi.object({
          tier: Joi.string().valid(...Object.values(TankTier)).required(),
          drop: Joi.boolean()
        })
      ).default([])
    }).default(),
    player: Joi.object({
      locations: Joi.array().items(
        Joi.object({
          x: Joi.number().required(),
          y: Joi.number().required()
        })
      ).default([])
    }).default()
  }).default(),
  terrain: Joi.object({
    regions: Joi.array().items(
      Joi.object({
        type: Joi.string().valid(...Object.values(TerrainType)).required(),
        x: Joi.number().required(),
        y: Joi.number().required(),
        width: Joi.number().required(),
        height: Joi.number().required()
      })
    ).default([])
  }).default()
});

// ../src/map/MapConfig.ts
var DEFAULT_TO_JSON_OPTIONS = {
  pretty: true
};
var MapConfig = class {
  static {
    __name(this, "MapConfig");
  }
  constructor() {
    this.dto = this.fillAndValidate({});
  }
  getDto() {
    return this.dto;
  }
  fromDto(dto) {
    this.dto = this.fillAndValidate(dto);
  }
  fillAndValidate(dto) {
    const dtoWithFieldDefaults = this.addMissingFieldDefaults(dto);
    const { value: validatedDto, error: schemaError } = MapDtoSchema.validate(
      dtoWithFieldDefaults
    );
    if (schemaError !== void 0) {
      throw schemaError;
    }
    const terrainError = TerrainFactory.validateRegionConfigs(
      validatedDto.terrain.regions
    );
    if (terrainError !== void 0) {
      throw terrainError;
    }
    return validatedDto;
  }
  addMissingFieldDefaults(dto) {
    if (dto.field !== void 0) {
      return dto;
    }
    const hasMapContent = dto.base !== void 0 || (dto.terrain?.regions?.length ?? 0) > 0 || (dto.spawn?.enemy?.locations?.length ?? 0) > 0 || (dto.spawn?.enemy?.list?.length ?? 0) > 0 || (dto.spawn?.player?.locations?.length ?? 0) > 0;
    const isLegacyMap = dto.version === void 0 || dto.version < 2;
    if (!hasMapContent || !isLegacyMap) {
      return dto;
    }
    return {
      ...dto,
      field: {
        widthTiles: LEGACY_FIELD_TILE_COUNT,
        heightTiles: LEGACY_FIELD_TILE_COUNT
      }
    };
  }
  addTerrainRegion(region) {
    this.dto.terrain.regions.push(this.createStorageRegion(region));
  }
  clearTerrainRect(rectToClear) {
    const storageRectToClear = this.createStorageRect(rectToClear);
    const { regions } = this.dto.terrain;
    for (let i = regions.length - 1; i >= 0; i -= 1) {
      const region = regions[i];
      const regionRect = new Rect(
        region.x,
        region.y,
        region.width,
        region.height
      );
      if (regionRect.intersectsRect(storageRectToClear)) {
        regions.splice(i, 1);
      }
    }
  }
  getTerrainRegions() {
    if (!this.shouldOffsetLegacyContent()) {
      return this.dto.terrain.regions;
    }
    const legacyOffset = this.getLegacyOffset();
    return this.dto.terrain.regions.map((region) => {
      return {
        ...region,
        x: region.x + legacyOffset.x,
        y: region.y + legacyOffset.y
      };
    });
  }
  getFieldTileWidth() {
    return this.dto.field.widthTiles;
  }
  getFieldTileHeight() {
    return this.dto.field.heightTiles;
  }
  getFieldWidth() {
    return getFieldPixelSize(this.getFieldTileWidth());
  }
  getFieldHeight() {
    return getFieldPixelSize(this.getFieldTileHeight());
  }
  setFieldTileCount(widthTiles, heightTiles) {
    this.upgradeLegacyContentToCurrentCoordinates();
    this.dto.field.widthTiles = widthTiles;
    this.dto.field.heightTiles = heightTiles;
    this.clampContentToFieldBounds();
  }
  getPlayerSpawnPositions() {
    const dtoLocations = this.dto.spawn.player.locations;
    const defaultLocations = this.getDefaultPlayerSpawnPositions();
    if (dtoLocations.length > 0) {
      return defaultLocations.map((defaultLocation, index) => {
        const location = dtoLocations[index];
        if (location === void 0) {
          return new Vector(defaultLocation.x, defaultLocation.y);
        }
        return this.createWorldPosition(location.x, location.y);
      });
    }
    return defaultLocations.map((location) => {
      return new Vector(location.x, location.y);
    });
  }
  getEnemySpawnPositions() {
    const dtoLocations = this.dto.spawn.enemy.locations;
    const defaultLocations = this.getDefaultEnemySpawnPositions();
    if (dtoLocations.length > 0) {
      return defaultLocations.map((defaultLocation, index) => {
        const location = dtoLocations[index];
        if (location === void 0) {
          return new Vector(defaultLocation.x, defaultLocation.y);
        }
        return this.createWorldPosition(location.x, location.y);
      });
    }
    return defaultLocations.map((location) => {
      return new Vector(location.x, location.y);
    });
  }
  getEnemySpawnList() {
    const types = this.dto.spawn.enemy.list.map((item) => {
      return new TankType3("enemy" /* Enemy */, item.tier, item.drop);
    });
    return types;
  }
  isEnemySpawnListEmpty() {
    return this.dto.spawn.enemy.list.length === 0;
  }
  fillEnemySpawnList(type) {
    for (let i = 0; i < ENEMY_MAX_TOTAL_COUNT; i += 1) {
      this.dto.spawn.enemy.list[i] = {
        tier: type.tier,
        drop: type.hasDrop
      };
    }
  }
  setEnemySpawnListItem(index, type) {
    this.dto.spawn.enemy.list[index] = {
      tier: type.tier,
      drop: type.hasDrop
    };
  }
  getBasePosition() {
    const location = this.dto.base;
    if (location === void 0) {
      return this.getDefaultBasePosition();
    }
    return this.createWorldPosition(location.x, location.y);
  }
  setBasePosition(position) {
    this.dto.base = this.createStorageLocation(position.x, position.y);
  }
  setPlayerSpawnLocation(index, position) {
    this.dto.spawn.player.locations[index] = this.createStorageLocation(
      position.x,
      position.y
    );
  }
  setEnemySpawnLocation(index, position) {
    if (this.isEnemySpawnListEmpty()) {
      this.fillEnemySpawnList(TankType3.EnemyA());
    }
    this.dto.spawn.enemy.locations[index] = this.createStorageLocation(
      position.x,
      position.y
    );
  }
  toJSON(argOptions = {}) {
    const options = Object.assign({}, DEFAULT_TO_JSON_OPTIONS, argOptions);
    let json;
    if (options.pretty) {
      json = JSON.stringify(this.dto, null, 2);
    } else {
      json = JSON.stringify(this.dto);
    }
    return json;
  }
  fromJSON(json) {
    const dto = JSON.parse(json);
    this.dto = this.fillAndValidate(dto);
  }
  createWorldPosition(x, y) {
    if (!this.shouldOffsetLegacyContent()) {
      return new Vector(x, y);
    }
    const legacyOffset = this.getLegacyOffset();
    return new Vector(
      x + legacyOffset.x,
      y + legacyOffset.y
    );
  }
  shouldOffsetLegacyContent() {
    return this.dto.version < 2 && this.getFieldHeight() > LEGACY_FIELD_SIZE;
  }
  createStorageLocation(x, y) {
    if (!this.shouldOffsetLegacyContent()) {
      return { x, y };
    }
    const legacyOffset = this.getLegacyOffset();
    return {
      x: x - legacyOffset.x,
      y: y - legacyOffset.y
    };
  }
  createStorageRect(rect) {
    const location = this.createStorageLocation(rect.x, rect.y);
    return new Rect(location.x, location.y, rect.width, rect.height);
  }
  createStorageRegion(region) {
    const location = this.createStorageLocation(region.x, region.y);
    return {
      ...region,
      x: location.x,
      y: location.y
    };
  }
  getLegacyOffset() {
    return new Vector(
      0,
      Math.max(0, this.getFieldHeight() - LEGACY_FIELD_SIZE)
    );
  }
  getDefaultBasePosition() {
    return new Vector(
      Math.floor((this.getFieldWidth() - BASE_DEFAULT_SIZE.width) / 2),
      this.getFieldHeight() - BASE_DEFAULT_SIZE.height
    );
  }
  getDefaultPlayerSpawnPositions() {
    const basePosition = this.getDefaultBasePosition();
    const y = this.getFieldHeight() - TILE_SIZE_LARGE;
    return [
      new Vector(
        Math.max(0, basePosition.x - TILE_SIZE_LARGE - TILE_SIZE_MEDIUM),
        y
      ),
      new Vector(
        Math.min(
          this.getFieldWidth() - TILE_SIZE_LARGE,
          basePosition.x + BASE_DEFAULT_SIZE.width + TILE_SIZE_MEDIUM
        ),
        y
      )
    ];
  }
  getDefaultEnemySpawnPositions() {
    const rightX = Math.max(0, this.getFieldWidth() - TILE_SIZE_LARGE);
    const centerX = Math.max(
      0,
      Math.floor(rightX / 2 / TILE_SIZE_LARGE) * TILE_SIZE_LARGE
    );
    return [new Vector(centerX, 0), new Vector(rightX, 0), new Vector(0, 0)];
  }
  clampContentToFieldBounds() {
    const fieldRect = new Rect(0, 0, this.getFieldWidth(), this.getFieldHeight());
    this.dto.terrain.regions = this.getTerrainRegions().map((region) => {
      const clampedX = Math.max(0, Math.min(region.x, fieldRect.width));
      const clampedY = Math.max(0, Math.min(region.y, fieldRect.height));
      const clampedWidth = Math.max(
        0,
        Math.min(region.width, fieldRect.width - clampedX)
      );
      const clampedHeight = Math.max(
        0,
        Math.min(region.height, fieldRect.height - clampedY)
      );
      if (clampedWidth === 0 || clampedHeight === 0) {
        return null;
      }
      return this.createStorageRegion({
        ...region,
        x: clampedX,
        y: clampedY,
        width: clampedWidth,
        height: clampedHeight
      });
    }).filter((region) => region !== null);
    const playerPositions = this.getPlayerSpawnPositions().map((position) => this.clampObjectPosition(position, TILE_SIZE_LARGE, TILE_SIZE_LARGE)).map((position) => this.createStorageLocation(position.x, position.y));
    this.dto.spawn.player.locations = playerPositions;
    const enemyPositions = this.getEnemySpawnPositions().map((position) => this.clampObjectPosition(position, TILE_SIZE_LARGE, TILE_SIZE_LARGE)).map((position) => this.createStorageLocation(position.x, position.y));
    this.dto.spawn.enemy.locations = enemyPositions;
    const basePosition = this.clampObjectPosition(
      this.getBasePosition(),
      BASE_DEFAULT_SIZE.width,
      BASE_DEFAULT_SIZE.height
    );
    this.dto.base = this.createStorageLocation(basePosition.x, basePosition.y);
  }
  clampObjectPosition(position, width, height) {
    return new Vector(
      Math.max(0, Math.min(position.x, this.getFieldWidth() - width)),
      Math.max(0, Math.min(position.y, this.getFieldHeight() - height))
    );
  }
  upgradeLegacyContentToCurrentCoordinates() {
    if (!this.shouldOffsetLegacyContent()) {
      return;
    }
    this.dto.terrain.regions = this.getTerrainRegions().map((region) => {
      return { ...region };
    });
    this.dto.spawn.player.locations = this.getPlayerSpawnPositions().map((position) => {
      return { x: position.x, y: position.y };
    });
    this.dto.spawn.enemy.locations = this.getEnemySpawnPositions().map((position) => {
      return { x: position.x, y: position.y };
    });
    const basePosition = this.getBasePosition();
    this.dto.base = { x: basePosition.x, y: basePosition.y };
    this.dto.version = 2;
  }
};

// ../src/network/webrtc/applyRemotePlayerInput.ts
init_modules_watch_stub();
function applyPlayerInputCommand(tank, input, deltaTime, checkIce = true) {
  if (tank.isStunned()) {
    tank.idle(false);
    return;
  }
  if (input.direction !== null) {
    tank.rotate(input.direction);
  }
  if (input.moving && input.direction !== null) {
    tank.move(deltaTime);
    return;
  }
  tank.idle(checkIce);
}
__name(applyPlayerInputCommand, "applyPlayerInputCommand");
function applyRemotePlayerInput(tank, input, deltaTime, lastFireSeq) {
  let appliedFireSeq = lastFireSeq;
  if (input.fire && input.seq > lastFireSeq) {
    appliedFireSeq = input.seq;
    tank.fire();
  }
  applyPlayerInputCommand(tank, input, deltaTime);
  return appliedFireSeq;
}
__name(applyRemotePlayerInput, "applyRemotePlayerInput");

// ../src/network/webrtc/OrderedInputBuffer.ts
init_modules_watch_stub();
var DEFAULT_MAX_PENDING_INPUTS = 240;
var DEFAULT_MAX_REALTIME_BACKLOG = 6;
var OrderedInputBuffer = class {
  constructor(maxPendingInputs = DEFAULT_MAX_PENDING_INPUTS, maxRealtimeBacklog = DEFAULT_MAX_REALTIME_BACKLOG) {
    this.maxPendingInputs = maxPendingInputs;
    this.maxRealtimeBacklog = maxRealtimeBacklog;
    this.pending = [];
    this.current = null;
    this.lastReceivedSeq = 0;
    this.lastReceivedAt = 0;
  }
  static {
    __name(this, "OrderedInputBuffer");
  }
  accept(input, receivedAt = Date.now()) {
    if (!Number.isInteger(input.seq) || input.seq <= this.lastReceivedSeq || this.pending.length >= this.maxPendingInputs) {
      return false;
    }
    this.pending.push(input);
    this.lastReceivedSeq = input.seq;
    this.lastReceivedAt = receivedAt;
    return true;
  }
  consumeNext() {
    if (this.pending.length > this.maxRealtimeBacklog) {
      this.current = this.pending[this.pending.length - 1];
      this.pending.length = 0;
      return this.current;
    }
    const next = this.pending.shift();
    if (next !== void 0) {
      this.current = next;
    }
    return this.current;
  }
  isStale(now, timeoutMs) {
    return this.lastReceivedAt <= 0 || now - this.lastReceivedAt > timeoutMs;
  }
  clear() {
    this.pending.length = 0;
    this.current = null;
    this.lastReceivedSeq = 0;
    this.lastReceivedAt = 0;
  }
};

// ../scripts/engine-battle-city-simulation.ts
var REMOTE_INPUT_TIMEOUT_MS = 500;
var BASE_WALL_REGIONS = [
  { x: 0, y: 0, width: 128, height: 32 },
  { x: 0, y: 32, width: 32, height: 64 },
  { x: 96, y: 32, width: 32, height: 64 }
];
var HeadlessWebRtcMatch = class {
  constructor(inputBuffers, pendingFireSeqs, pendingPowerSlots, disableEnemyShooting) {
    this.inputBuffers = inputBuffers;
    this.pendingFireSeqs = pendingFireSeqs;
    this.pendingPowerSlots = pendingPowerSlots;
    this.disableEnemyShooting = disableEnemyShooting;
    this.lastFireSeqs = /* @__PURE__ */ new Map();
    this.controlledTanks = /* @__PURE__ */ new Map();
    this.lastProcessedInputSeq = [0, 0];
  }
  static {
    __name(this, "HeadlessWebRtcMatch");
  }
  handlePlayerTank(tank, updateArgs) {
    const playerIndex = tank.partyIndex;
    if (this.controlledTanks.get(playerIndex) !== tank) {
      this.observeAuthoritativePlayerTank(tank);
      tank.idle(false);
      return true;
    }
    const inputBuffer = this.inputBuffers[playerIndex];
    if (inputBuffer.isStale(Date.now(), REMOTE_INPUT_TIMEOUT_MS)) {
      tank.idle(false);
      return true;
    }
    const input = inputBuffer.consumeNext();
    if (input === null) {
      tank.idle(false);
      return true;
    }
    const fireSeqs = this.pendingFireSeqs[playerIndex];
    const shouldFire = fireSeqs.length > 0 && fireSeqs[0] <= input.seq;
    if (shouldFire) {
      while (fireSeqs.length > 0 && fireSeqs[0] <= input.seq) {
        fireSeqs.shift();
      }
    }
    const appliedInput = shouldFire && !input.fire ? { ...input, fire: true } : input;
    const lastFireSeq = this.lastFireSeqs.get(tank.partyIndex) ?? 0;
    const appliedFireSeq = applyRemotePlayerInput(
      tank,
      appliedInput,
      updateArgs.deltaTime,
      lastFireSeq
    );
    this.lastFireSeqs.set(
      tank.partyIndex,
      appliedFireSeq
    );
    this.lastProcessedInputSeq[playerIndex] = input.seq;
    return true;
  }
  getLastProcessedInputSeq() {
    return [...this.lastProcessedInputSeq];
  }
  observeAuthoritativePlayerTank(tank) {
    const playerIndex = tank.partyIndex;
    this.controlledTanks.set(playerIndex, tank);
    this.inputBuffers[playerIndex].clear();
    this.pendingFireSeqs[playerIndex].length = 0;
  }
  isEnabled() {
    return true;
  }
  isBroadcaster() {
    return true;
  }
  consumeRemotePowerSlot(playerIndex) {
    const slots = this.pendingPowerSlots.get(
      playerIndex
    );
    if (slots === void 0 || slots.length === 0) {
      return null;
    }
    const slot = slots.shift() ?? null;
    if (slots.length === 0) {
      this.pendingPowerSlots.delete(playerIndex);
    }
    return slot;
  }
  isWaitingForPeer() {
    return false;
  }
  shouldDisableEnemyShooting() {
    return this.disableEnemyShooting;
  }
  setEnemyShootingDisabled(disabled) {
    this.disableEnemyShooting = disabled;
  }
};
var EngineBattleCitySimulation = class {
  constructor(map, options) {
    this.root = new GameObject(
      CANVAS_WIDTH,
      CANVAS_HEIGHT
    );
    this.collisionSystem = new CollisionSystem();
    this.gameState = new State(0 /* Playing */);
    this.mapConfig = new MapConfig();
    this.eventBus = new LevelEventBus();
    this.session = new Session();
    this.inputBuffers = [new OrderedInputBuffer(), new OrderedInputBuffer()];
    this.pendingFireSeqs = [[], []];
    this.pendingPowerSlots = /* @__PURE__ */ new Map();
    this.playerElapsed = [0, 0];
    this.playerFire = /* @__PURE__ */ new Map();
    this.enemyFire = /* @__PURE__ */ new Map();
    this.previousPlayers = /* @__PURE__ */ new Map();
    this.previousEnemies = /* @__PURE__ */ new Map();
    this.pendingEnemyDeaths = [];
    this.alwaysUpdateScripts = [];
    this.playingUpdateScripts = [];
    this.enemyExplosionCount = 0;
    this.enemyDeathSeq = 0;
    this.currentTick = 0;
    this.frameSeq = 0;
    this.pendingHitStopSeconds = 0;
    this.requestHitStop = /* @__PURE__ */ __name((seconds) => {
      if (Number.isFinite(seconds) && seconds > this.pendingHitStopSeconds) {
        this.pendingHitStopSeconds = seconds;
      }
    }, "requestHitStop");
    this.tickRate = Math.max(10, Math.floor(options.tickRate ?? 60));
    this.deltaTime = 1 / this.tickRate;
    this.rng = new Prng(options.seed);
    this.mapConfig.fromDto(map);
    const level = Math.max(1, Math.floor(options.level ?? 1));
    this.stageNumber = level;
    this.session.setMultiplayer();
    this.session.start(level, 35);
    const runState = options.runState;
    this.session.setRunBoosts(
      runState?.runBoosts ?? {
        hull: options.runBoosts?.hull ?? 0,
        armor: options.runBoosts?.armor ?? 0,
        engine: options.runBoosts?.engine ?? 0,
        salvage: options.runBoosts?.salvage ?? 0
      }
    );
    this.session.setRunConsumables({
      powerups: [],
      powerupItems: [],
      powerupCounts: [],
      extraLives: runState === void 0 ? Math.max(0, Math.floor(options.extraLives ?? 0)) : 0
    });
    if (runState === void 0) {
      this.applyInitialPlayerTiers(options.initialPlayerTiers);
    } else {
      this.currentTick = runState.tick;
      this.frameSeq = runState.frameSeq;
      this.playerElapsed[0] = runState.playerElapsedSeconds[0];
      this.playerElapsed[1] = runState.playerElapsedSeconds[1];
      runState.scores.forEach((score, player) => {
        this.session.getPlayer(player).setAuthoritativeGamePoints(score);
      });
      runState.lives.forEach((lives, player) => {
        this.session.getPlayer(player).setLivesCount(lives);
      });
      this.applyInitialPlayerTiers(runState.tankTiers);
    }
    prepareLevelSession(
      this.session,
      this.mapConfig.getEnemySpawnList().length
    );
    this.world = new LevelWorld(
      this.root,
      this.mapConfig.getFieldWidth(),
      this.mapConfig.getFieldHeight()
    );
    this.world.field.position.set(
      BORDER_LEFT_WIDTH,
      LEVEL_PLAY_TOP_OFFSET + BORDER_TOP_BOTTOM_HEIGHT
    );
    this.world.field.add(
      new GroundField(this.world.field.size.width, this.world.field.size.height)
    );
    this.world.field.add(
      new WallShadowField(
        this.world.field.size.width,
        this.world.field.size.height
      )
    );
    this.world.field.add(
      new Border(this.world.field.size.width, this.world.field.size.height)
    );
    this.root.add(this.world.field);
    this.createTerrain();
    this.webRtcMatch = new HeadlessWebRtcMatch(
      this.inputBuffers,
      this.pendingFireSeqs,
      this.pendingPowerSlots,
      options.disableEnemyShooting === true
    );
    this.updateArgs = this.createUpdateArgs(this.webRtcMatch);
    this.baseScript = new LevelBaseScript(true);
    this.enemyScript = new LevelEnemyScript(false, true);
    this.explosionScript = new LevelExplosionScript();
    this.gameOverScript = new LevelGameOverScript();
    this.introScript = new LevelIntroScript();
    this.playerOverScript = new LevelPlayerOverScript();
    this.playerScript = new LevelPlayerScript({
      headless: true,
      playerRunConsumables: (runState?.playerRunConsumables ?? options.playerRunConsumables)?.map(
        (consumables) => ({
          powerups: consumables.powerups,
          powerupCounts: consumables.powerups.map((_, index) => {
            return Math.max(
              0,
              Math.floor(consumables.powerupCounts?.[index] ?? 1)
            );
          })
        })
      )
    });
    this.pointsScript = new LevelPointsScript();
    this.powerupScript = new LevelPowerupScript({
      isLocalServerMatch: false,
      isWebRtcClient: false,
      headless: true
    });
    this.spawnScript = new LevelSpawnScript();
    this.winScript = new LevelWinScript();
    this.allScripts = [
      this.baseScript,
      this.enemyScript,
      this.explosionScript,
      this.gameOverScript,
      this.introScript,
      this.playerOverScript,
      this.playerScript,
      this.pointsScript,
      this.powerupScript,
      this.spawnScript,
      this.winScript
    ];
    this.allScripts.forEach((script) => {
      script.invokeInit(
        this.world,
        this.eventBus,
        this.session,
        this.mapConfig
      );
    });
    this.matchLifecycle = new LevelMatchLifecycle(
      this.eventBus,
      this.session,
      {
        gameOver: this.gameOverScript,
        playerOver: this.playerOverScript,
        player: this.playerScript,
        win: this.winScript
      }
    );
    this.alwaysUpdateScripts.push(this.introScript);
    this.introScript.completed.addListener(() => {
      this.alwaysUpdateScripts.push(this.gameOverScript, this.winScript);
      this.playingUpdateScripts.push(
        this.baseScript,
        this.explosionScript,
        this.enemyScript,
        this.spawnScript,
        this.playerOverScript,
        this.playerScript,
        this.pointsScript,
        this.powerupScript
      );
    });
    this.playerScript.tankCreated.addListener((tank) => {
      this.webRtcMatch.observeAuthoritativePlayerTank(tank);
      this.observeTankFire(tank, this.playerFire);
    });
    this.enemyScript.tankCreated.addListener((tank) => {
      this.observeTankFire(tank, this.enemyFire);
      tank.died.addListener((event) => {
        const center = tank.getCenter();
        this.pendingEnemyDeaths.push({
          seq: ++this.enemyDeathSeq,
          partyIndex: tank.partyIndex,
          x: center.x,
          y: center.y,
          reason: event.reason,
          hitterPartyIndex: event.hitterPartyIndex === 0 || event.hitterPartyIndex === 1 ? event.hitterPartyIndex : null
        });
      });
    });
    this.eventBus.enemyExploded.addListener(() => {
      this.enemyExplosionCount += 1;
      this.requestHitStop(
        HIT_STOP_KILL * CAMERA_SHAKE_INTENSITY
      );
    });
    this.eventBus.playerDied.addListener(() => {
      this.requestHitStop(
        HIT_STOP_DEATH * CAMERA_SHAKE_INTENSITY
      );
    });
    this.eventBus.baseDied.addListener(() => {
      this.requestHitStop(
        HIT_STOP_DEATH * CAMERA_SHAKE_INTENSITY
      );
    });
    this.root.updateMatrix(true);
    this.root.updateWorldMatrix(false, true);
  }
  static {
    __name(this, "EngineBattleCitySimulation");
  }
  get tick() {
    return this.currentTick;
  }
  get seq() {
    return this.frameSeq;
  }
  consumeHitStopSeconds() {
    const seconds = this.pendingHitStopSeconds;
    this.pendingHitStopSeconds = 0;
    return seconds;
  }
  setEnemyShootingDisabled(disabled) {
    this.webRtcMatch.setEnemyShootingDisabled(disabled);
  }
  acceptInput(packet) {
    if (packet.player !== 0 && packet.player !== 1 || !Number.isInteger(packet.seq) || !isRotation(packet.direction) || !isPowerSlot(packet.powerSlot)) {
      return false;
    }
    if (!this.inputBuffers[packet.player].accept(packet)) {
      return false;
    }
    if (packet.fire) {
      const fireSeqs = this.pendingFireSeqs[packet.player];
      if (fireSeqs.length < 16) {
        fireSeqs.push(packet.seq);
      }
    }
    if (packet.powerSlot !== null && packet.powerSlot !== void 0) {
      let slots = this.pendingPowerSlots.get(packet.player);
      if (slots === void 0) {
        slots = [];
        this.pendingPowerSlots.set(packet.player, slots);
      }
      if (slots.length < 16) {
        slots.push(packet.powerSlot);
      }
    }
    if (Number.isFinite(packet.elapsedSeconds)) {
      this.playerElapsed[packet.player] = Math.max(0, packet.elapsedSeconds);
    }
    return true;
  }
  step() {
    this.currentTick += 1;
    this.session.recordLevelTick();
    this.alwaysUpdateScripts.forEach((script) => {
      script.invokeUpdate(this.updateArgs);
    });
    if (!this.gameState.is(1 /* Paused */)) {
      this.playingUpdateScripts.forEach((script) => {
        if (!this.alwaysUpdateScripts.includes(script)) {
          script.invokeUpdate(this.updateArgs);
        }
      });
    }
    this.prepareNetworkTick();
    this.root.traverseDescedants((node) => {
      const shouldUpdate = this.gameState.is(0 /* Playing */) || node.ignorePause;
      if (shouldUpdate) {
        node.invokeUpdate(this.updateArgs);
      }
    });
    this.root.updateWorldMatrix(false, true);
    this.collisionSystem.update();
    this.collisionSystem.collide();
    this.clampTanksToFieldBounds();
    return this.createFrame();
  }
  getScores() {
    return [
      this.session.getPlayer(0).getGamePoints(),
      this.session.getPlayer(1).getGamePoints()
    ];
  }
  getLives() {
    return [
      this.session.getPlayer(0).getLivesCount(),
      this.session.getPlayer(1).getLivesCount()
    ];
  }
  getEnemyExplosionCount() {
    return this.enemyExplosionCount;
  }
  isComplete() {
    return this.matchLifecycle.isComplete();
  }
  isTerminal() {
    return this.matchLifecycle.isComplete() && this.matchLifecycle.getResult() === "loss";
  }
  createNextStageRunState() {
    if (!this.matchLifecycle.isComplete() || this.matchLifecycle.getResult() !== "win") {
      throw new Error("Cannot advance an unfinished or lost stage");
    }
    const maxLevelPoints = this.session.getMaxLevelPoints();
    this.session.getPlayers().forEach((player) => {
      if (player.getLevelPoints() > 0 && player.getLevelPoints() === maxLevelPoints) {
        player.addBonusPoints();
      }
    });
    this.session.activateNextLevel();
    const playerRunConsumables = this.playerScript.getAuthoritativeRunConsumables() ?? [
      { powerups: [], powerupCounts: [] },
      { powerups: [], powerupCounts: [] }
    ];
    return {
      stageNumber: this.session.getLevelNumber(),
      tick: this.currentTick,
      frameSeq: this.frameSeq,
      scores: this.getScores(),
      lives: this.getLives(),
      tankTiers: this.session.getPlayerTankTiers(),
      runBoosts: this.session.getRunBoosts(),
      playerRunConsumables,
      playerElapsedSeconds: [...this.playerElapsed]
    };
  }
  applyInitialPlayerTiers(tiers) {
    if (tiers === void 0) {
      return;
    }
    tiers.forEach((tier, player) => {
      this.session.getPlayer(player).setTankTier(tier);
      this.session.setPlayerTankTier(player, tier);
    });
  }
  createUpdateArgs(webRtcMatch) {
    const silentSound = {
      play: /* @__PURE__ */ __name(() => void 0, "play"),
      playLoop: /* @__PURE__ */ __name(() => void 0, "playLoop"),
      resume: /* @__PURE__ */ __name(() => void 0, "resume"),
      pause: /* @__PURE__ */ __name(() => void 0, "pause"),
      stop: /* @__PURE__ */ __name(() => void 0, "stop"),
      canResume: /* @__PURE__ */ __name(() => false, "canResume"),
      setMuted: /* @__PURE__ */ __name(() => void 0, "setMuted"),
      isMuted: /* @__PURE__ */ __name(() => true, "isMuted"),
      setGlobalMuted: /* @__PURE__ */ __name(() => void 0, "setGlobalMuted"),
      isGlobalMuted: /* @__PURE__ */ __name(() => true, "isGlobalMuted")
    };
    const spriteLoader = {
      load: /* @__PURE__ */ __name(() => null, "load"),
      loadList: /* @__PURE__ */ __name((ids) => ids.map(() => null), "loadList"),
      loadSequence: /* @__PURE__ */ __name(() => [null], "loadSequence"),
      has: /* @__PURE__ */ __name(() => false, "has")
    };
    const neutralInputMethod = {
      isDownAny: /* @__PURE__ */ __name(() => false, "isDownAny"),
      isHoldAny: /* @__PURE__ */ __name(() => false, "isHoldAny"),
      isNotHoldAll: /* @__PURE__ */ __name(() => true, "isNotHoldAll"),
      getHoldLastIndex: /* @__PURE__ */ __name(() => -1, "getHoldLastIndex")
    };
    const inputManager = {
      isReplaying: /* @__PURE__ */ __name(() => false, "isReplaying"),
      getActiveMethod: /* @__PURE__ */ __name(() => neutralInputMethod, "getActiveMethod"),
      getMethodByVariant: /* @__PURE__ */ __name(() => neutralInputMethod, "getMethodByVariant")
    };
    const magicBlockMovement = {
      isWatching: /* @__PURE__ */ __name(() => false, "isWatching"),
      isRemoteTank: /* @__PURE__ */ __name(() => false, "isRemoteTank"),
      isLocalServerMatchWaitingForStart: /* @__PURE__ */ __name(() => false, "isLocalServerMatchWaitingForStart"),
      isOnlineMatch: /* @__PURE__ */ __name(() => false, "isOnlineMatch"),
      isLocalServerMatch: /* @__PURE__ */ __name(() => false, "isLocalServerMatch"),
      recordLocalFire: /* @__PURE__ */ __name(() => void 0, "recordLocalFire"),
      setPlayerMirrorBulletsSuppressed: /* @__PURE__ */ __name(() => void 0, "setPlayerMirrorBulletsSuppressed")
    };
    return {
      audioLoader: { load: /* @__PURE__ */ __name(() => silentSound, "load") },
      collisionSystem: this.collisionSystem,
      deltaTime: this.deltaTime,
      stageNumber: this.stageNumber,
      gameState: this.gameState,
      gameStorage: null,
      hitStop: this.requestHitStop,
      inputManager,
      magicBlockMovement,
      particles: {
        spawn: /* @__PURE__ */ __name(() => void 0, "spawn"),
        flash: /* @__PURE__ */ __name(() => void 0, "flash"),
        clear: /* @__PURE__ */ __name(() => void 0, "clear")
      },
      rng: this.rng,
      session: this.session,
      spriteLoader,
      webRtcMatch
    };
  }
  createTerrain() {
    const basePosition = this.mapConfig.getBasePosition();
    const baseRect = new Rect(
      basePosition.x,
      basePosition.y,
      BASE_DEFAULT_SIZE.width,
      BASE_DEFAULT_SIZE.height
    );
    const regions = [
      ...this.mapConfig.getTerrainRegions(),
      ...BASE_WALL_REGIONS.map((region) => ({
        type: "brick" /* Brick */,
        x: basePosition.x + region.x,
        y: basePosition.y + region.y,
        width: region.width,
        height: region.height
      }))
    ];
    const tiles = TerrainFactory.createMapFromRegionConfigs(
      regions,
      this.world.field.size.width,
      this.world.field.size.height,
      [baseRect]
    );
    tiles.forEach((tile) => {
      tile.destroyed.addListener(() => {
        this.eventBus.mapTileDestroyed.notify({
          type: tile.type,
          position: tile.position.clone(),
          size: tile.size.clone()
        });
      });
      if (tile instanceof BrickSuperTerrainTile) {
        tile.subTileDestroyed.addListener(() => void 0);
      }
    });
    this.world.field.add(...tiles);
  }
  prepareNetworkTick() {
    this.world.getPlayerTanks().forEach((tank) => {
      if (tank === null || tank === void 0) {
        return;
      }
      this.observeTankFire(tank, this.playerFire);
      const previous = this.previousPlayers.get(tank.partyIndex);
      if (previous?.tank !== tank) {
        this.previousPlayers.set(tank.partyIndex, {
          tank,
          x: tank.position.x,
          y: tank.position.y
        });
      }
    });
    this.enemyScript.getAliveTanks().forEach((tank) => {
      this.observeTankFire(tank, this.enemyFire);
      if (!this.previousEnemies.has(tank.partyIndex)) {
        this.previousEnemies.set(tank.partyIndex, {
          x: tank.position.x,
          y: tank.position.y
        });
      }
    });
  }
  observeTankFire(tank, states) {
    const existing = states.get(tank.partyIndex);
    if (existing?.tank === tank) {
      return;
    }
    const state = existing ?? {
      tank,
      seq: 0,
      x: 0,
      y: 0,
      rotation: tank.rotation
    };
    state.tank = tank;
    state.x = tank.position.x;
    state.y = tank.position.y;
    state.rotation = tank.rotation;
    states.set(tank.partyIndex, state);
    tank.fired.addListener(() => {
      state.seq += 1;
      state.x = tank.position.x;
      state.y = tank.position.y;
      state.rotation = tank.rotation;
    });
  }
  clampTanksToFieldBounds() {
    const maxX = this.world.field.size.width - TILE_SIZE_LARGE;
    const maxY = this.world.field.size.height - TILE_SIZE_LARGE;
    this.world.field.children.forEach((node) => {
      if (!(node instanceof Tank)) {
        return;
      }
      const nextX = Math.max(0, Math.min(node.position.x, maxX));
      const nextY = Math.max(0, Math.min(node.position.y, maxY));
      if (node.position.x === nextX && node.position.y === nextY) {
        return;
      }
      node.position.set(nextX, nextY);
      node.updateMatrix(true);
      node.collider.update();
    });
  }
  createFrame() {
    const activeEnemyIds = this.enemyScript.getActiveEnemyIds();
    const activeEnemyIdSet = new Set(activeEnemyIds);
    Array.from(this.previousEnemies.keys()).forEach((partyIndex) => {
      if (!activeEnemyIdSet.has(partyIndex)) {
        this.previousEnemies.delete(partyIndex);
        this.enemyFire.delete(partyIndex);
      }
    });
    const players = this.world.getPlayerTanks().filter((tank) => tank !== null && tank !== void 0).map((tank) => this.createPlayerFrame(tank));
    const enemies = this.enemyScript.getAliveTanks().map((tank) => this.createEnemyFrame(tank));
    const powerup = this.powerupScript.getWebRtcPowerup();
    const pickup = this.powerupScript.getWebRtcPickup();
    return {
      type: "webrtc-host-frame",
      seq: ++this.frameSeq,
      tick: this.currentTick,
      lastProcessedInputSeq: this.webRtcMatch.getLastProcessedInputSeq(),
      deltaTime: this.deltaTime,
      stageNumber: this.stageNumber,
      matchResult: this.matchLifecycle.getResult(),
      playerScores: this.getScores(),
      playerLives: this.getLives(),
      playerKillCounts: this.session.getPlayers().map((player) => {
        const record = player.getLevelPointsRecord();
        return [
          record.getTierKillCount("a" /* A */),
          record.getTierKillCount("b" /* B */),
          record.getTierKillCount("c" /* C */),
          record.getTierKillCount("d" /* D */)
        ];
      }),
      sharedElapsedSeconds: this.currentTick * this.deltaTime,
      playerOneElapsedSeconds: this.playerElapsed[0],
      playerTwoElapsedSeconds: this.playerElapsed[1],
      players,
      powerup,
      powerupPickup: pickup,
      activeEnemyIds,
      enemyDeaths: this.pendingEnemyDeaths.splice(
        0,
        this.pendingEnemyDeaths.length
      ),
      enemies
    };
  }
  createPlayerFrame(tank) {
    const previous = this.previousPlayers.get(tank.partyIndex);
    const sameTank = previous?.tank === tank;
    const deltaX = sameTank ? tank.position.x - previous.x : 0;
    const deltaY = sameTank ? tank.position.y - previous.y : 0;
    this.previousPlayers.set(tank.partyIndex, {
      tank,
      x: tank.position.x,
      y: tank.position.y
    });
    const fire = this.playerFire.get(tank.partyIndex);
    return {
      partyIndex: tank.partyIndex,
      tier: tank.type.tier,
      x: tank.position.x,
      y: tank.position.y,
      rotation: tank.rotation,
      moving: tank.state === 2 /* Moving */,
      deltaX,
      deltaY,
      alive: tank.isAlive(),
      fireSeq: fire?.seq ?? 0,
      fireX: fire?.x ?? tank.position.x,
      fireY: fire?.y ?? tank.position.y,
      fireRotation: fire?.rotation ?? tank.rotation,
      initialSync: !sameTank
    };
  }
  createEnemyFrame(tank) {
    const previous = this.previousEnemies.get(tank.partyIndex);
    const deltaX = previous === void 0 ? 0 : tank.position.x - previous.x;
    const deltaY = previous === void 0 ? 0 : tank.position.y - previous.y;
    this.previousEnemies.set(tank.partyIndex, {
      x: tank.position.x,
      y: tank.position.y
    });
    const fire = this.enemyFire.get(tank.partyIndex);
    return {
      partyIndex: tank.partyIndex,
      x: tank.position.x,
      y: tank.position.y,
      rotation: tank.rotation,
      moving: tank.state === 2 /* Moving */,
      deltaX,
      deltaY,
      alive: tank.isAlive(),
      fireSeq: fire?.seq ?? 0,
      fireX: fire?.x ?? tank.position.x,
      fireY: fire?.y ?? tank.position.y,
      fireRotation: fire?.rotation ?? tank.rotation
    };
  }
};
function isRotation(value) {
  return value === null || value === 0 /* Up */ || value === 90 /* Right */ || value === 180 /* Down */ || value === 270 /* Left */;
}
__name(isRotation, "isRotation");
function isPowerSlot(value) {
  return value === void 0 || value === null || Number.isInteger(value) && value >= 0 && value < 4;
}
__name(isPowerSlot, "isPowerSlot");

// src/maps.ts
init_modules_watch_stub();

// ../data/maps/original/01.json
var __default = {
  spawn: {
    enemy: {
      list: [
        { tier: "a" },
        { tier: "a" },
        { tier: "a" },
        { tier: "a", drop: true },
        { tier: "a" },
        { tier: "a" },
        { tier: "a" },
        { tier: "a" },
        { tier: "a" },
        { tier: "a" },
        { tier: "a", drop: true },
        { tier: "a" },
        { tier: "a" },
        { tier: "a" },
        { tier: "a" },
        { tier: "a" },
        { tier: "a" },
        { tier: "a", drop: true },
        { tier: "b" },
        { tier: "b" }
      ]
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 64,
        height: 288
      },
      {
        type: "brick",
        x: 192,
        y: 64,
        width: 64,
        height: 288
      },
      {
        type: "brick",
        x: 320,
        y: 64,
        width: 64,
        height: 224
      },
      {
        type: "brick",
        x: 448,
        y: 64,
        width: 64,
        height: 224
      },
      {
        type: "brick",
        x: 576,
        y: 64,
        width: 64,
        height: 288
      },
      {
        type: "brick",
        x: 704,
        y: 64,
        width: 64,
        height: 288
      },
      {
        type: "brick",
        x: 320,
        y: 352,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 352,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 416,
        width: 64,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 416,
        width: 64,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 416,
        width: 128,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 416,
        width: 128,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 544,
        width: 64,
        height: 224
      },
      {
        type: "brick",
        x: 192,
        y: 544,
        width: 64,
        height: 224
      },
      {
        type: "brick",
        x: 576,
        y: 544,
        width: 64,
        height: 224
      },
      {
        type: "brick",
        x: 704,
        y: 544,
        width: 64,
        height: 224
      },
      {
        type: "brick",
        x: 320,
        y: 480,
        width: 64,
        height: 192
      },
      {
        type: "brick",
        x: 448,
        y: 480,
        width: 64,
        height: 192
      },
      {
        type: "brick",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 448,
        width: 64,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 448,
        width: 64,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/02.json
var __default2 = {
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      }
    ]
  },
  version: 1
};

// ../data/maps/original/03.json
var __default3 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 0,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 800,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 544,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/04.json
var __default4 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "jungle",
        x: 704,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 800,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 768,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 608,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 544,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 480,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 288,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 160,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "water",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 96,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "water",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/05.json
var __default5 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 320,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "water",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 672,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 608,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 64,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/06.json
var __default6 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "jungle",
        x: 768,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 672,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 800,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 608,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 672,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 768,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 736,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 544,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 480,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 800,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/07.json
var __default7 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "jungle",
        x: 384,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 96,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 96,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 608,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 736,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 736,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 256,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/08.json
var __default8 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 320,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 352,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 800,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 224,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 608,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 768,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/09.json
var __default9 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 192,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 608,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 608,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 448,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 416,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 416,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 224,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/10.json
var __default10 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 192,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 256,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 160,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 32,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 224,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 800,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 64,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/11.json
var __default11 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "jungle",
        x: 192,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 32,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 544,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 32,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 96,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 0,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/12.json
var __default12 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 448,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 416,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "water",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 288,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 800,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 480,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 416,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 352,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 800,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/13.json
var __default13 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 0,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 352,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 192,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/14.json
var __default14 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "steel",
        x: 224,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 800,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 800,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 768,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 224,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 544,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 576,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/15.json
var __default15 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 320,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 288,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 416,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 480,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/16.json
var __default16 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 192,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 416,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/17.json
var __default17 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 0,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "ice",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "ice",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "ice",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 544,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 800,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 672,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 640,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/18.json
var __default18 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "jungle",
        x: 704,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/19.json
var __default19 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 64,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 736,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 608,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 800,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 416,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 160,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 480,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 768,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/20.json
var __default20 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 320,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "water",
        x: 192,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 800,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 32,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 192,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/21.json
var __default21 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 512,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 32,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/22.json
var __default22 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "jungle",
        x: 320,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/23.json
var __default23 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "steel",
        x: 384,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 416,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 768,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/24.json
var __default24 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "steel",
        x: 320,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 32,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 416,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 672,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 416,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 672,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 608,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "ice",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 704,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/25.json
var __default25 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 320,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/26.json
var __default26 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "steel",
        x: 0,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 800,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 800,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 608,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 416,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 416,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 480,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 224,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 192,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 608,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "water",
        x: 128,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/27.json
var __default27 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "steel",
        x: 0,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/28.json
var __default28 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "ice",
        x: 192,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 800,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 256,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 288,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 416,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 672,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 0,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/29.json
var __default29 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 768,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/30.json
var __default30 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 512,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 32,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 800,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "jungle",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 160,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 672,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/31.json
var __default31 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "water",
        x: 0,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 448,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/32.json
var __default32 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: true
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "a",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 64,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 672,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 480,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 416,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 416,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 448,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 480,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 352,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 320,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 736,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 672,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 768,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 704,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 576,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 384,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 320,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "ice",
        x: 0,
        y: 640,
        width: 64,
        height: 64
      }
    ]
  }
};

// ../data/maps/original/33.json
var __default33 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "steel",
        x: 0,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 0,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 160,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 608,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 768,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 736,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 512,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 576,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 0,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 64,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 640,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 640,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 608,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 576,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 128,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 96,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 64,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 512,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 544,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 736,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "steel",
        x: 704,
        y: 256,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/34.json
var __default34 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 224,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 288,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 704,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 768,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 704,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 736,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 800,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 736,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 704,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 608,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 608,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 544,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 480,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 512,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 576,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 640,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 448,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 288,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 384,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 288,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 160,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 224,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 256,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 320,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 32,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 352,
        y: 0,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 224,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 192,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 160,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 512,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 544,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 576,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 608,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 640,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 672,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 704,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 736,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 768,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 800,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 480,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 448,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 416,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 384,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 352,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 320,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 288,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 256,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 96,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 192,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 32,
        y: 224,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 64,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 64,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 128,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 160,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 128,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 96,
        width: 32,
        height: 32
      },
      {
        type: "brick",
        x: 0,
        y: 64,
        width: 32,
        height: 32
      }
    ]
  }
};

// ../data/maps/original/35.json
var __default35 = {
  version: 1,
  spawn: {
    enemy: {
      locations: [],
      list: [
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: false
        },
        {
          tier: "c",
          drop: true
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "b",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: true
        },
        {
          tier: "d",
          drop: false
        },
        {
          tier: "d",
          drop: false
        }
      ]
    },
    player: {
      locations: []
    }
  },
  terrain: {
    regions: [
      {
        type: "brick",
        x: 384,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 64,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 128,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 192,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 768,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 704,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 640,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 576,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 512,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 448,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 384,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 320,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 256,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 192,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 128,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 64,
        y: 640,
        width: 64,
        height: 64
      },
      {
        type: "jungle",
        x: 0,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 256,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 448,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 384,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 448,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 576,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 768,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 704,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 640,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 576,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 512,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 128,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 64,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 0,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 0,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 256,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "steel",
        x: 384,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 256,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 320,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 192,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 448,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 384,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 512,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 640,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 576,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 384,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 256,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 320,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 128,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 0,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 512,
        width: 64,
        height: 64
      },
      {
        type: "brick",
        x: 64,
        y: 448,
        width: 64,
        height: 64
      },
      {
        type: "water",
        x: 192,
        y: 448,
        width: 64,
        height: 64
      }
    ]
  }
};

// src/maps.ts
var maps = [
  __default,
  __default2,
  __default3,
  __default4,
  __default5,
  __default6,
  __default7,
  __default8,
  __default9,
  __default10,
  __default11,
  __default12,
  __default13,
  __default14,
  __default15,
  __default16,
  __default17,
  __default18,
  __default19,
  __default20,
  __default21,
  __default22,
  __default23,
  __default24,
  __default25,
  __default26,
  __default27,
  __default28,
  __default29,
  __default30,
  __default31,
  __default32,
  __default33,
  __default34,
  __default35
];
function getMap(level) {
  const index = (Math.max(1, Math.floor(level)) - 1) % maps.length;
  return maps[index];
}
__name(getMap, "getMap");

// src/latency-page.ts
init_modules_watch_stub();
var latencyPage = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Battle Cities WebSocket Latency</title><style>
:root{color-scheme:dark;font-family:Consolas,"Courier New",monospace;background:#10130f;color:#f2ead2}*{box-sizing:border-box}
body{margin:0;min-height:100vh;display:grid;place-items:center;padding:20px;background:radial-gradient(circle at 50% 0,#273522,#0c0f0b 58%)}
main{width:min(720px,100%);border:4px solid #090b08;outline:2px solid #887c58;background:#1a2018;box-shadow:0 18px 60px #000b}
header{padding:18px 20px;border-bottom:2px solid #887c58;background:#262d21}h1{margin:0;color:#f0b52d;font-size:clamp(20px,4vw,30px);letter-spacing:2px}p{line-height:1.5;color:#bcb79f}
.grid{display:grid;grid-template-columns:repeat(2,1fr);gap:2px;background:#3d4935}.card{background:#171c15;padding:18px;min-height:105px}.wide{grid-column:1/-1}
.label{font-size:12px;letter-spacing:1.5px;color:#8f987e}.value{display:block;margin-top:8px;font-size:clamp(24px,6vw,38px);color:#e8e1c8}.good{color:#74d242}.warn{color:#f0b52d}.bad{color:#e45445}
button{width:100%;border:3px solid #11180e;background:#497f2a;color:#fff7d2;padding:14px;font:700 16px inherit;letter-spacing:1px;cursor:pointer;box-shadow:inset 0 0 0 2px #79a34e}button:focus-visible{outline:3px solid #ffd33d;outline-offset:3px}button:active{background:#d5a51f;color:#15130c}
#log{height:130px;overflow:auto;margin:0;white-space:pre-wrap;color:#aeb79d;font-size:12px}.footer{padding:16px 20px}.status{font-weight:700;color:#f0b52d}@media(max-width:520px){.grid{grid-template-columns:1fr}.wide{grid-column:auto}}
</style></head><body><main><header><h1>WEBSOCKET LATENCY</h1><p>Direct browser test against the Cloudflare game transport. Samples are sent individually\u2014no batching.</p><div class="status" id="status">CONNECTING\u2026</div></header>
<section class="grid"><div class="card"><span class="label">PING</span><strong class="value" id="ping">\u2014</strong></div><div class="card"><span class="label">JITTER</span><strong class="value" id="jitter">\u2014</strong></div><div class="card wide"><span class="label">CLOUDFLARE EDGE / REGION</span><strong class="value" id="region" style="font-size:20px">\u2014</strong><p id="detail">Waiting for server metadata\u2026</p></div><div class="card wide"><pre id="log"></pre></div></section>
<div class="footer"><button id="toggle">DISCONNECT</button></div></main><script>
const el=id=>document.getElementById(id),samples=[];let ws,timer,seq=0,last=null;
function write(s){el('log').textContent=(new Date().toLocaleTimeString()+'  '+s+'
'+el('log').textContent).slice(0,4000)}
function connect(){const u=new URL('/ws-latency',location.href);u.protocol=location.protocol==='https:'?'wss:':'ws:';ws=new WebSocket(u);el('status').textContent='CONNECTING\u2026';el('toggle').textContent='DISCONNECT';
ws.onopen=()=>{el('status').textContent='CONNECTED';write('socket connected');ping();timer=setInterval(ping,500)};
ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.type==='latency-hello'){el('region').textContent=[m.colo,m.city,m.country].filter(Boolean).join(' \xB7 ')||'Cloudflare edge';el('detail').textContent='Ingress colo '+(m.colo||'unknown')+'; Durable Object placement is managed by Cloudflare.';return}if(m.type!=='latency-pong')return;const r=performance.now()-m.sentAt;samples.push(r);if(samples.length>40)samples.shift();const j=last===null?0:Math.abs(r-last);last=r;el('ping').textContent=r.toFixed(1)+' ms';el('jitter').textContent=j.toFixed(1)+' ms';el('ping').className='value '+(r<80?'good':r<160?'warn':'bad');write('pong #'+m.id+' '+r.toFixed(1)+' ms')};
ws.onclose=()=>{clearInterval(timer);el('status').textContent='DISCONNECTED';el('toggle').textContent='CONNECT';write('socket closed')}}
function ping(){if(ws?.readyState===1)ws.send(JSON.stringify({type:'latency-ping',id:++seq,sentAt:performance.now()}))}
el('toggle').onclick=()=>ws?.readyState<2?ws.close():connect();connect();
<\/script></body></html>`;

// src/index.ts
var MATCH_ROUTE = /^\/matches\/(match-[0-9a-z-]+)$/i;
var PLAYER_ROUTE = /^\/matches\/(match-[0-9a-z-]+)\/players\/([01])$/i;
var PLAYER_CONFIG_ROUTE = /^\/matches\/(match-[0-9a-z-]+)\/players\/([12])$/i;
var MAX_FRAME_HISTORY = 60 * 60;
var BattleCityMatch = class extends DurableObject {
  constructor(ctx, env) {
    super(ctx, env);
    this.simulation = null;
    this.matchId = "";
    this.level = 1;
    this.startedAt = 0;
    this.matchStarted = false;
    this.resultSubmitted = false;
    this.stopped = false;
    this.timer = null;
    this.nextTickAt = 0;
    this.sockets = /* @__PURE__ */ new Map();
    this.activePlayers = /* @__PURE__ */ new Set();
    this.frameHistory = [];
    this.simulationOptions = {};
    this.pendingRunState = null;
    this.stageReadyPlayers = /* @__PURE__ */ new Set();
    this.configuredStagePlayers = /* @__PURE__ */ new Set();
  }
  static {
    __name(this, "BattleCityMatch");
  }
  async fetch(request) {
    const url = new URL(request.url);
    if (request.method === "POST" && url.pathname === "/start") {
      return this.startMatch(await request.json());
    }
    if (request.method === "GET" && url.pathname === "/status") {
      return Response.json(this.status());
    }
    if (request.method === "DELETE" && url.pathname === "/stop") {
      this.stop();
      return Response.json(this.status());
    }
    if (request.method === "PUT" && url.pathname === "/configure-player") {
      return this.configureStagePlayer(await request.json());
    }
    if (request.headers.get("upgrade")?.toLowerCase() === "websocket") {
      const slot = Number(request.headers.get("x-battlecity-player-slot"));
      if (slot !== 0 && slot !== 1) return new Response("Invalid player slot", { status: 400 });
      return this.connect(slot);
    }
    return new Response("Not found", { status: 404 });
  }
  startMatch(body) {
    if (this.simulation !== null) {
      return Response.json({ ...this.status(), error: "Match already exists" }, { status: 409 });
    }
    this.matchId = body.matchId;
    this.level = Math.max(1, Math.floor(body.level || 1));
    this.startedAt = Date.now();
    this.simulationOptions = {
      extraLives: body.extraLives,
      initialPlayerTiers: body.initialPlayerTiers,
      playerRunConsumables: body.playerRunConsumables,
      runBoosts: body.runBoosts,
      disableEnemyShooting: body.disableEnemyShooting === true
    };
    this.simulation = new EngineBattleCitySimulation(getMap(this.level), {
      seed: seedFromMatchId(this.matchId),
      level: this.level,
      ...this.simulationOptions
    });
    return Response.json(this.status(), { status: 201 });
  }
  connect(slot) {
    if (this.simulation === null || this.stopped) return new Response("Match is not running", { status: 409 });
    const reconnecting = this.matchStarted;
    this.sockets.get(slot)?.close(1012, "replaced by reconnect");
    const pair = new WebSocketPair();
    const client = pair[0];
    const server = pair[1];
    server.accept();
    this.sockets.set(slot, server);
    if (this.pendingRunState === null && !reconnecting) this.activePlayers.add(slot);
    server.addEventListener("message", (event) => this.receive(slot, server, event.data));
    server.addEventListener("close", () => this.disconnect(slot, server));
    server.addEventListener("error", () => this.disconnect(slot, server));
    if (this.pendingRunState === null && this.sockets.size === 2 && !this.matchStarted) {
      this.matchStarted = true;
      this.startTicking();
    }
    setTimeout(() => this.broadcastReady(reconnecting ? slot : null), 0);
    return new Response(null, { status: 101, webSocket: client });
  }
  receive(slot, socket, data) {
    if (typeof data !== "string") return;
    let packet;
    try {
      packet = JSON.parse(data);
    } catch {
      return;
    }
    if (packet.type === "webrtc-ping") {
      this.send(socket, {
        type: "webrtc-pong",
        id: packet.id,
        sentAt: packet.sentAt,
        senderPlayerIndex: -1
      });
      return;
    }
    if (packet.type === "webrtc-input") {
      if (this.activePlayers.has(slot) && packet.player === slot) {
        this.simulation?.acceptInput(packet);
      }
      return;
    }
    if (packet.type === "webrtc-stage-ready" && packet.player === slot && this.pendingRunState !== null && packet.stageNumber === this.pendingRunState.stageNumber) {
      this.stageReadyPlayers.add(slot);
      this.tryStartNextStage();
      return;
    }
    if (packet.type === "webrtc-client-debug" && packet.player === slot) {
      this.simulation?.setEnemyShootingDisabled(packet.disableEnemyShooting);
      return;
    }
    if (packet.type === "webrtc-resume" && packet.player === slot) {
      this.replay(socket, packet.lastAppliedFrameSeq);
      return;
    }
    if (packet.type === "webrtc-client-ready" && packet.player === slot) {
      this.activePlayers.add(slot);
      this.send(socket, { type: "webrtc-replay-ready", appliedSeq: packet.appliedSeq });
    }
  }
  startTicking() {
    if (this.timer !== null || this.simulation === null) return;
    const interval = 1e3 / this.simulation.tickRate;
    this.nextTickAt = performance.now() + interval;
    this.timer = setInterval(() => {
      const simulation = this.simulation;
      if (simulation === null || !this.matchStarted) return;
      const now = performance.now();
      let catchUp = 0;
      while (now >= this.nextTickAt && catchUp < 4) {
        const frame = simulation.step();
        this.frameHistory.push(frame);
        if (this.frameHistory.length > MAX_FRAME_HISTORY) this.frameHistory.shift();
        this.broadcast(frame);
        this.nextTickAt += interval;
        catchUp += 1;
        const hitStop = simulation.consumeHitStopSeconds();
        if (hitStop > 0) this.nextTickAt += hitStop * 1e3;
        if (simulation.isTerminal() && !this.resultSubmitted) {
          this.resultSubmitted = true;
          this.stopTicking();
          this.ctx.waitUntil(this.submitResult());
          break;
        } else if (simulation.isComplete() && this.pendingRunState === null) {
          this.beginStageTransition();
          break;
        }
      }
    }, Math.max(4, interval / 2));
  }
  stopTicking() {
    if (this.timer !== null) clearInterval(this.timer);
    this.timer = null;
  }
  replay(socket, lastAppliedSeq) {
    const serverSeq = this.simulation?.seq ?? 0;
    const oldest = this.frameHistory[0]?.seq ?? serverSeq + 1;
    if (!Number.isInteger(lastAppliedSeq) || lastAppliedSeq + 1 < oldest) {
      this.send(socket, { type: "webrtc-replay-unavailable", oldestAvailableSeq: oldest, serverSeq });
      return;
    }
    this.send(socket, { type: "webrtc-replay-start", fromSeq: lastAppliedSeq + 1, targetSeq: serverSeq });
    this.frameHistory.forEach((frame) => {
      if (frame.seq > lastAppliedSeq) this.send(socket, frame);
    });
    this.send(socket, { type: "webrtc-replay-complete", targetSeq: serverSeq });
  }
  disconnect(slot, socket) {
    if (this.sockets.get(slot) !== socket) return;
    this.sockets.delete(slot);
    this.activePlayers.delete(slot);
    this.broadcastReady(slot);
  }
  beginStageTransition() {
    const simulation = this.simulation;
    if (simulation === null || this.pendingRunState !== null) return;
    this.pendingRunState = simulation.createNextStageRunState();
    this.level = this.pendingRunState.stageNumber;
    this.matchStarted = false;
    this.activePlayers.clear();
    this.stageReadyPlayers.clear();
    this.configuredStagePlayers.clear();
    this.stopTicking();
    const openSlots = this.pendingRunState.lives.map((lives, slot) => lives > 0 ? null : slot).filter((slot) => slot !== null);
    this.broadcastReady(null);
    this.ctx.waitUntil(this.publishStageTransition(openSlots));
  }
  configureStagePlayer(body) {
    const runState = this.pendingRunState;
    if (runState === null || body.playerSlot !== 0 && body.playerSlot !== 1) {
      return new Response("No stage transition is active", { status: 409 });
    }
    runState.tankTiers[body.playerSlot] = body.tankTier;
    runState.playerRunConsumables[body.playerSlot] = {
      powerups: [...body.runConsumables?.powerups ?? []],
      powerupCounts: [...body.runConsumables?.powerupCounts ?? []]
    };
    this.configuredStagePlayers.add(body.playerSlot);
    if (body.replaceConnection === true) {
      this.sockets.get(body.playerSlot)?.close(1012, "stage player replaced");
      this.sockets.delete(body.playerSlot);
      this.stageReadyPlayers.delete(body.playerSlot);
    }
    return Response.json({ ok: true });
  }
  tryStartNextStage() {
    const runState = this.pendingRunState;
    if (runState === null || this.stageReadyPlayers.size === 0) return;
    const connectedSlots = [...this.sockets.keys()];
    if (!connectedSlots.every((slot) => this.stageReadyPlayers.has(slot))) return;
    connectedSlots.forEach((slot) => {
      if (runState.lives[slot] <= 0 && this.configuredStagePlayers.has(slot)) {
        runState.lives[slot] = 3;
        runState.scores[slot] = 0;
      }
    });
    this.ctx.waitUntil(this.startNextStage(runState));
  }
  async startNextStage(runState) {
    const response = await fetch(
      `${this.env.API_BASE_URL}/api/multiplayer/matches/${encodeURIComponent(this.matchId)}/stage-started`,
      {
        method: "POST",
        headers: this.apiHeaders(),
        body: JSON.stringify({ stageNumber: runState.stageNumber })
      }
    );
    if (!response.ok || this.pendingRunState !== runState) return;
    this.simulation = new EngineBattleCitySimulation(getMap(runState.stageNumber), {
      seed: seedFromMatchId(`${this.matchId}:stage:${runState.stageNumber}`),
      level: runState.stageNumber,
      ...this.simulationOptions,
      runState
    });
    this.pendingRunState = null;
    this.matchStarted = true;
    this.activePlayers.clear();
    this.stageReadyPlayers.forEach((slot) => this.activePlayers.add(slot));
    this.stageReadyPlayers.clear();
    this.configuredStagePlayers.clear();
    this.broadcastReady(null);
    this.startTicking();
  }
  async publishStageTransition(openSlots) {
    const runState = this.pendingRunState;
    if (runState === null) return;
    const response = await fetch(
      `${this.env.API_BASE_URL}/api/multiplayer/matches/${encodeURIComponent(this.matchId)}/stage`,
      {
        method: "POST",
        headers: this.apiHeaders(),
        body: JSON.stringify({
          stageNumber: runState.stageNumber,
          openSlots,
          scores: runState.scores.map((score, playerSlot) => ({ playerSlot, score }))
        })
      }
    );
    if (!response.ok) console.error("stage transition publication failed", { matchId: this.matchId, status: response.status });
  }
  stop() {
    this.stopped = true;
    this.matchStarted = false;
    this.stopTicking();
    this.sockets.forEach((socket) => socket.close(1e3, "match stopped"));
    this.sockets.clear();
    this.activePlayers.clear();
  }
  broadcastReady(syncPlayer) {
    this.broadcast({
      type: "webrtc-ready",
      ready: this.matchStarted && this.sockets.size === 2,
      syncPlayer,
      serverFrameSeq: this.simulation?.seq ?? 0
    });
  }
  broadcast(packet) {
    this.sockets.forEach((socket) => this.send(socket, packet));
  }
  send(socket, packet) {
    if (socket.readyState !== WebSocket.OPEN) return;
    try {
      socket.send(JSON.stringify(packet));
    } catch {
    }
  }
  status() {
    return {
      ok: this.simulation !== null,
      id: this.matchId,
      level: this.level,
      status: this.stopped || this.resultSubmitted ? "stopped" : this.pendingRunState === null ? "running" : "transition",
      tick: this.simulation?.tick ?? 0,
      frameSeq: this.simulation?.seq ?? 0,
      connectedPlayers: [...this.sockets.keys()],
      matchStarted: this.matchStarted,
      startedAt: this.startedAt > 0 ? new Date(this.startedAt).toISOString() : null,
      runtime: "cloudflare-durable-object"
    };
  }
  async submitResult() {
    const scores = (this.simulation?.getScores() ?? [0, 0]).map((score, playerSlot) => ({
      playerSlot,
      score
    }));
    const response = await fetch(
      `${this.env.API_BASE_URL}/api/multiplayer/matches/${encodeURIComponent(this.matchId)}/result`,
      {
        method: "POST",
        headers: this.apiHeaders(),
        body: JSON.stringify({ scores })
      }
    );
    if (!response.ok) {
      this.resultSubmitted = false;
      console.error("authoritative result submission failed", {
        matchId: this.matchId,
        status: response.status
      });
    }
  }
  apiHeaders() {
    return {
      authorization: `Bearer ${this.env.BROADCASTER_SERVICE_TOKEN}`,
      "content-type": "application/json"
    };
  }
};
var src_default = {
  async fetch(request, env) {
    const url = new URL(request.url);
    if (url.pathname === "/ws-latency.html") {
      return new Response(latencyPage, {
        headers: {
          "content-type": "text/html; charset=utf-8",
          "cache-control": "public, max-age=300",
          "content-security-policy": "default-src 'none'; style-src 'unsafe-inline'; script-src 'unsafe-inline'; connect-src 'self' ws: wss:",
          "x-content-type-options": "nosniff"
        }
      });
    }
    if (url.pathname === "/ws-latency") return latencySocket(request);
    if (request.method === "GET" && url.pathname === "/health") {
      return Response.json({ ok: true, runtime: "cloudflare-worker-websocket" });
    }
    if (request.method === "POST" && url.pathname === "/matches") {
      if (!authorizedServiceRequest(request, env)) return new Response("Forbidden", { status: 403 });
      const body = await request.json();
      if (!/^match-[0-9a-z-]+$/i.test(body.matchId || "")) return new Response("Invalid match ID", { status: 400 });
      return env.MATCHES.getByName(body.matchId).fetch(new Request("https://match/start", {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify(body)
      }));
    }
    const player = url.pathname.match(PLAYER_ROUTE);
    if (player !== null && request.headers.get("upgrade")?.toLowerCase() === "websocket") {
      const matchId = player[1].toLowerCase();
      const playerSlot = Number(player[2]);
      const ticket = await verifyTicket(url.searchParams.get("ticket") || "", env.WEBSOCKET_TICKET_SECRET);
      if (ticket === null || ticket.matchId !== matchId || ticket.playerSlot !== playerSlot) {
        return new Response("Invalid or expired ticket", { status: 403 });
      }
      const headers = new Headers(request.headers);
      headers.set("x-battlecity-player-slot", String(playerSlot));
      return env.MATCHES.getByName(matchId).fetch(new Request("https://match/connect", { headers }));
    }
    const playerConfig = url.pathname.match(PLAYER_CONFIG_ROUTE);
    if (playerConfig !== null && request.method === "PUT") {
      if (!authorizedServiceRequest(request, env)) return new Response("Forbidden", { status: 403 });
      const body = await request.json();
      return env.MATCHES.getByName(playerConfig[1].toLowerCase()).fetch(
        new Request("https://match/configure-player", {
          method: "PUT",
          headers: { "content-type": "application/json" },
          body: JSON.stringify({ ...body, playerSlot: Number(playerConfig[2]) - 1 })
        })
      );
    }
    const match = url.pathname.match(MATCH_ROUTE);
    if (match !== null && (request.method === "GET" || request.method === "DELETE")) {
      if (!authorizedServiceRequest(request, env)) return new Response("Forbidden", { status: 403 });
      return env.MATCHES.getByName(match[1].toLowerCase()).fetch(
        new Request(
          request.method === "DELETE" ? "https://match/stop" : "https://match/status",
          { method: request.method }
        )
      );
    }
    return new Response("Not found", { status: 404 });
  }
};
function latencySocket(request) {
  if (request.headers.get("upgrade")?.toLowerCase() !== "websocket") {
    return new Response("Expected WebSocket upgrade", { status: 426 });
  }
  const pair = new WebSocketPair();
  const client = pair[0];
  const server = pair[1];
  server.accept();
  const cf = request.cf;
  server.send(JSON.stringify({
    type: "latency-hello",
    colo: cf?.colo ?? null,
    city: cf?.city ?? null,
    region: cf?.region ?? null,
    country: cf?.country ?? null
  }));
  server.addEventListener("message", (event) => {
    if (typeof event.data !== "string") return;
    try {
      const packet = JSON.parse(event.data);
      if (packet.type === "latency-ping") {
        server.send(JSON.stringify({ type: "latency-pong", id: packet.id, sentAt: packet.sentAt }));
      }
    } catch {
    }
  });
  return new Response(null, { status: 101, webSocket: client });
}
__name(latencySocket, "latencySocket");
function authorizedServiceRequest(request, env) {
  return request.headers.get("authorization") === `Bearer ${env.BROADCASTER_SERVICE_TOKEN}`;
}
__name(authorizedServiceRequest, "authorizedServiceRequest");
async function verifyTicket(ticket, secret) {
  const [payload, signature, extra] = ticket.split(".");
  if (!payload || !signature || extra !== void 0 || secret.length < 32) return null;
  try {
    const key = await crypto.subtle.importKey(
      "raw",
      new TextEncoder().encode(secret),
      { name: "HMAC", hash: "SHA-256" },
      false,
      ["verify"]
    );
    const valid = await crypto.subtle.verify(
      "HMAC",
      key,
      decodeBase64Url(signature),
      new TextEncoder().encode(payload)
    );
    if (!valid) return null;
    const parsed = JSON.parse(new TextDecoder().decode(decodeBase64Url(payload)));
    return parsed.expiresAt >= Date.now() && /^match-[0-9a-z-]+$/i.test(parsed.matchId) && (parsed.playerSlot === 0 || parsed.playerSlot === 1) ? parsed : null;
  } catch {
    return null;
  }
}
__name(verifyTicket, "verifyTicket");
function decodeBase64Url(value) {
  const normalized = value.replace(/-/g, "+").replace(/_/g, "/");
  const bytes = atob(normalized.padEnd(Math.ceil(normalized.length / 4) * 4, "="));
  return Uint8Array.from(bytes, (character) => character.charCodeAt(0));
}
__name(decodeBase64Url, "decodeBase64Url");
function seedFromMatchId(matchId) {
  let hash = 2166136261;
  for (let index = 0; index < matchId.length; index += 1) {
    hash ^= matchId.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return hash >>> 0;
}
__name(seedFromMatchId, "seedFromMatchId");

// ../node_modules/wrangler/templates/middleware/middleware-ensure-req-body-drained.ts
init_modules_watch_stub();
var drainBody = /* @__PURE__ */ __name(async (request, env, _ctx, middlewareCtx) => {
  try {
    return await middlewareCtx.next(request, env);
  } finally {
    try {
      if (request.body !== null && !request.bodyUsed) {
        const reader = request.body.getReader();
        while (!(await reader.read()).done) {
        }
      }
    } catch (e) {
      console.error("Failed to drain the unused request body.", e);
    }
  }
}, "drainBody");
var middleware_ensure_req_body_drained_default = drainBody;

// ../node_modules/wrangler/templates/middleware/middleware-miniflare3-json-error.ts
init_modules_watch_stub();
function reduceError(e) {
  return {
    name: e?.name,
    message: e?.message ?? String(e),
    stack: e?.stack,
    cause: e?.cause === void 0 ? void 0 : reduceError(e.cause)
  };
}
__name(reduceError, "reduceError");
var jsonError = /* @__PURE__ */ __name(async (request, env, _ctx, middlewareCtx) => {
  try {
    return await middlewareCtx.next(request, env);
  } catch (e) {
    const error = reduceError(e);
    const body = JSON.stringify(error);
    const headers = {
      "Content-Type": "application/json",
      "MF-Experimental-Error-Stack": "true"
    };
    const encoded = encodeURIComponent(body);
    if (encoded.length <= 8192) {
      headers["MF-Experimental-Error-Stack-Payload"] = encoded;
    }
    return new Response(body, { status: 500, headers });
  }
}, "jsonError");
var middleware_miniflare3_json_error_default = jsonError;

// .wrangler/tmp/bundle-i7Md8O/middleware-insertion-facade.js
var __INTERNAL_WRANGLER_MIDDLEWARE__ = [
  middleware_ensure_req_body_drained_default,
  middleware_miniflare3_json_error_default
];
var middleware_insertion_facade_default = src_default;

// ../node_modules/wrangler/templates/middleware/common.ts
init_modules_watch_stub();
var __facade_middleware__ = [];
function __facade_register__(...args) {
  __facade_middleware__.push(...args.flat());
}
__name(__facade_register__, "__facade_register__");
function __facade_invokeChain__(request, env, ctx, dispatch, middlewareChain) {
  const [head, ...tail] = middlewareChain;
  const middlewareCtx = {
    dispatch,
    next(newRequest, newEnv) {
      return __facade_invokeChain__(newRequest, newEnv, ctx, dispatch, tail);
    }
  };
  return head(request, env, ctx, middlewareCtx);
}
__name(__facade_invokeChain__, "__facade_invokeChain__");
function __facade_invoke__(request, env, ctx, dispatch, finalMiddleware) {
  return __facade_invokeChain__(request, env, ctx, dispatch, [
    ...__facade_middleware__,
    finalMiddleware
  ]);
}
__name(__facade_invoke__, "__facade_invoke__");

// .wrangler/tmp/bundle-i7Md8O/middleware-loader.entry.ts
var __Facade_ScheduledController__ = class ___Facade_ScheduledController__ {
  constructor(scheduledTime, cron, noRetry) {
    this.scheduledTime = scheduledTime;
    this.cron = cron;
    this.#noRetry = noRetry;
  }
  static {
    __name(this, "__Facade_ScheduledController__");
  }
  #noRetry;
  noRetry() {
    if (!(this instanceof ___Facade_ScheduledController__)) {
      throw new TypeError("Illegal invocation");
    }
    this.#noRetry();
  }
};
function wrapExportedHandler(worker) {
  if (__INTERNAL_WRANGLER_MIDDLEWARE__ === void 0 || __INTERNAL_WRANGLER_MIDDLEWARE__.length === 0) {
    return worker;
  }
  for (const middleware of __INTERNAL_WRANGLER_MIDDLEWARE__) {
    __facade_register__(middleware);
  }
  const fetchDispatcher = /* @__PURE__ */ __name(function(request, env, ctx) {
    if (worker.fetch === void 0) {
      throw new Error("Handler does not export a fetch() function.");
    }
    return worker.fetch(request, env, ctx);
  }, "fetchDispatcher");
  return {
    ...worker,
    fetch(request, env, ctx) {
      const dispatcher = /* @__PURE__ */ __name(function(type, init) {
        if (type === "scheduled" && worker.scheduled !== void 0) {
          const controller = new __Facade_ScheduledController__(
            Date.now(),
            init.cron ?? "",
            () => {
            }
          );
          return worker.scheduled(controller, env, ctx);
        }
      }, "dispatcher");
      return __facade_invoke__(request, env, ctx, dispatcher, fetchDispatcher);
    }
  };
}
__name(wrapExportedHandler, "wrapExportedHandler");
function wrapWorkerEntrypoint(klass) {
  if (__INTERNAL_WRANGLER_MIDDLEWARE__ === void 0 || __INTERNAL_WRANGLER_MIDDLEWARE__.length === 0) {
    return klass;
  }
  for (const middleware of __INTERNAL_WRANGLER_MIDDLEWARE__) {
    __facade_register__(middleware);
  }
  return class extends klass {
    #fetchDispatcher = /* @__PURE__ */ __name((request, env, ctx) => {
      this.env = env;
      this.ctx = ctx;
      if (super.fetch === void 0) {
        throw new Error("Entrypoint class does not define a fetch() function.");
      }
      return super.fetch(request);
    }, "#fetchDispatcher");
    #dispatcher = /* @__PURE__ */ __name((type, init) => {
      if (type === "scheduled" && super.scheduled !== void 0) {
        const controller = new __Facade_ScheduledController__(
          Date.now(),
          init.cron ?? "",
          () => {
          }
        );
        return super.scheduled(controller);
      }
    }, "#dispatcher");
    fetch(request) {
      return __facade_invoke__(
        request,
        this.env,
        this.ctx,
        this.#dispatcher,
        this.#fetchDispatcher
      );
    }
  };
}
__name(wrapWorkerEntrypoint, "wrapWorkerEntrypoint");
var WRAPPED_ENTRY;
if (typeof middleware_insertion_facade_default === "object") {
  WRAPPED_ENTRY = wrapExportedHandler(middleware_insertion_facade_default);
} else if (typeof middleware_insertion_facade_default === "function") {
  WRAPPED_ENTRY = wrapWorkerEntrypoint(middleware_insertion_facade_default);
}
var middleware_loader_entry_default = WRAPPED_ENTRY;
export {
  BattleCityMatch,
  __INTERNAL_WRANGLER_MIDDLEWARE__,
  middleware_loader_entry_default as default
};
//# sourceMappingURL=index.js.map
