// Compatibility entry for the existing frontend Vercel project. The
// canonical API dispatcher and backend modules now live under api-server/.
export { default } from '../api-server/src/api/router';
